package io.infochat.provider;

import io.quarkus.test.junit.QuarkusTest;
import org.junit.jupiter.api.Test;

@QuarkusTest
class QuarkusBootstrapTest {

    @Test
    void contextStarts() {
        // The assertion is implicit: @QuarkusTest fails if the Quarkus
        // container cannot start. An empty body is sufficient at this stage.
    }
}
