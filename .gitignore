*.iml
.vscode/
.DS_Store
Thumbs.db
.claude/scheduled_tasks.lock
.claude/settings.local.json
.claude/worktrees/
.quarkus/
.idea/
.reviews/
.scratch/
# Local-only bulk working data: .bench/ holds unpacked test/migration backups
# (DB dumps, SimpleX identity DBs, secrets.env) plus the model-evaluation plan and
# results; .models/ holds LLM weights and .embeddings/ holds embedding-model GGUFs.
.bench/
.models/
.embeddings/
.mt/
target/
docs/deep-code-review/
docker-compose.override.yml
scripts/__pycache__/

# Secrets & credentials — never commit
.env
.env.*
!.env.example
!.env.template
*.pem
*.key
*.priv
*.p12
*.pfx
*.jks
*.keystore
*.ppk
*.asc
*.gpg
*.pgp
id_rsa
id_ed25519
*.secret
secrets.*
credentials.json

# Wizard runtime directory — generated secrets.env, filled config, .setup-state
# (§7.7.2). Never commit. The committed secrets.env.example template is
# re-included below (the `secrets.*` rule above would otherwise swallow it —
# negation must come after it for gitignore last-match-wins).
prod/runtime/
!prod/config/secrets.env.example
.agents/memory-local/

# Device-migration payload: bot addresses, prod runtime state, tier-2 memory.
# Sensitive by construction (see .migration/**/MIGRATION-README.md) — never commit.
.migration/

# Private planning, audit, spike, and live-test artifacts.
docs/plan/audit/
docs/plan/live-e2e/026-08-11-m1-784-817-report.md
docs/plan/m1/drafts/
docs/plan/m1/redteam/
docs/plan/m1/redteam-multi/
docs/plan/m1/tick-analysis/
docs/plan/m1/flaws.md
docs/plan/m1/now-clock-audit.md
docs/plan/m1/sanitize-caller-census.md
docs/plan/m1/scan-windows-fixture-census.md
docs/plan/m1/scan-window-fixture-census.md
docs/plan/m1/spikes/

# Live-test working notes (user-directed sessions) — never part of a ticket diff.
bench/
