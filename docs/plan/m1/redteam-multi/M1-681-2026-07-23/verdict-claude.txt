RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Zombie-reader TOCTOU on the death latch. latchTransportDeath's identity guard (SignalJsonRpcClient.java:905 `readerSocket != socket`) and connect()'s latch reset (SignalJsonRpcClient.java:372-381: socket published, THEN closed=false) leave a window: a reader thread that outlived disconnect()'s 2 s bounded join (SignalJsonRpcClient.java:426) can pass the guard while `socket` still points at the old connection, be descheduled across the entirety of connect(), then resume at :908 — reading `closed=false` as peer-initiated, latching the FRESH connection dead, draining its pending futures PERMANENT, shutting its new dispatcher, and firing a spurious subprocess restart (restartRequested was reset at :381, so the CAS at :926 succeeds). Preconditions are deep-tail (a read that fails to wake on Socket.close for >2 s, plus two precise deschedules), the trigger is local thread scheduling on the loopback signal-cli channel — not steerable by any adversary in the documented model (internet users sending text via adapters) — and the outcome self-heals through the supervised restart. No security.md bullet promises Signal transport-restart correctness, so this is a resilience note, not a finding. If the model is ever extended to cover local-transport availability guarantees, this is the spot to harden (e.g., latch state carried per-connection rather than on the one client instance that spans reconnects, as SimpleX's per-rebuild client already is by construction).
  - "At most once per death" is enforced in one direction only. The new spec text (docs/spec/messaging.md, diff lines 101-108) says the restart fires "at most once per death, whichever detector observes it first," but recordTimeout (SignalJsonRpcClient.java, diff hunk at :755-760) sets restartRequested unconditionally and never consults it — the code comment (:214-217) admits the one-directionality. A latch-first-then-timeout-escalation ordering would fire restartHung twice for one death. Practically unreachable: the latch drains `pending` (:910-913), so post-latch calls cannot accrue the 3 consecutive 5 s await-timeouts the escalation needs before the reconnect resets the counter, and SignalSubprocess.restartHung (:372-383) no-ops on a dead child. Noted because the spec sentence overstates what the code delivers; the practical residual is nil.
  - Readiness now couples orchestrator-visible state to live transport state. AdapterReadinessCheck.evaluate (diff, AdapterReadinessCheck.java:87-91) folds adapter.connected() into up/down, so an upstream actor who can repeatedly crash signal-cli (a signal-cli parsing bug triggered by hostile Signal traffic — upstream/supply-chain, out of this model) would flip a single-adapter deployment's readiness DOWN for every reconnect window, and an orchestrator gating traffic on readiness amplifies that into service unavailability windows. Truthful reporting is the ticket's explicit intent and trust boundary 6 keeps the endpoint loopback-bound by default; flagged only so the operator runbook can anticipate readiness flapping under a crash-looping daemon.
