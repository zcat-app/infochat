RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: AUTH-BYPASS
    SEVERITY: low
    PROMISE: "Inputs rejected ahead of the cancel sweep are the exceptions. The transport rate-cap drop, the transport byte cap, and empty-after-normalization all fire before the users-row read, so no drain is possible there without breaking the query-free hostile-flood path …" (commands.md §Surface conventions, amended by this diff) — i.e. the only justification the amended spec gives for the transport-byte-cap no-drain exception is the query-free constraint, and the retained rule "any other input cancels" is what keeps a pending destructive confirm from being redeemed by input the arming admin did not intend.
    GAP: The drain M1-774 adds at the two body caps (InboundRouter.java:736, :752) closes the stale-payload window for bodies over `chatBodyCap`/`commandBodyCap`, but the M1-038 transport byte cap fires EARLIER (InboundRouter.java:538, before the users-row read) and still returns with NO drain. A chat body in (65536, ∞) bytes never reaches the draining chat-cap branch (line 726), so the pending entry survives. It is then redeemable by any later confirm-shaped input for the same command: the step-4.5 sweep (InboundRouter.java:936) leaves a matching confirm-shape in place, and the handler's `takeMatching` pops the STORED payload, whose stored args win over the retyped ones (the spec'd "args retyped" relaxation; e.g. BanCommandHandler.java:190-199 executes `pendingBan.targetContactId()`). The diff's own enumeration comment (InboundRouter.java:898-899) documents the exception but only asserts the query-free rationale; it never addresses that the surviving entry stays redeemable — the exact wrong-target-execution hazard the ticket's own body-cap drains exist to close.
    REPRO: (1) Bot admin (DM) arms `/ban contact-A` → `BanConfirm(target-A)` stored for (actor, DM scope), TTL 60s. (2) Within 60s the admin pastes a >64 KiB chat body (accidental mega-paste / paste bomb) → transport byte cap at InboundRouter.java:538 sends the fixed MESSAGE_TOO_LARGE reply and returns; `drainPendingConfirm` is never called; the ban payload survives. (3) The admin then sends `/ban contact-B confirm` (single line, under the command cap) → step 4.5 sweep sees the confirm-shape and leaves the entry in place → `BanCommandHandler.takeMatching("ban")` pops the stored payload → `executeBan` runs against `contact-A`, not the typed `contact-B`, silently. The system should not allow a stale destructive payload to override the admin's freshly typed target; before this diff the same sequence also worked on the over-command-cap path, and this diff fixes that path but leaves the one-gate-earlier path open while codifying the exception in spec.
    SUGGESTED-FIX-CLASS: trust-boundary-tightening

OUT-OF-MODEL:
  - Confirm-existence disclosure timing in group scope: M1-774 moves the step-4.5 cancellation acknowledgement ("Pending `<command>` cancelled.", with the bot-authored command name) one message earlier on the caps paths — a group member now learns an admin (or any user with a pending `/forget`-class confirm) held a pending destructive confirm at the moment of an over-cap body, instead of at the next non-confirm input. The model already accepts group-visible cancellations (M1-051 sweep); it does not address disclosure timing. Flagging so the user can decide whether the model should promise confirm-existence secrecy (e.g. DM-only acks) or accept this as an extension of the existing disclosure.
