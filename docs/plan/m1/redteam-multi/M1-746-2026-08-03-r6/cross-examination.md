# Cross-examination report

Run directory: `/home/infochat/infochat/.claude/worktrees/M1-746/docs/plan/m1/redteam-multi/M1-746-2026-08-03-r6`
Auditors: claude, opencode, codex

## Summary

- 5 distinct finding cluster(s) across all auditors.
- 0 corroborated (flagged by >=2 auditors).
- 5 single-auditor -- each is either a real gap the others missed or a false positive; see the per-cluster detail and the falsification candidates section.
- Per-auditor raw finding counts: {'claude': 3, 'opencode': 2}.

## Per-auditor verdicts

- **claude**: FINDINGS (3 finding(s))
- **opencode**: FINDINGS (2 finding(s))
- **codex**: CLEAN (0 finding(s))

## Finding clusters (side-by-side)

| # | Category | Primary location | claude | opencode | codex | Severity (max) | Attribution |
|---|---|---|---|---|---|---|---|
| 1 | INFO-LEAK | `infochat-provider/src/main/java/app/zcat/infochat/provider/chat/ChatAgent.java:689-694` | medium | -- | -- | medium | claude-only -- needs review |
| 2 | DOS | `QueryAnchorTranslator.java:250` | low | -- | -- | low | claude-only -- needs review |
| 3 | DOS | `QueryTranslationCache.java:90-94` | -- | low | -- | low | opencode-only -- needs review |
| 4 | INFO-LEAK | `infochat-provider/src/main/java/app/zcat/infochat/provider/translation/QueryTranslationCache.java:92-95` | low | -- | -- | low | claude-only -- needs review |
| 5 | INJECTION | `QueryAnchorTranslator.java:131` | -- | low | -- | low | opencode-only -- needs review |

## Per-cluster detail

### Cluster 1: INFO-LEAK @ `infochat-provider/src/main/java/app/zcat/infochat/provider/chat/ChatAgent.java:689-694`

**claude** (severity: medium, fix-class: trust-boundary-tightening)

- PROMISE: security.md §Secrets handling — "The post-setup `prod/switch-llm.sh` backend switcher records the LLM API key in `secrets.env` through the same dotenv-escaped channel and prints a per-task privacy disclosure naming exactly which generative tasks now call a remote provider and what each exposes — `chat` (private user messages) flagged loudest, the ingest tasks (`security`/`tagger`/`entity`/`classif...
- GAP (first 400 chars): The diff routes VERBATIM inbound user chat text into `ModelTask.TRANSLATOR` but leaves the spec-committed per-task privacy disclosure describing the pre-M1-746 exposure.
      - infochat-provider/src/main/java/app/zcat/infochat/provider/chat/ChatAgent.java:689-694 (unchanged by the diff) dispatches the D28 pre-fetch with `query = userMessage` (first 500 chars of the user's raw message).
      - in...


### Cluster 2: DOS @ `QueryAnchorTranslator.java:250`

**claude** (severity: low, fix-class: rate-limit)

- PROMISE: security.md §Rate limiting, the bullet this diff adds — "**Query-anchoring translation (M1-746, D58)** — a disclosed exception to the 'LLM-triggering operations' bucket: a non-English scope's retrieval issues generative `ModelTask.TRANSLATOR` calls — one per turn for the D28 pre-fetch, plus one per DISTINCT model-elected `semanticSearch` query — that draw NO per-user bucket token ... Accepted v1 p...
- GAP (first 400 chars): The un-metered exception is disclosed, but three of the four bounds offered in exchange are not delivered by the code:
      - "local translator model": QueryAnchorTranslator.java:250 resolves the provider through `llmRouter.forTask(ModelTask.TRANSLATOR, sourceLanguage)`; LlmRouter applies no locality constraint (contrast the embedding leg, which the same tool row pins local under D54). The diff's...


### Cluster 3: DOS @ `QueryTranslationCache.java:90-94`

**opencode** (severity: low, fix-class: rate-limit)

- PROMISE: "the translation leg is cached per (scope, query, language) so repeated queries cost nothing" (docs/spec/security.md §Rate limiting, "Query-anchoring translation (M1-746, D58)" bullet) and the R2 commitment "no cross-scope cache state exists" (ticket redteam_findings R2; QueryTranslationCache javadoc).
- GAP (first 400 chars): The R2 fix partitioned the cache's KEY space — (scope_kind, scope_id, hash, lang) in QueryTranslationCache.java:90-94 — but the underlying store is a single process-wide Caffeine cache with one shared capacity budget: `Cache<QueryKey, String> ... expireAfterWrite(24h).maximumSize(10_000)` (QueryTranslationCache.java:66-69). No per-scope capacity exists, so one scope's churn evicts another scope's ...


### Cluster 4: INFO-LEAK @ `infochat-provider/src/main/java/app/zcat/infochat/provider/translation/QueryTranslationCache.java:92-95`

**claude** (severity: low, fix-class: other)

- PROMISE: security.md §What's intentionally NOT in v1 — "v1 relies on minimization (chat-memory TTL, `/forget`, `/export`) instead"; and, on what a translation cache may hold, the accepted-residual bullet in the same section: "v1 accepts this as a minor trade-off: the cached strings are presentation prose generated by the bot (cluster summaries, headers, status lines), **not user-authored content**".
- GAP (first 400 chars): The diff introduces a second, user-content-bearing retention store that no minimization lever reaches. infochat-provider/src/main/java/app/zcat/infochat/provider/translation/QueryTranslationCache.java:92-95 builds a Caffeine cache with `expireAfterWrite(Duration.ofHours(24))` / `maximumSize(10_000)` whose VALUE is the English translation of the caller's query — and on the D28 pre-fetch path that q...


### Cluster 5: INJECTION @ `QueryAnchorTranslator.java:131`

**opencode** (severity: low, fix-class: other)

- PROMISE: "Every prompt that includes user-derived text is wrapped in a delimiter block whose marker contains a per-call random value. Attackers cannot pre-guess the marker and therefore cannot forge a closing tag inside the body." (docs/spec/security.md §Prompt-injection defenses) and the amended semanticSearch row: "**Query anchoring (M1-746, D58):** when the scope declares a non-English `/lang`, the quer...
- GAP (first 400 chars): The r5 root cause — "PROMPT_TEMPLATE carries a LITERAL `...` between the markers" — is only half-removed. QueryAnchorTranslator.java:131 still renders the instruction prose `<<<UNTRUSTED_CONTENT id="{{id}}">>> ... <<<END id="{{id}}">>>` with the literal ellipsis, and after substitution that illustrative pair carries the SAME per-call uuid as the real constructed block at QueryAnchorTranslator.java...


## Single-auditor findings (falsification candidates)

Each finding below was reported by exactly one auditor. Either the others missed a real gap, or this auditor produced a false positive. A v2 synthesizer subagent would re-audit each against the threat model; this v1 surfaces them for human review.

- **claude-only**: INFO-LEAK @ `infochat-provider/src/main/java/app/zcat/infochat/provider/chat/ChatAgent.java:689-694` (severity medium). See `verdict-claude.txt` for full PROMISE/GAP/REPRO.
- **claude-only**: DOS @ `QueryAnchorTranslator.java:250` (severity low). See `verdict-claude.txt` for full PROMISE/GAP/REPRO.
- **opencode-only**: DOS @ `QueryTranslationCache.java:90-94` (severity low). See `verdict-opencode.txt` for full PROMISE/GAP/REPRO.
- **claude-only**: INFO-LEAK @ `infochat-provider/src/main/java/app/zcat/infochat/provider/translation/QueryTranslationCache.java:92-95` (severity low). See `verdict-claude.txt` for full PROMISE/GAP/REPRO.
- **opencode-only**: INJECTION @ `QueryAnchorTranslator.java:131` (severity low). See `verdict-opencode.txt` for full PROMISE/GAP/REPRO.

