RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - /summary --short skips the summarizer-post-cap gate
    (SummaryCommandHandler: `args.form() != SHORT && result.posts().size()
    > summarizerPostCap`), so a wide window drives one
    CategoryRollupGenerator roll-up LLM call per category whose prompt
    (CategoryRollupGenerator.buildPrompt) concatenates EVERY cluster's
    title/body/url — unbounded by the 50-post ceiling the other three
    forms degrade behind. This persists from r1/r2 and is still NOT a
    threat-model violation: §Rate limiting bounds the RATE of
    LLM-triggering ops (the per-user bucket, per-group backstop, and
    in-flight slot all still apply to --short), the retrieval cluster-cap
    bounds the absolute prompt size, and the roll-up degrades gracefully
    on failure (empty -> D43 notice + D17 degraded prose per cluster, the
    r2 remediation now present in DigestRenderer.renderShortBody). Flagged
    a third time only so the operator can decide whether to extend the
    model to bound per-invocation LLM fan-out or roll-up input magnitude.
  - Notes on surfaces checked and found honoring the model (no findings):
    the --short roll-up output passes llmOutputSanitizer.sanitize +
    translationPipeline.run inside generateRollupUnconditional (verified
    in the working tree), satisfying §LLM output sanitizer's "any future
    LLM-emitted text" clause; the roll-up prompt wraps feed-derived
    fields in a per-call random <<<UNTRUSTED_CONTENT>>> marker per
    §Prompt-injection defenses; the r2 remediation's degraded fallback
    renders SummaryProseGenerator.degradedProseFor per cluster, keeping
    the M1-697 one-author's-field sanitize unit and the per-occurrence
    audit rows; the --short footers are deterministic bundle text
    interpolating only controlled-vocabulary section tags (delivery-
    ordering contract path (a), driven by the parsed request); a second
    form flag is rejected at parse (misleadingly keyed
    error.summary.window_out_of_range — a UX nit, not a security gap);
    the /retry 'short' arm re-runs the roll-up under the same bucket
    draw and in-flight slot as every other replay form; the over-cap
    --full uncapped render is deterministic (no LLM) and bounded by the
    retrieval/cluster caps.
