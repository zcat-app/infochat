RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - `/summary --short` deliberately skips the summarizer-post-cap (>50)
    gate (SummaryCommandHandler.java:
    `if (args.form() != SHORT && result.posts().size() > summarizerPostCap)`),
    so a wide window (e.g. `-w 4w`) drives one CategoryRollupGenerator
    roll-up LLM call per category whose prompt (CategoryRollupGenerator.
    buildPrompt, lines 185-211) concatenates EVERY cluster's title/body/
    url — unbounded by the 50-post ceiling the other three forms degrade
    behind. The r3 remediation did NOT change this: it only added
    deterministic degraded-prose rendering on the roll-up-FAILURE branch
    (DigestRenderer.renderShortBody else-arm calls
    SummaryProseGenerator.degradedProseFor per cluster), which adds no
    LLM cost. NOT a violation of any §security.md commitment: §Rate
    limiting bounds the RATE of LLM-triggering operations (the per-user
    LLM bucket and the interruptible-concurrency cap still apply to
    --short, which acquires the in-flight slot and draws from the bucket
    like the other forms), and no spec text bounds the /summary
    summarizer's per-call prompt magnitude — `summarizer-post-cap` is an
    implementation detail, not a §security.md promise. The retrieval
    cluster-cap (`infochat.summary.cluster-cap`) bounds the absolute
    prompt size, and the roll-up degrades gracefully on context-overflow
    (catch at CategoryRollupGenerator.java:169-172 → empty → the r3
    degraded-prose path renders the deterministic headlines+URLs+UIDs
    instead). Flagged (as r1 opencode and r2 kimi+opencode both flagged
    it, accepted out-of-model in the ticket) only so the operator can
    decide whether to extend the model to bound per-invocation LLM
    fan-out or roll-up input size; both candidate fixes contradict
    pinned acceptance (item 2: the roll-up sees ALL clusters including
    past-cap ones — that is --short's stated purpose).

    Note on the r2 finding closure (informational, not a finding): the
    r2 DOS finding named two undelivered halves of security.md
    §Failure handling's `/summary summarizer unreachable` bullet on the
    --short surface — (a) the D43 degraded_notice and (b) the D17
    degraded FORM (headlines + URLs + post UIDs). r3 closes both:
    SummaryCommandHandler --short branch and RetryCommandHandler 'short'
    arm prefix REPLY_SUMMARY_DEGRADED_NOTICE when ShortResult.
    anyRollupMissing() is true (notice half), and DigestRenderer.
    renderShortBody's roll-up-failure else-arm now appends
    SummaryProseGenerator.degradedProseFor(c, llmOutputSanitizer) per
    cluster (form half — the same sanitized degraded path the other
    three forms use). generateRollupUnconditional continues to
    llmOutputSanitizer.sanitize(text) on the success path
    (CategoryRollupGenerator.java:166) and wraps every user-derived
    field in a per-call-random `<<<UNTRUSTED_CONTENT>>>` marker
    (buildPrompt, :185-211), so the roll-up LLM surface inherits the
    §Prompt-injection defenses and §LLM output sanitizer commitments
    unchanged. No §security.md commitment is violated by this diff.
