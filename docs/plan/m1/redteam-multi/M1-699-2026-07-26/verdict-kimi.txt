RED-TEAM VERDICT: CLEAN

Audit notes (supporting evidence, not findings):

- Scope of the diff: V65 migration adds `summary_anchor.render_form`
  (TEXT, NOT NULL, DEFAULT 'bare', CHECK IN ('bare','short','full','flat')),
  backfilled from `command_name LIKE '%--full%'`; `SummaryAnchorRepository`
  UPSERT/SELECT carry the new column; `SummaryCommandHandler` writes
  `args.full() ? "flat" : "bare"`; `RetryCommandHandler` switches dispatch
  from `hasFlag(commandName, "--full")` to the typed column; tests updated.
  The change is a pure dispatch-axis refactor with no new inbound surface,
  no new LLM call site, no new SQL shape, and no authz-path edit.

- INJECTION: all new SQL remains parameterized (`ps.setString` binds for
  render_form at SummaryAnchorRepository.java UPSERT/SELECT); the CHECK
  constraint closes the value domain at the DB layer. The backfill UPDATE
  contains no user-derived expression.

- INFO-LEAK / sanitizer parity (security.md §LLM output sanitizer, "/retry
  re-rolls" surface): both render arms are byte-for-byte the pre-existing
  M1-696 render paths — 'flat' keeps per-title redaction through
  ClusterBlockRenderer, 'bare' keeps renderSummarySections. A wrong-form
  replay (worst case of a backfill misclassification, explicitly accepted by
  D70 under the V37 ephemerality tolerance) cannot widen disclosure: both
  forms draw from the same anchor's post_uids for the same (user, scope),
  both are sanitized, and the flat form's extra headline bytes are reachable
  to the same user via `/summary --full` directly.

- AUTH-BYPASS / PERM-ESCAL: the SELECT that reads render_form remains keyed
  by (user_id, scope_kind, scope_id, command_kind='personal') — no
  cross-user read path introduced. The probation gate on /retry, the
  admin-tier checks in RetryCommandHandler, and router-level audit of
  command intent (security.md §Authorization model steps 7-9) are untouched.

- DOS: the switch's `default -> throw new IllegalStateException(...)` is
  reachable only for the CHECK-admitted-but-unwritten values 'short'/'full'
  (reserved for M1-700). No current writer can produce them; reaching the
  throw requires a DB write foothold, at which point it yields at most a
  loud failure of that user's own /retry — a deliberate fail-loud guard, and
  the exception message interpolates only the CHECK-bounded ASCII literal,
  never user prose (consistent with §User content in exceptions).

- AUDIT-EVASION: no command-intent or LLM_OUTPUT_SANITIZED audit path is
  altered; the per-title redaction behavior (and its per-occurrence audit
  rows) is inherited unchanged by both replay arms.
