RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Tier-flat topic delivery has no runtime guard on the served bytes: the only defense keeping bot-admin command strings out of the post-sanitize topic block is the build-time pin (HelpTopicCorpusTest.noTopicReferencesAdminSurface, pre-existing from M1-649, covering match text AND answer values in EN/CS). The delivery path added by this diff (ChatAgent.java step 9b, ~lines 540-551) appends bundleLoader.get(topic.answerBundleKey(), scopeLanguage) verbatim with no runtime check. A future bundle edit that adds an admin command name to a topic.<slug>.answer value while the corresponding test is weakened would ship unsanitized to every chat user. The threat model authorizes this shape under the §LLM output sanitizer delivery-ordering contract path (a) (deterministic emission decision + deterministic bytes), so it is not a finding — but the safety rests on test discipline rather than a structural runtime property, unlike the sanitizer's CI-enforced match-set derivation. The user may want to extend the model to require the CLOSED_LIST bot-admin subset be mechanically excluded from bundle values on this surface.
  - Asymmetry between the two help blocks' tier posture: the D67 command-usage block is tier-filtered twice (SQL target_ref = ANY(?) + composeUsageBlock re-check), so a plain user never receives a group-admin command's usage; the D69 topic block is tier-flat by construction and may name group-admin-tier CLOSED_LIST commands (/add-source, /follow-tag per D68) in prose delivered to any caller in any scope. The threat model's existence-oracle promise is scoped to the model context and the helpLookup tool, and topic answers are reviewed public product copy (USER_GUIDE-derived), so no documented commitment is violated — flagged only so the asymmetry is a recorded decision, not an oversight.
  - Pre-existing, untouched by this diff: the sanitize→translate ordering (ChatAgent steps 8-9) means the TranslationPipeline's output — LLM-authored when the translation provider is an LLM — is delivered without a post-translation sanitizer pass. The threat model says the sanitizer applies to "the full set of LLM-authored output surfaces." This diff neither introduced nor widened that ordering (the topic block deliberately bypasses translation entirely, which is the safe direction), so it is out of this audit's diff scope; noted in case the translation surface has no compensating control elsewhere.
