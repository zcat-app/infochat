RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Wire-derived metric VALUES remain attacker-influenced even though labels are now bounded: `MeteredLlmProvider.generate` (MeteredLlmProvider.java:93) passes `response.usage()` through to `LlmMetrics.recordLlmCall`, which increments `llm.tokens.in`/`llm.tokens.out` (LlmMetrics.java:89-94) by endpoint-reported token counts, and `MeteredEmbeddingProvider.embed` (MeteredEmbeddingProvider.java:64) sets the `embedding.dimension` gauge from the endpoint-reported vector dimension. A hostile or compromised endpoint can report arbitrary (huge) counts and corrupt cost/usage observability. This is bounded (no memory growth — fixed meter set, numeric values only) and the threat model plus the new llm.md bullet commit only to label sourcing, not metric-value integrity against a hostile endpoint. Flagging so the user can decide whether metric-value integrity belongs in the model.
  - The new "Metric labels are never wire-derived" commitment landed in docs/spec/llm.md (§ observability, diff lines 77-82), not in docs/spec/security.md. Future red-team audits instructed to read only security.md as the threat model will not see this commitment and cannot enforce it against later diffs that add wire-derived tags elsewhere (I verified the current codebase-wide tag surface — LlmMetrics.java, AdapterMetrics.java:184-254, Stage2Worker.java:198 — is enum/constant/config-sourced today). Not an attack, a threat-model-coverage observation.
