# Cross-examination report

Run directory: `/home/infochat/infochat/.claude/worktrees/M1-746/docs/plan/m1/redteam-multi/M1-746-2026-08-03`
Auditors: opencode, codex

## Summary

- 4 distinct finding cluster(s) across all auditors.
- 0 corroborated (flagged by >=2 auditors).
- 4 single-auditor -- each is either a real gap the others missed or a false positive; see the per-cluster detail and the falsification candidates section.
- Per-auditor raw finding counts: {'opencode': 2, 'codex': 2}.

## Per-auditor verdicts

- **opencode**: FINDINGS (2 finding(s))
- **codex**: FINDINGS (2 finding(s))

## Finding clusters (side-by-side)

| # | Category | Primary location | opencode | codex | Severity (max) | Attribution |
|---|---|---|---|---|---|---|
| 1 | DOS | `QueryTranslationCache.java:42-45` | -- | high | high | codex-only -- needs review |
| 2 | DOS | `infochat-provider/src/main/java/app/zcat/infochat/provider/chat/tool/SemanticSearchTool.java:125` | medium | -- | medium | opencode-only -- needs review |
| 3 | INFO-LEAK | `QueryTranslationCache.java:39` | -- | medium | medium | codex-only -- needs review |
| 4 | INJECTION | `QueryAnchorTranslator.java:95-106` | low | -- | low | opencode-only -- needs review |

## Per-cluster detail

### Cluster 1: DOS @ `QueryTranslationCache.java:42-45`

**codex** (severity: high, fix-class: input-sanitization)

- PROMISE: "Everything a generative or embeddings endpoint returns is endpoint-chosen input" and "the response body is read under an operator-configurable cap (clamped to 1–8 MiB) before parsing, so a pathological multi-GB reply cannot exhaust the JVM."
- GAP (first 400 chars): QueryTranslationCache.java:42-45 retains up to 10,000 entries for 24 hours by entry count only, while QueryAnchorTranslator.java:173-176 accepts the translator response verbatim and QueryAnchorTranslator.java:184 stores it without a byte/weight bound. A hostile endpoint can return an in-cap multi-megabyte translation for every distinct query, converting the transport cap into tens of gigabytes of ...


### Cluster 2: DOS @ `infochat-provider/src/main/java/app/zcat/infochat/provider/chat/tool/SemanticSearchTool.java:125`

**opencode** (severity: medium, fix-class: rate-limit)

- PROMISE: "Per-user token buckets bound, grouped explicitly so commands that share a cost profile share a bucket: ... LLM-triggering operations (chat replies + on-demand /summary + /retry re-rolls) — its own bucket, capped lower, profile-driven." (docs/spec/security.md §Rate limiting). The semanticSearch tool row additionally describes the tool's only LLM interaction as "The query is embedded on the local e...
- GAP (first 400 chars): M1-746 inserts a GENERATIVE LLM call (ModelTask.TRANSLATOR) inside SemanticSearchTool.execute — reached both by the deterministic D28 pre-fetch on every chat turn and by every model-elected semanticSearch call (infochat-provider/src/main/java/app/zcat/infochat/provider/chat/tool/SemanticSearchTool.java:125,130,136; QueryAnchorTranslator.java:145-147 via llmRouter.forTask + provider.generate). The ...


### Cluster 3: INFO-LEAK @ `QueryTranslationCache.java:39`

**codex** (severity: medium, fix-class: trust-boundary-tightening)

- PROMISE: "The presentation-layer translation cache ... is shared across scopes" is accepted only because "the cached strings are presentation prose generated by the bot ... not user-authored content."
- GAP (first 400 chars): QueryTranslationCache.java:39 and :75-77 key the application-scoped cache solely by raw sourceText and sourceLanguage, omitting user and scope. QueryAnchorTranslator.java:151-154 returns a hit before the translator call. Unlike the documented presentation cache, these keys are user-authored search queries, so the hit/miss latency is a cross-scope oracle for another user's query.


### Cluster 4: INJECTION @ `QueryAnchorTranslator.java:95-106`

**opencode** (severity: low, fix-class: trust-boundary-tightening)

- PROMISE: "Every prompt that includes user-derived text is wrapped in a delimiter block whose marker contains a per-call random value. Attackers cannot pre-guess the marker and therefore cannot forge a closing tag inside the body." (docs/spec/security.md §Prompt-injection defenses) — and the semanticSearch tool row: "the fused candidate set and its order are decided entirely by SQL ... reproducible on uncha...
- GAP (first 400 chars): The delimiter wrapper is delivered correctly (QueryAnchorTranslator.java:95-106, per-call UUID), but the OUTPUT of the attacker-coaxable translator is trusted as the retrieval query with no validation beyond a non-blank check (QueryAnchorTranslator.java:119-123) and is then used verbatim as the embed input and the plainto_tsquery bind (SemanticSearchTool.java:130,136). Two consequences the diff do...


## Single-auditor findings (falsification candidates)

Each finding below was reported by exactly one auditor. Either the others missed a real gap, or this auditor produced a false positive. A v2 synthesizer subagent would re-audit each against the threat model; this v1 surfaces them for human review.

- **codex-only**: DOS @ `QueryTranslationCache.java:42-45` (severity high). See `verdict-codex.txt` for full PROMISE/GAP/REPRO.
- **opencode-only**: DOS @ `infochat-provider/src/main/java/app/zcat/infochat/provider/chat/tool/SemanticSearchTool.java:125` (severity medium). See `verdict-opencode.txt` for full PROMISE/GAP/REPRO.
- **codex-only**: INFO-LEAK @ `QueryTranslationCache.java:39` (severity medium). See `verdict-codex.txt` for full PROMISE/GAP/REPRO.
- **opencode-only**: INJECTION @ `QueryAnchorTranslator.java:95-106` (severity low). See `verdict-opencode.txt` for full PROMISE/GAP/REPRO.

