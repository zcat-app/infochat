RED-TEAM VERDICT: CLEAN

AUDIT SUMMARY (advisory; not parsed as findings):

Scope — M1-703 changes ONLY the degraded-mode notice-prefix selection in
/summary and /retry replies: the prior code emitted the total
REPLY_SUMMARY_DEGRADED_NOTICE ("...no prose...") whenever ANY cluster
degraded; the new code distinguishes partial (new
REPLY_SUMMARY_PARTIAL_DEGRADED_NOTICE, "{0} of {1} topics...") from total
(degradedClusters == clustersTotal). Files touched: BundleKeys.java
(one key added), RetryCommandHandler.java, SummaryCommandHandler.java,
DigestRenderer.java (ShortResult record widened from a boolean to two
int counts), en/cs .properties (one string each), two test files.

Threat-model commitments checked against the surface this diff occupies
(§LLM output sanitizer, §Failure handling, delivery-ordering contract,
per-title redaction M1-697, ]( chokepoint M1-691):

- Deterministic-output exemption is satisfied. The new notice is fixed
  bundle text interpolated only with server-computed integer counts via
  MessageFormat.format / the local format() helper
  (SummaryCommandHandler.java:756-762; RetryCommandHandler.java:53-57).
  The args are String.valueOf(int) — digit/minus output only, never
  inbound-derived bytes — so "interpolates no inbound-derived text"
  holds. The decision to emit is driven by ClusterProse.degraded /
  rollup Optional.empty (transport/result state), not by model-elected
  content, so it qualifies under the deterministic-end-to-end branch of
  the delivery-ordering contract rather than requiring the sanitizer.

- Per-title redaction (M1-697) is preserved. DigestRenderer.renderShortBody
  still renders failed-category clusters through
  SummaryProseGenerator.degradedProseFor(c, llmOutputSanitizer)
  (DigestRenderer.java:351-354), so a command-shaped feed title is
  redacted at the one-author's-field unit exactly as before; the diff
  only changes which notice prefix precedes that already-sanitized body.

- The ]( outbound chokepoint (M1-691) still covers the whole delivered
  message. The notice is prepended to prefixes/out and the assembled
  message still routes through progressNotifier.complete(...) /
  reply(...) -> OutboundDelivery, which carries breakLinkAdjacency over
  the full body regardless of how it was assembled. No new
  ](-manufacturing path and no direct adapter-send bypass is introduced.

- §Failure handling honesty is IMPROVED, not weakened: the partial copy
  accurately describes the degraded clusters' D17 render form; the prior
  "no prose" banner above a mostly-prose body was the misrepresentation.

- No INFO-LEAK (the N/M counts are the caller's own world data, already
  visible in the reply body), no INJECTION (integer MessageFormat), no
  DOS (O(clusters), bounded by the summarizer cap), no AUTH-BYPASS /
  PERM-ESCAL / AUDIT-EVASION (intake, permission, audit, and persistence
  paths are untouched by this diff).

No gap between the threat model's promises and what the diff delivers.
