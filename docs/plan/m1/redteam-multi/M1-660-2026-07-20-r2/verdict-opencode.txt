RED-TEAM VERDICT: CLEAN

Audit target: M1-660 re-audit after in-branch remediation. The prior
audit's sole finding (low-severity DOS asymmetry — the delivery-path
caller ChatAgent.lookupIntentForDelivery borrowed a bare connection with
no statement_timeout, so the newly-armed strict_order iterative scan
could walk up to hnsw.max_scan_tuples unbounded while the tool-path
caller kept its armToolConnection time cap) is verified CLOSED, and the
remainder of the diff introduces no new gaps against the threat model.

Prior-finding remediation — verified closed:

- ChatAgent.lookupIntentForDelivery (infochat-provider/src/main/java/
  app/zcat/infochat/provider/chat/ChatAgent.java:805) now calls
  cancellationService.applyStatementTimeout(conn) before
  CommandIntentIndex.lookupCommand. CancellationService.applyStatementTimeout
  (CancellationService.java:112-118) does setAutoCommit(false) +
  SET LOCAL statement_timeout = <validated positive int millis>, the
  validatedTimeoutMillis() range check (CancellationService.java:128-137)
  guaranteeing the formatted text is a plain int4-range integer — no
  injection surface in the SET text. The setting is transaction-local
  (SET LOCAL inside an autocommit-off txn), so it dies at pool release
  and cannot leak to a later borrower. This is the SAME mechanism
  armToolConnection applies (CancellationService.java:151 calls
  applyStatementTimeout) — the tool-path caller (HelpLookupTool.java:169)
  has had it all along, so the delivery/tool asymmetry the prior audit
  flagged is eliminated.
- The bound delivered is exactly the threat model's commitment
  (§Failure handling, Provider-side LLM failures): "the deterministic
  digest-first semantic pre-fetch ... is bounded — read-only, capped in
  time (the pgvector probe by `statement_timeout` ...)". The iterative
  scan now runs under that cap on both call sites.
- The delivery caller is gated at ChatAgent.java:445 by
  !breakerRegistry.wouldShortCircuit(ModelTask.CHAT_AGENT), satisfying
  §Failure handling's "Once the chat endpoint's circuit breaker ...
  is OPEN, the pre-fetch is skipped entirely" — so an outage window
  pays the bounded probe cost at most once per breaker cycle.

Rest-of-diff scan — no new findings against the closed category set:

- AUTH-BYPASS / PERM-ESCAL: not in scope. The diff touches no auth
  gate; ChatAgent.lookupIntentForDelivery computes the caller-visible
  command set via helpHandler.resolveCallerTier /
  visibleCommandNames (unchanged) and passes it as visibleTargets to
  lookupCommand, which binds it via target_ref = ANY(?) INSIDE the
  WHERE (CommandIntentIndex.java:140) — the tier-filter-before-return
  invariant is preserved byte-for-byte.
- INFO-LEAK: lookupCommand's SELECT target_ref FROM doc_embedding
  WHERE doc_kind = ? AND target_ref = ANY(?) is unchanged; the
  doc_kind and tier filters apply before any row reaches the result,
  exactly as before. No new exfiltration surface introduced.
- INJECTION: the only new SQL text executed is the static literal
  "SET LOCAL hnsw.iterative_scan = strict_order"
  (CommandIntentIndex.java:179) — no interpolation. The probe's
  dynamic inputs (vector literal, similarity threshold, visibleTargets
  array) remain bound via PreparedStatement parameters
  (CommandIntentIndex.java:154-158). applyStatementTimeout's SET text
  is formatted from a range-validated integer, never raw config text.
- DOS: the strict_order iterative scan is bounded in WORK by
  hnsw.max_scan_tuples (server-side) and in TIME by statement_timeout
  (now armed on both callers). No new unbounded loop, no LLM-call
  amplification, no event-loop blocking surface introduced.
- AUDIT-EVASION: the diff touches no audit-log write path; the probe
  is a read-only SELECT.

Connection hygiene: lookupCommand now does setAutoCommit(false)
(CommandIntentIndex.java:150) before enableIterativeScan. For the
tool caller this is a no-op (armToolConnection already flipped
autocommit off); for the delivery caller it is a redundant no-op
(applyStatementTimeout already flipped it). The probe is read-only
and never commits — the transaction (with both SET LOCAL GUCs) dies
at pool release, matching SemanticSearchTool's reference shape cited
in the diff's own commentary.

Test additions: CommandIntentIndexIT adds
commandRecallSurvivesForeignKindInterleaving (the retrieval proof)
and armingOutsideTransactionIsSilentNoOpAndRealPathRecovers (the
no-op-trap pin). Test isolation is preserved via deleteForeignKindRows
in @BeforeEach/@AfterEach; planner pinning (enable_seqscan/enable_sort
off, hnsw.ef_search) is RESET in the finally block, so no GUC drift
across tests. The four ChatAgent test constructors that gained a
trailing null (CancellationService) are unused on the
lookupIntentForDelivery path — no production impact (the dependency is
always injected in prod via the new constructor parameter).
