RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: DOS
    SEVERITY: low
    PROMISE: docs/spec/security.md §Failure handling, Provider-side LLM
             failures: "'/summary (and /retry --digest) summarizer
             unreachable' → fall back to the headlines + URLs + post UIDs
             degraded form (the same fallback as a saturated periodic
             digest per decision D17). No prose, deterministic post
             selection unchanged. The friendly notice is a
             localization-bundle string (D43); the user is not shown a
             hung response."
    GAP: The r1 remediation closes the honesty half of this promise for
         --short (the D43 REPLY_SUMMARY_DEGRADED_NOTICE now rides on both
         the /summary --short path and the /retry short replay when
         ShortResult.anyRollupMissing() is true — SummaryCommandHandler
         --short branch and RetryCommandHandler 'short' arm in this diff),
         and the user is not shown a hung response. But the fallback-FORM
         half is still undelivered: on a total summarizer outage the
         --short reply contains zero post content — no headlines, no URLs,
         no post UIDs. DigestRenderer.renderShortBody ships each failed
         category as header + footer only (the rollup.isPresent() else
         branch just sets anyRollupMissing and appends the footer), and
         neither the handler's --short branch nor the /retry 'short' arm
         composes the D17 degraded prose the other three forms get through
         SummaryProseGenerator's degraded path. The diff's own
         docs/spec/commands.md re-states the generic "Summarizer LLM
         unreachable → /summary falls back to the same degraded form as a
         saturated periodic digest (decision D17): headlines + URLs +
         post UIDs" clause unamended, with no --short carve-out, so the
         commitment stands over the new surface while the delivery does
         not match it. Low because the deceptive-silence core of the r1
         finding is fixed (the notice fires) and post SELECTION stays
         deterministic; what remains is a content-fidelity gap in the
         failure path, not a hang or a dishonest reply.
    REPRO: Register, subscribe a source with READY posts, then take the
           SUMMARIZER endpoint down (or force every roll-up reply
           unparseable/REFUSAL) and run /summary --short. Every
           generateRollupUnconditional call yields Optional.empty; the
           delivered message is the degraded notice plus bare category
           headers plus "/summary <tag> --full" steering footers — the
           promised headlines + URLs + UIDs degraded form never appears.
           Same on /retry against a render_form='short' anchor during the
           outage.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - --short skips the summarizer-post-cap gate (SummaryCommandHandler:
    `args.form() != SHORT && result.posts().size() > summarizerPostCap`),
    so one invocation drives one CategoryRollupGenerator call per category
    — bounded only by infochat.summary.cluster-cap and the qualifying-tag
    threshold, not by the 50-post ceiling the other three forms degrade
    behind — with each roll-up prompt concatenating every cluster's
    title/body/url. This exceeds the per-invocation LLM-call-count and
    prompt-size envelope the gate gives bare/full/flat, but it violates
    no explicit commitment: §Rate limiting bounds the RATE of
    LLM-triggering operations and --short still draws the per-user bucket
    and holds the in-flight slot, and no spec text bounds summarizer
    prompt magnitude or per-invocation call count. Both r1 auditors
    dispositioned this identically (accepted out-of-model); flagged again
    only so the operator can decide whether to extend the model to bound
    per-invocation LLM fan-out or roll-up input size.
