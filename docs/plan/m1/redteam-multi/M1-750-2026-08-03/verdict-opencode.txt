RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - DOS-adjacent: ingest translation-cost amplification via a non-admin user's --lang declaration. The diff lets any registered, non-probation DM user declare `--lang cs` on a user-chosen feed (AddSourceArgs.java:159-175, AddSourceCommandHandler.java:219-222, SourceUpsertService.java:127-143), which flips on per-post LLM translation of that feed in the M1-749 ingest pipeline (V74's translation_done cursor). Before M1-750 no source could be declared non-English, so this is a new user-influenced LLM-cost surface on the Collector. docs/spec/security.md §Rate limiting commits only to provider-side buckets; no commitment bounds collector-side per-source translation spend, so this is not a FINDING — but the marginal cost is real (one translation call per post of a user-chosen high-volume feed, bounded only by the per-user /add-source bucket and feed volume) and the declaration persists globally on a D7-global source row that only operator bootstrap re-list can correct. Decide whether the model should bound it (e.g. per-source translation cap, or --lang restricted to bot-admins / bootstrap).
  - Hardening: the raw `--lang` token has no length bound at parse (same class as every other inbound token — AddSourceArgs.resolveLang, AddSourceArgs.java:217-225, runs one O(n) lower-case pass before the closed-set rejection) and the bootstrap parser's resolveLanguage (BootstrapSourcesParser.java:157-169) lower-cases but does not trim or NFKC-normalize its token (the provider path trims), so a bootstrap entry declaring `" cs "` or a fullwidth code fails startup rather than defaulting. Both fail closed and neither input is attacker-influenced in a way the transport rate cap does not already bound; flagging the asymmetry only so the operator corpus is not surprised by the stricter bootstrap side.
