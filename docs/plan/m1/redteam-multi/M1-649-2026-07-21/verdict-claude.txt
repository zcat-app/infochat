RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Provider-role corpus tampering via a hypothetical SQL-injection foothold: V60 already grants the Provider role INSERT + DELETE on `doc_embedding` (pre-existing, M1-664; this diff adds no grant), so a Provider SQLi foothold could insert/delete `doc_kind='topic'` rows or forge a row whose `content_hash` matches the builder's expected hash, making a poisoned vector survive the content-hash skip across restarts (TopicCorpusBuilder.buildCorpus, diff lines ~915-923). Impact is bounded by the match-not-assert design the diff correctly delivers — `lookupTopic` returns only a slug (CommandIntentIndex.lookupTopic, diff lines ~344-371) and `HelpTopicCorpus.byTargetRef` resolves only in-memory curated topics — so the worst case is match-steering/denial among the ten curated answers, never attacker-chosen served text. The DB is internal per the threat model ("The DB is internal — only the two services and the operator reach it"), and the DB-roles section already accepts this table's write grant, so this is noted, not counted.
  - Tier-flat is test-enforced, not lookup-time enforced: `lookupTopic` deliberately binds no `target_ref = ANY(?)` visibility set (unlike `lookupCommand`), resting entirely on `HelpTopicCorpusTest.noTopicReferencesAdminSurface`, whose pin is slash-command literals of the BOT_ADMIN tier only. A future topic that discusses admin-tier CONCEPTS (e.g. the SimpleX admin-token bootstrap flow) without naming a `/command` would pass the guard. No current topic does this, the corpus is curated in-code reviewed copy, D68 explicitly defers admin-tier conceptual answers to "a separate ticket with their own threat review", and the threat-model promise (invisible command NAMES never enter model context) is not literally violated — so this is a forward-hardening note for the M1-666 delivery ticket, not a finding against this diff.
  - Probation-duration literal baked into bundle copy: `topic.probation.answer` states "24h" and the guard test pins it to the `infochat.probation.duration` value in the checked-in main `application.properties`, not to the runtime-effective config. An operator overriding the property at deploy time (env var, runtime properties file) gets a factually wrong served answer with a green build. Accuracy drift, not a security-property violation (the lazy `probation_until` permission check is unaffected); flagging so the user can decide whether M1-666 should compose the duration at delivery time instead.
