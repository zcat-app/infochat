[0m
> threat-actor · glm-5.2
[0m
[0m→ [0mRead .claude/agents/threat-actor.md
[0m→ [0mRead docs/plan/m1/redteam-multi/M1-660-2026-07-20/prompt-opencode.txt
[0m→ [0mRead docs/spec/security.md
[0m→ [0mRead docs/plan/m1/redteam-multi/M1-660-2026-07-20/diff.patch
[0m→ [0mRead docs/spec/security.md[90m [offset=696][0m
[0m→ [0mRead docs/spec/security.md[90m [offset=1566][0m
Now let me look at the actual callers of `lookupCommand` to understand whether the connection-state change is handled properly, since the diff modifies the contract of `lookupCommand` (it now opens a transaction and doesn't close it).
[0m✱ [0mGrep "lookupCommand"[90m in infochat-provider · 13 matches[0m
[0m→ [0mRead infochat-provider/src/main/java/app/zcat/infochat/provider/help/CommandIntentIndex.java
Now let me examine the two callers to see how they handle the connection after `lookupCommand` returns (since the method now opens a transaction but doesn't commit/rollback).
[0m→ [0mRead infochat-provider/src/main/java/app/zcat/infochat/provider/chat/ChatAgent.java[90m [offset=740, limit=90][0m
[0m→ [0mRead infochat-provider/src/main/java/app/zcat/infochat/provider/chat/tool/HelpLookupTool.java
Let me verify the `CancellationService.armToolConnection` behavior (since the diff claims HelpLookupTool's connection is already autocommit-off via this path, and statement_timeout arming depends on transaction state).
[0m→ [0mRead infochat-provider/src/main/java/app/zcat/infochat/provider/chat/CancellationService.java
Let me check where `lookupIntentForDelivery` is called from and whether it runs on a worker thread with /stop cancellation, plus verify the `SemanticSearchTool` reference shape that the diff claims to mirror.
[0m✱ [0mGrep "lookupIntentForDelivery"[90m in infochat-provider/src/main/java · 3 matches[0m
[0m✱ [0mGrep "enableIterativeScan|setAutoCommit|applyStatementTimeout|armToolConnection"[90m in infochat-provider/src/main/java/app/zcat/infochat/provider/chat/tool · 11 matches[0m
[0m→ [0mRead infochat-provider/src/main/java/app/zcat/infochat/provider/chat/ChatAgent.java[90m [limit=90, offset=410][0m
[0m→ [0mRead infochat-provider/src/main/java/app/zcat/infochat/provider/chat/tool/SemanticSearchTool.java[90m [offset=80, limit=50][0m
I now have a complete picture. Let me consolidate my analysis:

**The change (M1-660):** In `CommandIntentIndex.lookupCommand`, before the pgvector probe:
1. `conn.setAutoCommit(false)` — opens a transaction (no-op if caller already opened one)
2. `enableIterativeScan(conn)` — `SET LOCAL hnsw.iterative_scan = strict_order`

This closes the "silent no-op" trap where `SET LOCAL` on an autocommit connection expires before the query runs.

**Callers and their arming posture:**
- `HelpLookupTool` (model-elected tool): calls `cancellationService.armToolConnection` BEFORE `lookupCommand` → `applyStatementTimeout` does `setAutoCommit(false)` + `SET LOCAL statement_timeout`. So `lookupCommand`'s `setAutoCommit(false)` is a no-op, `SET LOCAL iterative_scan` joins the same transaction, and **statement_timeout is armed**. Matches the SemanticSearchTool reference shape.
- `ChatAgent.lookupIntentForDelivery` (deterministic M1-665 delivery trigger): borrows a **bare** connection, does NOT call `armToolConnection`. So `lookupCommand` opens the transaction and arms iterative_scan, but **statement_timeout is NOT armed** on this path.

**Threat-model commitments I checked against:**
1. **helpLookup tier filter / existence-oracle** ("an invisible command's name never enters the model context" via `target_ref = ANY(?)` INSIDE the WHERE) — untouched; iterative_scan only improves recall over the already-filtered set, cannot surface invisible commands.
2. **D19 determinism** ("same DB state + same message → same set/order") — preserved: `strict_order` keeps distance-ascending order.
3. **Match-not-assert invariant** (description composed at call time from runtime catalogue, never from indexed text) — untouched.
4. **D54 local backend** — untouched (no embedding change).
5. **§Failure handling pre-fetch bounding** ("capped in time (the pgvector probe by `statement_timeout`...)") — this commitment is explicitly scoped to the D28 semanticSearch pre-fetch. lookupIntentForDelivery is a separate (M1-665) pre-fetch the spec doesn't name; its lack of statement_timeout is a pre-existing condition M1-660 neither creates nor violates a commitment by leaving.

The one observable effect of M1-660 on the lookupIntentForDelivery path: the probe goes from non-iterative (bounded by `ef_search` default 40 candidates) to iterative (bounded by `hnsw.max_scan_tuples` default 20k), still without statement_timeout. This is a defense-in-depth amplification of a pre-existing gap, but no threat-model commitment is failed since the spec's statement_timeout promise names only the D28 pre-fetch. The probe is still bounded by max_scan_tuples and the per-user LLM rate bucket.

No AUTH-BYPASS, INFO-LEAK, INJECTION, PERM-ESCAL, or AUDIT-EVASION exposure introduced. Verdict: CLEAN, with one OUT-OF-MODEL observation.
[0m
[0m← [0mWrite docs/plan/m1/redteam-multi/M1-660-2026-07-20/verdict-opencode.txt
Wrote file successfully.
[0m
RED-TEAM VERDICT: CLEAN
Verdict file: /home/infochat/infochat/.claude/worktrees/M1-660/docs/plan/m1/redteam-multi/M1-660-2026-07-20/verdict-opencode.txt
Findings: critical=0 high=0 medium=0 low=0
Out-of-model: 1
