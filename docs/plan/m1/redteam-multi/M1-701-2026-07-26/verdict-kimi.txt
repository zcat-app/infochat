RED-TEAM VERDICT: CLEAN

SCOPE NOTE: The diff (M1-701) is a presentation-only change to the periodic
group digest's capped-section overflow line: instead of steering readers to
@mention the bot, a capped real-category section now steers to
`/summary <tag> --full` and a capped Other section (tag == null) steers to
bare `/summary --full`. Touchpoints: DigestRenderer.java (key selection on
section.tag() == null, MessageFormat args (overflow, section.tag())),
BundleKeys.java (new REPLY_DIGEST_CATEGORY_MORE_OTHER constant), cs/en
bundle strings, spec text (commands.md, decisions.md D62 row), tests, and
ticket/STATUS bookkeeping. The sensitive-surface inventory in the audit
prompt (invite/ban/admin bundle keys) is context-line noise from
BundleKeys.java; the diff touches none of that logic.

COMMITMENTS CHECKED (docs/spec/security.md), with the diff as the only
evidence of behavior:

1. §LLM output sanitizer — deterministic-output exemption ("bot-authored —
   interpolates no inbound-derived text (parse-validated echoes ... count
   as bot-authored)"). The overflow line interpolates two values: {0} an
   integer cluster count, and {1} the section's category tag. The tag is
   the category's raw CONTROLLED-VOCABULARY tag (closed Tier-1 set per the
   tool-allowlist table and the D62 tag-arithmetic design) — a
   parse-validated echo, not a raw attacker token — and the identical value
   was already rendered pre-diff in the section header
   (`reply.digest.category.header={0} news`). No new reflection surface is
   introduced; the closed-list `reply.*`-echo residual the spec flags does
   not worsen here.

2. §LLM output sanitizer — delivery-ordering contract. The emission
   decision (overflow > 0, computed by deterministic tag arithmetic) and
   the content (fixed bundle text + count + controlled-vocab tag) are both
   deterministic end-to-end; no model-elected tool call decides whether
   the line is delivered. Path (a) of the contract is satisfied.

3. §LLM output sanitizer — match-set derivation / social-engineering
   surface. The newly emitted command string `/summary <tag> --full` is a
   NON-privileged command (it is on the probation-ALLOWED read-only list
   in §Slow-start tier); it is not in the privileged-tier closed list the
   sanitizer is derived from, and its dispatch requires no is_admin /
   is_group_admin bit. A reader copy-pasting it cannot reach any elevated
   functionality, so emitting it verbatim in a digest creates no
   admin-command social-engineering channel.

4. §Rate limiting / DOS. The line invites every digest reader to fire an
   LLM-triggering operation (`/summary --full` renders uncapped). That
   cost is inside the documented model: `/summary` draws from the
   per-user LLM-triggering bucket with the per-group LLM sub-bucket as
   backstop, and the pre-diff line steered to @mention (chat mode), which
   draws from the same bucket class. No unbounded-cost path is created.

5. INFO-LEAK. The "+N more" count of capped clusters was disclosed
   pre-diff; the change alters only the affordance text, not what is
   revealed. `/summary` in group scope runs under the D59 world predicate
   (scope-filtered retrieval), unchanged by this diff.

6. Auth gates, ban handling, invite-code registration, audit coverage,
   DB-role surface: untouched by the diff. The null-tag branch correctly
   avoids rendering the literal "null" (asserted by the new test
   overflowForOtherBucketSteersToBareSummaryFull), and bare
   `/summary --full` is the documented valid form for the Other bucket.

No commitment in the threat model is unmet by this diff.

OUT-OF-MODEL: none.
