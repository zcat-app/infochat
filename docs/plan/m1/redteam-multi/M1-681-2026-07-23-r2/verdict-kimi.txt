RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - The MessagingAdapter.connected() interface default of true persists in
    this tree: a FUTURE transport adapter that forgets to override it would
    silently report transport-up forever, reintroducing the false-green
    readiness payload this change removes, with no compile-time or test
    signal. Both v1 production adapters override (SignalAdapter delegates to
    the client's now-latched isConnected(); SimpleX reports its own rebuild
    latch), so there is no current breach, and trust boundary 6 already
    accepts the health surface's topology disclosure with a loopback default.
    Flagged in round 1 and already tracked as M1-682 (the ticket is filed in
    this same diff) — restated only because the hazard stays live until that
    build guard lands; no new decision is needed.
