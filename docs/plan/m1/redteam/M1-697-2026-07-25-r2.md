---
target: M1-697
date: 2026-07-25
round: 2
base: 3a655d5681b26de2214fda232a2ca6a3d778a9fe
head: working-tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Re-audit after remediating round 1. The round-1 low (render-time
  redaction replaced by a provenance assumption on ClusterProse.degraded)
  is CLOSED by derive-at-render: all three render arms
  (DigestRenderer.renderSummarySections, DigestRenderer.renderSections,
  ClusterBlockRenderer) now obtain degraded prose via
  SummaryProseGenerator.degradedProseFor(cp.cluster(), sanitizer) and
  ignore the record's prose bytes, so a hand-assembled degraded
  ClusterProse cannot smuggle unsanitized titles onto any render path.
  The re-audit prompt carried the round-1 finding verbatim with explicit
  do-not-assume-closed framing and an explicit CLEAN authorization.
---
RED-TEAM VERDICT: CLEAN

Round-2 verification notes (no findings):

- The round-1 INJECTION/low finding (renderers trusting cp.prose() on a
  public, caller-populated ClusterProse record) is genuinely closed, and
  closed structurally as the finding demanded. Verified sentence by
  sentence against the code:
  - DigestRenderer.renderSections (diff hunk at DigestRenderer.java:127-141)
    replaces `sb.append(cp.prose())` in the degraded arm with
    `SummaryProseGenerator.degradedProseFor(cp.cluster(), llmOutputSanitizer)`.
  - DigestRenderer.renderSummarySections (DigestRenderer.java:232-252)
    replaces the `sanitize(cp.prose())`-then-append assembly with the same
    derive-from-cluster call for the degraded arm; the non-degraded arm
    still sanitizes one LLM-authored value from the record, the correct unit.
  - ClusterBlockRenderer.appendClusterBlock (ClusterBlockRenderer.java:78-125)
    derives via `degradedProseFor(cluster, llmOutputSanitizer)`; the
    `cluster` variable is assigned from `cp.cluster()` at line 79, so the
    derivation is anchored to the record's own cluster — no pairing seam.
  - A repo-wide census of `.prose()` readers in infochat-provider main
    sources shows exactly three remaining reads, all on NON-degraded arms
    (DigestRenderer.java:139, DigestRenderer.java:252,
    ClusterBlockRenderer.java:125), each behind an immediate
    `llmOutputSanitizer.sanitize(...)`. No degraded render path reads the
    record's prose bytes anywhere. The trust is now mechanical:
    `degradedProseFor` requires an LlmOutputSanitizer parameter
    (compile-enforced), so a future caller cannot compose degraded prose
    without per-title sanitization, and hand-assembled record bytes are
    ignored at render — the exact attack shape round 1 named (a /retry
    anchor replay or new caller building ClusterProse from stored fields)
    now renders the cluster's own sanitized titles, which the new
    DigestRendererTest pins with deliberately lying prose fixtures.
  - The accepted cross-field split residual (/list-sources in one title,
    --all in another: neither redacted nor audited) is stated in the diff's
    own security.md amendment and ticket body as a deliberate accept
    bounded by dispatch requiring is_admin=true; it is part of the amended
    threat model, not a gap against it.
  - Double audit emission (one LLM_OUTPUT_SANITIZED row at producer
    composition, one at renderer derivation) is truthful per-occurrence
    accounting, pinned at exactly 2 rows by
    fullFormRedactsAndAuditsCommandShapedTitleOnNonFirstPost. Emission
    frequency did not drop for any token within one field; the render
    paths that generate rows sit behind the per-user/per-group LLM rate
    buckets, so the extra row per render is not an unbounded audit-spam
    vector.
- The new security.md section "Bare feed URLs rest on the ingest scheme
  allowlist" (round 1's out-of-model item, now pulled in-model) was
  spot-verified against the claimed mechanism:
  PostPersister.normalizeUrlForStorage (infochat-collector/.../outbox/
  PostPersister.java:244-258) strips obfuscation codepoints and binds the
  url only when it parses as an http/https URI, storing NULL otherwise —
  the spec's claim is accurate, so the bare `title — url (uid)` operand
  that no sanitize pass sees is constrained at the write boundary as
  documented.
- Remediation-delta surface checked: producer wiring (SummaryProseGenerator
  injected sanitizer; SummaryCommandHandler:455 passes the handler's real
  sanitizer field), static signature change making the sanitizer
  non-omittable, test-double wiring confined to test sources, and
  byte-for-byte ClusterBlockRendererTest expectations updated to the
  derived composition rather than weakened. No new trust assumptions,
  unvalidated inputs, auth-gate changes, or audit gaps introduced.
