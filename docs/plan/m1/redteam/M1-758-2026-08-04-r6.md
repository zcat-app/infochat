---
target: M1-758
date: 2026-08-04
base: 29aa59afbd80e13df6265e6934c77f1a4a06ffe6
head: "<working tree>"
verdict: FINDINGS
findings_count: {critical: 0, high: 0, medium: 1, low: 1}
out_of_model_count: 2
disposition: |
  Round 6. All thirteen prior findings confirmed closed; the structural
  claim (security.md enumerates every production ModelTask.TRANSLATOR call
  site) re-derived independently a THIRD time and VERIFIED TRUE.

  Both new findings were introduced by the round-5 remediation itself, and
  both are the same error: text was rendered from `docs/spec/llm.md`
  §D29 display-leg amendment WITHOUT checking that section against the
  code. Verified in-branch before accepting: `finishDisplayHit`
  (TranslationPipeline:341-350) returns `bounded + " " + marker` — the
  translation REPLACES the headline and the only bracket is the
  `[machine translated]` marker on the SAME line, so "the original stays
  on a bracketed line beneath" was false; and `runForDisplayHit:269-272`
  passes `Locale.of(sourceLanguage)`, with zero Provider reads of
  `title_en`/`body_en`, so "from the English anchor field" was false.
  Both remediated: USER_GUIDE.md:124 and §6, and design 05:932-940 now
  describe the SHIPPED render (translation replaces the headline, carries
  the marker; over-budget and degraded rows render untranslated AND
  unmarked, so an untagged line means "not translated", NOT "already in
  your language").

  OUT-OF-MODEL 1 RE-DISPOSITIONED — the auditor is right that no shipped
  code implements llm.md:303-317, but it is NOT a spec defect. M1-759
  ("Anchor-first headline display: reader-language line, bracketed
  original beneath", status pending, security_relevant, complexity high)
  is the ticket that implements exactly that render. The spec was amended
  ahead of its implementing ticket; this diff's error was reading a
  target-state spec as current behaviour. No action here beyond describing
  shipped behaviour, which is done. NOTE FOR M1-759: its files_scope is
  provider Java + tests only and contains no docs, so USER_GUIDE.md §6 and
  design 05's translated-list — both corrected here to the pre-M1-759
  render — go stale the moment it lands.

  OUT-OF-MODEL 2 (no end-user disclosure surface exists; §Secrets handling
  designates only operator-facing surfaces, while `/lang` is what a USER
  activates) left for the user to decide — it is a threat-model scope
  question, not a defect in this diff.
---
RED-TEAM VERDICT: FINDINGS

PRIOR-FINDING VERIFICATION (re-verified against the working tree; NOT re-reported)
  r1(a) CLOSED — prod/switch-llm.sh:410-418 and prod/scripts/4-llm.sh:514-522 now
        carry "Regardless of /lang, even if every scope is English"; the "strict
        no-op"/"sends nothing" wordings are barred by SwitchLlmWiringTest:434-439.
  r1(b) CLOSED — the reply-prose leg is enumerated at docs/spec/security.md
        §Secrets handling ("translator also carries the bot's own REPLY prose")
        and as SETUP_GUIDE.md's first leg-by-leg bullet.
  r1(c) CLOSED — SETUP_GUIDE.md:568-572 puts chat AND translator in the loud tier.
  r2(d) CLOSED — prod/scripts/4-llm.sh:514-522 carries the same two-fact block.
  r2(e) CLOSED — SETUP_GUIDE.md:559-561 ("asks once ... every AI task") and
        prod/scripts/4-llm.sh:482-484 both corrected.
  r3(f) CLOSED — no "decline its consent prompt" text survives; verified against
        prod/switch-llm.sh:288-308 (bare-Enter refusal only) and :348-355
        (unconditional pin delete). Both disclosures state this correctly.
  r3(g) CLOSED — OVERVIEW.md:287-296 names translator.
  r3(h) CLOSED — both assertFalse sets target wording that is actually false
        (tier marker, "one at a time", "sends nothing"), never a live exposure.
  r3(i) CLOSED — 4-llm.sh:510-513 carries the BodySummaryWorker leg; pinned by
        RemoteLlmWiringTest ("abstracts of EVERY long fetched PUBLIC post").
  r4(j) CLOSED — prod/scripts/4-llm.sh:487-491 now denies the fabricated safeguard.
  r4(k) CLOSED — decisions.md D57 and design 05:1102 both list translator.
  r5(l) CLOSED for the D29 strong form — USER_GUIDE.md no longer says "original
        posts aren't translated". The replacement text is Finding 1 below.
  r5(m) CLOSED — TranslationProvider.java javadoc is now accurate, and is the one
        copy in the diff that describes the display leg's input correctly.

  Structural claim re-derived independently and VERIFIED TRUE: docs/spec/security.md
  §Secrets handling enumerates every production ModelTask.TRANSLATOR call site.
  Grep over src/main returns exactly IngestTranslationWorker:274/318,
  LlmTranslationProvider:93/94 (fed only by TranslationPipeline.run at
  ChatAgent:558, ClusterBlockRenderer:166, DigestRenderer:552/825,
  CategoryRollupGenerator:213, and runForDisplayHit at ClusterBlockRenderer:132,
  DigestRenderer:886, SavedCommandHandler:453/457) and QueryAnchorTranslator:259/260.
  All are enumerated. No new leg.

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: docs/spec/security.md §Trust boundaries item 9 — "**LLM/embeddings
             provider response -> system.** Everything a generative or embeddings
             endpoint returns is endpoint-chosen input, not a trusted internal
             value: whatever answers on the configured base-url picks every field
             of the reply, so a hostile or compromised endpoint is in scope here
             for the same reason a hostile feed is on the ingest side."
             Paired with §Rate limiting, "/saved display-hit translation (M1-755)":
             "rows beyond the budget render untranslated, unmarked, and a rejected
             draw degrades the page's cache-miss rows the same way", and the same
             wording for the digest budget.
    GAP: The diff replaces one false claim in the only end-user-facing document
         with two new ones, both about a reader-side control that does not exist.
         - USER_GUIDE.md:124 (added): "each original headline stays visible on a
           bracketed line beneath".
         - USER_GUIDE.md:299-301 (added): "Each original headline stays visible on
           a bracketed line beneath it, so the publisher's own words are always
           reachable; a line with no brackets is already in your language."
         - docs/design/05-llm-and-embeddings.md:932-933 (added): "the original
           headline stays on a bracketed line beneath".
         No render emits an original-headline line. The display-hit leg's only
         output shape is
         infochat-provider/.../translation/TranslationPipeline.java:341-350
         (`finishDisplayHit`): `return bounded + " " + marker;` — the translation
         REPLACES the publisher's headline and the only bracketed token is the
         machine-translation MARKER (`[machine translated]`, en.properties:842;
         `[strojovy preklad]`, cs.properties:550) appended to the SAME line. All
         three call sites render one line per post: ClusterBlockRenderer:130-134,
         DigestRenderer:886-897, SavedCommandHandler:452-476. The shape is pinned
         by the shipped test DisplayHitTranslationTest:145-146
         (`assertEquals("prelozeny titulek " + marker("cs"), result, "translated
         headline must carry the machine-translation marker, space-separated")`).
         The second sentence is false with no adversary at all: an unbracketed line
         is the DOCUMENTED degraded shape. SavedCommandHandler:455-459 stops calling
         `runForDisplayHit` once `translationBudget` (infochat.save.
         translation-max-per-page, default 5) is spent, and sets it to 0 outright
         when the per-user LLM token or the D47 group sub-bucket is refused
         (:442-447); DigestRenderer:882-888 does the same against
         infochat.digest.translation-max-per-render (default 5); a degraded cluster
         skips the leg entirely (ClusterBlockRenderer:123-131, its own comment:
         "untranslated, unmarked"). Every one of those rows renders with no bracket
         and is NOT in the reader's language.
         Same-diff self-contradiction confirming the claim was never checked
         against the code: TranslationProvider.java:14-19 (added by this diff)
         correctly says the display leg is handed "the post's own title, or a
         bounded prefix of its body", while design 05:932 (added by the same diff)
         says the display translation runs "from the English anchor field".
    REPRO: 1. Default deployment. V74 gives `source.language NOT NULL DEFAULT 'en'`,
              so bootstrap sources are 'en' and the display leg is live for any
              non-English reader.
           2. A user sets `/lang cs` and runs `/saved` over a page holding more
              than 5 uncached rows.
           3. Rows 1-5 render `<Czech headline> [strojovy preklad]`. Rows 6-20
              render the raw English headline with no bracket. No line on the page
              carries an original beneath it, and every unbracketed line is in
              English.
           4. The user consults USER_GUIDE.md:299-301 — the only end-user document
              — and is told the unbracketed lines are already in their language and
              that the publisher's own words are always reachable beneath. Both are
              false about every line they are looking at.
           5. Adversarial leg, in scope under trust boundary 9 and reachable
              precisely by the switch this ticket's disclosures govern: with
              `translator` routed remote, whatever answers on
              `infochat.llm.default.base-url` chooses the bytes of every translated
              headline. Those bytes are delivered under the publisher's attribution
              line ("covered by: <source display name> (uid p-...)",
              ClusterBlockRenderer:137-142) with `[machine translated]` appended,
              and the reader — told the original stays reachable beneath — has no
              in-band way to compare against the publisher's text. The marker is
              the only provenance signal on this surface and the guide teaches it
              backwards (brackets read as "the original", absence of brackets read
              as "already yours").
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling (as amended by this diff) —
             "The enumeration above covers every production call site reaching
             `ModelTask.TRANSLATOR` as of M1-758. It is a snapshot, not a
             guarantee: a new call site is a new leg, and the disclosure texts ...
             are corrected against THIS section, so a leg missing here propagates
             into both." The same section states what the display leg sends: "each
             rendered headline — the retrieved post's snapshot `title`, or its
             `body` prefix when the title is empty — reaches `ModelTask.TRANSLATOR`,
             which may be remote."
    GAP: docs/design/05-llm-and-embeddings.md:932 (added by this diff) states that
         post headlines are translated at display time "from the English anchor
         field (D29 display-leg amendment 2026-08-04)". That is false, and it
         contradicts the authority the same diff corrects. The Provider never reads
         the anchor columns: a grep for `title_en|body_en` over
         infochat-provider/src/main returns exactly one hit, a comment in
         LlmOutputSanitizer.java:39. `DisplayHeadline.of` (DisplayHeadline.java:108-166)
         reads `post.title()` / `post.body()` — the ORIGINAL columns — and
         `TranslationPipeline.runForDisplayHit:269-272` calls
         `translationProvider.translate(displayHeadline, Locale.of(sourceLanguage),
         Locale.of(scopeLanguage))`, i.e. FROM the source language, not from
         English. Pinned by the shipped test DisplayHitTranslationTest:160-162
         (`assertEquals("es", translatorStub.lastFrom().getLanguage())`).
         What actually crosses to a possibly-remote endpoint at display time is
         therefore the publisher's original bytes: up to 200 chars of `post.title`,
         or for an empty-titled post a body prefix (DisplayHeadline
         BODY_SCAN_LIMIT = 2000, cut to MAX_LENGTH = 200) — not derived,
         already-scrubbed English.
    REPRO: 1. An operator sizing a remote-routing decision follows the chain this
              ticket builds: security.md §Secrets handling -> design 05
              §"What is and isn't translated".
           2. design 05:932 tells them the display leg reads the English anchor
              field, so they conclude the raw publisher text never leaves at
              display time and only the ingest-derived English does — i.e. that
              the display leg adds no new data class over the ingest leg they
              already consented to.
           3. In the shipped code the original source-language title (or body
              prefix) of every rendered post is what goes to the endpoint, for
              every non-English reader, on `/saved`, `/summary`, `/retry` and every
              scheduled digest.
           4. Because the disclosure texts are rendered from this chain, the next
              author rendering from design 05 writes an understated disclosure —
              the exact "a leg missing here propagates into both" propagation the
              amended §Secrets handling sentence exists to prevent.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - Pre-existing, NOT introduced by this diff, but it is the upstream authority
    both false renderings above were copied from, so correcting only the copies
    will regenerate them. `docs/spec/llm.md:303-317` (the D29 display-leg
    amendment) and `docs/spec/verification.md:469-477` specify a render that no
    shipped code implements: (a) "from the English field directly when the reader
    is English (a column read — no translator call)" — for an `en` scope
    `runForDisplayHit` short-circuits at TranslationPipeline.java:245-251 and the
    English reader is shown the ORIGINAL foreign-language headline, and no Provider
    query selects `title_en`/`body_en`; (b) "The display translator's source is
    therefore always English" — the from-locale is `Locale.of(sourceLanguage)`
    (:271); (c) "The original headline remains visible on a bracketed line beneath
    it; an unbracketed line always means the text is already in the reader's
    language" — falsified above. This diff's verification.md hunk retires the old
    first clause and elevates these four sentences to "the corrected doctrine"
    without checking them, which is how they reached USER_GUIDE.md. Flagged here
    rather than as a finding because the text predates this diff.
  - The end-user has no designated disclosure surface at all. §Secrets handling
    names `prod/switch-llm.sh` Phase 4 and `SETUP_GUIDE.md` §"Switching your AI
    backend later" — both operator-facing. A user typing `/lang cs` is the person
    who activates six of the seven translator legs, including the one that sends
    their own raw chat message (truncated, not redacted) and their bookmark set's
    headlines to a possibly-remote endpoint, and USER_GUIDE.md §6 (as rewritten
    here) discloses only the INGEST leg's post text. That is a gap in the threat
    model's disclosure mandate rather than a violation of it, so it is not counted
    as a finding; the user may want to decide whether §Secrets handling should
    designate an end-user surface.
