---
target: M1-758
date: 2026-08-04
base: 29aa59afbd80e13df6265e6934c77f1a4a06ffe6
head: "<working tree>"
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 1
out_of_model_count: 1
disposition: |
  Round 2, a RE-AUDIT of the rewritten disclosure (round 1's three findings
  were fixed by rewriting, not patching). All three round-1 findings are
  closed and were not re-reported. Both new findings survived a falsification
  pass. R2-1 (medium): prod/scripts/4-llm.sh:485-500 is a SECOND runtime
  privacy disclosure, printed at INSTALL time before the operator types the
  remote URL/key, still carrying the exact pre-M1-746 translator line this
  diff deleted from switch-llm.sh, with chat as the sole loud tier. It is the
  earlier and, for an operator who picks remote at install and never runs the
  switcher, the ONLY disclosure they ever see. Pre-existing; needs a 5th
  files_scope entry. R2-2 (low): SETUP_GUIDE.md:556-558 claims the switcher is
  per-task ("Press Enter to keep a task as-is") — false since M1-603/D56
  (switch-llm.sh:162 prompts once for ALL generative tasks) — and this diff
  ADDED a contradicting sentence 47 lines below at :603-605 that bases
  mitigation advice on the true behaviour. The contradiction is
  self-inflicted and is in-scope to fix.

  CENSUS (run at this round because two audit passes found DIFFERENT file
  sets, the documented decompose trigger). Operator-facing "what leaves your
  machine" surfaces, 7 spots / 4 files; this diff corrects 2:
    1. prod/scripts/4-llm.sh:485-500  install-time disclosure       WRONG
    2. prod/scripts/4-llm.sh:482      "switch-llm.sh ... one at a time" WRONG
    3. prod/switch-llm.sh:411+        switch-time disclosure        FIXED here
    4. SETUP_GUIDE.md:564             switch-section tier line      FIXED here
    5. SETUP_GUIDE.md:556-558         "asks for each AI task"       WRONG
    6. SETUP_GUIDE.md:287,315-319     install-time remote choice    INCOMPLETE
    7. README.md:215-221              omits translator; claims the wizard
                                      "spells out exactly what each task
                                      exposes" (it does not — that is #1) WRONG
---
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: docs/spec/security.md §Secrets handling — "**`translator` carries
              private user messages too (M1-746, D58).** ... For a scope on a
              non-English `/lang`, routing `translator` to a remote provider
              therefore exposes private user messages, exactly as `chat` does —
              it is not the bot-prose-only exposure the presentation and ingest
              translation legs imply." And, in the same section (added by this
              diff): "**No disclosure may state or imply that an `en`-only
              deployment sends nothing through `translator`.**" That rule is
              written unqualified — it binds every disclosure, not only the two
              texts the closing sentence happens to enumerate.
    GAP: The diff corrects two of the THREE operator-facing privacy disclosures
         in the repo. The third — and the earliest one an operator meets — is the
         initial-setup wizard, `prod/scripts/4-llm.sh:485-495`, and it still
         carries the pre-M1-746 wording verbatim:
           :487  `  !! chat — YOUR PRIVATE MESSAGES to the bot are sent ...`
           :493  `  -  translator — translation of the bot's replies; exposes the
                     bot-reply text (can echo your queries).`
         `chat` remains the sole `!!` (private) tier and `translator` sits in the
         `-` tier the block itself defines as "your source list / topic
         interests, not private user data" (:491). This is byte-for-byte the line
         the diff deleted from `prod/switch-llm.sh:430` precisely because it
         understates the surface, and the wizard block names none of the other
         six legs: not the query-anchoring leg (the user's raw chat message,
         `QueryAnchorTranslator:259-260` via `ChatAgent:689-694`), not the
         `/saved`, `/summary` or digest display-hit headlines
         (`SavedCommandHandler:453,457`, `ClusterBlockRenderer:132`,
         `DigestRenderer:886`), not the unbudgeted prose/roll-up legs
         (`ClusterBlockRenderer:166`, `DigestRenderer:552,825`,
         `CategoryRollupGenerator:213`), and not the ingest leg
         (`IngestTranslationWorker:264,274,318`; `renderPrompt:358-366` splices
         `{{title}}` plus the full untruncated `{{body}}`, gated only on
         `"en".equals(row.language())` — the SOURCE's column, V74). Describing
         translator's whole exposure as "translation of the bot's replies" is the
         bot-prose-only implication the spec bullet names, and because bot-reply
         translation only fires for a non-`en` scope
         (`TranslationPipeline:88`, `ChatAgent:557`), the line also IMPLIES an
         all-English deployment sends nothing through translator — the exact
         negative the spec forbids and the exact claim the round-1 finding
         falsified. The wizard block is not a stale doc; it is live code that
         prints at the decision point, by its own design ("Privacy disclosure
         BEFORE the operator commits to a remote endpoint", :477-483), whereas
         the corrected `switch-llm.sh` Phase 4 text prints only in the
         post-setup switcher, after Phase 3 has already rewritten
         `application.properties`.
    REPRO: Fresh deployment. Operator runs `prod/setup.sh`; step 4
           (`prod/scripts/4-llm.sh`) asks for a backend. Their box is small, so
           they consider `remote`. Before the URL/key prompt the wizard prints
           the PRIVACY DISCLOSURE: exactly one `!!` line (chat), with translator
           listed among the low tier as "translation of the bot's replies". The
           operator reads that as "the only private-data task is chat", accepts,
           and types the endpoint and key. Two exposures follow that the
           disclosure never named. (1) Add one non-English source (or set
           `source.language` on an existing one via the D29 bootstrap correction
           path) — from the next Collector tick every such post's full title AND
           untruncated body is POSTed to the remote endpoint, continuously,
           unattended, and independently of every scope's `/lang`. (2) Any user
           who runs `/lang cs|es|ru|tr` sends their raw chat message to the same
           endpoint on every turn via the D28 pre-fetch's anchoring call. This
           operator never runs `prod/switch-llm.sh` — they chose remote at
           install — so the corrected Phase 4 text and the new SETUP_GUIDE
           leg-by-leg section are printed to them never.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — the switcher "prints a
              per-task privacy disclosure naming exactly which generative tasks
              now call a remote provider and what each exposes ... see
              `SETUP_GUIDE.md` §'Switching your AI backend later'", and (added by
              this diff) "the disclosure texts (`prod/switch-llm.sh` Phase 4 and
              `SETUP_GUIDE.md` §'Switching your AI backend later') are corrected
              against THIS section".
    GAP: Inside the very section the diff designates as the corrected
         operator-facing text, the surviving paragraph contradicts the diff's own
         new sentence about which tasks a switch sends remote.
         `SETUP_GUIDE.md:556-558` (untouched by the diff) states: "It asks, for
         each AI task, which backend to use — `remote` (a cloud API), `ollama`,
         or `llamacpp` — defaulting to whatever that task uses now. Press Enter
         to keep a task as-is". `SETUP_GUIDE.md:603-605` (added by the diff)
         states the opposite and relies on it as the whole basis of its
         mitigation advice: "keep `translator` on a local backend — the switcher
         sets one backend for everything, so use per-task config ... to pin it."
         The code says the new sentence is the true one:
         `prod/switch-llm.sh:162` prompts once — `"LLM backend for ALL generative
         tasks"` — and the per-task loop at :223-227 asks only for MODEL names;
         the script header (:11-14) states "one backend choice for the whole
         deployment ... this tool no longer drives mixed routing". Worse, a
         switch actively DISCARDS existing per-task pins: :241-261 collects every
         `infochat.llm.<task>.base-url` line and Phase 3 sweeps them, with only a
         typed-answer consent gate (:301-308). An operator who believes :556 will
         run the switcher intending to leave `chat` and `translator` — the two
         private-user-text tasks this ticket exists to flag — on a local backend,
         and will instead route both remote. The stale paragraph is
         pre-existing, but it sits in the designated consent surface, it is
         directly contradicted by a line this diff adds 47 lines below it, and it
         answers wrongly precisely the question the promise commits the surface
         to answering ("exactly which generative tasks now call a remote
         provider").
    REPRO: Operator has a working local deployment and wants better summary
           quality, but considers their DMs private. They read SETUP_GUIDE
           §"Switching your AI backend later", line 556: the switcher is per-task
           and Enter keeps a task as-is. They run `./prod/switch-llm.sh`, see one
           prompt, type `remote` (the disclosure and the model prompts read like
           the per-task walkthrough the guide promised), and press Enter through
           the rest expecting `chat` and `translator` to stay local. Phase 3
           writes the shared `infochat.llm.default.base-url`/api-key that ALL
           seven tasks inherit, and sweeps any per-task pin they had. Every chat
           reply, every raw search query on a non-`en` scope, and every
           non-English post body now goes to the cloud endpoint.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - `SETUP_GUIDE.md:287` ("**remote** ... Costs money; your prompts go to that
    provider") and `:315-319` ("Only the generative tasks — chat, summaries,
    tagging — use the remote provider; **embeddings always run locally**") are
    the documentation twin of the setup-wizard disclosure in finding 1: they
    describe the first-time remote choice, name neither `translator` nor the
    unattended ingest leg, and enumerate three task families where seven exist.
    They make no tiered private-vs-public claim, and `docs/spec/security.md`
    §Secrets handling designates only the switcher's Phase 4 and SETUP_GUIDE
    §"Switching your AI backend later" as disclosure surfaces — the initial-setup
    path is not in the documented model at all. Flagged so the user can decide
    whether the threat model should name the install-time consent surface
    (script + guide) alongside the switch-time one, rather than leaving the
    stricter of the two decisions covered by the weaker text.
