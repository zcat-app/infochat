---
target: "M1-521 --in-progress"
date: 2026-06-29
base: "256e14e6 (fork point; impl uncommitted in working tree)"
head: "working tree"
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: >-
  CLEAN, no findings. One OUT-OF-MODEL wording observation: SafeLog's javadoc
  and the test comment reference "the same bidi-control set
  IngestTextNormalizer.stripBidiAndZeroWidth strips", and that method also
  strips zero-width codepoints which SafeLog deliberately omits. Not a
  threat-model gap (the spec promises no control/bidi/zero-width stripping of
  log lines at all; zero-width chars neither reorder nor split a line, so they
  fall outside the log-line-spoof threat). The javadoc is already scoped to
  "bidi-control set", and the ticket out_of_scope documents the zero-width
  exclusion. No follow-up ticket filed; wording left as-is (accurate as scoped).
---

RED-TEAM VERDICT: CLEAN

FINDINGS: (none)

OUT-OF-MODEL:
  - The widened javadoc/comment in SafeLog.java:39-42 and the test comment
    (SafeLogBidiTest.java:172-173) claim this is "the same bidi-control set
    IngestTextNormalizer.stripBidiAndZeroWidth strips on the ingest path."
    SafeLog.stripControls covers only the bidi-control codepoints, not the
    zero-width characters (e.g. U+200B ZERO WIDTH SPACE, U+FEFF BOM, U+2060
    WORD JOINER) that the ingest-path method also removes. This is not a
    threat-model gap: the §User content in exceptions / §Secrets handling
    logging commitments do not promise control/bidi/zero-width stripping at
    all (that defense is hardening beyond the spec), and zero-width
    characters do not break one-line log integrity (they neither reorder nor
    split a line) — so they fall outside the log-line-spoof threat this
    change targets. Flagged only so the user can decide whether the comment's
    "same set" wording should be tightened, or whether zero-width neutralization
    in logs is worth a separate hardening ticket. No exploitable gap against
    the documented threat model.
