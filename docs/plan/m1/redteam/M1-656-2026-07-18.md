---
target: M1-656
date: 2026-07-18
base: 9aa6788b
head: working tree on m1/M1-656-friendly-errors-must-not-reflect-inbound-text
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 0
out_of_model_count: 0
disposition: |
  The CODE fix is sound and was verified in detail, not accepted:
    - Placeholder renumbering audited per-template against every call site in
      both en and cs; a grep for every consumer of the five BundleKeys
      constants and their literal key strings returns exactly one call site
      each, all updated — no stale second consumer left re-interpolating the
      removed value into {0}.
    - Both deliberately-unchanged claims hold: SummaryArgs stores only the
      isValid-gated normalized value (provably ^[a-z0-9][a-z0-9-]{0,47}$), and
      error.asset.sub_verb_not_enabled is reachable only after an exact
      findSubVerb match, so its echo is operator-authored config.
    - The `/{1}` slash-synthesis shape newly rendered in the asset templates
      carries assetName, which is registry-authored and double-gated
      (isAssetCommand then assetRegistry.getAsset) — not attacker-chosen.

  The single medium finding is a FALSE CLAIM in the spec text this diff adds,
  not a code defect: docs/spec/commands.md now says /summary is "the one
  deliberate exception", which is untrue. AddSourceArgs.unknownKind:228-231
  and unknownCategory:233-236 still interpolate a raw inbound token into
  error.add_source.unknown_kind / unknown_category, reachable by any
  registered user in DM (commands.md:513-514). Five bot-admin-only twins share
  the shape: error.quarantine.invalid_id, error.audit.unknown_action,
  error.invite.unknown_adapter, error.group_not_found,
  error.recover_pool.not_found.

  Aggravating: commands.md:530-532 already documents /add-source's unknown
  values as taking "the same path as an unknown tag argument" — a
  cross-reference this diff makes stale in the unsafe direction.

  Halted to the user per run.md step 5. Resolution requires a scope decision
  (AddSourceArgs.java is outside files_scope), which the workflow reserves to
  the human.
---
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INJECTION
    SEVERITY: medium
    PROMISE: docs/spec/security.md §LLM output sanitizer — "Before any
              **LLM-generated** text is delivered to a user, the candidate
              output is passed through a deterministic outbound regex pass
              that strips or refuses output containing admin command strings
              (`/grant-admin`, `/ban`, `/promote`, `/remove-source`, etc.).
              ... It does **not** apply to deterministic command output
              (`/help`, `/status`, `/list-sources`, etc.) because that text
              never passes through an LLM. ... the sanitizer closes the
              social-engineering surface where a small LLM emits
              plausible-looking admin commands."
              The exemption is sound only while deterministic output really is
              bot-authored. The diff itself states the load-bearing condition at
              docs/spec/commands.md:263-266: "`security.md` §LLM output sanitizer
              exempts deterministic command output from the admin-command strip
              on the premise that such output is bot-authored; **not
              interpolating is what makes that premise true**."
    GAP: The five reflecting templates the diff removes are genuinely closed —
         the renumbering was audited placeholder-by-placeholder against every
         call site and is correct (see NOTE below). The gap is the NEW
         spec-level claim the diff writes at docs/spec/commands.md:254-259:
         "The same rule now holds for the unknown-tag, unknown-timezone and
         unknown-asset-sub-verb replies (M1-656): none of them reflects the
         user's token either. The one deliberate exception is `/summary` ...".
         "The one deliberate exception" is false. Deterministic friendly errors
         that still interpolate an unvalidated raw inbound token remain:
           - infochat-provider/src/main/java/app/zcat/infochat/provider/command/AddSourceArgs.java:106,116
             -> unknownKind(String supplied) at AddSourceArgs.java:228-231 passes the
             raw `--type` token as {0} into `error.add_source.unknown_kind`
             (infochat-provider/src/main/resources/bundles/en.properties:160).
             No charset filter, no length bound — the token is taken verbatim
             from the inbound split and only compared against the enum.
           - AddSourceArgs.java:123,133 -> unknownCategory(String supplied) at
             AddSourceArgs.java:233-236, same shape, into
             `error.add_source.unknown_category` (en.properties:161).
           - Lower-value bot-admin-only twins of the same shape:
             `error.quarantine.invalid_id` (en.properties:663),
             `error.audit.unknown_action` (en.properties:686),
             `error.invite.unknown_adapter` (en.properties:253),
             `error.group_not_found` (en.properties:738),
             `error.recover_pool.not_found` (en.properties:751).
         The `/add-source` pair is the material one: docs/spec/commands.md:513-514
         puts it at "DM: any non-banned user ... Group: group admin only", and
         docs/spec/commands.md:530-532 explicitly ties it to the surface the diff
         just changed — "unknown values produce the friendly-error path with fuzzy
         suggestions over the enum, **the same path as an unknown tag argument**".
         That cross-reference is now stale in the unsafe direction: the unknown-tag
         path stopped reflecting, `/add-source` did not.
    REPRO:
         1. Attacker is a registered, non-probation user who is group admin of an
            approved group (the group-scope permission `/add-source` requires per
            commands.md:514). The DM variant needs only registration.
         2. Attacker sends into the group:
              /add-source https://example.com --tags news --type /grant-admin
         3. AddSourceArgs.parse hits the `--type` branch (AddSourceArgs.java:110-117),
            SourceKind.fromString returns empty, and unknownKind(tokens.get(i + 1))
            passes the literal string "/grant-admin" as {0}.
         4. Every member of the group receives bot-authored output containing the
            copy-pasteable string `/grant-admin`:
              Unknown source type `/grant-admin`. Valid types: rss, nitter, ...
         5. This is byte-for-byte the outcome the diff removed from /follow-tag,
            /unfollow-tag, /group-timezone and the two asset templates. It reaches
            users through the one output channel security.md deliberately leaves
            unfiltered, and it does so because the "bot-authored" premise that
            justifies leaving it unfiltered is violated. Dispatch still requires
            is_admin=true, so this is a social-engineering surface, not a direct
            escalation — which is exactly the residual risk the sanitizer exists
            to bound.
    SUGGESTED-FIX-CLASS: input-sanitization

NOTE (not a finding — verification record for the two claims put to the audit):
  1. Placeholder renumbering: CLEAN. All five templates were checked against
     their call sites in both en and cs:
       - error.follow_tag.unknown_tag {0}=suggestions vs FollowTagCommandHandler.java:185-187 — correct.
       - error.unfollow_tag.unknown_tag {0}=suggestions vs UnfollowTagCommandHandler.java:237-239 — correct.
       - error.group_timezone.invalid_zone {0}=suggestions vs GroupTimezoneCommandHandler.java:113-116 — correct.
       - error.asset.unknown_sub_verb {0}=bestMatch,{1}=assetName,{2}=allSubVerbs vs AssetHandler.java:116-118 — correct (en and cs agree).
       - error.asset.unsupported_quote_currency {0}=bestMatch,{1}=assetName,{2}=supported vs AssetHandler.java:138-140 — correct (en and cs agree).
     A grep for every consumer of the five BundleKeys constants and their literal
     key strings returns exactly one call site each, all updated by the diff — no
     stale second consumer left passing the old argument list (which would have
     silently re-interpolated the removed inbound value into {0}).
     All interpolated values are bot- or registry-authored: fuzzySuggest in all
     three handlers emits only vocabulary entries (FollowTagCommandHandler.java:408-423,
     UnfollowTagCommandHandler.java:530-545, AssetHandler.java:191-206), and
     GroupTimezoneCommandHandler.fuzzySuggestions:204-225 emits only
     ZoneId.getAvailableZoneIds() members or a fixed literal fallback.
     The `assetName` value newly rendered as `/{1}` (a slash-synthesis shape) is
     NOT attacker-chosen: InboundRouter.java:1429-1430 dispatches only when
     assetCommandFamilyOracle.isAssetCommand(commandName) passes, and
     AssetHandler.java:75-80 re-gates on assetRegistry.getAsset — so the value is
     byte-equal to a registry-authored asset name. No new defect introduced.
  2. Both "deliberately not changed" claims hold:
     - SummaryArgs.java:96-100 stores only `normalized` and only after
       TagNormalizer.isValid; TagNormalizer.java:33 pins the pattern to
       ^[a-z0-9][a-z0-9-]{0,47}$ under matches(), so no "/", whitespace, or
       newline can survive. SummaryCommandHandler.java:225-230 echoes
       args.tag().get(), i.e. that constrained value. Provably constrained — the
       echo cannot fabricate a command because the attacker cannot supply the slash.
     - error.asset.sub_verb_not_enabled (AssetHandler.java:120-123) echoes
       args.subVerb only after AssetRegistry.AssetEntry.findSubVerb
       (AssetRegistry.java:71-78) returned non-null on `sv.subVerb.equals(name)` —
       an exact match — so the echoed bytes are identical to an asset_config row
       value, i.e. operator-authored config, which security.md §Threat model
       classifies as trusted. Registry-authored, not attacker-chosen. Confirmed.
