---
target: M1-582 --in-progress
date: 2026-07-06
base: bfe4c88b27f2b4cf2c00daf8a11b43087c81d837
head: working tree (uncommitted pre-commit audit, branch m1/M1-582-single-owner-cutover-guard)
verdict: CLEAN
findings_count: {critical: 0, high: 0, medium: 0, low: 0}
out_of_model_count: 3
disposition: >-
  Pre-commit --in-progress audit during /m1-tick run; CLEAN, so the ticket
  proceeded straight to commit with this audit file folded into the ticket
  commit (lifecycle-path exemption). Out-of-model items reported in chat as
  advisory; none auto-filed.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - pack.sh's live-deployment WARN is best-effort and fail-silent (prod/scripts/pack.sh:128-146): any error from `docker compose ps -q` or `docker inspect` is swallowed (`2>/dev/null || true` / `|| echo`), and a multi-replica `ps -q` result (newline-joined ids) makes the quoted `docker inspect` fail, so in all those cases the WARN never prints even though the Provider IS running. Detection also keys solely on the `infochat-provider` compose container — identity stores being written by an out-of-band `simplex-chat`/`signal-cli` process would not trigger it. The torn-identity-bundle hazard the WARN exists for therefore survives on exactly the degraded paths where the operator most needs the signal. This is operator-infrastructure integrity; the security.md threat model trusts the operator and their host, so it is not a FINDING — flagging in case the user wants the detection to fail loud instead of silent.
  - restore.sh's single-owner gate is consent, not verification (prod/scripts/restore.sh:658-697): `--source-stopped` (or an interactive "y") is an unverified operator assertion — nothing probes the source host, and nothing prevents the source restarting between consent and Provider start (TOCTOU). Two live consumers corrupting the unrecoverable Signal/SimpleX identity remains fully reachable via a false assertion. The script header documents this as "operator-observed, NOT lock-enforced", and operator error is outside the documented threat model, but the residual risk is on the asset the diff itself calls UNRECOVERABLE — the user may want a cross-host liveness probe (e.g. reachability check against the source's health endpoint) as future hardening.
  - The consent decision leaves no durable record: neither the consented `--source-stopped`/"y" path nor the withheld path (prod/scripts/restore.sh:678-695) writes anything an operator can later audit — security.md's audit-log commitments cover in-app actions only, so this is not AUDIT-EVASION, but "who asserted the source was stopped before the identity was double-mounted" is exactly the question a post-corruption forensic would ask. Extending the model to require an operator-action trail for identity-consuming script decisions is a design choice for the user.
