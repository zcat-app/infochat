---
target: "M1-451 --in-progress"
date: 2026-06-25
base: main
head: m1/M1-451-probation-timing-injected-clock-router-promote-vouch
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  CLEAN — no threat-model gaps in the diff. Two OUT-OF-MODEL observations, both
  PRE-EXISTING and unchanged by this diff (the diff only swaps inline Instant.now()
  for the injected clock, byte-for-byte preserving operators): (1) ProbationCheck
  .clearIfPromoted's housekeeping UPDATE still uses SQL NOW() — a §9-hygiene split,
  but it is a pure column-null write, not a decision gate, and the file is out of
  this ticket's scope (existing-site backlog M1-447). (2) isAlreadyPastProbation
  treats equality (now == probation_until) as past-probation; pre-existing isAfter
  semantics, harmless at the exact boundary (lazy promotion graduates one inbound
  later). Neither warrants a follow-up here.
---
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Lazy-clear app/DB clock split (NOT a threat-model gap; engineering-rules
    §9 hygiene only). InboundRouter step-5 reads the probation gate against
    the injected clock (clock.instant(), diff line 679) but the housekeeping
    UPDATE it guards — ProbationCheck.clearIfPromoted, CLEAR_IF_PROMOTED_SQL
    "... probation_until <= NOW()" (ProbationCheck.java:50, unchanged by this
    diff) — still uses SQL NOW(). This is the "never split one component
    across two clocks" shape §9 warns about, but (a) clearIfPromoted is a pure
    column-null housekeeping write, not a decision gate — the block/allow
    decision is made by the snapshot read above it, so a skew at most defers
    nulling the column by one inbound; (b) the file is not part of this diff.
    Flagging so the user can decide whether the existing-site migration
    backlog (M1-447) should cover it; it is outside the threat-model
    commitments this audit scores.

  - Probation boundary semantics in VouchCommandHandler.isAlreadyPastProbation
    (VouchCommandHandler.java:281-286): equality (now == probation_until) is
    treated as past-probation (no-op), whereas the spec §Slow-start tier
    predicate "promoted at the instant NOW() > probation_until" is strict (a
    user is still in probation AT the boundary). This off-by-the-boundary
    behavior is PRE-EXISTING and unchanged by this diff — the diff only swaps
    Instant.now() for the injected now argument, preserving the isAfter
    operator byte-for-byte. Noted for completeness; not introduced here and
    not a confidentiality/integrity/availability gap (a /vouch no-op at the
    exact boundary instant is harmless: the lazy-promotion path graduates the
    user one inbound later regardless).
