---
target: M1-425
date: 2026-06-21
base: 72d1f0280009a7de603811f2b3cabcb1afcc486c
head: WORKING-TREE
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Audit run --in-progress between /m1-tick review APPROVE and /m1-tick commit.
  No findings. The diff narrows the Stage 1 tool-call-simulation regex (drops the
  bare `function` alternative — a false-positive source, not a security weakening),
  reconciles the design note, and pins the new behavior in unit tests. It removes
  match coverage rather than adding an attack surface; the scrub+quarantine+RAW
  Stage 1 semantics are untouched.
---
RED-TEAM VERDICT: CLEAN

Audited diff: M1-425 (base 72d1f028 → WORKING-TREE).

The diff narrows the Stage 1 tool-call-simulation regex by dropping the
bare `function` alternative — pattern goes from
`\b(?:function[_-]?call|tool[_-]?call|tool|function)\s*[:(]` to
`\b(?:function[_-]?call|tool[_-]?call|tool)\s*[:(]`
(infochat-collector/.../Stage1RegexSet.java:151) — plus the matching
design-doc reconciliation (docs/design/04-security.md:9) and test
updates (Stage1RegexSetTest.java), and routine STATUS/ticket bookkeeping.

No spec-level commitment in docs/spec/security.md is broken:

1. Stage 1 is explicitly a COARSE FILTER, not a security boundary. The
   threat model states verbatim: "Stage 1 is a coarse filter, not a
   complete defense... Stage 2 is the actual security boundary" and
   "Stage 1 never blocks release on its own — it scrubs and routes to
   review." Reducing Stage 1 match coverage therefore cannot violate a
   confidentiality/integrity commitment — the spec assigns those to
   Stage 2 and to the prompt-wrapper/tool-allowlist boundary (trust
   boundary 5), both untouched.

2. The Stage 1 regex catalogue lives in DESIGN NOTES, not spec ("What
   lives in design notes" — "The Stage 1 regex catalogue and ReDoS
   mitigation specifics"). The spec nowhere commits that the bare
   `function(` form must be matched. Removing one over-matching
   design-tier alternative is within design-tier latitude, and the diff
   correctly reconciles the design doc with the implementation.

3. Genuine tool-call-simulation shapes are still caught: `function_call:`,
   `function-call(`, `tool_call:`, `tool:`, `tool(` all still match
   (asserted positively in the updated test). Only bare `function(` /
   `function foo()` — ordinary code prose — is dropped.

4. The LLM OUTPUT SANITIZER (the outbound admin-command-strip surface,
   §LLM output sanitizer) is a SEPARATE regex set over admin command
   strings and is untouched by this diff. No admin-command leakage path
   is affected.

5. DoS / quarantine-exhaustion: the change REDUCES false-positive
   quarantine rows (the old bare-`function` alternative scrubbed
   ordinary `function(` spans and recorded spurious quarantine rows per
   the test rationale). It shrinks, never grows, the admin-review and
   quarantine surface. No new resource-exhaustion vector.

6. ReDoS: the alternation is shortened, not lengthened; backtracking
   surface for the watchdog-guarded java.util.regex engine is reduced.

No gap found between threat-model promise and delivered behavior.

FINDINGS:
  (none)

OUT-OF-MODEL:
  (none)
