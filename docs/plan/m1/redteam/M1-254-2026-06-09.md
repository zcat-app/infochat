---
target: M1-254
date: 2026-06-09
base: main
head: working tree (uncommitted, branch m1/M1-254-remove-orphaned-bancheck)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  CLEAN. Pure orphaned-dead-code removal (BanCheck.java + its IT) plus a
  stale-comment correction in AddSourceCommandHandler's javadoc. The adversary
  ground-truthed the full chain: the authoritative step-4 ban gate is live in
  InboundRouter (reading is_banned from the M1-244 step-1 snapshot SELECT),
  BanCheck had no remaining production callers (all surviving references are
  comments / test names naming the concept, not the type), and the
  AddSourceCommandHandler defense-in-depth re-check keeps its own independent
  SELECT. No security promise weakened.

  One OUT-OF-MODEL advisory: the folded-snapshot design leaves a step-1→step-4
  TOCTOU window (a ban landing mid-dispatch lets one in-flight message through).
  This is a PRE-EXISTING M1-244 design decision, documented inline as
  intentional, and NOT introduced by the M1-254 diff under audit. The spec
  §Authorization model commits to ban-check ordering, not to a re-query, so it
  is within the threat model and not a finding. No remediation ticket warranted;
  recorded here only so the trade-off is discoverable.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - The folded-snapshot design (InboundRouter lines 445-468, 227-229) reads
    `is_banned` from the step-1 `UserSnapshot` rather than a fresh step-4 query,
    leaving a step-1→step-4 TOCTOU window (a ban landing mid-dispatch lets one
    in-flight message through). This is a pre-existing M1-244 design decision
    documented inline as intentional ("takes effect on the next inbound
    regardless"); it is NOT introduced by the M1-254 diff under audit, which only
    deletes the already-orphaned `BanCheck` class and corrects a stale comment.
    Flagging only so the user is aware the single-SELECT TOCTOU trade-off now has
    no second-query backstop anywhere in the intake path; the spec §Authorization
    model commits to ordering, not to a re-query, so this is within model and not
    a finding.

---

## Audit notes (verbatim adversary reasoning)

The `AddSourceCommandHandler` defense-in-depth ban re-check (line 130) and admin
gate (lines 144-146) are intact and unchanged in behavior — the diff only updated
the comment at lines 49/53 to reflect the new source of the authoritative ban
check (the folded `UserSnapshot` rather than the deleted `BanCheck.isBanned`). The
re-check here does its own `lookupActor` SELECT (line 88, projecting
`is_admin, is_banned`), independent of the router's snapshot, so it remains correct
after the deletion.

Verified chain:
- The authoritative step-4 ban gate is live in `InboundRouter` (lines 445-468),
  reading `is_banned` from the step-1 snapshot SELECT (lines 227-229).
- `BanCheck.java` had no remaining production callers — all 6 remaining references
  are comments/test names referencing the *concept*, not the type.
- The `AddSourceCommandHandler` defense-in-depth re-check is intact with its own
  independent SELECT.
- The deleted test (`BanCheckTest`) tested only the deleted class.

The diff is a pure orphaned-dead-code removal plus a stale-comment correction. No
security promise is weakened. The TOCTOU note in the router (step-1→step-4, "at
most one in-flight message") is a pre-existing design choice from M1-244, not
introduced by this diff, and the spec's "ban check before parser/LLM/DB-write"
ordering is satisfied.
