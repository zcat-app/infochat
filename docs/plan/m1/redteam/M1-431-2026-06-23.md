---
target: "M1-431 --in-progress"
date: 2026-06-23
base: c64cacf53abf2cba1a5d75d06fa0c0bc251ebe41
head: working-tree (uncommitted branch impl)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  Pre-merge adversarial review of the SimpleX wizard-provisioning diff
  (security_relevant ticket; operator asked to redteam the auto-accept design
  + spec carve-out before merge). CLEAN — no findings. One out-of-model
  advisory recorded below.
---

RED-TEAM VERDICT: CLEAN

The M1-431 diff adds operator-run, pre-startup SimpleX bot-identity
provisioning (a new shell script `prod/scripts/6b-simplex-provision.sh`
invoked by `7-apps.sh`, a wizard display-name prompt in `6-adapter.sh`,
a spec/design carve-out, a README/SETUP_GUIDE rewrite, and one Java
test). It makes NO change to the running Provider's Java code paths.
Audited against every commitment in docs/spec/security.md that the
sensitive-surface inventory flagged; each holds:

- Invite gating / AUTH-BYPASS (trust boundary 2, D44, authorization
  step 2): provisioning enables SimpleX transport-level auto-accept,
  which opens only the messaging connection. The invite/registration
  gate is unchanged in this diff (no Java intake-path change), and the
  spec amendment (docs/spec/deployment.md:161-164) explicitly reaffirms
  that "the invite/registration gate (decision D44) still rejects any
  un-invited contact." The script comment at
  prod/scripts/6b-simplex-provision.sh:682-684 echoes the same
  property. No bypass introduced; this merely automates the previously
  manual `/auto_accept on` step that already existed in the SETUP_GUIDE
  walkthrough being replaced.

- Trust boundary 7 (SimpleX ws-port must stay loopback): the
  provisioning script drives `simplex-chat` via one-shot
  `docker compose run --entrypoint /usr/local/bin/simplex-chat ... -e
  "/ad"` CLI commands (6b-simplex-provision.sh:648-650), NOT the
  WebSocket bot API. It never touches the ws-port bind. The runtime
  adapter's loopback ws-port binding is unchanged (no edit to
  SimpleXConfig / SimpleXSubprocess in the diff).

- §Secrets handling / D37 (contact ids and secrets never persisted to
  config/secrets/logs): the provisioned contact link is surfaced
  transiently to the operator terminal only
  (6b-simplex-provision.sh:699-708) and is never written to
  application.properties, secrets.env, or a log. The error-handling
  path echoes only lines matching `bad chat command|error`
  (6b-simplex-provision.sh:662-666), not the `https://smp...` link
  line. Verified that 6-adapter.sh writes
  `infochat.adapters.simplex.display-name` (a non-secret operator-typed
  name), not the link.

- D10 / authorization isolation: the operator-supplied `display_name`
  is passed to `--create-bot-display-name` (6b-simplex-provision.sh:676)
  but is never read by the runtime adapter for authorization. Confirmed
  no `@ConfigProperty` binds `display-name` in
  infochat-messaging-adapter/src/main; SimpleXGroupHandler anchors
  identity to the cryptographic contact id only, never display-name
  text (SimpleXGroupHandler.java:26,93). The wizard comment at
  prod/scripts/6-adapter.sh:505-508,517-522 pins this same property.

- Trust model boundary: every changed line executes inside the trusted
  operator boundary ("Operator-set config ... is trusted",
  security.md:27) before any container serves traffic. There is no new
  adversary-reachable surface — the SimpleX adapter, Collector ingest,
  LLM tool surface, ban check, and audit writes are all untouched by
  the diff.

No commitment in the threat model is left undelivered by the diff.

OUT-OF-MODEL:
  - `docker compose run --env-file "$SECRETS_FILE"`
    (6b-simplex-provision.sh:648) injects the deployment's secrets
    (DB role passwords, LLM API key) into the one-shot provisioning
    container's environment. This is the same secret set the app
    containers already receive and runs wholly inside the trusted
    operator boundary before any service starts, so it is not a new
    confidentiality exposure under the documented threat model
    ("Operator-set config ... is trusted"; supply-chain / operator-
    infrastructure attacks are explicitly out of scope, security.md:29).
    Flagged only so the user can decide whether to scope the env-file
    to just the variables the compose definition substitutes.
