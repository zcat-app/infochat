---
target: M1-119
date: 2026-05-31
base: dd69fcb^
head: dd69fcb
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 1
out_of_model_count: 1
disposition: |
  M1-119 is the remediation for M1-103 Findings 1 and 4 (drainStream
  leak + Ignored/MalformedFrameException interpolation). The original
  commit closed both cited sites but the parallel reasoning ("attacker-
  influenceable JSON values must not flow to the WS-client log at any
  level") was not applied to the other two interpolation sites in the
  same codec — top-level resp.type and the decodeError envelope fallback.

  Per user direction, both findings are fixed in the same M1-119 scope
  rather than spawned as a new remediation ticket. A follow-up commit
  on the branch extends the structural-over-filtering pattern: the
  `default` branch in `decode` now emits a fixed
  `Ignored("unknown-resp-type")`, and `decodeError` substitutes the
  fixed sentinel `"unrecognized-error-envelope"` for the missing-tag
  envelope dump. Three sentinel-string tests guard the new contracts
  (`SimpleXMessageCodecTest.unknownRespTypeYieldsFixedIgnoredReason`,
  `SimpleXMessageCodecTest.unrecognizedErrorEnvelopeYieldsFixedDetail`,
  and the end-to-end
  `SimpleXWebSocketClientTest.unrecognizedErrorEnvelopeDoesNotLeakBytesToLog`).

  Note on base/head: this audit was originally run against e478a10 on
  the M1-119 branch; the branch was subsequently rebased onto main
  (which had landed M1-118) to pick up the input-validator hardening,
  so the original commit appears as dd69fcb in the rebased history.
  The diff content under audit is byte-identical between e478a10 and
  dd69fcb — only the parent SHA changed.
---

RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: "Exception messages and stack traces emitted via the application logger MUST NOT contain user-authored prose (chat-mode message bodies, post bodies, saved-post annotations, command arguments)." AND "the bodies of inbound chat-mode messages never appear in non-audit logs, at any log level (decision D37)." (`docs/spec/security.md` §User content in exceptions and §Secrets handling)
    GAP: `infochat-messaging-adapter/src/main/java/app/zcat/infochat/messaging/impl/simplex/SimpleXMessageCodec.java:302` still falls back to `resp.toString()` (the entire JSON `resp` envelope, including any user-message-body fields simplex-chat echoes back in `chatCmdError` / `chatItemUpdateError` frames) when no recognized `chatError`/`errorType`/`error` tag is present. That string is stored in `CommandError.detail()`, which then (a) flows verbatim into the application logger at `SimpleXWebSocketClient.java:305` (`LOG.debug("no pending command for error corrId={}: {}", err.corrId(), err.detail());`) and (b) becomes the message text of the `MessagingException` constructed at `SimpleXWebSocketClient.java:303` (`"simplex-chat error: " + err.detail()`). The diff under audit fixed the `MalformedFrameException` and the non-direct `chatType` `Ignored` reason, but the `decodeError` envelope-dump leak survives unchanged. The spec's "at any log level" wording explicitly covers DEBUG.
    REPRO: An adversary who can influence the bytes that simplex-chat emits on the bot WebSocket (compromised simplex-chat process, MITM at the unauthenticated localhost ws://, or a malicious upstream simplex relay echoing crafted error envelopes) sends `{"resp":{"type":"chatCmdError","attackerPayload":"REDTEAM-SENTINEL plus chat body bytes echoed back by simplex-chat"}}` — i.e. an error frame whose envelope carries content but contains no `chatError`/`errorType`/`error` field at any of the depths `findFirstString` probes. Codec falls into the `resp.toString()` branch; the entire envelope (including the inbound chat body fragments simplex-chat included in the error) lands at DEBUG via `LOG.debug` and is also embedded into the `MessagingException` returned to the adapter caller, where it may be re-logged elsewhere. The spec promised this content never reaches non-audit logs.
    SUGGESTED-FIX-CLASS: input-sanitization

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: "Exception messages and stack traces emitted via the application logger MUST NOT contain user-authored prose" — by parity, neither should the parallel non-exception `Ignored.reason()` channel: M1-103 Finding 4 (cited in the ticket body itself) treats interpolation of untrusted JSON values into `Ignored` reason strings as a §User content in exceptions violation because they flow into the WS-client DEBUG log via `LOG.debug("simplex-chat frame ignored: {}", ignored.reason())` (`SimpleXWebSocketClient.java:285`). D37 §User-content logging also covers this path "at any log level".
    GAP: `SimpleXMessageCodec.java:217` still emits `new Ignored(type)` where `type` is the attacker-controllable top-level `resp.type` value from the inbound JSON frame. The diff fixed the parallel `chatType`-non-direct branch at line 245 with the comment "The variant carries no bytes from chatType: that field is attacker-influenceable through the inbound frame and the Ignored.reason() value flows into the WS-client's DEBUG log" — the exact same reasoning applies to the top-level `type` field at line 217, but that site was left unchanged. The ticket's Finding 4 description ("Ignored variants interpolate untrusted JSON values into reason strings") covers both sites; only one was remediated.
    REPRO: A peer sends `{"resp":{"type":"REDTEAM-SENTINEL-XXXXX-with-chosen-bytes"}}`. The codec's `switch (type)` falls through to `default -> new Ignored(type)`; dispatch logs `simplex-chat frame ignored: REDTEAM-SENTINEL-XXXXX-with-chosen-bytes` at DEBUG. The attacker chose those bytes; the §User-content / §User content in exceptions rules say they must not reach the application logger.
    SUGGESTED-FIX-CLASS: input-sanitization

OUT-OF-MODEL:
  - The unauthenticated localhost `ws://` between adapter and simplex-chat means any local process (or a compromised simplex-chat itself) is the "peer" in the threat model. The diff documents the structural choice ("structural over filtering") but does not constrain who can drive the WebSocket; this is consistent with the existing M1-103 design and is not a regression from this diff. Whether peer-controlled bytes count as "user-authored prose" is a spec-interpretation question (the spec's enumeration is "chat-mode message bodies, post bodies, saved-post annotations, command arguments" — bytes from simplex-chat's own envelopes are a step removed) — the findings above interpret the rule broadly because the M1-103 audit Finding 4 (the ticket's source-of-truth for what to fix) already established that interpretation.
