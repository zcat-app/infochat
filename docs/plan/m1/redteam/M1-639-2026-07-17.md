---
target: M1-639 --in-progress
date: 2026-07-17
base: 3b87558e
head: working-tree (uncommitted impl on branch m1/M1-639-settle-the-inline-dispatch-cap)
verdict: CLEAN
findings_count: {critical: 0, high: 0, medium: 0, low: 0}
out_of_model_count: 1
disposition: >-
  Pre-commit audit of the M1-639 working tree (D61 decision row, security.md
  cross-ref, cap<pool boot refusal + test, properties doc block). CLEAN — no
  remediation; the single out-of-model note is reported in-session for a
  follow-up-ticket recommendation.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - D61's soundness rests on two structural preconditions — (a) /retry --digest
    stays on the single-threaded transport dispatch path, and (b) every inline
    LLM surface draws from the per-minute LLM bucket — that are enforced by
    process (the D61 row's "a future ticket must revisit this row deliberately")
    plus paired javadoc comments (InboundRouter.isInterruptible, lines
    998-1027, mirroring RetryCommandHandler.hasFlag), not by a mechanical
    guard. No test in infochat-provider/src/test pins the classification
    isInterruptible("/retry --digest") == false (DigestRetryConcurrencyIT
    covers digest single-flight, not the router's inline classification), so a
    future refactor moving /retry --digest onto the pooled/async path would
    silently void the self-serialization bound the exclusion depends on
    without failing CI. Not an in-model finding: the threat model as written
    (security.md §Rate limiting, per-user interruptible concurrency bullet)
    accepts the exclusion and its residual, and drift prevention is assigned
    to the decision-record process. Flagged so the user can decide whether a
    classification pin test or CI coupling check is worth a ticket.

Audit notes (verification performed, no gaps found):
  - Promise "per-user-cap >= max-concurrency REFUSES BOOT" (decisions.md D61
    companion coupling; application.properties comments): delivered at
    InterruptibleDispatcher.java:164-173, inside @PostConstruct init() on an
    @Startup @ApplicationScoped bean, so the check runs on every real Provider
    boot before the executor is built. Covered by
    InterruptibleDispatcherValidationTest (equal, exceeds, and below cases).
  - Bypass check: InterruptibleDispatcher.direct() skips init(), but the only
    production reference is InboundRouter:370, an @Inject field whose direct()
    initializer CDI replaces with the validated managed bean; direct() is live
    only in hand-constructed plain-JUnit router tests. Config is boot-time
    @ConfigProperty injection with no runtime mutation path — no TOCTOU.
  - D61 structural claims verified against code: /retry --digest is classified
    D35 non-interruptible (InboundRouter.isInterruptible, whitespace-token
    equality on the step-1.7-normalized body, matching
    RetryCommandHandler.hasFlag semantics) and dispatches inline at
    InboundRouter:922; the M1-636 per-user cap check (InboundRouter:871-878)
    guards only the interruptible branch, exactly as D61 describes.
  - The security.md change is the "(D61)" cross-reference only; the exclusion
    and its "accepted, 2026-07-17 redteam" residual were already threat-model
    text before this diff. No commitment weakened.
  - No new attack surface: no new input parsing, SQL, LLM path, auth gate, or
    audit-relevant operation. The new IllegalStateException message carries
    only property names and integer knob values (no secrets, no user content —
    §Secrets handling unaffected), surfaced only in operator boot logs.
