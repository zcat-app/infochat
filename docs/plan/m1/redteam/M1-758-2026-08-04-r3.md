---
target: M1-758
date: 2026-08-04
base: 29aa59afbd80e13df6265e6934c77f1a4a06ffe6
head: "<working tree>"
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 3
out_of_model_count: 2
disposition: |
  Round 3 re-audit. All FIVE prior findings (R1-1..3, R2-1..2) re-verified
  CLOSED and correctly not re-reported. Two spot-checks PASSED: spec
  completeness (every production caller of TranslationPipeline.run /
  runForDisplayHit / ModelTask.TRANSLATOR maps to a security.md §Secrets
  handling bullet) and pointer reachability (the SETUP_GUIDE anchor resolves).

  All four new findings confirmed by falsification. THREE ARE REGRESSIONS THE
  ROUND-3 EDIT INTRODUCED, not new discoveries about the system:
    R3-1 (medium) — the new "a later switch SWEEPS such pins unless you decline
      its consent prompt" claim (4-llm.sh:522, SETUP_GUIDE.md:569-570) is FALSE.
      switch-llm.sh has five read prompts (:162,:188,:196/201/204,:225,:330),
      none about pins. The M1-605 gate (:301) fires only on a bare-Enter answer
      and then exit 0 — "declining" means not switching at all. On a typed
      backend, Phase 3 (:352-355) deletes every per-task base-url/api-key
      unconditionally and merely ANNOUNCES the swept list. An operator cannot
      both switch and keep a pin. RemoteLlmWiringTest:172 pins the false half.
    R3-3 (low) — RemoteLlmWiringTest:151-153
      assertFalse(contains("translation of the bot's replies")) forbids stating
      a TRUE exposure (ChatAgent:558, spec-enumerated by this same diff at
      security.md:1965-1973). This REINSTATES in the sibling file the exact
      anti-pattern round 1 found, and violates this ticket's own acceptance
      item forbidding it. Correct target is the false framing: the "-  translator"
      tier marker or "exposes the bot-reply text (can echo your queries)".
    R3-4 (low) — the round-3 comment at 4-llm.sh:491-494 asserts the install
      block "must not be the weaker of the two", but its summarizer line (:505,
      "summaries of the posts you query") is still weaker than switch-llm.sh:429
      ("ingest-time abstracts of EVERY long fetched PUBLIC post"). The omitted
      leg is unattended and whole-body: BodySummaryWorker:202 @Scheduled,
      :231/:276 ModelTask.SUMMARIZER — the same shape this ticket surfaces for
      translator. The census framed row 1 as translator-only, so per-task parity
      between the two runtime renderings was never dispositioned.

  R3-2 (low) is a genuine CENSUS GAP: OVERVIEW.md:288-293 §7 "Privacy note"
  still reads "public post bodies for ingest tasks; private chat messages if
  chat is routed remotely" — translator absent, private data gated on chat
  alone, i.e. the identical defect fixed at README.md and SETUP_GUIDE.md:287.
  It is reader-facing (linked README.md:178) and outside the census carve-out.
  The auditor correctly identified the METHOD flaw: the census header claims
  "enumeration by invocation, not by phrase", but its second grep is both
  path-restricted (prod/ SETUP_GUIDE.md README.md) and phrase-based, so
  OVERVIEW.md was invisible by construction.

  CORRECTED CENSUS re-run at this round over every maintained reader-facing
  *.md (excluding docs/design, docs/plan, docs/process, .claude), selecting by
  capability (mentions remote/cloud LLM routing) then filtering to those making
  an exposure claim. Result: OVERVIEW.md:288-293 is the ONLY newly-wrong
  surface. SECURITY.md and DEVELOPER.md mention remote routing but make no
  exposure claim; SETUP_GUIDE.md:975 says "your prompts and post content go to
  that provider", which is vague but not false. One marginal in-scope item:
  security.md:1904 still describes the disclosure as " (private user
  messages) flagged loudest" without placing translator in a tier — incomplete
  rather than false, and in files_scope already.
---
RED-TEAM VERDICT: FINDINGS

Round-1 and round-2 findings re-verified against the CURRENT working tree, all
five confirmed CLOSED and deliberately NOT re-reported:
  R1-1 (whole-task /lang gating + "sends nothing") — closed: prod/switch-llm.sh:414-421
       and prod/scripts/4-llm.sh:506-514 both carry fact 2 (source-gated, full
       title+body, "all-English deployment is NOT exempt"); verified against
       IngestTranslationWorker:264 (`"en".equals(row.language())`), :232 (@Scheduled),
       :358-366 ({{body}} + {{title}}), and PostRow javadoc :657-658
       (`source.language TEXT NOT NULL DEFAULT 'en'`, so unset sources really are
       skipped and "non-English source" is the right predicate).
  R1-2 (A1 reply-prose leg) — closed in the SPEC (security.md:1965-1973) and in the
       long form (SETUP_GUIDE.md:591-592). See finding 3 for the test residue.
  R1-3 / R2-2 (SETUP_GUIDE stale tier line, per-task claim) — closed at
       SETUP_GUIDE.md:559-563 and :574-576.
  R2-1 (install-time disclosure) — closed for translator at prod/scripts/4-llm.sh:506-514.
       See finding 4 for what that same block still understates.
Spec completeness spot-check PASSED: every production caller of
TranslationPipeline.run / runForDisplayHit / ModelTask.TRANSLATOR
(ChatAgent:558, QueryAnchorTranslator:259-260, SavedCommandHandler:453/457,
ClusterBlockRenderer:132/166, DigestRenderer:552/825/886,
CategoryRollupGenerator:213, IngestTranslationWorker:274/318) maps onto a bullet
in security.md §Secrets handling, so "covers every production call site" holds.
Pointer reachability spot-check PASSED: the in-file anchor
SETUP_GUIDE.md:320 `(#what-translator-sends-leg-by-leg)` resolves to the new
heading at :584, and the runtime pointer's named section (:549) is immediately
adjacent to it.

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: docs/spec/security.md §Secrets handling — "**`translator` carries
              private user messages too (M1-746, D58).** ... For a scope on a
              non-English `/lang`, routing `translator` to a remote provider
              therefore exposes private user messages, exactly as `chat` does",
              and the disclosure commitment it hangs off: the switcher "prints a
              per-task privacy disclosure naming exactly which generative tasks
              now call a remote provider and what each exposes".
    GAP: The diff introduces, on BOTH runtime/doc surfaces, a new specific claim
         about the one control that lets an operator keep private-text tasks
         local — and the claim is false. prod/scripts/4-llm.sh:522 prints "a
         later switch SWEEPS such pins unless you decline its consent prompt";
         SETUP_GUIDE.md:569-570 says "a later switcher run **sweeps**
         hand-pinned routes unless you decline the prompt it shows you". There
         is no declinable prompt on any run that switches. prod/switch-llm.sh
         has exactly five `read` prompts (:162 backend, :188 dialect, :196/201/204
         base-url, :225 per-task model, :330 api-key) — none of them mentions the
         pins. The M1-605 gate at :301 fires only when
         `has_pinned_baseurl == 1 && -z "$answer"`, i.e. only when the operator
         pressed Enter at :162; it then exits without writing (:307), so
         "declining" means not switching at all. On the normal path — the
         operator types a backend, which the code itself calls consent (:299
         "A typed backend answer IS the consent") — Phase 3 deletes every
         per-task base-url/api-key line at :352-355 with no interaction; the pin
         list at :348-351 is an after-the-fact announcement, printed for the
         first time AFTER the decision and after the backup. An operator cannot
         both switch and keep a pin. On the install-time surface the phrase is
         worse than imprecise: the reader has never run switch-llm.sh, so "its
         consent prompt" names a control they have no way to recognise, and the
         4-llm.sh copy does not even carry SETUP_GUIDE's softening "re-check
         your pins after switching". RemoteLlmWiringTest:172 pins the sentence's
         first half into the build.
    REPRO: 1. Operator picks `remote` at install, reads 4-llm.sh:519-524, and
              follows its advice: hand-pins
              `infochat.llm.translator.base-url=http://ollama:11434/v1` to keep
              chat replies, raw search queries and full non-English post bodies
              off the cloud provider. (The advice does work: OpenAiCompatibleProvider
              :183-192 prefers the per-task base-url.)
           2. Months later they run `./prod/switch-llm.sh` for an unrelated
              reason — new remote model, or a dialect flip — and type `remote`
              at the :162 prompt, expecting the promised prompt to let them
              decline the sweep for translator.
           3. No prompt appears. :352-355 deletes the pin; the shared default
              wins; from the next Collector tick and the next non-English chat
              turn, whole post bodies and raw user messages go to the remote
              endpoint. The operator was told a guard existed for exactly this
              and acted on that belief.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — "**`translator` carries
              private user messages too (M1-746, D58).** ... routing
              `translator` to a remote provider therefore exposes private user
              messages, exactly as `chat` does — it is not the bot-prose-only
              exposure the presentation and ingest translation legs imply", and
              "the disclosure texts ... are corrected against THIS section, so a
              leg missing here propagates into both."
    GAP: The census (ticket §Census) misses an operator-facing exposure
         enumeration: OVERVIEW.md:288-293, the "> **Privacy note.**" block under
         §7 Hardware profiles. It still reads "`remote-llm` — or routing any
         individual task to a cloud API — is an explicit opt-in to send the
         content being processed (public post bodies for ingest tasks; private
         chat messages if chat is routed remotely) to that external provider."
         That is the pre-M1-746 world: `translator` appears nowhere, and private
         message content is conditioned on `chat` alone — the identical defect
         the diff judged worth fixing at README.md:216-222 (census row 7) and
         SETUP_GUIDE.md:287 (row 6). OVERVIEW.md is a maintained reader-facing
         guide (linked from README.md:178, and in the standing guide-accuracy
         audit scope per docs/process/guide-accuracy-audit.md:44), not a design
         note, so the census's "Deliberately NOT in the census" carve-out does
         not cover it. It is invisible to BOTH enumeration greps by construction:
         grep 1 is `--include=*.sh`, and grep 2 is path-restricted to
         `prod/ SETUP_GUIDE.md README.md` AND phrase-based — none of `exposes`,
         `private messages`, `goes to that provider`, `leaves the machine`,
         `never leave` matches OVERVIEW's "private chat messages" / "to that
         external provider", so even an unrestricted re-run of the recorded
         greps would not surface it. The census header claims "Enumeration rule
         (by invocation, not by phrase)", but grep 2 is a phrase grep; this row
         is the proof that it under-enumerates the doc class.
    REPRO: An operator evaluating a cloud move opens OVERVIEW.md (README's
           "Understanding the architecture" entry point), reads §7's Privacy
           note, and concludes that the private-data exposure is gated on
           routing `chat`. They keep `chat` pinned local, route everything else
           remote, and believe no private message content leaves the host. In a
           non-English scope every bot reply (ChatAgent:558) and every raw chat
           turn used as a search query (QueryAnchorTranslator:259-260) is then
           sent to the remote provider through `translator`, and full bodies of
           non-English posts ship continuously (IngestTranslationWorker:274/318)
           — the exact conclusion §Secrets handling's M1-746 bullet exists to
           forbid a disclosure from supporting.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — "**`translator` also
              carries the bot's own REPLY prose (pre-existing; enumerated by
              M1-758).** For a scope on a non-English `/lang` the chat-mode
              reply is translated before delivery, so the reply text — which
              quotes and paraphrases the user's private message — reaches
              `ModelTask.TRANSLATOR`."
    GAP: RemoteLlmWiringTest.java:151-153 asserts
         `assertFalse(run.output().contains("translation of the bot's replies"))`
         against the install-time disclosure. The A1 reply-prose leg is live
         (ChatAgent:558 `translationPipeline.run(sanitized, scopeLanguage)`) and
         is now spec-enumerated by this very diff (security.md:1965-1973), so
         this is a build-enforced prohibition on stating a TRUE exposure, not on
         a false one. It reinstates in the sibling test file exactly the
         anti-pattern round 1 found in SwitchLlmWiringTest — the substring is a
         fragment of the leg's natural phrasing, not of the weak pre-M1-746
         framing the comment says it targets (that framing is identifiable by
         its tier marker `-  translator` or by "exposes the bot-reply text (can
         echo your queries)"). Compounding it, the install-time text names the
         reply-prose leg only obliquely ("it sends your messages and what you
         read", 4-llm.sh:507-509); the bot's own composed reply is neither, so
         the leg an operator most cares about is the one the suite now blocks
         from being spelled out. The new SwitchLlmWiringTest does this correctly
         (its two assertFalse targets, "sends nothing" and "strict no-op", are
         genuinely false claims), which makes the asymmetry a drift the next
         reader will not expect.
    REPRO: A follow-up ticket tries to close the gap this finding names by
           adding to the install-time disclosure: "1. ... including the
           translation of the bot's replies to you, which quote what you said."
           `mvn verify` fails on RemoteLlmWiringTest:151. The path of least
           resistance is to drop the sentence, so the suite keeps the strongest
           statement of a real private-text exposure out of the disclosure an
           install-time-remote operator is the only one to see.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — the disclosure names
              "exactly which generative tasks now call a remote provider and
              what each exposes"; §Prompt-injection defenses — "post bodies
              reaching the summarizer or chat agent are considered untrusted",
              i.e. the summarizer is a whole-body consumer, not a
              query-time-only one.
    GAP: The diff rewrote the install-time block and added a comment asserting
         "This one prints EARLIER and to MORE operators ... so it must not be
         the weaker of the two" (prod/scripts/4-llm.sh:491-494) — but the block
         is still strictly weaker than its switch-time sibling on `summarizer`.
         prod/switch-llm.sh:429 discloses "ingest-time abstracts of EVERY long
         fetched PUBLIC post (BodySummaryWorker) plus summaries of the posts you
         query"; prod/scripts/4-llm.sh:505 still says only "summaries of the
         posts you query; exposes which topics / posts you read". The omitted
         leg is real and is the same shape the whole ticket exists to surface
         for translator — unattended, continuous, whole-body:
         BodySummaryWorker:202 `@Scheduled(every =
         "{infochat.llm.summarizer.poll-interval}")`, :231/:276
         `llmRouter.forTask(ModelTask.SUMMARIZER, "en")` /
         `provider.generate(ModelTask.SUMMARIZER, ...)`. The census framed row 1
         as a translator-only defect ("`translator` sits in the 'not private
         user data' tier ... none of the other six legs named"), so the
         per-task divergence between the two runtime renderings inside that same
         block was never dispositioned, and the new
         RemoteLlmWiringTest case pins four translator facts without pinning
         parity for any other task.
    REPRO: Operator runs `prod/setup.sh`, picks `remote`, reads the disclosure
           at the moment it is designed for — before typing the URL and key.
           They size their provider spend and their content-exposure consent on
           "summarizer — summaries of the posts you query", i.e. a per-request
           cost proportional to their own `/summary` usage. They never run
           switch-llm.sh, so they never see the accurate line. In production
           every long fetched post body is shipped to the remote provider on a
           timer with no user present, at ingest volume — an exposure and a cost
           class they were not shown at the only consent point they reach.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - docs/design/05-llm-and-embeddings.md:1103 enumerates the tasks whose content
    goes to a remote provider ("security / tagger / entity / classifier /
    summarizer / chat-agent") and omits `translator` — 6 of 7. It is design-tier,
    not an operator consent surface, and the census excludes `docs/design/**`
    with a stated rationale, so it is not counted as a finding; flagged only
    because the spec's "one authority, N renderings" posture will eventually
    have to decide whether design-tier enumerations are renderings too.
  - The long-form leg list landed as a NEW sibling section
    (SETUP_GUIDE.md:584 "### What `translator` sends, leg by leg") while
    docs/spec/security.md:1990-1992 designates §"Switching your AI backend
    later" (:549) as the corrected long form, and both runtime disclosures point
    at the designated title, not the new one. Verified reachable in practice
    (adjacent section; the one in-file link at :320 uses the correct anchor), so
    not a finding — noted so the user can decide whether the spec's designated
    section name should be updated to match where the enumeration now lives.
