---
target: M1-128
date: 2026-06-02
base: 03f78b84197fd0681c907c98c4f1754bfc58342b
head: de4db544dcc874ee99bf37d125b51bd33347845c
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: >
  CLEAN. The diff makes the spec-mandated cap-exhaustion → NEEDS_REVIEW
  transition (security.md §Re-evaluation job) reachable from the scheduled
  path, and preserves the separate per-class caps, throttled cap-exhaustion
  notification, and the RE_EVAL_RELEASED audit on the BENIGN path. The
  adversary confirmed cap-exhausted rows skip the LLM (>= cap check precedes
  judgeBody, so no extra LLM cost / DOS) and that a transitioned row is
  excluded from both enumerate branches on the next tick (enumerated exactly
  once more, then drops out — no unbounded loop). One OUT-OF-MODEL advisory:
  overlapping @Scheduled ticks could double-enumerate a cap-exhausted row
  before the first transitionToNeedsReview commits; the resulting duplicate
  UPDATE is idempotent and the duplicate admin notification is throttle-
  coalesced, and the NEEDS_REVIEW path writes no audit row — so no audit
  duplication and no security-state divergence. The spec makes no concurrency
  commitment for this job; no remediation ticket warranted. Recorded for the
  user to decide whether single-flight scheduling (@ConcurrentExecution(SKIP))
  is worth asserting as a separate hardening item.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Concurrent/overlapping @Scheduled ticks (no @ConcurrentExecution(SKIP)
    visible on onTick) could enumerate the same cap-exhausted row twice before
    the first transitionToNeedsReview commits, producing a duplicate idempotent
    UPDATE and a (throttled, hence coalesced) duplicate admin notification. No
    audit row is written on the NEEDS_REVIEW path, so this causes no audit
    duplication and no security-state divergence — it is a benign idempotency
    edge, not a threat-model violation. Flagging only so the user can decide
    whether single-flight scheduling is worth asserting; the spec makes no
    concurrency commitment for this job. Relevant file:
    infochat-collector/src/main/java/app/zcat/infochat/collector/eval/reeval/ReEvaluationJob.java:86-104.
