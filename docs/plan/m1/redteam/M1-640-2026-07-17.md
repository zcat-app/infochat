---
target: "M1-640 --in-progress"
date: 2026-07-17
base: 7b9c92d35172c6b87dd9a6d61ee56bb2a8cd1b22
head: working-tree (uncommitted, branch m1/M1-640-pin-retry-digest-classification)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: >-
  Pre-commit --in-progress audit of the M1-640 diff (widen
  InboundRouter.isInterruptible private -> package-private + new
  InboundRouterInterruptibleClassificationTest). CLEAN — the diff adds no
  attack surface (a visibility widening plus a unit test) and the test
  hardens the D35/D61 concurrency boundary it audits. No findings, no
  out-of-model items. Folds into the M1-640 commit per the lifecycle-path
  exemption.
---

RED-TEAM VERDICT: CLEAN

Audited diff M1-640 (base 7b9c92d3, working-tree, branch m1/M1-640).

Scope of the change (two functional elements plus doc/ticket bookkeeping):
  1. infochat-provider/.../messaging/InboundRouter.java:1017 — the static
     predicate isInterruptible widened from `private` to package-private,
     plus a javadoc addition. The method BODY is byte-identical to the
     pre-diff version (lines 1018-1034 unchanged).
  2. New test InboundRouterInterruptibleClassificationTest.java — a table
     pinning the D35/D61 dispatch classification.
  3. STATUS.md / ticket front-matter — documentation only.

Why CLEAN:

- No behavioral change. The dispatch fork (InboundRouter.java:855 interruptible
  → per-user M1-636 cap + worker offload; else inline runDispatchStage) is
  untouched. A diff that alters no runtime behavior cannot regress a
  threat-model commitment. Every §Rate limiting / M1-636 / D61 promise that
  held before still holds byte-for-byte.

- The visibility widening is `private → package-private` on a PURE static
  function of the step-1.7-normalized body. It is not exposed to any adapter,
  HTTP endpoint, LLM tool, or external caller; it makes no authorization
  decision (it only classifies dispatch routing). No threat-model bullet
  commits to this method's encapsulation, so widening it to the test's package
  opens no AUTH-BYPASS / PERM-ESCAL / INFO-LEAK surface.

- The test is honest, not masking. Every asserted row matches the actual code:
  non-slash → interruptible; /summary → interruptible; non-retry slash →
  non-interruptible; /retry with a whitespace-split token == "--digest" →
  non-interruptible (else interruptible). It pins exactly the classification
  the security property depends on — the branch at InboundRouter.java:855 that
  applies the M1-636 per-user concurrency cap is the same branch that offloads
  to a pool worker, and non-interruptible bodies (including /retry --digest)
  run inline and take no worker. The pin turns a future refactor that moved
  /retry --digest onto the offloaded path (voiding the D61 self-serialization
  bound) into a red build — a defense-in-depth strengthening, not a weakening.

- The inline-/retry --digest-on-the-transport-thread trade-off (LLM-invoking,
  metered only by the per-minute bucket, self-serialized to one concurrent
  call) is pre-existing behavior explicitly documented and accepted in the
  threat model (§Rate limiting: "accepted, 2026-07-17 redteam"). It is neither
  introduced nor altered by this diff, so it is not a finding here.

FINDINGS: (none)

OUT-OF-MODEL: (none)
