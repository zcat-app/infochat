---
target: M1-036
date: 2026-05-18
base: f2ca176bb2c5a40c635c4b2c092187e7c913c95f^
head: f2ca176bb2c5a40c635c4b2c092187e7c913c95f
verdict: FINDINGS
findings_count:
  critical: 0
  high: 1
  medium: 1
  low: 1
out_of_model_count: 2
disposition: |
  /redteam M1-036 ran post-/m1-tick-commit, pre-/m1-tick-merge (the
  branch commit f2ca176 is the audit head; the squash-merge to main
  has not yet happened). Verdict FINDINGS with 3 entries plus 2
  OUT-OF-MODEL items.

  CAUTION (pitfall recorded in
  .claude/projects/-home-ubuntu5-Projects-quarkus-projects-infochat/memory/feedback_redteam_postcommit_merge_pitfall.md):
  this audit file and the ticket-frontmatter updates land in the
  working tree AFTER the implementation commit. Committing the audit
  artifacts on the per-ticket branch produces a SECOND commit whose
  subject (e.g. `process: Red-team audit for M1-036`) will become the
  branch tip — `/m1-tick merge`'s `-C <tip>` will then carry that
  wrong canonical message. Two safer paths: (a) commit the audit
  artifacts on `main` AFTER `/m1-tick merge` lands the squash-merge
  of M1-036; (b) commit on the branch with `process:` prefix but
  verify and amend the merge commit's subject to point at the
  implementation commit SHA. Path (a) is cleaner.

  Classification of the three findings:

  Net-new to this diff (FIXABLE in a remediation ticket against
  M1-036; the gap is in this diff's code, not deferred infrastructure):

    1. INFO-LEAK (medium) — group-scope banned-user reply leak.
       AddSourceCommandHandler.java:108-118 short-circuits ANY group
       inbound with the `error.add_source.group_admin_only` literal
       BEFORE running the ban check at lines 126-127. A banned user
       invoking `/add-source` in a group context sees the group-admin
       error instead of the spec's required "one fixed reply
       regardless of input" ban literal. The DM branch checks
       `is_banned`; the group branch does not. The threat model's
       "Banned user receives one fixed reply per inbound message,
       regardless of input" (§User ban) is violated as soon as a
       banned user picks any group context.

       Suggested fix (code-level):
         - Reorder `handle()` to lookup actor + run ban check BEFORE
           the scope discriminator. Specifically: parse args first
           (parse-time errors short-circuit without DB lookup, which
           is fine — the spec doesn't require a banned-user check for
           unparseable input), then `lookupActor(scope)`, then ban
           check, then scope-specific gates (group → group_admin_only,
           DM → proceed), then kind resolution and the rest.
         - Alternative narrower fix: keep the scope discriminator
           first but resolve actor + ban-check INSIDE the group
           branch too. Less elegant; the reorder is preferred.
         - The reorder requires moving the actor lookup above the
           Args.parse return-shape; the upsert flow only consumes
           the user.id at line 160, so no other downstream code is
           affected.

    2. INFO-LEAK (low) — unredacted contact_id in exception messages.
       AddSourceCommandHandler.java:178-181
       (`IllegalStateException("...contact_id=" + contactId, e)`)
       and SourceUpsertService.java:138-141 (equivalent shape with
       `identifier`) embed the raw, unredacted contact id and source
       URL in exception messages. A transient SQL failure causes the
       default Quarkus exception logger to write the unredacted
       value to stdout / structured log. §Secrets handling commits:
       "Contact IDs are logged in redacted form (prefix + ellipsis
       + suffix) outside the audit log." Note: the source URL is
       per §Source URL visibility already bot-admin-visible via
       `/list-sources --all`, so the URL leak is less load-bearing
       — the contact-id case is the spec-promise gap.

       Suggested fix (code-level):
         - Introduce a `ContactIds.redact(String)` helper (or reuse
           one if M1-035b's remediation lands first) returning
           `prefix + "..." + suffix` for any contact_id-like string
           (e.g. first 6 chars + "..." + last 4 chars; the spec
           pins "prefix + ellipsis + suffix" but not the exact
           cutoffs).
         - Replace both `+ contactId` and `+ identifier` in
           exception construction with `+ redact(contactId)` /
           `+ redactUrl(identifier)`. The exception's `cause`
           (`SQLException`) still carries the full SQL diagnostic
           for ops debugging; only the user-derived strings are
           redacted.
         - Same redaction discipline applies if the M1-035b
           remediation lands a shared `ContactIds.redact()` first;
           this ticket can consume it then.

  Deferred to T2-A (acceptably out-of-scope per the same logic that
  applied to M1-035b's high-severity findings; the spec promise
  remains valid but the enforcement seam is upstream of the handler):

    3. AUTH-BYPASS (high) — probation bypass for /add-source.
       §Slow-start tier explicitly names `/add-source` as a blocked
       command during the D45 probation window. The handler doesn't
       check `probation_until`; the actor lookup at
       AddSourceCommandHandler.java:76 only loads
       `id, is_admin, is_banned`. There is no upstream probation
       gate visible in the diff (the in-handler comment at line 47
       confirms "the upstream T2-A ban gate is not yet wired" —
       probation is in the same T2-A bucket). A probation user can
       invoke `/add-source` on their first DM after registration.

       Suggested fix (deployment-level):
         - T2-A wires the probation gate upstream of InboundRouter,
           alongside ban (D11) and invite (D44) per the spec
           §Authorization model evaluation order. The same upstream
           seam handles probation, ban, and invite gating with one
           consistent fixed-reply path.

       Optional defense-in-depth fix (THIS ticket's surface, only if
       the user opts into a remediation ticket):
         - Extend the handler's SELECT to also load `probation_until`
           from `users`; if `NOW() < probation_until`, return a
           friendly "probation period" reply (requires a new bundle
           key `error.add_source.probation_blocked`). This narrows
           the gap to the `/add-source` surface but does not solve
           the broader probation enforcement; T2-A is still required
           for `/save`/`/follow-tag`/chat mode.

  The two OUT-OF-MODEL items are flagged advisories, not failures:

    - No rate limit on `/add-source` — §Rate limiting promises its
      own bucket. The rate-limit subsystem isn't built yet (same
      T2-A territory). Compounds with finding 3 if both remain
      unimplemented; tracked under T2-A's acceptance.
    - DM-only gate for `registration_state='group_only'` users —
      §Invite-code registration commits "Group-registered users do
      not get free DM access." The handler doesn't check
      `registration_state`. Whether AutoRegisterService enforces it
      upstream is not visible in this diff. Same T2-A vintage as
      finding 3.

  Followups feeding M1-036 remediation OR T2-A:

    1. (THIS ticket — remediation) — fix finding 1 (group-scope ban
       reply leak): reorder handle() so the ban check runs before
       the scope-specific branch.
    2. (THIS ticket — remediation) — fix finding 2 (contact-id log
       leak): redact contact_id in exception messages, mirror the
       M1-035b §Secrets handling discipline.
    3. (T2-A) — wire probation gate (finding 3), rate limit (OOM 1),
       group-only DM gate (OOM 2) upstream of InboundRouter.
    4. (optional, this ticket's scope) — defense-in-depth probation
       check in the handler itself; only chosen if the user wants
       the gap narrowed before T2-A lands.

  The threat-actor opened with a brief narrative confirmation before
  the structured verdict (the template asks for structure only); the
  structured FINDINGS + OUT-OF-MODEL is preserved verbatim in the
  body below.
---

# /redteam M1-036 — 2026-05-18

Verbatim output from the threat-actor subagent (`Agent(subagent_type: "threat-actor", ...)`).

---

RED-TEAM VERDICT: FINDINGS

FINDINGS:

  - CATEGORY: AUTH-BYPASS
    SEVERITY: high
    PROMISE: From `docs/spec/security.md` §"Slow-start tier": "Every newly registered user enters a probation period (decision D45). … **Blocked**: chat mode, `/add-source`, `/save`, `/unsave`, `/follow-tag`, `/unfollow-tag`, `/clear`, `/compress`, `/group-timezone`, `/retry` …" and §"Authorization model" step 7: "**Permission check** against the matrix. **Probation restrictions (D45) are part of the permission matrix**: blocked commands return a friendly 'probation period' reply and never reach execution."
    GAP: `infochat-provider/src/main/java/io/infochat/provider/command/AddSourceCommandHandler.java` lines 76 (`SELECT id, is_admin, is_banned FROM users WHERE contact_id = ?`) and 120-170 (handler flow): the actor lookup never reads `probation_until` from the `users` row, and no probation gate is enforced in the handler. There is no upstream probation gate visible in the diff (the in-handler comment explicitly admits the upstream T2-A gating is "not yet wired"). The handler proceeds straight from ban check → kind resolution → URL probe → upsert.
    REPRO: Adversary sends a first DM to the bot. AutoRegisterService (per D45 and the commented assumption that probation is the entry tier) creates a `users` row with `probation_until > NOW()`. Adversary's same message — or any subsequent message inside the probation window — is `/add-source https://attacker.example/feed --tags news`. The handler resolves the user (banned=false, admin=false), passes the ban check, runs the URL probe, and writes a `source` row and `source_subscription` for the probation user. The spec says the call should have been rejected with the "probation period" friendly reply and never reached execution. The exploit is reachable on the very first DM after registration; chains with the `/add-source` lack of rate limit (separate finding territory) to let one probation user pour many sources into the global source table, broadening the Collector's outbound fetch surface and the global tag vocabulary before any admin sees them.
    SUGGESTED-FIX-CLASS: missing-auth-check

  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: From `docs/spec/security.md` §"User ban": "Banned-user check is the first thing after identity resolution. Banned user receives **one fixed reply per inbound message, regardless of input**." And §"Authorization model" step 4: "**Ban check.** If `is_banned=true`: fixed reply, stop. No parser, no DB query past the ban check, no LLM."
    GAP: `infochat-provider/src/main/java/io/infochat/provider/command/AddSourceCommandHandler.java` lines 108-118 (group-scope short-circuit): `if (scope instanceof ScopeRef.Group) { return reply(scope, bundleLoader.get(BundleKeys.ERROR_ADD_SOURCE_GROUP_ADMIN_ONLY)); }`. The group-scope return path fires unconditionally BEFORE any ban check, so a banned user invoking `/add-source` in a group context receives `error.add_source.group_admin_only` ("Only group admins can add sources to a group.") rather than the fixed `error.add_source.banned` reply (which is also a per-handler variant of the fixed ban reply, not even one global literal — itself another spec gap). The handler's own JavaDoc and an inline comment confirm "the upstream T2-A ban gate is not yet wired so the handler reads the flag itself" — meaning the upstream Step-4 ban check does not currently exist either. The DM branch DOES check `is_banned`; the group branch does not.
    REPRO: Bot admin bans contact `evil-1` via a separate path. `evil-1` is a known member of a group the bot serves. `evil-1` sends `/add-source https://attacker.example/feed --tags news` in the group. The bot replies "Only group admins can add sources to a group." instead of the spec-required fixed ban reply. `evil-1` now knows (a) the bot is alive in this group, (b) the bot recognises `/add-source`, and (c) the ban gate did not fire — useful reconnaissance to probe other commands and to confirm the ban is not enforced across all surfaces. The spec's "fixed reply, regardless of input" guarantee is violated because the input-driven friendly error leaks beyond the ban path.
    SUGGESTED-FIX-CLASS: trust-boundary-tightening

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: From `docs/spec/security.md` §"Secrets handling": "Contact IDs are logged in redacted form (prefix + ellipsis + suffix) outside the audit log."
    GAP: `infochat-provider/src/main/java/io/infochat/provider/command/AddSourceCommandHandler.java` lines 178-181: `throw new IllegalStateException("AddSourceCommandHandler.lookupActor failed for contact_id=" + contactId, e);` — the raw, unredacted `contactId` is embedded in an exception message. A `SQLException` propagated from `dataSource.getConnection()` or `ps.executeQuery()` (e.g. transient connection refusal during a Postgres failover, the very class of "real" runtime errors that produce stack traces) re-throws this `IllegalStateException`, which Quarkus default exception logging will print unredacted to stdout / structured log. `infochat-provider/src/main/java/io/infochat/provider/source/SourceUpsertService.java` lines 138-141 has the equivalent pattern with `identifier` (the user-supplied URL) — less load-bearing per the threat model since URLs are bot-admin-visible, but the same shape.
    REPRO: Adversary picks a moment when Postgres is briefly unreachable (or any other SQL-layer hiccup), sends `/add-source <something> --tags x` from contact id `simplex://abc123def456…verylongstableid`. The Provider logs `IllegalStateException: AddSourceCommandHandler.lookupActor failed for contact_id=simplex://abc123def456…verylongstableid` at ERROR with the full stack. An operator with read access to the Provider's stdout / log aggregator (a wider audience than the audit-log Postgres role) now sees the full contact id, defeating the redaction promise. The vector is reachable any time the DB has a transient failure or the connection pool exhausts.
    SUGGESTED-FIX-CLASS: input-sanitization

OUT-OF-MODEL:
  - The diff adds no rate limit on `/add-source`, while `docs/spec/security.md` §"Rate limiting" promises "**`/add-source`** — its own bucket". The rate-limit framework as a whole is not exercised anywhere in the diff and appears to be a separate (unbuilt) subsystem; flagging this as a defense-in-depth gap but not a per-ticket finding, since the spec's own §"Rate limiting" framing is at the deployment level rather than per-command-handler. Recommend confirming whether rate limiting is a separately-tracked ticket; if it is not, the lack of a token-bucket for this surface compounds the probation-bypass finding above (one probation user can pump many sources in seconds).
  - The handler does not validate the DM-only gate for `registration_state='group_only'` users (spec §"Invite-code registration": "Group-registered users do not get free DM access"). A `group_only` user invoking `/add-source` in DM is accepted by this handler. Whether AutoRegisterService or the inbound router enforces the gate upstream is not visible in this diff; flagging in case the upstream gate is also unbuilt. If both gates are missing, this would compound to a fourth finding (group-only DM access via `/add-source`).
