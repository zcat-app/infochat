---
target: M1-624
date: 2026-07-15
base: c8ff68b1755e166cb64f41f5f9b08b4ffedad6dc
head: working-tree (uncommitted M1-624 impl on m1/M1-624-admin-claim-probation-welcome)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  In-progress audit fired by /m1-tick run (security_relevant: true, set during
  the clarity-fail self-refine because the change lives in the SimpleX
  bootstrap-admin claim branch of the DM-intake gate, D50). CLEAN — no findings.
  Diff-range note: the branch carried 0 commits beyond main at audit time (the
  implementation is uncommitted per the run pre-commit flow) and a same-prefix
  refine commit ("M1-624: refine ticket spec") sat on main, so the skill's
  merged-form step-1 would have falsely matched the refine (ticket-file only)
  and the branch-form would have produced an empty diff. Audited the actual
  implementation = working-tree code diff vs the fork point c8ff68b1 (the same
  5-file change the code-reviewer APPROVEd): a new reply.welcome.admin_claim
  bundle key (en+cs) + BundleKeys constant, the InboundRouter Claimed-branch
  reply switch from the shared probation welcomeReply to the admin welcome, and
  the authorized test assertion update. No auth/authz/ban/audit logic changed —
  only the user-facing reply string on an already-decided Claimed outcome.
---

RED-TEAM VERDICT: CLEAN

Audited M1-624 (working-tree vs fork c8ff68b1) against docs/spec/security.md
in full. The diff swaps the welcome message sent on a SUCCESSFUL SimpleX
bootstrap-admin claim from the shared probation welcome
(REPLY_WELCOME_DM_FRESH) to a new distinct admin welcome
(REPLY_WELCOME_ADMIN_CLAIM), adds the en/cs bundle strings, and updates
SimpleXAdminClaimTokenTest.

Files in scope:
  - infochat-provider/src/main/java/app/zcat/infochat/provider/bundle/BundleKeys.java
  - infochat-provider/src/main/java/app/zcat/infochat/provider/messaging/InboundRouter.java
  - infochat-provider/src/main/resources/bundles/en.properties
  - infochat-provider/src/main/resources/bundles/cs.properties
  - infochat-provider/src/test/java/app/zcat/infochat/provider/messaging/SimpleXAdminClaimTokenTest.java

Threat-model commitments checked against the diff, none breached:

1. No validity oracle on the SimpleX admin token (§Authorization model,
   Bot-admin bullet; §Per-adapter admin threat profile). The spec commits
   that "while that admin exists a later presentation grants nothing and
   gives the same fixed reply an invalid invite would (no validity oracle)."
   The diff changes ONLY the success branch
   (InboundRouter.java:535-537), which is reachable exclusively by the first
   holder of the operator-secret token while no admin yet exists — a caller
   who thereby BECOMES admin. The failure / later-presentation branch is
   unchanged: a NotClaimed outcome still falls through to
   InviteCodeConsumer and returns the fixed ERROR_INVITE_REQUIRED reply
   (InboundRouter.java:540-548). No new distinguishing signal is exposed to
   a token-less attacker; the post-admin-exists oracle surface is untouched.

2. Audit logging of the admin bootstrap (§Authorization model; CLAUDE.md
   bootstrap admin). The is_admin flip and any audit write live inside
   simpleXAdminClaim.claim(...), which the diff does not modify. No
   AUDIT-EVASION introduced or regressed.

3. Cross-user / other-user state disclosure (§Trust boundaries;
   Prompt-injection defenses). The new admin welcome is delivered only to
   the connection that legitimately just claimed admin. It reveals nothing
   about any other user or scope. Rendered in the "en" context default
   (unknown contact has no scope_preferences row), consistent with the
   surrounding code.

4. Plain-text formatting / LLM output sanitizer (§LLM output sanitizer;
   CLAUDE.md plain-text rule). The reply is a static localization-bundle
   string emitted on the deterministic command path (never through an LLM,
   so the sanitizer does not apply), containing only non-privileged command
   hints (/follow-tag, /add-source, /help) — no /grant-admin, /ban,
   /promote, etc. Sent via raw bundleLoader.get (no MessageFormat), so the
   single apostrophes and the absent {0} placeholder are correct and carry
   no injection vector.

5. Authorization evaluation order, ban ordering, rate limits, permission
   tiers (§Authorization model steps 1-9; §Rate limiting). None are altered;
   the change lives entirely inside the already-gated step-2 success branch.

The change is a UX copy correction that improves accuracy — previously a
newly-minted bot admin was told (via the probation welcome) that they were
on probation. No gap between the threat model's promises and the diff's
delivery.

FINDINGS: (none)

OUT-OF-MODEL: (none)
