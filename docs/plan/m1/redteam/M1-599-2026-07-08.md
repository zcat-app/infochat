---
target: M1-599 --in-progress
date: 2026-07-08
base: 457b2dac
head: working-tree-uncommitted (--in-progress branch tip, pre-commit)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: >-
  CLEAN — no findings. D21 tagger title-wrap (item 1) and the classifier
  remote-routing privacy disclosure (items 4-5) are both delivered as the
  threat model promises; routing completeness verified (set_llm_base_urls /
  clear_remote_llm_creds wildcard / RemoteLlmWiringTest assertions). One
  out-of-model doc-consistency observation: docs/design/05 §5.8 summary-prompt
  template shows title outside the <<<UNTRUSTED_CONTENT>>> wrapper, but the code
  (SummaryProseGenerator.buildPrompt) wraps it correctly — a stale design note,
  NOT a live D21 gap. Out of M1-599 scope; candidate for a doc-sync follow-up
  alongside the §5.10 privacy-tasks-list staleness the clarity/review WARNs
  flagged (both docs/design/05, both non-blocking).
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Doc-consistency observation, NOT an exploitable finding: the design-note
    summary/digest prompt template in docs/design/05-llm-and-embeddings.md
    (~line 385, §5.8) still shows the post `title:` as a metadata line OUTSIDE
    the `<<<UNTRUSTED_CONTENT>>>` wrapper, with only `{{{body_or_summary}}}`
    inside. This diff aligned the tagger/entity design templates (lines 257,
    280, 302) but left the §5.8 template untouched. The actual behavior is
    safe: SummaryProseGenerator.buildPrompt (infochat-provider/.../summary/
    SummaryProseGenerator.java:170-182) appends the per-call `open` marker
    BEFORE the post loop, so title/body/url all sit inside the delimiter
    (matching M1-040). No live D21 gap — the design template is merely stale
    relative to the code. Flagged only so the user can decide whether to
    re-sync the design note; the threat-model commitment is met in code.

Audit notes (why CLEAN):
  - D21 promise ("Every prompt that includes user-derived text is wrapped in
    a delimiter block whose marker contains a per-call random value") is
    DELIVERED for the tagger by this diff: prompts/tagger.md:26-30 and
    prompts/tagger-fallback.md:33-37 now place `Post title: {{title}}` INSIDE
    the `<<<UNTRUSTED_CONTENT id="{{id}}">>>` ... `<<<END id="{{id}}">>>`
    block. The {{id}} is a per-call UUID (TaggerWorker.renderPrompt), so an
    attacker-controlled feed title can no longer reach the model as
    un-delimited instructions. TaggerWorkerTest.renderPrompt_wrapsTitleInsideDelimiter
    asserts the invariant.
  - Adjacent LLM call sites carrying a user-derived title were checked and are
    already compliant: EntityExtractorWorker's inline PROMPT_TEMPLATE sits the
    title between the two `{{id}}` markers; classifier.md was fixed in M1-597;
    SummaryProseGenerator wraps title/body/url inside a per-call marker.
    security-judge.md and translator.md wrap only body/content, no title.
  - Secrets-handling promise ("prints a per-task privacy disclosure naming
    exactly which generative tasks now call a remote provider") is DELIVERED
    for the newly-added classifier ingest task: prod/switch-llm.sh and
    prod/scripts/4-llm.sh add `classifier` to LLM_TASKS and add a `classifier)`
    disclosure arm. Routing is complete (no partial-wiring INFO-LEAK):
    set_llm_base_urls iterates $LLM_TASKS (base-url set remote),
    clear_remote_llm_creds clears api-key via wildcard sed on switch-away, and
    RemoteLlmWiringTest asserts both infochat.llm.classifier.base-url and
    .api-key. Routing the classifier remote INCREASES disclosed exposure of
    PUBLIC post-kind classification, which is now surfaced to the operator; it
    does not touch private user data.
  - No authentication, authorization/admin-tier, input-validation/intake,
    ban-handling, or audit-write surface is modified by the diff. Remaining
    hunks are STATUS.md / ticket-metadata bookkeeping.
