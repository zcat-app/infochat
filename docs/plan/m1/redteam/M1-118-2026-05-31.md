---
target: M1-118
date: 2026-05-31
base: e5ce26e93a390eb0776a8a3ff762314c234c1950
head: 13ec3689bd2a12b9d5141e9e62f3f8dd68d548e0
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 3
disposition: |
  M1-118 remediates two findings from the M1-103 red-team audit (INJECTION/high on
  contactId, DOS/medium on inbound text cap). The post-remediation audit confirms
  the diff closes both gaps and introduces no new finding against the documented
  threat model. The three OUT-OF-MODEL observations all reduce to "the
  simplex-chat subprocess is operator-controlled and therefore trusted per
  docs/spec/security.md §Threat model — Operator-set config is trusted":
    - (1) IllegalStateException from encode-time validator is propagated as
      RuntimeException rather than MessagingException(PERMANENT); reachable only
      via a hostile simplex-chat process fabricating a tainted SendAck.
    - (2) decodeSendAck does not call isValidQueueAddressId on chatItemId at
      capture time; rejection is deferred to the encode-time check on next
      update/finalize.
    - (3) JSON frames up to 1 MiB (SimpleXWebSocketClient.MAX_FRAME_BYTES) are
      parsed before the 16 KiB text cap fires; CPU cost of pre-cap JSON parsing
      is bounded by the operator-controlled simplex-chat process.
  None are escalated as remediation tickets — moving the simplex-chat subprocess
  into the threat model would be a spec amendment, not a remediation. If a
  future ticket tightens the SimpleX adapter's trust assumption (e.g. M1-119
  hardening expands beyond logging hygiene), observations (1) and (2) become
  candidate hardening items at that time.
  The threat-actor reply leaked a short reasoning trace before the structured
  verdict despite the prompt's "return only" instruction; the structured verdict
  below is captured verbatim, the reasoning trace is summarized into this
  disposition note and discarded.
  Audited the per-ticket branch tip BEFORE squash-merge to main (head SHA above
  is the branch tip; the post-merge squash commit on main is fcc47b6).
---

# Red-team verdict — M1-118 (2026-05-31)

**Target:** M1-118 (single ticket, post `/m1-tick commit` pre `/m1-tick merge`)
**Base:** `e5ce26e93a390eb0776a8a3ff762314c234c1950` (fork-point with main)
**Head:** `13ec3689bd2a12b9d5141e9e62f3f8dd68d548e0` (per-ticket branch tip; squashed onto main as `fcc47b6` afterwards)
**Threat model:** `docs/spec/security.md` (verbatim as substituted into the prompt)

## Structured verdict (verbatim)

```
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Encode-time IllegalStateException from requireValidQueueAddressId in
    infochat-messaging-adapter/src/main/java/app/zcat/infochat/messaging/impl/simplex/SimpleXMessageCodec.java:187-196
    is propagated as RuntimeException (not MessagingException) up through
    SimpleXAdapter.send/update/finalize/setTyping. The threat model promises a
    SimpleX peer cannot inject command verbs; the diff delivers that promise.
    But a hostile simplex-chat process that fabricates an injection-bearing
    chatItemId in a SendAck would currently surface as an unchecked
    IllegalStateException at the Provider's next update/finalize on that
    handle, not as the SPI-contracted MessagingException(PERMANENT). This is a
    reliability/contract-shape concern, not a security gap against the
    documented threat model (the simplex-chat subprocess is operator-controlled
    per docs/spec/security.md §Threat model — "Operator-set config is
    trusted"), so it is out of model.
  - decodeSendAck at
    infochat-messaging-adapter/src/main/java/app/zcat/infochat/messaging/impl/simplex/SimpleXMessageCodec.java:347-356
    does not call isValidQueueAddressId on the chatItemId before storing it in
    the handle map; rejection is deferred to the encode-time check on next
    update/finalize. Same trust-anchor reasoning as above — the simplex-chat
    process is operator-controlled, and a one-shot send that never updates
    leaves the bad id in memory only.
  - The decode path processes JSON frames up to MAX_FRAME_BYTES (1 MiB) per
    infochat-messaging-adapter/src/main/java/app/zcat/infochat/messaging/impl/simplex/SimpleXWebSocketClient.java:53
    before the 16 KiB inbound text cap fires. The transport-level rate cap
    (security.md §Authorization model step 1.5) lives on the Provider side and
    gates the inbound after decode; flood-driven JSON parsing CPU cost at up
    to 1 MiB/frame is bounded by the operator-controlled simplex-chat process,
    not the remote peer. Out of model for the same reason.
```

## Findings

None. The diff closes the two M1-103 audit gaps it set out to remediate and
introduces no new gap against the documented threat model.

## Out-of-model observations

See the structured verdict above. All three observations are bounded by the
operator-trusted simplex-chat subprocess; promoting any of them to an in-model
finding would require a spec amendment that narrows the trust assumption on
the SimpleX subprocess, not a remediation ticket.
