---
target: M1-758
date: 2026-08-04
base: 29aa59afbd80e13df6265e6934c77f1a4a06ffe6
head: "<working tree>"
verdict: FINDINGS
findings_count: {critical: 0, high: 0, medium: 0, low: 1}
out_of_model_count: 3
disposition: |
  Round 9, against the post-round-8 rework. That rework retired the
  machine-translation marker entirely: `finishDisplayHit`
  (TranslationPipeline:346-354) now returns `bounded + "\n[" + original + "]"`,
  and `reply.translation.hit_marker` / `REPLY_TRANSLATION_HIT_MARKER` are
  deleted from BundleKeys and all five bundle files. That closes the r8
  finding at its root — there is no longer a constant, forgeable, per-language
  tag for a publisher to pre-craft, and the bracketed line carries no
  authorship claim.

  The single low is the stale half of that removal, VERIFIED in-tree before
  accepting: USER_GUIDE.md:124 (a line this diff ADDS, in the `/lang` command
  table) still reads "translated headlines are usually tagged, in the language
  you picked", while a repo-wide grep for hit_marker/HIT_MARKER over
  **/*.java and **/*.properties returns nothing — no tag is emitted on any
  leg. USER_GUIDE.md:298-311, added by the same diff, states the real rule
  (translation on the primary line, original bracketed beneath, unbracketed
  means publisher-verbatim). The two are mutually exclusive and :124 is the
  one a reader reaches first. The reachable harm is provenance confusion on a
  headline that is LLM output over hostile-feed input — bounded, hence low,
  and the same class rounds 5-8 kept re-finding: every rewrite of a
  security-adjacent sentence is new surface for the same review.

  PATTERN (rounds 5-9): r5 false invariant -> r6 false render detail -> r7
  unreachable literal -> r8 overclaimed trust property -> r9 one stale
  sentence surviving the mechanism's removal. Strictly narrowing; this round's
  finding is a single stale line, not a new claim.

  REMEDIATED after the audit (both text-only, no executable line changed):
  USER_GUIDE.md:124 now reads "a translated headline shows the original in
  square brackets underneath", matching :298-311 — the finding is closed.
  Out-of-model item 2 also fixed: the `finishDisplayHit` javadoc no longer
  renders llm.md §D29's "unbracketed always means already in the reader's
  language" as a property of this leg; it states the one-way implication and
  names the two arms that falsify the converse (runForDisplayHit's entry
  guard on an absent/malformed source language; a caller whose per-render
  translation budget is spent), attributing the absolute form to the D29
  target render (M1-759).

  Out-of-model item 1 (split sanitize units) DISMISSED, verified: the
  residual class is pre-existing and explicitly accepted by security.md:539
  ("a privileged command split ACROSS two posts' fields — `/list-sources` in
  one title, `--all` in another — is neither redacted nor audited"), and the
  new pair does not widen it. An attacker already gets two adjacent
  separately-sanitized units from two co-rendered titles; the title+
  translation pair is a strictly HARDER path to the same accepted outcome,
  since the flag half must be coaxed out of the model rather than typed.
  Dispatch still requires is_admin=true either way.

  NOT ACTIONED (read-only skill):
  Out-of-model item 1 (split sanitize units — original and translation now
  reach the reader as two separately-sanitized strings joined by `\n`, where
  before only one string reached the reader on this leg) is the one worth a
  decision: it is a real widening introduced by the round-8 rework, argued
  down to advisory because dispatch still needs is_admin and the only
  flag-bearing payoff is already admin-visible.
---
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INJECTION
    SEVERITY: low
    PROMISE: docs/spec/security.md §Threat model — "**LLMs** (local or remote) are
              black boxes that can be coaxed into emitting attacker-chosen output.
              Local and remote LLMs have the same trust level." — and §Rate limiting,
              as this diff itself writes it, which names the *bracket* as the shipped
              render-side signal distinguishing machine-translated headlines from
              publisher text: "rows beyond the budget render untranslated,
              **unbracketed**" (security.md:1670) and "headlines beyond the budget
              render untranslated and **unbracketed**, the same degraded shape the
              `/saved` page uses" (security.md:1696).
    GAP: This round replaced the machine-translation provenance marker with a
          bracketed-original render, and deleted the marker from the product —
          but the ONLY end-user-facing document still tells users the marker is
          how they identify a machine translation, and the diff itself is the
          commit that added that line.
          - Removed: TranslationPipeline.java:346-354 — `finishDisplayHit` now
            returns `bounded + "\n[" + displayHeadline + "]"`; the
            `bundleLoader.get(BundleKeys.REPLY_TRANSLATION_HIT_MARKER, ...)` call
            is gone, and its own javadoc (:341-344) states "no bundle-resolved
            label is involved: the bracketed line carries no claim about who
            produced the translation."
          - Removed: BundleKeys.java (REPLY_TRANSLATION_HIT_MARKER declaration)
            and `reply.translation.hit_marker` from ALL FIVE bundles —
            en.properties, cs.properties, es.properties, ru.properties,
            tr.properties. A repo-wide grep returns no remaining source or
            resource occurrence: no tag of any kind is emitted on any leg.
          - Left asserting the removed mechanism: USER_GUIDE.md:124 (an ADDED
            line in this diff, the `/lang` row of the command table) —
            "translated headlines are usually **tagged**, in the language you
            picked". Nothing is tagged, and the one line that does appear (the
            bracketed original) is in the SOURCE language, i.e. the opposite of
            "the language you picked".
          - The same file contradicts itself 180 lines later: USER_GUIDE.md:298-311
            (also added by this diff) states the real rule — translation on the
            primary line, original in brackets beneath, and "A headline with no
            bracketed line under it is shown exactly as the publisher wrote it".
            The two rules are mutually exclusive, and :124 is the one a reader
            reaches first (it is in the command reference, not the tutorial).
          A reader applying :124 finds no tag on any headline — because none
          exists — and concludes no headline was machine-translated, i.e. that
          every primary line is the publisher's own words. The primary line on a
          translated headline is LLM output over a hostile-feed-controlled input,
          which the threat model classes as attacker-influencable.
    REPRO: 1. A group is on `/lang cs` (or es/ru/tr). An attacker controls, or can
              inject into, one subscribed feed (§Threat model: "Every RSS
              publisher, Reddit poster, Bluesky user, etc. is untrusted").
           2. The attacker publishes a post whose title carries an instruction to
              the translator rather than translatable prose (the translator input
              is the raw `DisplayHeadline` — a source-authored title — per
              TranslationProvider.java's own amended javadoc). Per §Threat model
              the model can be coaxed into emitting attacker-chosen output, so the
              translation line becomes attacker-chosen Czech text rather than a
              rendering of the title.
           3. The scheduled digest broadcasts to the group:
                · <attacker-chosen Czech text>
                [<innocuous original title>]
                https://…
           4. A group member who learned `/lang` from USER_GUIDE.md:124 looks for
              "a tag in the language you picked", finds none anywhere in the
              digest, and reads the Czech line as the publisher's headline. The
              signal that would have let them discount it — the machine-translation
              marker — was shipped until this diff and is now gone from code and
              from all five bundles, while the document that names it remains.
           5. Nothing in the delivery is malformed, so no sanitizer, audit row or
              WARN fires: the closed-list pass only redacts privileged command
              tokens, and the payload carries none.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - New split-sanitize-unit pair inside ONE post's render. `finishDisplayHit`
    (TranslationPipeline.java:353) now delivers the publisher's original headline
    and its translation adjacent, separated by a bare `\n`. The two are sanitized
    by SEPARATE `LlmOutputSanitizer` calls — the original inside
    `DisplayHeadline.of` (DisplayHeadline.java:138), the translation by
    sanitizer-2 — so a flag-bearing closed-list entry whose command word lands in
    one string and whose flag lands in the other is neither redacted nor audited,
    while §"Flag position mirrors the parser's own scan" notes `split("\s+")`
    treats `\n` as a token separator, so the pasted pair still dispatches. Before
    this diff only ONE of the two strings reached the reader on the translating
    leg. security.md accepts the residual ("a privileged command split ACROSS two
    posts' fields … is neither redacted nor audited") but justifies it by an
    argument — merging would let a co-clustered attacker delete a third party's
    post — that does not apply here, since both strings are the SAME author's
    field and its translation. Not raised as a finding: reaching it needs the
    translator to emit a token the original did not carry (model manipulation),
    dispatch still requires `is_admin=true`, and the only flag-bearing payoff
    (`/list-sources --all`) discloses a catalogue §Source URL visibility already
    says is admin-visible. Flagged so the user can decide whether the residual's
    stated justification should be widened or the two strings merged into one
    sanitize call.
  - Code comment renders a target-state claim the same code falsifies.
    TranslationPipeline.java:337-338 cites "docs/spec/llm.md §D29 display leg: an
    unbracketed line always means the text is already in the reader's language".
    That "always" is false of the shipped leg on at least four arms —
    budget-exhausted rows (`infochat.digest.translation-max-per-render`,
    `infochat.save.translation-max-per-page`), a rejected LLM rate-cap draw, a
    degraded cluster, and a null `sourceLanguage` — each of which renders
    unbracketed in the SOURCE language. USER_GUIDE.md:307-311, added by the same
    diff, enumerates those cases correctly, so the user-facing rendering is
    stronger than the code comment it derives from. llm.md is outside the threat
    model and was dispositioned at round 6 as target state for M1-759, so this is
    noted, not filed.
  - README.md:216 (touched by this diff) still calls the summarizer "the
    request-time `summarizer`", while both runtime disclosures this diff corrects
    now name its unattended ingest-time leg ("abstracts of EVERY long fetched
    PUBLIC post (BodySummaryWorker, on a timer, no user present)"). Not filed: the
    same README sentence already places public post bodies in the remote-exposure
    set, so no data class is hidden — only the schedule is understated, and
    security.md designates the two runtime blocks plus SETUP_GUIDE §"Switching
    your AI backend later" as the disclosure surfaces, not README.

VERIFIED-AND-CLOSED (spot-checks run this round, reported so the loop can stop
re-deriving them):
  - The structural claim security.md now makes — "The enumeration above covers
    every production call site reaching `ModelTask.TRANSLATOR` as of M1-758" — is
    TRUE. Independent re-derivation over main sources returns exactly three
    `generate(ModelTask.TRANSLATOR, …)` sites: LlmTranslationProvider.java:94
    (prose + display-hit, via TranslationPipeline), QueryAnchorTranslator.java:260,
    IngestTranslationWorker.java:318. All three are enumerated.
  - The Tier-B claim printed by BOTH runtime disclosures is TRUE against code:
    IngestTranslationWorker.java:232 is `@Scheduled` with no enable flag;
    :264 the sole skip arm is `"en".equals(row.language())` — the SOURCE's
    declared language; :358-366 `renderPrompt` interpolates `{{title}}` plus the
    full untruncated `{{body}}`. No "sends nothing"/"strict no-op" wording
    survives in either block.
  - The pin-survival claims are TRUE against switch-llm.sh: Phase 2b (:301-308)
    refuses ONLY on an empty backend answer over a pinned config and exits without
    writing; Phase 3 (:352-355) deletes every per-task base-url/api-key
    unconditionally on any run that proceeds, announcing them at :348-351. No
    fabricated "decline the consent prompt" safeguard remains in either the
    printed text or the block comments (4-llm.sh:482-501 now states the opposite
    explicitly).
  - Per-task parity between the two runtime disclosures holds: chat and translator
    lines are byte-equal; the install block's summarizer line is now STRONGER than
    the switcher's (adds "on a timer, no user present").
  - No `assertFalse` in either wiring test targets a still-true exposure claim:
    RemoteLlmWiringTest's negatives target tier placement ("-  translator"), the
    retired pre-M1-746 phrasing, "decline its consent prompt", "one at a time" and
    "sends nothing"; SwitchLlmWiringTest's target "sends nothing" and "strict
    no-op". The round-1 anti-pattern is not reinstated.
