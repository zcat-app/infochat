---
target: "M1-609 --in-progress"
date: 2026-07-12
base: 1a0d24ad490563f3f36e2156f9867e0bb8b6b49b
head: working-tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: >-
  Spike diff is a findings report + standalone Python eval harness + JSONL
  fixture under docs/plan/ — no runtime/service code, no auth/authz/ban/audit
  surface, no routing-surface change. CLEAN. The single OUT-OF-MODEL item
  (harness sends INFOCHAT_LLM_API_KEY as a Bearer header to the operator-set
  INFOCHAT_LLM_BASE_URL) is not a threat-model violation: base URL is trusted
  operator/developer config, the harness is non-runtime spike tooling, and the
  key is never written to output artifacts. No follow-up ticket — the pattern
  matches the already-merged M1-610 harness. No remediation owed.
---

RED-TEAM VERDICT: CLEAN

FINDINGS: (none)

OUT-OF-MODEL:
  - Developer eval harness credential egress. `docs/plan/m1/spikes/local-vs-remote-eval.py`
    resolves its endpoint from the `INFOCHAT_LLM_BASE_URL` environment variable
    (resolve_backend, lines ~757-768) and sends `INFOCHAT_LLM_API_KEY` as an
    `Authorization: Bearer` header to that URL (call(), lines ~604-627). A hostile
    value of `INFOCHAT_LLM_BASE_URL` would exfiltrate the key to an attacker host.
    This is OUT-OF-MODEL: (a) the base URL is operator/developer-set config, which
    §Threat model classifies as trusted; and (b) the script is a non-runtime spike
    harness under docs/plan/, not part of the built services, so it is not an
    adversary-reachable surface. The harness does NOT write the key into its output
    JSON (only base_url, results, latencies), so there is no secret-in-artifact leak.
    Flagged only so the user can decide whether dev-tooling credential handling
    warrants any hardening; it is not a threat-model violation.
