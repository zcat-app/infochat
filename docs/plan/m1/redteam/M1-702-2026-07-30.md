---
target: M1-702
date: 2026-07-30
base: 5a63ff1a26eb70229139ee9020a0baa767551e93
head: "<working tree>"
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 3
disposition: |
  Audit ran at the /m1-tick run redteam gate, ahead of review, against the
  uncommitted working-tree diff (branch carries zero commits at gate time).
  No findings. Three out-of-model observations recorded below; none blocks
  this ticket and none is auto-filed — the redteam skill is advisory on
  out-of-model items.
---

RED-TEAM VERDICT: CLEAN

Scope audited
-------------
Diff: target/redteam-diff-M1-702.diff (base 5a63ff1a26eb70229139ee9020a0baa767551e93,
head = working tree). Three files touched:
  - docs/plan/m1/STATUS.md            (process bookkeeping only)
  - docs/plan/m1/tickets/M1-702-*.md  (front-matter status/clarity_check only)
  - infochat-core/pom.xml:158-195     (new maven-enforcer bannedDependencies execution)

Threat-model surface engaged
----------------------------
docs/spec/security.md §"The chokepoint routing is build-guarded" (lines 629-666),
residual route 2: "a sender compiled in a sibling module (scoped to the provider
module today, bounded by the module DAG — collector, llm-adapter, ssrf, and
messaging-adapter are enforcer-blocked from the messaging-adapter dependency,
though infochat-core's edge is convention-only and tracked as a follow-up)".

The diff narrows that residual rather than widening it. Verified:

1. Rule shape is byte-equivalent to the already-proven collector execution
   (infochat-collector/pom.xml:184-206): same goal (`enforce`), same phase
   (`validate`), same `<bannedDependencies>` rule, same GA exclude pattern
   `app.zcat.infochat:infochat-messaging-adapter`, `<fail>true</fail>`.
2. Plugin version is genuinely managed — parent pom.xml:126-130 pins
   maven-enforcer-plugin 3.5.0 in <pluginManagement>, so the child stanza is
   not a version-less no-op.
3. The exclude pattern is groupId:artifactId only, so it matches every version,
   type and scope — including the `<type>test-jar</type>` form the provider uses
   (infochat-provider/pom.xml:51-55) and provided/optional scopes. No
   type-shaped or scope-shaped bypass.
4. `searchTransitive` is left at its default (true), so a transitive pull of the
   messaging SPI into core is caught, not just a direct declaration.
5. Module-DAG coverage after this diff is complete for the non-provider set the
   threat model enumerates: collector (ban-messaging-adapter-from-collector),
   ssrf (ban-sibling-shared-modules, infochat-ssrf/pom.xml:63-64), llm-adapter
   (infochat-llm-adapter/pom.xml:139-140), messaging-adapter (siblings ban,
   infochat-messaging-adapter/pom.xml:123-124), and now core.
6. Build-time only: `validate` phase, no dependency added or removed, no change
   to any shipped runtime artifact, no runtime code path, no auth gate, no audit
   emission, no input-handling surface. Nothing in the diff reroutes or replaces
   an existing code path, so there are no incidental controls (sanitize calls,
   audit rows, authorization checks) to carry across.

Adversarial probes that came back negative
------------------------------------------
- Paper-tiger check: the execution is not shadowed by a competing parent
  execution (parent declares enforcer in <pluginManagement> only, never in
  <build><plugins>), and no `enforcer.skip` property is set anywhere in the repo.
- Scope-evasion check: a test-scope or provided-scope edge from core to the
  messaging SPI is still banned (all-scope match).
- Transitive-evasion check: pulling the SPI in behind another internal module is
  still banned (searchTransitive default).
- Widening check: the new stanza grants nothing and relaxes nothing; it cannot
  be used to reach MessagingAdapter.send, .update or .finalizeMessage from core.

No promise in docs/spec/security.md is left undelivered by this diff.

OUT-OF-MODEL:
  - Not an attack, a documentation-drift note for the user's judgement: after
    this diff, docs/spec/security.md line ~648 ("infochat-core's edge is
    convention-only and tracked as a follow-up") understates the control that
    now ships. Understatement is not a security weakness — the code is stronger
    than the prose, not weaker — so it is not scored as a finding, but the user
    may want a `spec:` commit to retire that clause so the residual list stays a
    truthful inventory.
  - Two residual routes the module ban structurally cannot reach remain exactly
    as the threat model already documents them, and this diff neither widens nor
    narrows either: reflective invocation from any module, and direct transport
    access that bypasses the MessagingAdapter SPI entirely. Both are already
    accepted residual risk in §"The chokepoint routing is build-guarded".
  - Also unchanged and already spec-acknowledged: classes inside
    infochat-messaging-adapter itself necessarily call the SPI's outbound
    methods and are outside the provider-scoped ArchUnit guard. That is inherent
    to the module that defines the SPI, is not an edge a bannedDependencies rule
    can express, and predates this diff.
