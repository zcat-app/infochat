---
target: M1-435
date: 2026-06-23
base: e38bd1e80ac233293105c4a24f6bea3b601fafaf
head: "(working tree — m1/M1-435 branch, uncommitted, --in-progress)"
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  Pre-commit --in-progress audit of M1-435 (add U+2028/U+2029 to the
  single-line metadata-field strip). CLEAN: zero findings across all
  severities. Two OUT-OF-MODEL advisories recorded verbatim below;
  advisory only, no remediation required for this ticket.
---

RED-TEAM VERDICT: CLEAN

FINDINGS: (none)

OUT-OF-MODEL:
  - The diff strips U+2028/U+2029 only from the single-line metadata
    fields (title / url) via stripMetadataField, and deliberately
    preserves them on the body path (stripBidiAndZeroWidth, pinned by
    the new test stripBidiAndZeroWidthPreservesLineAndParagraphSeparators).
    The threat model (security.md §Ingest pipeline) commits the body to
    "NFKC, bidi-control and zero-width stripping" only — it does NOT
    commit to removing U+2028/U+2029 from the body, and NFKC does not
    fold them. So a post BODY can still carry U+2028/U+2029 line/paragraph
    separators into user-facing prose. This is consistent with the spec
    (the body legitimately spans lines), so it is NOT a delivery gap
    against any stated commitment — flagged only so the user can decide
    whether the body's line/paragraph-separator handling deserves an
    explicit spec stance. The bare-emitted-extra-line obfuscation the
    diff closes for titles/urls remains theoretically reachable in body
    rendering, but the body is multi-line by design and downstream
    rendering treats it as such.
  - Stage 2 LLM judge sees the original pre-redaction body, not the
    title/url; the metadata-field strip does not feed Stage 2 and
    introduces no new judge-input surface. Noted only to confirm the
    diff does not perturb the Stage 1 -> Stage 2 contract.
