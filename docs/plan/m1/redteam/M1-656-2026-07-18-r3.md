---
target: M1-656
date: 2026-07-18
base: d07b2a95
head: working tree on m1/M1-656 (r3 re-audit)
verdict: CLEAN
findings_count: {critical: 0, high: 0, medium: 0, low: 0}
out_of_model_count: 1
disposition: |
  r3 re-audit after the r2 FINDINGS verdict. The r2 finding (approve-group
  authorization-ordering, error.group_not_found reachable below bot admin)
  is disposed OUTSIDE this diff per the user-endorsed 2026-07-18 handoff:
  the code fix is filed as M1-657, the recurrence guard as M1-658, and this
  diff's spec text now names the violation as tracked instead of claiming
  the key is bot-admin-only. The reviewed code half is byte-identical to
  what r2 verified sound. This audit confirms the corrected spec text and
  the two filed tickets faithfully carry the finding — CLEAN.
---
RED-TEAM VERDICT: CLEAN

The two claims put to this r3 re-audit both hold.

CLAIM 1 — the corrected spec text (docs/spec/commands.md §Discovery) states
only true properties. Verified independently, not accepted:

  - The fixed surfaces genuinely stopped reflecting. The diff drops the raw
    inbound token from every one of the five below-bot-admin echoes the ticket
    targeted:
      * error.follow_tag.unknown_tag (FollowTagCommandHandler.java:187 now
        passes only the suggestion list),
      * error.unfollow_tag.unknown_tag (UnfollowTagCommandHandler.java:239),
      * error.group_timezone.invalid_zone (GroupTimezoneCommandHandler.java:116),
      * error.asset.unknown_sub_verb / error.asset.unsupported_quote_currency
        (AssetHandler.java:118 / :140 now pass bestMatch/assetName/available —
        all bot- or registry-authored),
      * error.add_source.unknown_kind / unknown_category (AddSourceArgs
        unknownKind()/unknownCategory() drop the supplied parameter).
    en.properties and cs.properties are renumbered consistently ({0}=validKinds
    / categories / suggestions), arity matches each call site, and the tests
    assert the supplied token is ABSENT from the reply.

  - The spec's "provably constrained is fine" category is accurate.
    /summary's echo (SummaryCommandHandler.java:228 -> args.tag().get()) can
    only ever be a TagNormalizer-validated value matching
    ^[a-z0-9][a-z0-9-]{0,47}$ — no slash or whitespace can survive, so it
    cannot fabricate a command.

  - Independent sweep for OTHER below-bot-admin friendly errors that reflect
    inbound-derived text found nothing that violates the property in the
    threat-relevant sense:
      * error.add_source.nitter_host_type_conflict (AddSourceCommandHandler.java:166)
        echoes KindResolver's canonicalHost — a URI host component
        (IDN.toASCII + toLowerCase) that must match the operator's configured
        Nitter allowlist (subdomain-aware, KindResolver.java:234-238). A URI
        host structurally cannot contain a slash, whitespace, backtick, or
        newline, so it cannot carry a copy-pasteable admin command. This is an
        unenumerated instance of the spec's own "provably constrained echo is
        fine" category, and the spec deliberately declines to enumerate such
        cases ("enumerating today's exceptions would only rot"). Consistent.
      * error.asset.sub_verb_not_enabled (AssetHandler.java:120-123) echoes
        args.subVerb only after an exact findSubVerb match against operator
        config — the reflected bytes equal a trusted asset_config value, not
        attacker-chosen text (r1-verified, byte-identical here).
      * error.summary.unknown_tag — constrained (above).
      * error.invite.* / error.quarantine.* / error.audit.unknown_action /
        error.recover_pool.not_found — all bot-admin-gated (r2-verified: each
        admin gate precedes each reflection).

  - The one genuine below-bot-admin reflection, error.group_not_found via
    /approve-group's parse-before-gate ordering, is named in the spec as a
    KNOWN VIOLATION tracked as M1-657 and explicitly excluded from the
    bot-admin-only claim until the fix lands. That is exactly the r2 finding,
    accurately disposed — not re-reported here per the re-audit charter.

CLAIM 2 — the two filed tickets faithfully carry the r2 finding.

  - M1-657 (docs/plan/m1/tickets/M1-657-approve-group-admin-gate-before-parse.md)
    carries the ordering fix: pre-parse admin gate in handle() mirroring
    RejectGroupCommandHandler.java:129-132, a non-admin+malformed-id test
    asserting error.admin_only with no "grant-admin" echo and no audit row,
    all existing tests unmodified, the stale :112-115 comment reconciled, and
    the spec note removed once true. Its out_of_scope correctly fences off the
    already-correct reject handler, the echo template (bot-admin-only after the
    fix), the in-tx TOCTOU re-check, and router/permissions tiering — matching
    the r2 disposition precisely.

  - M1-658 (docs/plan/m1/tickets/M1-658-inbound-reflection-build-guard.md)
    carries the recurrence guard: a build-time census over all three
    interpolation forms (inline MessageFormat, Failure/interpolationArgs,
    format(KEY,...) helper), a no-dead-entries baseline with per-site
    provenance justifications of exactly three kinds, a non-vacuity self-check,
    and the security.md exemption reworded to the real condition. Correctly
    scoped to test+docs only.

Both tickets exist in the diff as pending, blocked_by M1-656, and neither
overstates its claim.

OUT-OF-MODEL:
  - error.add_source.nitter_host_type_conflict reflects an attacker-influenced
    subdomain of a configured Nitter host into below-bot-admin bot output.
    Flagged for awareness only: it is NOT a finding because the reflected value
    is a URI host (no slash/whitespace/backtick can survive url.getHost() +
    canonicalize), so the copy-pasteable-admin-command threat the sanitizer
    exemption bounds is unreachable, and the value is further pinned to the
    operator's configured Nitter allowlist. It sits inside the spec's
    "provably constrained echo is fine" category. If a future M1-658 census
    wants a fully mechanical property, this site would be a baseline entry with
    a "provably-constrained (URI host)" justification rather than a fix.
