---
target: M1-758
date: 2026-08-04
base: 29aa59afbd80e13df6265e6934c77f1a4a06ffe6
head: "<working tree>"
verdict: FINDINGS
findings_count: {critical: 0, high: 0, medium: 0, low: 1}
out_of_model_count: 3
disposition: |
  Round 7. No prior finding re-opened; severity declined to a single low.
  The finding is REAL and is the THIRD consecutive round in which the
  previous round's fix introduced a new false claim — the r6 remediation
  named `[machine translated]` as the tag a reader looks for, and that is
  the ONE marker value this leg can never emit: `finishDisplayHit`
  (TranslationPipeline:348) resolves `reply.translation.hit_marker` in the
  SCOPE's language, and `runForDisplayHit` (:245-251) short-circuits for
  every `en` scope, so the reachable values are cs `[strojový překlad]`,
  es `[traducción automática]`, ru `[машинный перевод]`, tr
  `[makine çevirisi]` — verified against the five bundle files and
  LanguageRegistry:73-78. The English literal appeared in a §6 worked
  example whose own command is `/lang cs`.

  Remediated: USER_GUIDE.md now says the tag is written in the reader's
  chosen language and shows the Czech value in the Czech example;
  design 05 names the bundle KEY and the scope-language resolution rather
  than any literal. One completeness item added from the code that the
  audit did not raise: `finishDisplayHit`'s byte-identical short-circuit
  is a THIRD unmarked case alongside over-budget and degraded rows.

  PATTERN, recorded because it is now the ticket's defining failure mode:
  every remediation since round 5 has been MORE SPECIFIC than the text it
  replaced, and specificity is where the falsity keeps landing. r5 fixed a
  vague-and-false invariant with a specific render claim (false). r6 fixed
  that with a specific marker literal (false). Round 7's fix deliberately
  moves back DOWN the specificity ladder — naming the bundle key and the
  resolution rule rather than a literal a future bundle edit would falsify.

  Out-of-model 1 (SETUP_GUIDE "backs up ... then a privacy disclosure"
  reads as pre-write; Phase 4 prints after Phase 3 writes) and 3 (§Privacy
  "/forget does not touch" list omits the translation caches, which
  security.md accepts as residual risk) are pre-existing and NOT actioned;
  both are worth the user's attention as their own items. Out-of-model 2
  (leg-by-leg pointer names the sibling section) carried forward from
  rounds 4-6, still cosmetic.
---
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Trust boundaries item 9 — "Everything a
             generative or embeddings endpoint returns is endpoint-chosen
             input, not a trusted internal value: whatever answers on the
             configured base-url picks every field of the reply, so a hostile
             or compromised endpoint is in scope here for the same reason a
             hostile feed is on the ingest side." Paired with §Rate limiting,
             "`/saved` display-hit translation (M1-755)": "rows beyond the
             budget render untranslated, unmarked, and a rejected draw
             degrades the page's cache-miss rows the same way", and the
             identical wording in "Periodic-digest headline translation
             (M1-756)": "headlines beyond the budget render untranslated and
             unmarked". Where endpoint-chosen bytes are delivered under a
             publisher's attribution line, the machine-translation marker is
             the reader's only in-band provenance signal, and the marked /
             unmarked distinction is what the spec's own budget wording rests
             on. This is the SAME promise the round-6 medium finding was
             grounded in; the remediation for that finding introduced this
             defect.
    GAP: The round-6 fix replaced a false render claim in the only end-user
         document with a new specific-and-false one: it names a marker literal
         that no reader of a translated headline can ever see.
         - USER_GUIDE.md:124 (rewritten by this diff, `/lang` table row):
           "a translated headline is tagged `[machine translated]`".
         - USER_GUIDE.md:299-301 (rewritten by this diff, §6 worked example,
           whose command is literally `/lang cs`): "A headline the bot
           translated is tagged `[machine translated]`, so you can always tell
           its rendering from the publisher's own words."
         - USER_GUIDE.md:303-306 builds a decision rule on that literal: "an
           untagged headline means 'not translated', which is not the same as
           'already in your language'."
         - docs/design/05-llm-and-embeddings.md:935 (rewritten by this diff)
           carries the same literal as a statement of shipped behaviour: "The
           translation REPLACES the headline and carries a `[machine
           translated]` marker on the same line".
         The marker is resolved from the localization bundle in the SCOPE's
         language, not in English:
         infochat-provider/src/main/java/app/zcat/infochat/provider/translation/TranslationPipeline.java:348
           `String marker = bundleLoader.get(BundleKeys.REPLY_TRANSLATION_HIT_MARKER, scopeLanguage);`
         :349 `return bounded + " " + marker;`
         and the leg short-circuits for every English scope:
         TranslationPipeline.java:245-251
           `if (scopeLanguage.equalsIgnoreCase("en") ... ) return displayHeadline;`
         The `en` bundle value is therefore the ONE value that can never be
         rendered by this leg. The values a reader actually sees are:
           infochat-provider/src/main/resources/bundles/cs.properties:550
             reply.translation.hit_marker=[strojový překlad]
           .../es.properties:857  =[traducción automática]
           .../ru.properties:823  =[машинный перевод]
           .../tr.properties:729  =[makine çevirisi]
           .../en.properties:842  =[machine translated]   (unreachable on this leg)
         All four reachable scopes are live —
         infochat-provider/src/main/java/app/zcat/infochat/provider/bundle/LanguageRegistry.java:73-78
         enables en/cs/es/ru/tr — so the guide's literal is wrong in 100% of
         the cases it describes, including the `/lang cs` example it is
         written against. The design-05 copy states it as "AS SHIPPED",
         which is the same target-state-vs-shipped confusion round 6 flagged,
         one level down.
    REPRO: 1. Default deployment: V74 gives `source.language NOT NULL DEFAULT
              'en'`, so bootstrap sources are 'en' and the display-hit leg is
              live for any non-English reader.
           2. Operator routes `translator` to a remote endpoint — the exact
              decision this ticket's disclosures govern. Under trust boundary
              9 that endpoint now picks the bytes of every translated headline.
           3. A user sets `/lang cs` and runs `/saved` (or receives a digest,
              or runs `/summary`). Rows within budget render
              `<endpoint-chosen Czech text> [strojový překlad]`; rows past
              `infochat.save.translation-max-per-page` / a refused rate draw /
              a degraded cluster render the raw publisher headline with no
              marker at all.
           4. The user consults USER_GUIDE.md — the only end-user document —
              which tells them the tag is `[machine translated]` and that an
              untagged headline means "not translated". Applying that rule as
              written, NO line on the page carries the tag, so every line
              reads as the publisher's own words — including the lines whose
              text was chosen by the remote endpoint and is delivered under
              the source's attribution line (ClusterBlockRenderer:137-142,
              "covered by: <source display name> (uid p-...)").
           5. The system should not have shipped a provenance rule keyed to a
              string the shipping code cannot emit on that path: the marker is
              the only in-band signal separating endpoint-chosen bytes from
              publisher-authored bytes, and the guide teaches the reader to
              look for the one form of it that never appears.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - SETUP_GUIDE.md:577-579 (context line the diff edits the tail of) still
    reads "Before writing anything it **backs up** your config and prints a
    **rollback** command, then a **privacy disclosure** ...", which reads as
    if the switch-time disclosure precedes the write. It does not:
    prod/switch-llm.sh Phase 3 (:310-383) backs up, prompts for and stores the
    API key, deletes every per-task pin and rewrites application.properties,
    and only then does Phase 4 (:409-432) print the disclosure. The sentence
    is pre-existing and unchanged by this diff, and security.md does not
    promise a pre-write disclosure at switch time — it words the switcher's as
    "naming exactly which generative tasks NOW call a remote provider" and
    assigns the pre-commitment role to the install-time wizard ("the EARLIER
    of the two consent surfaces"), which this diff correctly strengthens.
    Flagged only because the diff edits that same sentence's tier clause and
    because security.md calls both surfaces "consent surfaces" while one of
    them fires after consent has already been executed.
  - The switcher and the install wizard both print `Full leg-by-leg list:
    SETUP_GUIDE.md, "Switching your AI backend later"`, and security.md
    designates that same section as the long form. The leg-by-leg list added
    by this diff actually lives in a SIBLING section, `### What `translator`
    sends, leg by leg` (SETUP_GUIDE.md:589), not inside `### Switching your AI
    backend later` (:549). The new section is immediately adjacent and opens
    with "The switcher's disclosure keeps this short and points here", so the
    pointer resolves in practice; noting it because security.md names the
    section by title and a future reorganisation could separate them.
  - USER_GUIDE.md §6 (added by this diff) now points the reader at
    §"Your privacy and your data" (:333-348) for what reaches the translation
    model. That section's "What `/forget` does **not** touch" list is written
    as exhaustive and omits the display-hit and query-anchoring translation
    caches, which security.md §Secrets handling records as retained for 24h
    with "`/forget` does not drain it". security.md expressly accepts that as
    residual risk, so this is not a gap against the threat model — but the
    diff newly makes that section load-bearing for a translation question it
    does not answer.
