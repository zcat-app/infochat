---
target: M1-732
date: 2026-07-31
base: 98b2be6c8d6563a819ae2e0dcd48c02d06d0a11d
head: working-tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Standing /m1-tick run gate for a security_relevant: true ticket (the
  render path carries the M1-697 sanitize control). CLEAN on round 1;
  no remediation, no re-audit needed.
---

RED-TEAM VERDICT: CLEAN

Audit scope: M1-732 (groups.digest_mode, V67 column, mode-aware
DigestRenderer.renderSections, DigestWorker digest_mode deserialization
boundary, bundle key swap, test retargeting). Threat model
(docs/spec/security.md) read in full; diff
(target/redteam-diff-M1-732.diff, 1456 lines) read in full.

Surfaces checked against the threat model:

1. LLM output sanitizer coverage (security.md §LLM output sanitizer —
   "periodic group digests" are in the sanitizer's full surface set):
   - brief/normal roll-up: CategoryRollupGenerator.generateRollup
     sanitizes then translates its own output
     (CategoryRollupGenerator.java:139-140, verified outside the diff).
   - normal-mode headlines: DisplayHeadline.of carries the load-bearing
     flatten -> sanitize -> truncate order over ONE author's field per
     call (DisplayHeadline.java:28-44,103,133, verified outside the
     diff), so the M1-697 redaction travels onto the new path before
     the section bytes are persisted post-sanitize and replayed
     verbatim (DigestWorker SECURITY INVARIANT comment, updated in the
     diff, stays true in every mode).
   - full mode keeps the pre-existing prose-loop sanitize.
   - The one unsanitized interpolation is the bare post URL in
     appendHeadlines — the spec explicitly assigns bare-URL safety to
     the ingest write boundary (PostPersister.normalizeUrlForStorage
     scheme allowlist, §"Bare feed URLs rest on the ingest scheme
     allowlist"), and the `](`-free property is carried once at
     OutboundDelivery over the whole assembled body (§"Sanitizer
     output never contains `](`"), which covers the persisted/replayed
     section bytes at delivery.

2. DigestWorker.readGroupMetadata / digestModeOrNormal: the
   SQL-deserialization boundary fails closed to NORMAL with one WARN
   per fallback event; the render path never sees an unvalidated
   storage string. The WARN interpolates the raw value, but a garbage
   value is unreachable in-model: the V67 CHECK constraint pins the
   closed set, and the Provider role's V62 column-scoped UPDATE on
   groups (timezone, digest_enabled, removed_at) does not cover
   digest_mode — writing garbage requires an out-of-band (operator-tier
   or constraint-bypassing) write, which the threat model trusts
   (§Threat model: operator-set config is trusted; DB is internal).

3. Injection: GROUP_META_SQL remains a parameterized prepared
   statement with only an added column; the new MessageFormat headers
   interpolate the controlled-vocabulary tag and integer counts only;
   no new string-concatenated SQL, no new command-parsing surface.

4. AuthZ/auth surface: the diff adds no command, no privilege write,
   no grant. V67 is additive (NOT NULL DEFAULT 'normal' + CHECK);
   runtime mutation of digest_mode is deferred to M1-733 and is not
   present here, so no new admin-tier gate exists to probe.

5. DOS: rendering stays on the renderExecutor inside the pre-existing
   renderFuture timeout for all modes; brief/normal reduce LLM calls
   to one roll-up per surviving section. full mode lifts the
   per-section item cap to a render-local Integer.MAX_VALUE without
   mutating the shared @ApplicationScoped field (no cross-slot race on
   the virtual-thread executor). The cap lift is an explicit,
   documented design decision; security.md §Rate limiting assigns
   periodic-digest cost to the aggregate system LLM budget as the
   stated backstop and commits to no per-digest item cap, so no
   spec commitment is breached. The M1-721 section cap (max-categories)
   still bounds section count in every mode.

6. Audit: no new privileged operation is introduced, so no new audit
   obligation arises; sanitizer audit rows continue to fire inside
   DisplayHeadline.of and the roll-up generator on the new paths.

7. Bundle changes: deleted keys (reply.digest.category.more /
   _more_other) are removed from both en and cs bundles together with
   their BundleKeys constants (D43 bilateral keyset preserved); new
   header-count keys land in both locales with matching token shapes.
