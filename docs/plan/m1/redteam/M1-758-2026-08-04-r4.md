---
target: M1-758
date: 2026-08-04
base: 29aa59afbd80e13df6265e6934c77f1a4a06ffe6
head: "<working tree>"
verdict: FINDINGS
findings_count: {critical: 0, high: 0, medium: 0, low: 2}
out_of_model_count: 6
disposition: |
  Round 4. All nine prior findings confirmed closed; spec-completeness and
  census-population-(a) spot-checks PASSED. Both findings remediated in
  branch, plus one the developer found while falsifying finding 2
  (README.md:214) and its two siblings (CLAUDE.md:50, docs/SPEC.md:258).
  D57's six-task enumeration is corrected together with the design line it
  quotes verbatim, since fixing one alone would create a new contradiction.
  Out-of-model items 1, 4, 5, 6 (pointer lands one heading early;
  over-broad assertFalse substrings; residual "walks you through each
  task"; README "request-time summarizer" labelling) accepted as cosmetic
  and NOT actioned. Out-of-model 2 (CLAUDE.md) and 3 (design 05) WERE
  actioned — they are the false-invariant family, not cosmetics.
---
RED-TEAM VERDICT: FINDINGS

Round-4 re-audit note (verification of the nine prior findings, against the
CURRENT working tree — none are re-reported below):

  r1(a) closed. Neither runtime disclosure asserts an /lang-gated hard
        negative; both carry the two-fact shape and the "an all-English
        deployment is NOT exempt" claim, which I verified against
        IngestTranslationWorker:264 (only skip arm is
        `"en".equals(row.language())`), :274/:318 (ModelTask.TRANSLATOR),
        :358-366 (renderPrompt interpolates {{title}} AND the full
        untruncated {{body}}), :232 (@Scheduled on
        infochat.llm.translator.poll-interval, no enable flag).
  r1(b) closed. No count claim survives; the A1 reply-prose leg is
        enumerated in security.md §Secrets handling and named first in
        SETUP_GUIDE.md:596. No assertion pins its absence.
  r1(c) closed. SETUP_GUIDE.md:315-319 now says "all seven of them,
        including `chat` and `translator`, which carry private user text".
  r2(d) closed. prod/scripts/4-llm.sh:509-517 carries the same `!!` tier
        text as prod/switch-llm.sh:414-422.
  r2(e) closed. SETUP_GUIDE.md:559-561 now says the switcher asks once and
        applies to every task; the contradicting sentence is gone.
  r3(f) PARTIALLY closed — see Finding 1. The printed text on both
        surfaces is corrected (4-llm.sh:525-527, SETUP_GUIDE.md:571-575);
        the fabricated safeguard survives in 4-llm.sh's block comment.
  r3(g) closed. OVERVIEW.md:290-296 names translator and the unattended
        whole-post leg.
  r3(h) closed. RemoteLlmWiringTest no longer bars "translation of the
        bot's replies"; it targets the tier marker and the pre-M1-746
        clause instead (residual noted under OUT-OF-MODEL 4).
  r3(i) closed. 4-llm.sh:505-508 now names BodySummaryWorker's unattended
        whole-body leg and is if anything stronger than switch-llm.sh:429.

Spec-completeness check (the new claim this diff adds, security.md
§Secrets handling: "The enumeration above covers every production call
site reaching `ModelTask.TRANSLATOR` as of M1-758"): VERIFIED TRUE. The
production call sites are QueryAnchorTranslator:259-260,
IngestTranslationWorker:274/318, and LlmTranslationProvider:93-94 reached
via TranslationPipeline.run / .runForDisplayHit from ChatAgent:558,
SavedCommandHandler:453/457, ClusterBlockRenderer:132/166,
DigestRenderer:552/825/886 and CategoryRollupGenerator:213 (RetryCommand
Handler and SummaryCommandHandler reach the translator only through
ClusterBlockRenderer). Every one maps to an enumerated leg. The
`infochat.digest.translation-max-per-render` budget the new §Rate limiting
bullet commits to is enforced in code (DigestRenderer:858-888) and
defaults to 5 (application.properties:631).

Census population (a) re-run: `PRIVACY DISCLOSURE` appears in exactly two
shell scripts (4-llm.sh, switch-llm.sh); no other prod/*.sh, *.properties
or *.yml surface makes an exposure claim (1-profile.sh:36-39 mentions
remote-llm but asserts no exposure). Population (b) re-run: see Finding 2.

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — "The install-time
              wizard (`prod/scripts/4-llm.sh`) prints the same disclosure
              before the operator commits to a remote endpoint; it is the
              EARLIER of the two consent surfaces and the only one an
              operator who never re-routes ever sees, so it carries the
              same claims at no less strength."
    GAP: prod/scripts/4-llm.sh:484-486 (added lines in this diff) still
          asserts the safeguard round 3 found fabricated: "a single task
          can still be kept local by hand-pinning its per-task base-url,
          but a later switch sweeps such pins unless the operator declines
          the consent prompt." There is no prompt an operator can decline
          while switching. prod/switch-llm.sh has five `read` prompts
          (:162 backend, :188 dialect, :196/:201/:204 base-url, :225
          per-task model, :330 api-key), none about pins. The M1-605 gate
          (:301-308) fires only when the backend answer is EMPTY and a pin
          exists, prints, and `exit 0` without writing — "declining" means
          not switching at all. Any run that proceeds deletes every
          per-task base-url and api-key unconditionally (:352-355) and
          only ANNOUNCES the swept list afterwards (:348-351). The same
          file's printed text at :525-527 states the correct and opposite
          fact ("there is no way to switch AND keep a pin"), so the file
          now contradicts itself, and the guard added for exactly this
          claim — RemoteLlmWiringTest
          .installTimeDisclosurePutsTranslatorInTheLoudTierWithBothExposure
          Facts, `assertFalse(run.output().contains("decline its consent
          prompt"))` — inspects captured stdout only and cannot see a
          comment.
    REPRO: Maintenance path, which is how this defect has landed twice
            already. An editor updating the install-time disclosure reads
            the block comment 40 lines above the echo lines, takes
            "sweeps such pins unless the operator declines the consent
            prompt" as the file's own statement of the control, and
            restores it into the printed block; `mvn verify` stays green
            because every assertion runs against stdout and the wording
            differs from the pinned literal. An operator then hand-pins
            `infochat.llm.translator.base-url` to a local backend
            precisely to keep private user text off the remote endpoint
            (the mitigation both surfaces recommend), later runs
            prod/switch-llm.sh, TYPES a backend at the :162 prompt
            expecting a further pin-related prompt to decline, and Phase 3
            deletes the pin. From the next container recreate on, every
            non-English scope's chat reply and raw search query, every
            /saved and /summary headline, every digest headline, and the
            full title+body of every non-English post go to the remote
            provider — the exact exposure the pin was taken out to
            prevent.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — "The enumeration
              above covers every production call site reaching
              `ModelTask.TRANSLATOR` as of M1-758. It is a snapshot, not a
              guarantee: a new call site is a new leg, and the disclosure
              texts ... are corrected against THIS section, so a leg
              missing here propagates into both." The diff's own census
              (ticket §Census) is the mechanism that carries that
              propagation to the operator-facing renderings, and it
              records: "Corrected rule run at the round-3 refine: it
              returns exactly one newly-wrong surface, row 8."
    GAP: That exhaustiveness claim is falsifiable by re-running the rule
          as written. The rule selects every maintained reader-facing
          `*.md` under the repo root and `docs/` (excluding `docs/design/`,
          `docs/plan/`, `docs/process/`, `.claude/`) that mentions
          remote/cloud LLM routing, then keeps those making an exposure
          claim. Re-running it also selects `docs/spec/decisions.md`,
          whose D57 row (line 74) states "(the post body of a `security` /
          `tagger` / `entity` / `classifier` / `summarizer` / `chat` call
          is sent to the remote provider)" — a six-task enumeration of
          what post bodies reach the remote endpoint, omitting
          `translator`, the seventh, which this very diff establishes
          ships the full untruncated body of every non-English post
          (IngestTranslationWorker:274/:318, renderPrompt:358-366).
          `docs/spec/decisions.md` is spec-tier, is not in the census's
          "Deliberately NOT in the census" list (which names only
          `docs/design/07-deployment.md`, `security.md` itself, and
          `docs/plan/`), and carries no disposition anywhere in the table.
          This is the same defect class the census filed as rows 6, 7 and
          8 — an enumeration of what leaves the machine that omits
          translator — one tier up from README/OVERVIEW.
    REPRO: A maintainer or agent answering "what does routing task X to a
            cloud provider expose?" consults the cross-cutting decisions
            log (the authority CLAUDE.md points at for exactly this kind
            of question) and reads D57. They come away with a six-task
            list in which `translator` sends no post bodies. Restating
            that in the next edit of any disclosure reproduces round 1's
            finding verbatim — and the census, which this ticket added as
            the structural guard against precisely that recurrence, has
            already declared its own run exhaustive, so the next reader
            re-runs a rule whose recorded output does not match what the
            rule selects.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - Pointer names the neighbouring section. Both runtime disclosures print
    `Full leg-by-leg list: SETUP_GUIDE.md, "Switching your AI backend
    later"` (4-llm.sh:517, switch-llm.sh:422), but that section
    (SETUP_GUIDE.md:549-587) carries only the tier summary; the leg-by-leg
    list lives in the adjacent sibling section `### What `translator`
    sends, leg by leg` (:589-621), and :549-587 contains no forward link
    to it. Reachable by scrolling one heading, and security.md names
    §"Switching your AI backend later" as the designated long form, so
    nothing is broken — but the pointer does not land on the promised
    list. (The in-guide link at :318-320 does resolve: the anchor
    `#what-translator-sends-leg-by-leg` matches the real heading even
    though its link TEXT names the other section.)
  - `CLAUDE.md` §Key conventions still states "Source post bodies are
    never translated." That is false since the V74 ingest translation
    worker, which translates the full title and body of every non-English
    source post. It is not selected by the census rule (CLAUDE.md's
    remote-llm mention carries no exposure claim) and CLAUDE.md is
    process-tier, so it is out of the threat model — but it is the file
    every agent loads first, and it asserts the exact opposite of the
    Tier B leg this ticket exists to disclose. Highest-value item in this
    list if the user wants a durable fix for the recurrence pattern.
  - `docs/design/05-llm-and-embeddings.md:1103` carries the same six-task
    post-body enumeration D57 cites ("Post bodies are sent to the remote
    provider as part of security / tagger / entity / classifier /
    summarizer / chat-agent calls"). The census excludes `docs/design/**`
    by construction and my role bars me from reading design notes, so I
    make no claim about the surrounding text — flagged only so the user
    can decide whether the design-tier privacy disclosure deserves a
    disposition row.
  - Over-broad `assertFalse` substrings. SwitchLlmWiringTest pins the
    absence of "strict no-op", and RemoteLlmWiringTest the absence of
    "one at a time", over the WHOLE captured output. Both substrings have
    true uses a future, more precise disclosure might want ("for an `en`
    scope the /lang-gated legs are a strict no-op — the ingest leg still
    runs"; "the switcher cannot move tasks one at a time"), so the suite
    would fail a correct sentence. Materially weaker than the round-1 and
    round-3 instances — neither substring is a leg description, and both
    failure messages explain the intent — so noted rather than filed.
  - `SETUP_GUIDE.md:585-586` still says the switcher "is fully interactive
    and walks you through each task", 25 lines after the corrected "asks
    **once** ... applies that choice to **every** AI task". Literally true
    only of the per-task MODEL prompts on the remote/openai-compatible
    path (switch-llm.sh:223-227). Residual ambiguity about the same claim
    census row 5 exists to kill; not an exposure claim, and the
    surrounding paragraphs state the true behaviour explicitly.
  - `README.md:219` keeps "the request-time `summarizer`", the framing
    round 3 rejected for the install block because it hides
    BodySummaryWorker's unattended whole-body leg. No data class is
    concealed here (the same sentence already discloses public post
    bodies for the ingest tasks), so this is labelling, not exposure.
