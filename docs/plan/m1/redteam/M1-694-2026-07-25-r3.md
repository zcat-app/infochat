---
target: M1-694
date: 2026-07-25
round: 3
base: 6c487d9cc65e1dc1fe3acd5b0418dbc4906a2379
head: working-tree
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 1
out_of_model_count: 4
disposition: |
  Re-audit after remediating round 2. Round-2 medium verified CLOSED sentence by sentence against the code. One NEW low: the remediation widened the sanitizer's UNIT OF INPUT on the default path from one feed title to one cluster's whole assembled multi-post prose, so redactFlagEntry's command-to-flag span (unbounded, crosses newlines) can now cross post boundaries and erase a third party's post. Fails safe (over-redaction, visible marker, audit row still emitted) but is a real trust-boundary widening introduced by this diff. NOT remediated - surfaced to the user.
---

RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INJECTION
    SEVERITY: low
    PROMISE: docs/spec/security.md §LLM output sanitizer, "Flag position
              mirrors the parser's own scan": "The cost of the whole-message
              bound is that a genuine mention of the command and a later,
              unrelated admin flag in the bot's own explanatory prose collapse
              into one redacted span; that is bot-authored text, and the same
              span copy-pasted from the `/` does dispatch, so the redaction is
              defensible." The whole-message span is accepted at spec level ONLY
              on the stated ground that the bytes swallowed between the command
              word and the flag are bot-authored. The same section also commits
              that a flag entry "matches its flag at ANY position in the
              command's argument run ... The argument run spans the whole
              message, across newlines."
    GAP: The remediation changes the sanitizer's UNIT OF INPUT on the default
          /summary path from one feed title to one cluster's whole assembled
          multi-post prose, and neither the amended paragraph nor the
          whole-message-span paragraph records that the swallowed span is no
          longer bot-authored.
          - infochat-provider/src/main/java/app/zcat/infochat/provider/digest/DigestRenderer.java:241
            `llmOutputSanitizer.sanitize(cp.prose())` is called ONCE per cluster
            over the entire assembled prose.
          - infochat-provider/src/main/java/app/zcat/infochat/provider/summary/SummaryProseGenerator.java:193-203
            `degradedProseFor` concatenates `title — url (uid)` for EVERY post in
            the cluster, so that single sanitize input is a concatenation of
            several independent, mutually untrusted upstream publishers' bytes.
          - infochat-provider/src/main/java/app/zcat/infochat/provider/llm/LlmOutputSanitizer.java:294-333
            `redactFlagEntry` deletes the span from the command word through the
            flag token: `out.append(input, appended, command).append(REDACTED_COMMAND_REPLACEMENT);
            ... appended = flagEnd;` (:324, :327). Everything between the two
            endpoints is dropped, and per findCommandToken/findFlagToken (:343,
            :367) the search is unbounded within the sanitize input and crosses
            newlines (isTokenSeparator :392 includes '\n').
          Every pre-existing feed-bytes sanitize site is scoped to a SINGLE
          author's field — ClusterBlockRenderer.java:87 (`first.title()` only),
          DegradedDigestRenderer.java:38 (per post title),
          SavedCommandHandler.java:275/277 (per row title / per row tags). This
          diff creates the first render path where raw multi-post feed bytes are
          concatenated into one sanitize call, so a redaction span can now cross
          post boundaries and erase a third party's content.
          The two flag-bearing closed-list entries are
          `/list-sources --all` and `/list-sources --include-deleted`
          (LlmOutputSanitizer.java:130-131).
          The amended spec text (docs/spec/security.md, §LLM output sanitizer
          bullet: "The default categorized form renders no headline and instead
          sanitizes the whole per-cluster prose — degraded prose included"; and
          §"Sanitizer output never contains `](`": "The default categorized
          `/summary` form is the exception that shows the shape of the fix")
          presents the widened unit as strictly better and records no cost. This
          matters beyond /summary because M1-691 is explicitly framed by the same
          amendment as deciding "whether the digest's own branch follows" — i.e.
          whether to extend the same cross-post span to the broadcast digest.
    REPRO: 1. Attacker controls (or compromises) any source in the victim's D59
              world; a bootstrap source is public to every scope, so one such
              feed reaches every user.
           2. Attacker gets two crafted posts co-clustered around a legitimate
              post V, with V between them in `cluster.posts()` order. A
              `post_reference` edge is what clusters them
              (PostReferenceEdgeSource.Jdbc:60-87) — reachable via cross-source
              entity/embedding linking, or directly via a Nostr kind-6 repost of
              V, which security.md §Per-source trust boundaries specifies writes
              a `post_reference` edge with `link_type='repost'`.
                post A title: "/list-sources"
                post V title: "Legitimate headline"   <- the story to suppress
                post B title: "--all"
           3. Attacker forces the DEGRADED prose branch deterministically — no
              LLM outage needed — by publishing more than
              `infochat.summary.summarizer-post-cap` posts in the window, so any
              bare /summary takes the over-cap path
              (SummaryCommandHandler.java:274-277 -> composeWindowTooLargeReply
              :449-457, which builds degraded ClusterProse for every cluster).
           4. Victim runs bare `/summary`. DigestRenderer.java:241 hands the
              whole cluster string
                "/list-sources — urlA (uid p-a)\nLegitimate headline — urlV (uid p-v)\n--all — urlB (uid p-b)"
              to one sanitize() call. redactFlagEntry matches `/list-sources` at
              the head and `--all` at the tail and replaces the whole span,
              including V's headline, URL and uid, with `[redacted command]`.
           5. The attacker has deleted a third party's story from another user's
              summary. Pre-diff the same cluster rendered V's headline through
              ClusterBlockRenderer, whose sanitize input is a single title, where
              no cross-post span is constructible.
           Impact is bounded and fails safe with respect to command injection
           (the direction is over-redaction, not under-redaction), the marker is
           visible to the reader, and each hit still emits the WARN plus the
           per-occurrence LLM_OUTPUT_SANITIZED row — hence low, not medium.
    SUGGESTED-FIX-CLASS: trust-boundary-tightening

OUT-OF-MODEL:
  - The `/summary --full` and `/retry` unsanitized-degraded-prose exposure is
    NOT re-reported as a finding: it is pre-existing, excluded by this ticket's
    out_of_scope, and the amended spec now describes it accurately.
    ROUND-2 RE-VERIFICATION: the round-2 medium is CLOSED. The false clause
    ("Both forms therefore redact ... neither leaves a command-shaped title
    unredacted") is gone. Each replacement sentence was checked against the
    code and holds: "renders no headline" (DigestRenderer.java:215-249 emits
    section header + prose + overflow only); "sanitizes the whole per-cluster
    prose — degraded prose included" (:241-243); "`--full` sanitizes the cluster
    headline only: its `summary:` field passes degraded prose through verbatim"
    (ClusterBlockRenderer.java:87 and :112-113); "that prose carries `title —
    url (uid)` for every post in the cluster" (SummaryProseGenerator.java:193-203);
    "the headline sanitize also only ever sees the cluster's first post"
    (ClusterBlockRenderer.java:80, `posts.get(0)`); "`/retry` ... always replays
    the flat form" (RetryCommandHandler.java:265-268 constructs
    ClusterBlockRenderer unconditionally); "open, not closed" and "no owning
    ticket" match STATUS.md's M1-691 line. Two sentences UNDERSTATE the shipped
    control rather than overstate it — `--full` also sanitizes non-degraded
    prose (ClusterBlockRenderer.java:115), and a non-degraded `--full` render
    can still emit a row from the prose sanitize — which is the safe direction
    and is not reported. The surviving residual now has no owning ticket, so it
    remains visible to the user as an unowned follow-up.
  - `DigestRenderer.forSummaryRendering` (DigestRenderer.java:270-284) widens the
    production API with a partially-initialised bean whose `render` /
    `renderSections` would NPE. Re-verified this round: the only caller is
    SummaryCommandHandlerTest.java:130 — no production call site, no
    adversary-reachable path. Same disposition as round 1.
  - The default form's per-category item cap means clusters past
    `infochat.digest.category-item-cap` are neither rendered nor scanned, so a
    command-shaped title in a capped-out cluster now produces no
    LLM_OUTPUT_SANITIZED row where the pre-diff flat form produced one. Not a
    finding — no delivery means no exposure, and the spec commits only that
    every MATCH is audited — but it is a real reduction in the operator's
    corpus-wide detector coverage and is worth a deliberate accept.
  - Not a finding, noted for completeness of the model: the section header
    interpolates the category tag, which is NOT sanitizer output. It is safe
    only by two accidents — TagNormalizer.java:33 constrains tags to
    `^[a-z0-9][a-z0-9-]{0,47}$` (no `/`, no `](`), and DigestRenderer.java:304-313
    uppercases the header, which the spec's exact-match command-name rule makes
    non-dispatchable. Both properties are load-bearing for a surface the spec
    does not name; a future tag charset relaxation would silently void them.
