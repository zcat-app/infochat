---
target: M1-741
date: 2026-08-01
base: 76de249dc0df56f600758d99c3d61caf51df012b
head: working tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Round-3 re-audit of the round-2 remediation (full bitmap predicate
  `stage1_flagged AND stage2_verdict IS NULL AND status <>
  'NEEDS_REVIEW' AND (status = 'QUARANTINED' OR stage2_failed = FALSE)`,
  RAW-seeded in-flight test, fail-open approve pin). Both prior findings
  verified closed; no new findings. This is the audit that covers the
  diff going to review.
---
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - None. Round-3 verification summary (no findings, so the detail lives
    here rather than in FINDINGS):
    (1) Round-1 finding (infra-failure re-eval-queue posts passing the
    guard) is GENUINELY CLOSED. The predicate now keys on
    `stage2_verdict IS NULL` instead of `stage2_done = FALSE`
    (V69__approve_quarantine_verdict_owed_guard.sql:124-130), and the
    infra-failure path leaves the verdict NULL, so the fail-closed
    re-eval-queue state (QUARANTINED, stage2_failed=TRUE, verdict NULL)
    trips the `status = 'QUARANTINED'` disjunct and is refused.
    `QuarantineProcedureNotifyIT.approveRaisesWhenInfraFailureReEvalOwed`
    seeds exactly that bitmap and asserts no row transition, no post
    UPDATE, no audit row, no NOTIFY.
    (2) Round-2 finding (the QUARANTINED-only conjunct never firing on
    the production first-pass-in-flight RAW state) is GENUINELY CLOSED.
    The added `OR stage2_failed = FALSE` disjunct fires on the RAW +
    stage2_failed=FALSE bitmap Stage 1 actually produces, independent of
    `status`. The pin tests were re-seeded to the real production states:
    `approveRaisesWhenStage2VerdictOwed` seeds
    (flagged, done=false, failed=false, verdict=null, RAW) and
    `QuarantineCommandHandlerTest.approve_verdictOwed_mapsToVerdictOwedNotInternal`
    updates its fixture to `status = 'RAW'` — the impossible QUARANTINED
    in-flight bitmap is gone from the suite.
    (3) The disjunct logic was audited against the schema:
    `stage1_flagged` and `stage2_failed` are BOOLEAN NOT NULL DEFAULT
    FALSE (V7__joins_post.sql:156-157), so no NULL hole exists in the
    `stage2_failed = FALSE` disjunct; `stage2_verdict` is nullable and
    CHECK-constrained to the closed set (V22, V36), and V47's re-eval
    candidate partial-index predicate corroborates that the only
    unjudged production bitmaps are (RAW, failed=FALSE) and
    (QUARANTINED, failed=TRUE) — both refused. The (QUARANTINED,
    failed=FALSE, verdict NULL) shape is also refused (fail-closed,
    harmless). Every judgment-recording path writes the verdict, so no
    judged post is blocked; watchdog/fail-closed quarantine
    (stage1_flagged=FALSE) is never blocked.
    (4) The check-vs-act race on the guard's unlocked post read is
    fail-closed in both directions: a verdict committed before the read
    makes `stage2_verdict IS NULL` false (judgment exists; approve is
    the sanctioned admin-override path), and a verdict committing after
    the read means the guard saw NULL and refused, aborting before any
    write — the admin's retry then succeeds. The quarantine-row FOR
    UPDATE taken before the guard additionally serializes against
    Stage2VerdictHandler's PENDING-row lock (M1-739).
    (5) The two deliberate exemptions were probed and stand as
    documented, spec-sanctioned accepted risk — NOT re-reported:
      * NEEDS_REVIEW (cap-exhausted) approvable: spec §Re-evaluation job
        makes cap exhaustion the admin-review escalation ("the admin
        notifier fires"); without the exemption these posts would be
        unapprovable forever. The admin's review IS the terminal
        judgment by design.
      * Fail-open released (RAW, stage2_failed=TRUE, verdict NULL)
        approvable, with approve's `stage2_failed = FALSE` clear
        permanently cancelling the owed re-evaluation: release-on-
        infra-failure with redactions retained is the spec DEFAULT
        posture (§Failure handling), and §Quarantine workflow makes
        admin `/quarantine approve` the ONLY redaction-lift path, so
        lifting redactions on already-visible content is the documented
        lifecycle (V41-pinned). This was escalated in round 2 and
        explicitly accepted in the ticket's refined acceptance text; the
        content was already user-visible (redacted) before the approve,
        every path through the guard still requires a live, non-banned
        bot admin (per-admin rate-limited, audit-before-effect logged),
        and an in-flight re-eval INJECTION verdict still re-hides per
        M1-739. The residual — an admin lifting redactions on content
        no judge ever saw, with re-evaluation cancelled — is real but
        is the accepted trade-off, not a gap against the spec.
    (6) No new surface elsewhere in the diff: the Provider error mapping
    adds a third substring shape ("stage 2 verdict still owed") that
    does not collide with the existing two; the new bundle strings are
    bot-authored with a UUID.fromString-validated token behind the admin
    gate (reflection-baseline entry added consistently); refused
    approves correctly write no audit row (no effect to audit, per the
    ticket's acceptance); reject_quarantine is untouched.
