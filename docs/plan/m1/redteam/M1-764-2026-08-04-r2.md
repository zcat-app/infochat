---
target: M1-764
date: 2026-08-04
base: 2722b68c28758c464ddd79810e726ba2046aaf3e
head: working-tree (branch m1/M1-764-pin-the-llm-transports-interru @ a0a23ab4 + uncommitted test)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Round 2, re-audit of the remediated diff. Round 1's single low DOS finding
  (guard installed on a platform thread while production runs virtual) was
  resolved via escalate -> refine: a new acceptance item now requires both
  legs, and the test runs the three assertions on a platform thread AND a
  virtual thread, each on its own fresh thread. Both legs were confirmed
  load-bearing by deleting the sendForBody re-arm and observing each fail in
  turn. The prompt carried the round-1 finding verbatim, instructed the
  auditor not to assume closure, and explicitly authorised a CLEAN verdict.

  Diff-range note: the skill's single-ticket algorithm would have taken the
  commit-range form (main...branch) because the branch carries the refine
  commit, which would have audited ONLY the ticket edit and missed the
  uncommitted test. Working-tree-vs-fork-point was used instead — the
  fail-closed choice, and byte-identical to what the reviewer sees.

  Round 1's finding is retained in the ticket's redteam_findings: for
  traceability rather than reset to [], since it is the recorded cause of
  the refine commit.
---

RED-TEAM VERDICT: CLEAN

RE-AUDIT NOTES (round 2, non-finding — recorded so the escalation decision
can see what was checked rather than assumed):

1. Test-only claim verified against the diff itself. The diff touches exactly
   three files: docs/plan/m1/STATUS.md (status board),
   docs/plan/m1/tickets/M1-764-llm-transport-interrupt-contract.md (ticket
   front-matter + two prose sections), and
   infochat-llm-adapter/src/test/java/app/zcat/infochat/llm/impl/
   HttpProviderSharedPipelineTest.java. No infochat-*/src/main/** hunk, no
   spec file, no migration, no config. The production trust path is byte-
   identical to base, so no authorization gate, intake step, sanitizer unit,
   audit emission, or rate bucket changes shape in this diff.

2. Round-1 finding (DOS/low, "the guard runs on the platform thread while the
   production render runs virtual") is genuinely closed, not merely attempted:
   - HttpProviderSharedPipelineTest.java:247-248 runs the SAME helper twice,
     once with Thread.ofPlatform() and once with Thread.ofVirtual(); the
     helper (:273-299) asserts all three properties inside each leg, so a
     virtual-only divergence fails on its own rather than being averaged away.
   - Each leg runs on a fresh thread and the leg's thread dies carrying the
     re-armed flag, so nothing leaks into JUnit's thread or, on the virtual
     leg, into the carrier (VirtualThread.unmount clears the carrier's flag).
   - The zero-request assertion is non-vacuous: the mock handler counts served
     requests (:305) and the control leg (:253-259) proves the counter can
     increment, asserting an absolute count of 1 after two interrupt legs.
   - Coverage of the production shape checked beyond the thread type: every
     generative and embedding call in the codebase bottoms out in
     LlmHttpSupport.sendForBody (AnthropicProvider :226 and
     OpenAiCompatibleProvider :272 via executeJsonCall,
     OpenAiCompatibleEmbeddingProvider :185 directly; DeepSeekProvider :62
     extends OpenAiCompatibleProvider; no langchain4j client exists in any
     main source), and the interrupt re-arm lives at that single site
     (LlmHttpSupport.java:190-194). The sibling tests in the same class pin
     that both HTTP providers reach that shared site, so exercising the
     interrupt leg through one provider does not narrow the guarantee.
   - The mechanism the test guards is intact upstream and downstream:
     DigestWorker.java:222/:256 still submits the render to the virtual-thread
     executor and cancels with cancel(true), and the one render-path site that
     legitimately consumes the flag (LlmOutputSanitizer.emitAuditRows :257)
     restores it in its finally (:284-288). No other flag-consuming site sits
     on the render path (the InFlightTracker :93 clear is the /stop
     in-flight-section boundary, a different path).

3. The pre-existing absence of §Rate limiting's named "aggregate system LLM
   budget" backstop for digest cost is unchanged by this diff and is not
   re-reported: it is production-side, recorded at the spec site under M1-758,
   listed in this ticket's out_of_scope, and already escalated out of round 1.
   Nothing in this diff widens or narrows it.
