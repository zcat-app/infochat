---
target: M1-766
date: 2026-08-05
round: 3
base: 749f4194c99931adda9437e44dc6ffbbb1349159
head: working-tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  Round 3 covers the two changes that landed AFTER round 2's CLEAN verdict —
  the DisplayHeadline.derive lines[0].isEmpty() branch (closing round 2's
  out-of-model item 1) and the security.md clarification naming the ingest
  anchor vs the presentation translation (out-of-model item 3). Both were
  new unaudited surface in a security-relevant path, which is why a CLEAN
  round 2 was not sufficient to merge on. CLEAN.

  Out-of-model disposition (user, 2026-08-05): RECORD BOTH, NO CHANGES.
  Neither item is backed by a security.md clause. The mirror case (an
  anchored block with an empty originalLine) was traced by the auditor
  through all four consumers and verified harmless — subordinateFor
  short-circuits on an empty original, so no bare [] reaches any surface.
  The reachability note documents a premise that already holds
  structurally: no sanitizer pass can empty a line or delete the
  renderer-authored newline, because the markdown passes re-emit their
  groups or only insert characters. Acting on either would have required a
  round-4 audit for advisory-only changes.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Mirror of the new branch: an ORIGINAL that canonicalizes away while the
    anchor survives. `derive` (DisplayHeadline.java:337) returns
    `AnchoredHeadline(truncate(lines[0]), truncate(lines[1]="" ), anchored=true)`,
    so the block reports as ANCHORED with an empty `originalLine`. Verified
    harmless on every consumer — `subordinateFor` (DisplayHeadline.java:436-440)
    short-circuits on `originalLine.isEmpty()`, so no bare `[]` reaches
    DegradedDigestRenderer:91-93, SummaryProseGenerator:284-288,
    DigestRenderer.appendHeadlines:977-979 or SavedCommandHandler:522-526 —
    but the reader is then shown ONLY the LLM-authored ingest anchor for a post
    whose publisher text rendered to nothing, with no bracket to mark the
    substitution. No security.md clause governs which derivation may fill the
    primary slot (D29 (c) governs the bracket, and the anchor legitimately
    renders bare for an `en` reader), so this is noted, not filed: if the threat
    model wants "an unbracketed line is the publisher's own words OR a declared
    anchor" to be checkable, the anchored-with-no-original shape is the case it
    would have to name.
  - Reachability of the new branch, stated so a future reader does not widen it
    by accident: `lines[0].isEmpty()` is reachable ONLY on the closed-list-match
    path (sanitize returns the canonical form, where `stripBidiAndZeroWidth`
    erased a zero-width-only anchor). It is NOT reachable through the markdown
    passes — `MARKDOWN_LINK`'s replacement is `$1 ($2)`
    (LlmOutputSanitizerCore.java:389,401-405) and `breakLinkAdjacency`
    (:468-469) only inserts characters, so neither can empty a line nor delete
    the renderer-authored `\n`. That is what makes the branch's premise ("the
    marker, if any, is on line 1") hold rather than being an assumption. A
    future sanitizer pass that could DELETE bytes outside a flag-span would
    invalidate it silently — there is no test pinning "no pass may empty a
    line", only the two behavioural tests at DisplayHeadlineTest:1509-1544.
