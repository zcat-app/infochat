---
target: M1-733
date: 2026-07-31
base: b6f13f968d886c0550d4f2e423c7e0b212fc865e
head: working tree (uncommitted, branch m1/M1-733-digest-mode-command)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: re-audit of the remediated diff — the r1 low finding (security.md §DB roles enumeration) verified closed; no new findings
---

RED-TEAM VERDICT: CLEAN

RE-AUDIT NOTE (round 2): The prior finding (PERM-ESCAL / low — V68's
GRANT UPDATE (digest_mode) widened the Provider role's writable column
set beyond the exact enumeration in docs/spec/security.md §DB roles
"...and nothing else") is verified CLOSED in the current diff. The diff
now amends docs/spec/security.md §DB roles to read "per V62 (V68 added
`groups.digest_mode`)" and adds `groups.digest_mode` to the column-scoped
UPDATE enumeration (diff hunk docs/spec/security.md @@ -1644,11 +1644,13
@@), matching V68__group_digest_mode_update_grant.sql's
`GRANT UPDATE (digest_mode) ON groups TO infochat_provider` one-for-one.
Spec text and live grant agree; the enumeration is exact again.

Fresh-audit coverage of the new surface, no findings:
- Authorization: the mode path flows through the unchanged DM-scope gate,
  actor resolve, and group-admin-OR-bot-admin check
  (DigestCommandHandler.java:74-104) before any write; a non-admin mode
  attempt is refused with no state change and no audit row (test-pinned).
- Injection: UPDATE_DIGEST_MODE_SQL binds the mode via PreparedStatement
  (DigestCommandHandler.java:59-60, 246-253); the mode value originates
  only from the closed lowercase switch in parseSubVerb
  (DigestCommandHandler.java:273-283), so the string-built details_json
  (lines 164-165) and the {0} reply interpolation carry no
  attacker-controlled bytes.
- Pause independence: the mode UPDATE touches digest_mode only; a paused
  group stays paused (test digestOffThenBrief_leavesGroupPaused).
- Audit integrity: DIGEST_MODE_SET is audit-logged before effect in the
  same transaction as the UPDATE, and is deliberately distinct from
  DIGEST_ENABLE so DigestScheduler.latestDigestEnableTime's
  paused-through-window carve-out boundary is unmoved; idempotent no-ops
  write no row, matching the on|off contract.
- Sanitizer surface: /digest was already a privileged-tier command; new
  sub-verbs do not change the command-name match set, and the new reply
  strings are deterministic bot-authored output interpolating only
  closed-set literals.
- Error path: SQLException rolls back and throws IllegalStateException
  with ContactIds.redact(callerContactId); no success reply can follow a
  failed transaction (successKey is unassigned on that path).
