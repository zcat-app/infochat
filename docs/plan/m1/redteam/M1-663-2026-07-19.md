---
target: M1-663 --in-progress
date: 2026-07-19
base: 7724fdbc580de83b1a07b5d7f1ccddf07c25319d
head: working tree (uncommitted on m1/M1-663-spec-deterministic-delivery-co)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Pre-review redteam gate fired by /m1-tick run (security_relevant: true,
  so the audit runs ahead of review per run.md step 4). Audited the
  working-tree-vs-fork-point diff: docs/spec/security.md §LLM output
  sanitizer gains a delivery-ordering contract paragraph (command-usage
  / help text in a chat-mode reply must be deterministic end-to-end OR
  pass through the sanitizer) and tightens the deterministic-output
  exemption to require a deterministic emission decision, not merely
  deterministic bytes; the non-qualifying shape (bot-authored content
  with a model-elected emission) is named and explicitly placed inside
  the sanitizer's mandate. docs/plan/m1/STATUS.md and the operand
  ticket file are workflow byproducts (start's status flip + STATUS
  regen). No code, no test, no migration. CLEAN.
---

RED-TEAM VERDICT: CLEAN

The diff is a spec-only amendment (M1-663) that adds a "Delivery-ordering
contract" paragraph to docs/spec/security.md §LLM output sanitizer. The
change is purely additive — no existing sentence is modified or removed,
and the only other touched files are routine ticket/STATUS metadata.

Audit against the threat model:

- The new contract (path (a) deterministic end-to-end OR path (b)
  sanitized) closes the M1-648 loophole — a post-sanitize, model-elected
  append of privileged command usage into a chat reply — at spec level.
  Both the "whether" and "what" clauses are explicit, matching the
  ticket's acceptance.

- The exemption for deterministic command output is tightened
  (deterministic emission decision required, not just deterministic
  bytes). The non-qualifying shape (bot-authored content + model-elected
  emission) is named and explicitly placed inside the sanitizer's
  mandate. This is a tightening, not a weakening.

- All neighboring commitments survive intact: the enumerated
  LLM-authored output surfaces (chat replies, /summary prose, digests,
  /retry re-rolls), the "bot-authored / no-inbound-interpolation"
  condition, the match-set derivation paragraph, the per-occurrence
  audit-logging sentence, and the residual-risk paragraph on non-error
  deterministic output are all unchanged.

- Contract scope (chat-mode reply specifically) is correct: the other
  LLM-authored surfaces are sanitized end-to-end and do not have the
  bot-authored-append loophole the contract targets.

- The "driven by the parsed user request" qualifier is well-justified
  by the threat model's own admission that the model's context is
  attacker-influenced (semanticSearch / D28 pre-fetch re-injecting
  feed-derived text every turn). The line is drawn exactly where the
  M1-648 injection lived.

- Tool allowlist, authorization model, ban handling, invite-code
  registration, DB roles, secrets handling — all untouched.

- The M1-648 defect lived in an abandoned ticket, so the loophole is
  not present in the shipped codebase; the new contract is structural
  prevention against re-introduction in M1-665/M1-649. The lack of
  mechanical enforcement for this surface is already documented in the
  surviving residual-risk paragraph, so there is no hidden gap.

No gap between the threat model's commitments and what the diff delivers.
