---
target: "M1-498 --in-progress"
date: 2026-06-29
base: fd566507 (branch fork point)
head: working tree (uncommitted, APPROVED r1)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Advisory audit run to honor the clarity WARN (SECURITY-FLAG-CONSISTENT) even
  though the ticket is security_relevant: false. Diff is test-only plus two
  behavior-preserving package-private test seams (FetchScheduler accessors,
  NostrStreamSource.deliverFor extraction); no new production behavior or attack
  surface. CLEAN — nothing to remediate.
---

RED-TEAM VERDICT: CLEAN

No findings. The diff under audit (M1-498) is a test-fidelity change. Its
only production-code edits are behavior-preserving test seams, and they do
not under-deliver any commitment in docs/spec/security.md.

Analysis summary (why each touched surface is not a finding):

- FetchScheduler.java:112-127 — adds package-private accessors
  (lastTickByKind, hostNextAllowed, pendingByKind, registeredKinds) that
  return live internal scheduling-state maps, replacing setAccessible
  reflection in FetchSchedulerClockIT / FetchSchedulerHostPacingIT. These
  are default-access (same-package) methods on a collector-internal class
  with no production caller. They cross none of the documented trust
  boundaries (§Trust boundaries 1-8: adapter->provider, ingest->store,
  authorization->execution, LLM<->state, health/SimpleX/LLM network
  surfaces). The exposed maps are outbound-fetch pacing/dispatch state, not
  a security rate limiter (§Rate limiting governs per-user inbound buckets,
  not host pacing). No adversary-reachable path; no commitment touched.

- NostrStreamSource.java:186-198 — extracts the inline Registrar `deliver`
  lambda into package-private `deliverFor(UUID sourceUuid)`. The lambda body
  is byte-identical to the prior inline version and the production call site
  (supervisor.register(..., deliverFor(sourceUuid))) is equivalent. It does
  not touch the Nostr StreamSource trust-boundary ordering (§Per-source trust
  boundaries / Nostr: signature verification -> kind allowlist -> outbox
  write), all of which run upstream of the deliver callback. No change to
  signature verification, kind allowlist, repost-edge handling, or
  read-only posture. sourceUuid originates from trusted DB enumeration.

- NostrSsrfIT.java — splits the single peer-IP test into a blocked-arm test
  and a NEW allowed-but-changed-IP arm (re-resolve to 127.0.0.2), plus a
  shared helper. This ADDS coverage for the §SSRF commitment "any peer-IP
  change observed at the socket layer is a hard close ... does not
  transparently accept it as a connection migration." Strengthens the
  defense's test coverage; does not weaken it.

- ResolveForWebSocketTest.java (new) — direct in-module pins for
  resolveForWebSocket: allowed-host -> validated set, blocked re-resolve ->
  BLOCKED_IP, non-ws scheme -> SCHEME_NOT_ALLOWED. Adds defense-in-depth
  coverage of the transport-agnostic SSRF gate (§SSRF, decision D38).

- OpenAiCompatibleProviderTest.java — adds a usage/model response-parse
  test; not security-relevant (LLM client response shape).

- STATUS.md / ticket markdown — pure workflow bookkeeping.

No negative-space gap: the reflection->seam swaps preserve the substance of
every prior assertion (map-clearing, kind-ticked marking); no security
assertion was removed or made vacuous.

FINDINGS:
  (none)

OUT-OF-MODEL:
  (none)
