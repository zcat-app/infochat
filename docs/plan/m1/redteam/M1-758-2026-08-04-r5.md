---
target: M1-758
date: 2026-08-04
base: 29aa59afbd80e13df6265e6934c77f1a4a06ffe6
head: "<working tree>"
verdict: FINDINGS
findings_count: {critical: 0, high: 0, medium: 1, low: 1}
out_of_model_count: 6
disposition: |
  Round 5. All eleven prior findings confirmed closed; structural claim 2
  (security.md enumerates every production ModelTask.TRANSLATOR call site)
  independently RE-DERIVED from the call graph and VERIFIED TRUE a second
  time. Structural claim 1 (the census) FALSIFIED — it named USER_GUIDE.md
  by hand and cleared it as asserting no invariant, while the file asserts
  the false strong form twice.

  Both findings remediated in branch, plus two the developer found while
  falsifying them: docs/design/05-llm-and-embeddings.md:932-934, a third
  copy of the false invariant that NO participant in the loop could see
  (census excludes docs/design/**; the auditor's role bars design notes);
  and the stale "English and Czech in v1" on the same USER_GUIDE line.
  Out-of-model 1 (verification.md:452-457) WAS actioned — it is the same
  false-guarantee class as the findings, and no test implements the
  retired clause, verified before editing. Out-of-model 2, 3, 4, 5, 6
  (SETUP_GUIDE "walks you through each task"; leg-by-leg pointer lands one
  heading early; /saved budget omitted from the guide; over-broad
  assertFalse substrings; stale language list — folded into row 12)
  carried forward unactioned as cosmetic.

  ROOT CAUSE, recorded because the rule has now failed three times: the
  ticket handles TWO defect classes with ONE enumeration method. An
  exposure claim is defined by meaning and needs a read (settled at round
  4). An invariant claim is a phrase family and greps reliably — but the
  census's population (b) is *.md, so javadoc and design notes asserting
  the same invariant are unreachable by construction. Population (c)
  added: all tracked file types, no path exclusions.
---
RED-TEAM VERDICT: FINDINGS

Round-5 re-audit note (verification of the eleven prior findings against the
CURRENT working tree — none are re-reported below):

  r1(a) closed. Neither runtime disclosure gates the whole task on a scope's
        /lang; both carry "an all-English deployment is NOT exempt"
        (switch-llm.sh:414-422, 4-llm.sh:514-522). Re-verified against
        IngestTranslationWorker:264 (only skip arm is
        `"en".equals(row.language())`), :274/:318 (ModelTask.TRANSLATOR),
        :358-366 (renderPrompt splices {{title}} AND the full untruncated
        {{body}}).
  r1(b) closed. No "FOUR legs" count claim survives; the reply-prose leg is
        enumerated at security.md §Secrets handling and named first at
        SETUP_GUIDE.md:596. No assertion pins its absence.
  r1(c) closed. SETUP_GUIDE.md:314-316 now says "all seven of them, including
        `chat` and `translator`, which carry private user text".
  r2(d) closed. 4-llm.sh:514-522 carries the same `!!` tier text as
        switch-llm.sh:414-422; the pre-M1-746 translator line is gone from
        both.
  r2(e) closed. SETUP_GUIDE.md:559-561 says the switcher asks once and applies
        to every task; the contradicting sentence is gone.
  r3(f) closed. Both surfaces now say the opposite of the fabricated
        safeguard: SETUP_GUIDE.md:571-575 and 4-llm.sh:530-532 ("there is no
        way to switch AND keep a pin"). Verified against switch-llm.sh:301-308
        (bare-Enter gate, `exit 0`, no write) and :352-355 (unconditional pin
        delete on any run that proceeds).
  r3(g) closed. OVERVIEW.md:290-296 names translator and the unattended
        whole-post leg.
  r3(h) closed. RemoteLlmWiringTest's two new assertFalse targets are "-
        translator" (the public tier marker) and "exposes the bot-reply text
        (can echo your queries)" — neither bars a still-true exposure claim.
  r3(i) closed. 4-llm.sh:510-513 names BodySummaryWorker's unattended
        whole-body leg, at parity with switch-llm.sh:429.
  r4(j) closed. 4-llm.sh's block comment (:482-491) now states the true
        behaviour and explicitly forbids restating it as a declinable prompt.
  r4(k) closed. docs/spec/decisions.md D57 now reads "... `chat` **or
        `translator`**", and docs/design/05-llm-and-embeddings.md:1102 — the
        line D57 quotes — is corrected in the same diff. The three false
        strong-form invariant copies (README.md:214, CLAUDE.md:50,
        docs/SPEC.md:258) are corrected to the D29 "never rewritten" wording.

Structural claim 1 — the census (ticket §Census, 11 rows, "Step 2 is a READ,
not a grep"): FALSIFIED. See Finding 1. The recorded read-based run names
`USER_GUIDE.md` by hand and dispositions it as asserting "no exposure or
invariant"; it asserts the invariant twice, in the false strong form the same
census filed as row 10.

Structural claim 2 — security.md §Secrets handling "The enumeration above
covers every production call site reaching `ModelTask.TRANSLATOR` as of
M1-758": VERIFIED TRUE, re-derived independently. Direct callers of
`provider.generate(ModelTask.TRANSLATOR, ...)` in main source are
QueryAnchorTranslator:260, IngestTranslationWorker:318 and
LlmTranslationProvider:94. `LlmTranslationProvider` is the only implementation
of the `TranslationProvider` SPI and is reached only through
`TranslationPipeline.translationProvider.translate` at TranslationPipeline:106
and :269, whose only main-source callers are ChatAgent:558,
SavedCommandHandler:453/457, ClusterBlockRenderer:132/166,
DigestRenderer:552/825/886 and CategoryRollupGenerator:213 (SummaryCommand
Handler and RetryCommandHandler reach the translator only via
ClusterBlockRenderer/DigestRenderer). Every one maps onto an enumerated leg.

Census population (a) re-checked without the phrase grep: no prod/*.sh other
than 4-llm.sh and switch-llm.sh makes an exposure claim (1-profile.sh:36-39
mentions the cloud but asserts no exposure; 2-secrets.sh/restore.sh/pack.sh
carry none), and no localization-bundle string asserts anything about
translation privacy (`help.cmd.lang.short/.usage`, `reply.lang.success`
checked), so there is no runtime user-facing disclosure surface at all.

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: docs/spec/security.md §Secrets handling, the bullets this diff
              lands: "**`translator` also carries the headlines of the posts a
              `/summary` run returns (M1-747).** ... each rendered headline —
              the retrieved post's snapshot `title`, or its `body` prefix when
              the title is empty — reaches `ModelTask.TRANSLATOR`, which may
              be remote"; "**`translator` ALSO runs on a leg that NO scope's
              `/lang` gates** ... it sends the post's full title AND
              untruncated body"; and the closing "a leg missing here
              propagates into" the renderings. The same section's honesty
              posture is stated at §Source URL visibility: "Hiding this would
              be dishonest to users; documenting it explicitly lets users make
              an informed choice."
    GAP: `USER_GUIDE.md` — the only end-user-facing document in the repo, and
          the only surface a non-operator ever reads — still asserts the FALSE
          strong form of the D29 invariant, twice:
            - USER_GUIDE.md:124 (`/lang` row of the command table):
              "original posts aren't translated"
            - USER_GUIDE.md:298-299 (§"Read it in another language"):
              "(The original posts themselves are never translated — only the
              bot's own wording.)"
          Both are false on two independent live paths. (1) Ingest:
          IngestTranslationWorker:264/274/318 translates the full title and
          untruncated body of every post from a non-English source
          (renderPrompt:358-366), gated on `source.language`, on a
          `@Scheduled` tick. (2) Display: ClusterBlockRenderer:116-133 passes
          `DisplayHeadline.of(first, ...)` — the post's own title, or its body
          prefix when the title is empty — to
          `TranslationPipeline.runForDisplayHit`, which calls
          `translationProvider.translate` at TranslationPipeline:269; the same
          shape at SavedCommandHandler:453/457 and DigestRenderer:886. The
          spec says so in the reverse direction at docs/spec/llm.md:303-317
          ("'Never rewritten' is a guarantee about the stored row, not about
          the render. A headline whose source language differs from the
          reader's is displayed in the reader's language") and at
          docs/spec/verification.md:461-469.
          This is byte-for-byte the defect class the diff's own census filed as
          row 10 and fixed in README.md:214, CLAUDE.md:50 and
          docs/SPEC.md:258 — a categorical negative about post translation
          asserted under a privacy framing. USER_GUIDE.md carries no
          disposition row in the census table at all; instead the census's
          recorded read-based run
          (docs/plan/m1/tickets/M1-758-switch-llm-translator-privacy-disclosure.md:701-706)
          names it explicitly and clears it: "It returned rows 10 and 11 and
          nothing else: `USER_GUIDE.md`, `SECURITY.md`, `DEVELOPER.md` and
          `docs/spec/messaging.md` mention remote routing or translation but
          assert no exposure or invariant." USER_GUIDE.md is selected by the
          census's own capability rule (maintained reader-facing repo-root
          `*.md`, `translat` selector added at round 4), so the surface was
          reached and mis-dispositioned rather than missed — the exhaustiveness
          record is what is wrong, which is the failure the round-4 refine
          rewrote the rule to prevent.
    REPRO: Deployment with `translator` routed to a remote provider (the
            posture both corrected runtime disclosures exist to obtain consent
            for) and at least one source whose `language` is non-English.
            A user runs `/lang cs`. They open USER_GUIDE.md — the document
            `README.md` and the welcome path point end users at — read §"Read
            it in another language" and the `/lang` row, and are told the
            original posts themselves are never translated, only the bot's own
            wording. They then subscribe to a private custom source (D59:
            reachable only through their own subscription row) and use
            `/summary` and `/saved` over it. On every render, that source's
            post titles (or body prefixes) are shipped to the third-party
            translator endpoint, and independently the collector ships those
            posts' full untruncated bodies there on a timer. The user was told
            in the only document written for them that exactly this does not
            happen; nothing they can read discloses it, because the corrected
            disclosures live in the operator-facing wizard output and
            SETUP_GUIDE.md, which a user never sees. The same false line is
            also what the next editor will restore the operator-facing copies
            from — the recurrence mechanism r4(j) and r4(k) each documented.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — the leg enumeration and
              its closing "The enumeration above covers every production call
              site reaching `ModelTask.TRANSLATOR` as of M1-758", read with
              the M1-755/M1-747 bullets that put a post's snapshot `title` or
              `body` prefix on the presentation translator path.
    GAP: `infochat-messaging-adapter/src/main/java/app/zcat/infochat/messaging/TranslationProvider.java`
          — the SPI's own contract, and the upstream statement the three
          copies this diff corrects are verbatim paraphrases of (docs/SPEC.md:258
          "SPI for bot-output translation ... Source post bodies are never
          translated"; CLAUDE.md:50; README.md:214) — still carries both false
          claims:
            - :7-12 "Translates <strong>only</strong> the LLM-authored strings
              the bot itself produces — cluster summaries, chat replies,
              digest headers, /retry re-rolls"
            - :16-18 "<strong>Source post bodies are NEVER translated.</strong>
              Posts arrive in whatever language the source publishes; Provider
              surfaces them as-is."
          Both are false about this SPI's own live call path:
          `TranslationPipeline.runForDisplayHit` calls
          `translationProvider.translate(displayHeadline, ...)` at
          TranslationPipeline:269 with `DisplayHeadline.of(post, ...)` — a
          source-authored title, or a `BODY_SCAN_LIMIT`-bounded prefix of the
          post body when the title is empty (ClusterBlockRenderer:116-133,
          SavedCommandHandler:453/457, DigestRenderer:886). "Provider surfaces
          them as-is" is the render-side claim docs/spec/llm.md:303-317
          explicitly retired on 2026-08-04.
          The diff's own acceptance items state the rule this violates — "THE
          'NEVER TRANSLATED' INVARIANT IS CORRECTED WHEREVER ASSERTED" and
          "ONE AUTHORITY, N RENDERINGS" — yet the authority the renderings
          derive from is left asserting the retired form. It is invisible to
          the census by construction (population (b) is `*.md` only) and to
          both new regression guards (every assertion reads captured shell
          stdout).
    REPRO: Maintenance path — the same one that produced r4(j) and r4(k). A
            developer or agent asked "does any source-authored post text reach
            the translator?" opens the SPI that defines the translator
            boundary, reads the two `<strong>`-marked out-of-scope bullets, and
            answers no. They then restate that in the next edit of any
            rendering — README.md's "Private by construction" bullet, the
            wizard block, or a new user-facing doc — and `mvn verify` stays
            green, because the only mechanical guards on this claim family
            assert over shell stdout and cannot see Java source. That is
            precisely how the pre-M1-746 translator line survived from the
            switcher into the install wizard (r2(d)) and how the fabricated
            pin safeguard survived from printed text into a comment (r4(j)).
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - `docs/spec/verification.md:452-457` states the D29 presentation-path
    verification requirement as "A spy on the **presentation** path asserts
    that no call argument equals or contains the body of any `post` row; only
    presentation strings (cluster prose, headers, system phrasing) reach it."
    The second clause is no longer true of the presentation path: the
    display-hit leg sends a post's snapshot `title` — and a bounded prefix of
    its `body` when the title is empty — through
    `translationProvider.translate` (TranslationPipeline:269), which
    security.md §Secrets handling's own M1-747/M1-755 bullets assert. The same
    bullet's next four sentences (the "D29 display-leg amendment (2026-08-04)"
    text) state the corrected doctrine, so the file contradicts itself rather
    than being uniformly wrong, and it is pre-existing, untouched by this diff
    and outside `files_scope`. Flagged so the user can decide whether a
    spec-tier verification requirement whose first clause the shipped code
    violates deserves its own ticket.
  - `SETUP_GUIDE.md:584-586` still says the switcher "is fully interactive and
    walks you through each task", 25 lines after the corrected "asks **once**
    ... applies that choice to **every** AI task". Literally true only of the
    per-task MODEL prompts on the remote/openai-compatible path
    (switch-llm.sh:223-227). Noted at round 4 and accepted as cosmetic; still
    present, still not an exposure claim.
  - Both runtime disclosures print `Full leg-by-leg list: SETUP_GUIDE.md,
    "Switching your AI backend later"` (4-llm.sh:522, switch-llm.sh:422), but
    that section (SETUP_GUIDE.md:549-587) carries only the tier summary; the
    leg-by-leg list is the adjacent sibling `### What `translator` sends, leg
    by leg` (:589-621), with no forward link from the named section. Noted at
    round 4 and accepted; unchanged.
  - `SETUP_GUIDE.md:603-605` marks the periodic-digest headline leg as the only
    capped one, but security.md §Rate limiting (M1-755) also budgets the
    `/saved` display-hit leg at `infochat.save.translation-max-per-page`
    (default 5). The omission understates an existing bound rather than
    overstating safety, so it is the harmless direction for a privacy
    disclosure; noted only because the ticket's own Tier-A3 acceptance item
    calls the digest leg "the only budgeted one", which is false and could be
    copied forward.
  - `SwitchLlmWiringTest` pins the absence of "strict no-op" and
    `RemoteLlmWiringTest` the absence of "one at a time" over the WHOLE
    captured output. Both substrings have true uses a future, more precise
    disclosure might want. Materially weaker than the r1/r3 instances (neither
    is a leg description) — carried over from round 4's list unchanged.
  - `USER_GUIDE.md:124` also says the supported languages are "English and
    Czech in v1", stale since `es`/`ru`/`tr` shipped (docs/spec/llm.md:323-325).
    Accuracy drift, no exposure claim; listed only because a fix to Finding 1
    touches the same line.
