---
target: M1-733
date: 2026-07-31
base: b6f13f968d886c0550d4f2e423c7e0b212fc865e
head: working tree (uncommitted, branch m1/M1-733-digest-mode-command)
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 1
out_of_model_count: 0
disposition: remediated in-branch (security.md §DB roles enumeration gained groups.digest_mode); user chose escalate → refine; closed by re-audit M1-733-2026-07-31-r2.md (CLEAN)
---

RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: PERM-ESCAL
    SEVERITY: low
    PROMISE: docs/spec/security.md §DB roles — "per V62 the Provider holds
      SELECT on users, groups, group_membership and invite_code plus a
      column-scoped UPDATE (users.probation_until, users.save_count;
      groups.timezone, groups.digest_enabled, groups.removed_at;
      group_membership.removed_at) ... and nothing else". The spec presents
      this exact column enumeration as the least-privilege trust path
      bounding what a Provider SQL-injection foothold can reach.
    GAP: infochat-core/src/main/resources/db/migration/V68__group_digest_mode_update_grant.sql:13
      adds `GRANT UPDATE (digest_mode) ON groups TO infochat_provider`,
      widening the Provider role's writable column set beyond the
      spec-enumerated list, but the diff does not amend
      docs/spec/security.md §DB roles. Post-diff, the spec's "and nothing
      else" is false: the source-of-truth trust-path document understates
      the role's write surface. The divergence is documentary, not a new
      privilege channel — digest_mode is a CHECK-constrained config column
      (V67: `TEXT NOT NULL DEFAULT 'normal' CHECK (digest_mode IN
      ('brief','normal','full'))`), in the same non-privilege class as the
      already-granted digest_enabled, so the practical blast-radius increase
      for an injection foothold is negligible (flipping a group's digest
      render detail).
    REPRO: An auditor (or a future change) reasoning from security.md §DB
      roles concludes the Provider role cannot write any groups column
      beyond timezone/digest_enabled/removed_at; the live role can also
      write digest_mode. Any downstream control designed against the stale
      enumeration (e.g. a trigger or review checklist keyed to the spec's
      column list) silently misses the new writable column.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - None. Checked and not raised as findings: the usage error is returned
    before the admin check (sub-verb set is public documentation, so no
    oracle); trailing tokens after a valid sub-verb are ignored
    (`/digest brief xyz` dispatches as brief) — pre-existing behaviour of
    the on|off path, unchanged by this diff; details_json is built by
    string concatenation but both interpolated values are closed-set
    (switch-derived mode and a CHECK-constrained column), so no JSON/log
    injection; the success reply echoes only the closed-set mode literal,
    so no inbound-reflection echo; DIGEST_MODE_SET is correctly distinct
    from DIGEST_ENABLE, preserving the DigestScheduler
    paused-through-window carve-out; audit-before-effect in the same
    transaction with no audit row on the idempotent no-op matches the spec.
