---
target: M1-404
date: 2026-06-20
base: 1679de85682aa9d01b89c168d319c35f5e78da57
head: working-tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Audit ran on the in-progress branch tip (working tree) between /m1-tick review
  APPROVE and /m1-tick commit. Pure mechanical extraction of the Stage 1 fail-closed
  whole-body quarantine write into a shared helper parameterized only by rule-id;
  the threat-actor confirmed no quarantine property shifted. No findings, no
  remediation needed.
---
RED-TEAM VERDICT: CLEAN

The diff is a behavior-preserving extraction. The two Stage 1 fail-closed
handlers (handleWatchdogAbort, handleMatchOverflow) previously held
byte-identical quarantine-write bodies differing only by the rule_id
constant. The change factors that body into a single private
quarantineWholeBody(...) parameterized by ruleId; each caller keeps its
own distinguishing WARN line before delegating.

Verified against docs/spec/security.md §Failure handling and §Quarantine
workflow that every security-relevant property is preserved:

- Stage 1 infrastructure failure remains fail-closed: post is set
  status='QUARANTINED' with NO auto-release (Stage1Result(..., true, true)),
  identical to both prior bodies.
- One quarantine row spanning [0, normalized.length()) holding the
  verbatim original; body overwritten with one whole-body
  [REDACTED:<id>] placeholder where <id> is a per-row random
  PlaceholderIds.next() token (the spec's anti-forgery requirement).
- Both writes stay inside a single TransactionHelper.inTransaction, so
  a partial commit cannot leave a QUARANTINED post without its
  quarantine/audit row.
- ruleId is the only varying value and is passed through correctly
  (REGEX_TIMEOUT_RULE_ID for watchdog, MATCH_OVERFLOW_RULE_ID for
  overflow), preserving the distinct rule_id recorded per failure class.

No authentication, authorization, ban-handling, input-validation,
audit-write, or rate-limit surface is touched. The sensitive-surface
inventory's single hit is a comment-line match, not a code path.

No gap between threat-model promise and delivery was found.
