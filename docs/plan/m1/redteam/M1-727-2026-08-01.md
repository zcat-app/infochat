---
target: M1-727
date: 2026-08-01
base: b8db467923432e4ad49a3eb30441cfd316aba708
head: working tree (in-progress branch m1/M1-727-personal-classification-routes-to-other)
verdict: CLEAN
findings_count: {critical: 0, high: 0, medium: 0, low: 0}
out_of_model_count: 1
disposition: CLEAN ahead of review (/m1-tick run gate for security_relevant: true). The single out-of-model item (V73 CHECK re-validation locks post partitions at boot; NOT VALID not used) was evaluated and FALSIFIED as a practical concern on 2026-08-01: the lock mode is real (AccessExclusiveLock observed on pg16, the compose image), but (a) `post` is bounded by the 30-day partition-drop TTL (`infochat.partitions.retention-days.post`, Invariant 6/D33), (b) the deployment's real corpus is ~10^4 rows, (c) a measured DROP+ADD of V73's exact shape over 1M rows across 3 monthly partitions completed in 1.28 s — the real corpus costs milliseconds — and (d) Flyway runs at boot, before the service accepts traffic, so the worst case is a sub-second deploy-time pause, not a runtime outage. NOT VALID + VALIDATE CONSTRAINT would add migration complexity for no measurable gain. Validation cannot fail: the old set is a strict subset of the new. Closed as accepted residual; no remediation opened.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - V73 (`infochat-core/src/main/resources/db/migration/V73__classification_personal_label.sql`)
    runs `ALTER TABLE post ... ADD CONSTRAINT ... CHECK` without `NOT VALID` on the
    partitioned parent, so existing rows across every child partition are validated
    under lock at boot. On a large `post` corpus this is a boot-time availability
    consideration for the operator (migration window), not an adversary-reachable
    path — the DB is internal per the threat model and migrations are trusted
    operator action. Flagged only so the user can decide whether boot-time
    migration cost belongs in the model.
