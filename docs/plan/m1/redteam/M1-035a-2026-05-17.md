---
target: M1-035a
date: 2026-05-17
base: 7a348c38^
head: 7a348c38
verdict: CLEAN
findings_count: 0
out_of_model_count: 3
disposition: |
  /redteam M1-035a ran post-/m1-tick-commit, pre-/m1-tick-merge (the branch
  commit 7a348c38 is the audit head; the squash-merge to main has not yet
  happened). Verdict CLEAN. Three OUT-OF-MODEL observations recorded for
  visibility; all three are forward dependencies on M1-035b's Provider-side
  AdapterRegistry / startup gate work and are NOT failures of this diff's
  scope (SPI + InMemoryAdapter test double, per the ticket's out_of_scope).

  Followups feeding M1-035b acceptance:
    1. M1-035b's AdapterRegistry MUST enforce LOW-trust rejection on
       admin-bearing paths (the spec promise InMemoryAdapter's LOW default
       is the safety net for).
    2. M1-035b's startup gate MUST refuse production adapter co-residency
       with the in-memory adapter (decision D46) — the InMemoryAdapter's
       LOW-or-HIGH constructors are public and unconstrained at instantiation
       in this diff.
    3. Whether the per-adapter maxInboundMessageBytes enforcement contract
       (CapabilityFlags.java javadoc lines 136-142) is exercised by a test
       harness on production adapters is a separate concern; the InMemoryAdapter
       intentionally does not enforce its own declared cap (test-double
       posture), so M1-035b/T3-A reviewers should ensure SimpleX/Signal
       implementations do honour the cap.

  The threat-actor included narrative preamble before the structured
  verdict (template asks for structure only); the structured CLEAN verdict
  plus three OUT-OF-MODEL items is preserved verbatim in the body below.
---

# /redteam M1-035a — 2026-05-17

Verbatim output from the threat-actor subagent (`Agent(subagent_type: "threat-actor", ...)`).

---

Now I have the full diff and threat model. Let me analyze.

Key observations:

1. **SPI surface** — this is an SPI definition + test-double implementation. It is NOT the Provider intake path, the authorization stack, the ban check, or the rate cap. The diff only delivers types and a test-only adapter.

2. **Trust level handling** — `InMemoryAdapter` defaults to LOW. The spec says "Provider rejects LOW-trust assertions for admin-bearing paths unless the operator opts in." That enforcement belongs to Provider, not the SPI/adapter. No diff content shows Provider doing the rejection — but this diff isn't the Provider; it's the SPI. The Provider enforcement is downstream work.

3. **`assertIdentity` in InMemoryAdapter** returns `msg.sender()` verbatim. The threat model says identity must be cryptographically anchored. For the test double, the operator-trusted bootstrap is that this adapter is only enabled in test-deployment shape (D46). The diff comment says so. No identity verification at the SPI layer — but this is a test double; no crypto material exists. The trust-level LOW default is the gate.

4. **`maxInboundMessageBytes`** — capability flag exists but the InMemoryAdapter's `deliverDm` does NOT enforce it. The spec commits in CapabilityFlags javadoc line 136-142: "transport-layer first-defense cap... messages over this size are dropped by the adapter before delivery to Provider." The InMemoryAdapter ignores this entirely. `deliverDm` accepts arbitrary-length text and synchronously dispatches.

5. **Rate limiting** — spec §1.5 promises "per-(adapter, contact_id) inbound rate cap" applied before every application-level check. The diff's `deliverDm` synchronously calls `current.onMessage(msg)` with no rate cap. But again, this is Provider intake stuff and InMemoryAdapter is the test harness.

6. **Unicode normalization** — spec §1.7 promises NFKC + bidi strip on body. Not in this diff. But again, Provider responsibility.

7. **`name()` returns "inmemory"** with no validation when registered. Cross-adapter isolation key is `(adapter, contact_id)`. If `name()` could be spoofed across adapters... not relevant here, only one adapter.

8. **`MessagingAdapter#finalize`** — uses Java's reserved-like name `finalize` from `Object`. This is interesting but not a security issue per se.

9. **`deliverDm` accepts arbitrary contact id** and creates an Identity with `cid` as both contactId and displayName. The threat model says display names must never be used for authorization (D10). Setting `displayName=cid` means if any downstream code accidentally used displayName for auth, it would match. But that's hypothetical and the spec already commits to contactId-only.

10. **`InMemoryAdapter` has no CDI annotations yet** — the diff explicitly notes the bean will become CDI in M1-035b. Meanwhile, the comment says it "lives in the production classpath but is only activated by the test-time deployment shape (`infochat.adapters=inmemory` exclusively per decision D46)." Since there's no CDI/wiring yet, the activation guard doesn't exist in this diff. But neither does Provider startup wiring, so the adapter cannot be invoked in production from this diff.

11. **`assertIdentity` returns whatever the test sets** — the spec says "The adapter asserts identity via a stable, cryptographically anchored ID." For InMemoryAdapter, there's literally no verification. But LOW trust level is the mechanism that bounds this. The Provider must check trustLevel and refuse admin paths.

12. **`MessagingException` exposes message** — exception message could leak internal details if surfaced to users. Threat model says no leaking internal errors. The diff doesn't show where exceptions are surfaced — `requireKnownAndOpen` puts `handle.opaqueValue()` (the inmem ID) in the message. Test-only impl; not a production leak path.

13. **`InMemoryAdapter` thread-safety on handler** — `volatile InboundHandler handler` set without ordering relative to `deliverDm`. Test code; not a security issue.

14. **`reset()` is publicly accessible** — anyone holding a reference can wipe state. Test helper. Not production.

Let me reconsider what's truly in scope. The diff introduces SPI types and a test-double. Most threat-model promises are about Provider intake, authorization, ingest pipeline — not the SPI surface itself.

Things that ARE about the SPI surface:
- D10 cryptographic identity → InMemoryAdapter has no crypto, but LOW trust signals that
- `supportsMarkdownLinks=false` required → InMemoryAdapter declares false ✓
- `supportsMentionByContactId` required for group adapters → declares true; group mode deferred ✓
- Transport-level inbound size cap → CapabilityFlags has the field but the InMemoryAdapter doesn't enforce it on `deliverDm`
- Cross-adapter trust-level isolation → trustLevel() added ✓

The `maxInboundMessageBytes` non-enforcement is the only real candidate finding. Let me check if `deliverDm` is even a production path. It's a test helper used by tests to simulate inbound. In production, the inbound path would be the messaging transport's own ingest. The InMemoryAdapter is explicitly test-only (D46). So `maxInboundMessageBytes` enforcement isn't relevant in a test double... unless test code accidentally relies on the cap firing. But it's a test double — bypassing its own capability is consistent with test-double purposes.

Let me also reconsider: the spec section being clearest about adapter-level inbound size dropping is in the new CapabilityFlags javadoc. That's the diff *itself* making that promise. So the diff promises this and the InMemoryAdapter doesn't keep that promise. But again, it's a test double on test classpath.

Looking again at `deliverDm` constructing Identity with `displayName=cid`. The threat model is clear that displayName is informational and never used for authorization. The diff's Identity record's javadoc also reinforces this. So this is not exploitable per spec contract.

One more concern: `InMemoryAdapter` is `final` with `name()="inmemory"`. The adapter registry (M1-035b) keys by name. If `name()` were to collide with another adapter... not in scope here.

The diff genuinely does not deliver the Provider-side enforcement of LOW-trust rejection — but that enforcement is documented as a Provider responsibility (M1-035b umbrella), so it's a forward dependency, not a missing-promise.

Given that this is an SPI-and-test-double diff with no Provider intake, no admin paths, no ban check, no LLM, no audit, no DB — most threat-model surfaces are simply not touched. The remaining concern is the test-double's permissiveness, but the trust-level LOW default is precisely the safety net the spec demanded.

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - The CapabilityFlags javadoc at infochat-messaging-adapter/src/main/java/io/infochat/messaging/CapabilityFlags.java:136-142 promises that "messages over this size are dropped by the adapter before delivery to Provider" for the maxInboundMessageBytes cap, but InMemoryAdapter.deliverDm at infochat-messaging-adapter/src/main/java/io/infochat/messaging/impl/inmemory/InMemoryAdapter.java:776-795 does not enforce this cap. Because InMemoryAdapter is explicitly a test double activated only under the test-time deployment shape per decision D46 (and never alongside production adapters), this does not reach a real user. Worth flagging so the Provider-side enforcement contract is exercised by the test double and so M1-035b's registry/router work explicitly covers oversize-inbound behavior on production adapters.
  - InMemoryAdapter.assertIdentity at infochat-messaging-adapter/src/main/java/io/infochat/messaging/impl/inmemory/InMemoryAdapter.java:113 returns msg.sender() verbatim with no cryptographic verification. The spec's trust-boundary commitment (cryptographically anchored ID per D10) is reconciled here by the LOW default trustLevel. The downstream Provider gate that refuses LOW-trust for admin paths is not in this diff; absent that gate landing in M1-035b, an operator that runs the test adapter alongside production (forbidden by D46 but mechanically uncontrolled in this diff) would bypass D10. The diff's defense-in-depth depends on Provider-side enforcement that does not yet exist; tracking this dependency in the umbrella ticket would be prudent.
  - InMemoryAdapter is a public final class with a public no-arg constructor and a public AdapterTrustLevel constructor (infochat-messaging-adapter/src/main/java/io/infochat/messaging/impl/inmemory/InMemoryAdapter.java:695-707). Any code on the production classpath can instantiate it with HIGH trust. The threat-model commitment to "operator must explicitly opt in" to LOW is preserved by the default, but HIGH selection is unrestricted in this diff. The M1-035b registry is presumably the gate that keeps this constructor from being invoked in production; that gate is not visible here.

Relevant files:
- /home/ubuntu5/Projects/quarkus-projects/infochat/docs/spec/security.md
- /tmp/m1-035a-redteam-diff.txt
- (diff target paths) infochat-messaging-adapter/src/main/java/io/infochat/messaging/CapabilityFlags.java
- (diff target paths) infochat-messaging-adapter/src/main/java/io/infochat/messaging/MessagingAdapter.java
- (diff target paths) infochat-messaging-adapter/src/main/java/io/infochat/messaging/impl/inmemory/InMemoryAdapter.java
