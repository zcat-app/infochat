---
target: M1-135
date: 2026-06-02
base: 254ad1a22cc6a3b8f45178280bf78b98eb38ced0^
head: 254ad1a22cc6a3b8f45178280bf78b98eb38ced0
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 0
out_of_model_count: 2
disposition: |
  One medium INFO-LEAK finding: the new IPv6-transition-form decode
  (6to4 / Teredo / NAT64 / IPv4-compatible) routes the embedded IPv4
  through isBlockedV4 only, which consults the static range table but
  NOT the host's-own-non-loopback-interface set. The host-interface
  check lives solely at the top of isBlocked and compares the
  as-supplied InetAddress; for the four transition forms the supplied
  address is a genuine 16-byte Inet6Address, so it never equals a v4
  interface entry, and the embeddedV4 → isBlockedV4 fall-through never
  re-consults host interfaces. The IPv4-mapped form escapes the gap
  only because the JDK normalizes ::ffff:a.b.c.d to an Inet4Address.

  This is a defense-in-depth gap against the explicit spec clause "...
  plus the host's own non-loopback interfaces" (security.md §SSRF).
  Rated medium, not high: end-to-end exploitability additionally
  requires the host to hold a non-RFC1918 interface IP bound to a
  sensitive service AND functioning 6to4/Teredo/NAT64 return-routing
  in the deployment network (the routing precondition is itself
  out-of-model — see OUT-OF-MODEL).

  Disposition: FIXED IN-BRANCH before merge, per user direction
  (the original commit 254ad1a was NOT amended — the fix landed as a
  follow-up commit on m1/M1-135-ssrf-hardening-bundle, which squashes
  into the single M1-135 commit at merge). The fix captures the
  per-call host-interface set once in isBlocked and routes every
  decoded embeddedV4 result through a new isHostInterfaceV4 check, so a
  6to4/Teredo/NAT64/IPv4-compatible spelling of a host public-interface
  IP is now blocked (the IPv4-mapped form was already covered because
  the JDK normalizes it to an Inet4Address). Five IpBlocklistTest cases
  were added (one per transition form asserting the host IP is blocked,
  plus a negative control asserting a non-host public IP in 6to4 form
  stays allowed). No remediation ticket is needed.

  Two OUT-OF-MODEL advisories recorded below (routing-precondition
  bound on the finding's severity; abandoned virtual-thread body-read
  DOS, which is not a regression introduced by this diff). Neither is
  converted to a ticket here.
---

RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: "DNS-resolved IPs are checked against a blocklist of private, loopback, link-local, multicast, CGNAT, and cloud-metadata ranges (notably `169.254.169.254` and IPv6 equivalents) plus the host's own non-loopback interfaces." (docs/spec/security.md §SSRF and outbound connections, lines 137-140)
    GAP: The new IPv6-transition-form decode in `IpBlocklist` routes an embedded IPv4 through `isBlockedV4` only, which checks the static range table but NOT the host's-own-interface set. The host-interface check lives solely at the top of `isBlocked` (infochat-ssrf/.../IpBlocklist.java:101, `hostInterfacesProvider.get().contains(addr)`) and compares the *as-supplied* `InetAddress`. For 6to4 / Teredo / NAT64 / IPv4-compatible inputs the supplied address is a genuine 16-byte `Inet6Address` (IpBlocklist.java:225-255), so it never equals the v4 interface entry in the set; the code then falls through to `embeddedV4(...)` → `isBlockedV4(embedded)` (IpBlocklist.java:117-118), which has no host-interface consultation (IpBlocklist.java:121-164). An embedded IPv4 equal to one of the host's own *public* non-loopback interface IPs (VPN tunnel, container bridge, freshly-attached cloud EIP — exactly the post-startup interfaces the per-call supplier was added to cover, per IpBlocklist.java:50-56) is therefore not blocked. The IPv4-mapped form escapes this because the JDK normalizes `::ffff:a.b.c.d` to an `Inet4Address`; the four new transition forms do not normalize and so expose the gap.
    REPRO: Host has a non-RFC1918 interface IP, e.g. a cloud EIP `203.0.113.5`, with a sensitive service bound to it. Attacker submits a feed/source URL whose host resolves to (or is the literal) `2002:cb00:7105::` (6to4 of 203.0.113.5) — or the equivalent Teredo / `64:ff9b::cb00:7105` NAT64 / `::203.0.113.5` IPv4-compatible spelling. `isBlocked` returns false (host-interface `contains` misses the Inet6Address; `isBlockedV6` misses; `embeddedV4` decodes to 203.0.113.5; `isBlockedV4` returns false because the public IP is in no static range and host interfaces are not re-checked), so the dial proceeds against an address the spec commits to blocking. The IPv4-mapped spelling of the same IP would be blocked, demonstrating the inconsistency.
    SUGGESTED-FIX-CLASS: trust-boundary-tightening

OUT-OF-MODEL:
  - End-to-end exploitability of the host-interface gap depends on 6to4/Teredo/NAT64 return-routing actually delivering packets back to the host's own interface, which requires functioning transition-gateway infrastructure in the deployment network — that routing precondition is outside the documented threat model. The blocklist-coverage gap itself (an address the spec says must be blocked is not) is in-model and is the finding above; the network-routing precondition only bounds its real-world severity, which is why this is rated medium rather than high.
  - DOS via abandoned virtual-thread body reads: on a per-read `TimeoutException` the code calls `readTask.cancel(true)` but a blocking `in.read` on a socket may ignore interrupt; the leaked virtual thread unwinds only when `perCallClient.close()` / `in.close()` (try-with-resources, SsrfGuardedHttpClient.java:347/497) tears the connection down. This matches the prior `shutdownNow()` semantics and the new client-close actually improves cleanup, so it is not a regression introduced by this diff.
