---
target: M1-035b
date: 2026-05-17
base: c68c51429de997e9550074a79c6ba4e73b28f6b2^
head: c68c51429de997e9550074a79c6ba4e73b28f6b2
verdict: FINDINGS
findings_count:
  critical: 0
  high: 2
  medium: 3
  low: 1
out_of_model_count: 2
disposition: |
  /redteam M1-035b ran post-/m1-tick-commit, pre-/m1-tick-merge (the
  branch commit c68c5142 is the audit head; the squash-merge to main has
  not yet happened). Verdict FINDINGS with 6 entries.

  Two of the six findings (the two high-severity AUTH-BYPASS / DOS items
  and one of the AUTH-BYPASS mediums) trace to spec promises the ticket
  itself acknowledges as deferred: InboundRouter.java:113 carries an
  explicit `T2-A wires the missing intake steps upstream of this point:
  ban check (D11), invite-code gate (D44), slow-start probation (D45)`
  comment, and the §Authorization model step 1.5 transport-level rate
  cap is in the same deferred-to-T2-A bucket. The threat-actor (correctly,
  by its prompt — "treat the diff as the only evidence") flagged these as
  current-state gaps; the user's lifecycle disposition is whether to
  capture them as a remediation ticket against M1-035b, fold them into
  T2-A's acceptance, or leave the audit record as the durable signal
  pending T2-A.

  Two findings are genuinely net-new to this diff and not "deferred to
  T2-A":
    - INFO-LEAK (medium): the InboundRouter.normalize() implementation
      applies whole-body NFKC + trim with NO fenced-code carve-out, even
      though §Authorization model step 1.7 + §Ingest pipeline (chat-input
      parity) commit verbatim that "Bytes inside fences are preserved
      verbatim" and the carve-out is the entire reason the rule exists.
      The InboundRouter.java:596-606 javadoc explicitly says "Whole-body
      NFKC is sufficient for MVP" — directly contradicting the spec
      promise. This is a code defect of THIS diff, not a deferred item.
    - INFO-LEAK (low): scope (which carries the contact id for DM scopes)
      is logged unredacted in the three SLF4J error paths in
      InboundRouter.java (lines 130, 138, 145). §Secrets handling commits
      "Contact IDs are logged in redacted form (prefix + ellipsis +
      suffix) outside the audit log." The router's error log is not the
      audit log.
    - DOS (medium): no body-size cap before NFKC normalization runs;
      defense-in-depth gap independent of T2-A.

  Followups feeding M1-035b remediation OR T2-A:
    1. Wire the §Authorization model step 1.5 transport-level rate cap
       upstream of InboundRouter so the diff's per-inbound fixed-reply
       paths cannot be weaponised. (Spec scope: T2-A; rate-cap is the
       step BEFORE ban check in the spec's authorization evaluation
       order.)
    2. Wire the §Authorization model step 2 / §Trust boundaries item 2
       chain (ban check, invite-code gate, slow-start probation) upstream
       of InboundRouter's dispatch and replace CHAT_MODE_REPLY /
       UNKNOWN_COMMAND_REPLY for unknown-DM contacts with the spec'd
       "access requires an invitation" fixed reply. (Spec scope: T2-A.)
    3. Add fenced-code carve-out to InboundRouter.normalize() per the
       CommonMark rule documented in §Ingest pipeline. (Spec scope: THIS
       ticket — M1-035b's spec_refs include `docs/spec/security.md
       §Authorization model` and the §1.7 carve-out is part of that
       section's verbatim commitment.)
    4. Redact contact ids in InboundRouter error-path logs per §Secrets
       handling. (Spec scope: THIS ticket.)
    5. Sanity-cap inbound body size before normalize() runs (the adapter's
       declared maxInboundMessageBytes is honor-system; the router is the
       centralized seam for defense in depth).

  The two OUT-OF-MODEL items concern operator-config posture for the
  `%test.infochat.adapters.inmemory.allow-low-trust=true` default
  accidentally being shipped to production — not a defect of this diff
  but worth surfacing for deployment review at T3-A.

  The threat-actor included narrative preamble before the structured
  verdict (template asks for structure only); the structured FINDINGS
  verdict plus two OUT-OF-MODEL items is preserved verbatim in the body
  below.
---

# /redteam M1-035b — 2026-05-17

Verbatim output from the threat-actor subagent (`Agent(subagent_type: "threat-actor", ...)`).

---

RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: DOS
    SEVERITY: high
    PROMISE: §Authorization model step 1.5: "Apply the per-`(adapter, contact_id)` inbound rate cap... Over-cap inbound is **dropped silently** for the rest of the cap window — no reply (including no fixed ban reply, no fixed invite-required reply, no friendly error). The cap runs after step 1 (the bucket is keyed by the resolved `(adapter, contact_id)`) and before every application-level check below, so a hostile flood cannot drive outbound cost via the per-inbound fixed-reply paths in steps 2 and 4."
    GAP: `infochat-provider/src/main/java/io/infochat/provider/messaging/InboundRouter.java:111-135` — `onMessage` runs no rate-cap step. Every inbound produces one of three deterministic outbound replies (CHAT_MODE_REPLY, UNKNOWN_COMMAND_REPLY, INTERNAL_ERROR_REPLY). There is no per-`(adapter, contact_id)` bucket anywhere in the Provider's main tree (grep for `RateLimit`, `is_banned`, `inviteCode` returns no files).
    REPRO: Adversary connects to any activated adapter and floods inbound messages with arbitrary bodies. The router replies to every single one with at least one of the fixed literals via `target.send(...)`. There is no cap on outbound — the only constraint is the adapter's `maxSendsPerSecond` (10k/s in `CapabilityFlags`). The hostile flood drives outbound cost 1:1 with inbound — exactly the failure mode the spec commits to preventing at this seam.
    SUGGESTED-FIX-CLASS: rate-limit

  - CATEGORY: AUTH-BYPASS
    SEVERITY: high
    PROMISE: §Trust boundaries item 2: "Identity resolution and the ban check run *before* parsing. Banned users get one fixed reply and never reach the parser, the chat agent, or any DB query past the ban check (decision D11)." §Authorization model step 2: "DM — unknown contact... Invalid / expired / absent: fixed 'access requires an invitation' reply, drop. No registration, no LLM, no DB write beyond the drop counter."
    GAP: `infochat-provider/src/main/java/io/infochat/provider/messaging/InboundRouter.java:111-135` — `onMessage` goes straight from normalization to slash/chat dispatch. The inline comment at line 113 (`T2-A wires the missing intake steps upstream of this point: ban check (D11), invite-code gate (D44), slow-start probation (D45).`) explicitly acknowledges that NONE of the spec'd preceding gates are present. The CommandHandler interface is wide-open: any `@ApplicationScoped CommandHandler` bean that lands in a future ticket is reachable by ANY inbound contact, banned or not, invited or not, in probation or not.
    REPRO: An unknown contact (no `users` row) sends `/somecommand`. The router runs handleSlash, which iterates `commandHandlers`. If even one CommandHandler with `name() == "somecommand"` is in the CDI container, the user reaches `handler.handle(scope, normalized)` with NO ban check, NO invite check, NO probation check. The spec promises the unknown DM never reaches anything beyond the fixed "access requires an invitation" reply. The diff promises only "unknown command" or the matched handler. Until T2-A lands, every CommandHandler — including M1-035c's HelpCommandHandler announced as "next ticket" — is reachable by anyone.
    SUGGESTED-FIX-CLASS: missing-auth-check

  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: §Ingest pipeline (chat-input parity): "**carves out fenced code blocks**: a user typing a deliberately exotic code snippet should see it round-trip unchanged. Fence recognition is the closed CommonMark rule... Bytes inside fences are preserved verbatim. The Provider does *not* run the Stage 1 regex set on chat input — chat-input safety relies on the delimiter convention plus the LLM tool boundary." §Authorization model step 1.7: "Unicode-normalize the body (NFKC + bidi-control strip + zero-width strip + leading/trailing whitespace trim **outside fenced code blocks**; fence recognition per the CommonMark rule documented in §Ingest pipeline)." Step 1.7 also commits: "**The normalized body replaces the raw body for all downstream processing**".
    GAP: `infochat-provider/src/main/java/io/infochat/provider/messaging/InboundRouter.java:200-230` (the `normalize` method). The implementation applies whole-body NFKC + bidi strip + zero-width strip + `trim()` with NO fence detection. The Javadoc at lines 596-606 explicitly says "Whole-body NFKC is sufficient for MVP" — directly contradicting the spec's commitment that bytes inside fences must be preserved verbatim. NFKC mutates compatibility-equivalent characters (fullwidth → halfwidth, ligatures → component letters, combining-form changes), and whole-body `trim()` will strip whitespace inside trailing fenced code.
    REPRO: A user (post-T2-A, when chat mode lands) sends a chat-mode message containing ```` ```\nﬁnal\n``` ```` (the `ﬁ` is U+FB01 LATIN SMALL LIGATURE FI). The normalized form delivered to the LLM is ```` ```\nfinal\n``` ```` — bytes inside the fence have been mutated. The spec says they must round-trip unchanged. A more impactful case: a user pasting a regex or password containing a fullwidth digit / homoglyph inside a fenced block loses the original bytes. Because the normalized body REPLACES the raw body for all downstream processing (per step 1.7), the original bytes are unrecoverable.
    SUGGESTED-FIX-CLASS: input-sanitization

  - CATEGORY: AUTH-BYPASS
    SEVERITY: medium
    PROMISE: §Authorization model step 2 (DM — unknown contact): "Invalid / expired / absent: fixed 'access requires an invitation' reply, drop. No registration, no LLM, no DB write beyond the drop counter." §User ban: "Banned user receives one fixed reply per inbound message, regardless of input."
    GAP: `infochat-provider/src/main/java/io/infochat/provider/messaging/InboundRouter.java:122-126`. A plain-English (non-slash) inbound from an unknown contact today receives `CHAT_MODE_REPLY` ("Chat-mode replies are not in the MVP; try /help..."), not the spec'd "access requires an invitation" fixed reply. The spec's promise is content-specific: the right reply for an unknown DM is the invite-required literal; the diff returns chat-mode-deferral or unknown-command instead. This is observable distinguishing behavior — an attacker can fingerprint which contacts are registered (would they get unknown-command for an invalid `/cmd`?) vs. unregistered (would they get unknown-command for the same input?). In the MVP everyone sees the same thing, but the spec's step 2 commits to a content invariant the diff breaks.
    REPRO: An adversary with no prior contact sends `/help` to the bot. The diff returns "Unknown command. Try /help for the available commands." Spec step 2 promises an unknown-DM contact receives ONLY the fixed "access requires an invitation" reply (and a drop). The current reply discloses that the bot exists, that slash commands are recognized, and that there's a `/help` to try — none of which is consistent with a "drop with fixed invite-required reply" promise.
    SUGGESTED-FIX-CLASS: missing-auth-check

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: §Secrets handling: "Contact IDs are logged in redacted form (prefix + ellipsis + suffix) outside the audit log." §Failure handling implicitly: user-visible errors are deterministic localized literals — the discipline extends to operator-visible logs being free of unredacted PII.
    GAP: `infochat-provider/src/main/java/io/infochat/provider/messaging/InboundRouter.java:127-133` — the exception branch returns INTERNAL_ERROR_REPLY (good — fixed literal, doesn't interpolate exception message), but the exception itself is logged at ERROR via `log.error("InboundRouter dispatch failed for scope={}", msg.scope(), e)` (line 130). The scope value is logged unredacted. For a DM scope, the scope carries the contact id (`ScopeRef.Dm(contactId)`); §Secrets handling says "Contact IDs are logged in redacted form (prefix + ellipsis + suffix) outside the audit log." The router's error log is NOT the audit log.
    REPRO: An attacker triggers any dispatch exception (e.g. by sending input that triggers a CommandHandler bug or a downstream SQL failure). The unredacted contact id is written to the SLF4J error log. Operators reviewing logs see raw contact ids — the §Secrets handling rule was designed to prevent exactly this. Same issue at line 145 (`log.error("InboundRouter reply send failed for adapter={} scope={}", ...)`) and line 138 (`log.error("InboundRouter has no replyTarget; dropping reply for scope={}", ...)`).
    SUGGESTED-FIX-CLASS: input-sanitization

  - CATEGORY: DOS
    SEVERITY: medium
    PROMISE: §Authorization model step 1.7 (the normalization step is the first body-content operation): the spec scopes normalization with the **carve-out** precisely because it must run on every inbound. Implicitly the operation must be bounded.
    GAP: `infochat-provider/src/main/java/io/infochat/provider/messaging/InboundRouter.java:200-218`. `normalize()` calls `Normalizer.normalize(raw, NFKC)` and then iterates code points. There is no length cap before the call. `CapabilityFlags.maxInboundMessageBytes` defaults to 100 KB on the in-memory adapter but the router neither enforces nor checks any per-message size cap. NFKC normalization on a maliciously crafted input (e.g. many Hangul characters that expand significantly) can amplify the cost. The cap-by-adapter contract is honor-system; the InboundRouter is the centralized seam where a body-size sanity check would belong.
    REPRO: An attacker sends a 1 MB payload via an adapter that fails to enforce `maxInboundMessageBytes` (a misbehaving 3rd-party adapter, or the InMemoryAdapter test path which has no enforcement). NFKC + the code-point iteration runs unbounded. The router has no defense in depth.
    SUGGESTED-FIX-CLASS: rate-limit

OUT-OF-MODEL:
  - The InMemoryAdapter's `LOW` trust level requiring `allow-low-trust=true` is bypassed by `%test.infochat.adapters.inmemory.allow-low-trust=true` set in `application.properties`. An operator who ships the in-memory adapter in production with the test-profile property accidentally enabled would have low-trust adapter active. The spec puts adapter trust level under operator configuration; this is operator-config risk, not a code defect.
  - The `Producer` for InMemoryAdapter unconditionally exposes it as a CDI bean in production. Production-exclusion gate 5 prevents `inmemory` from running alongside another adapter, but a deployment configured as `infochat.adapters=inmemory` in production with `allow-low-trust=true` would pass all gates and ship the test adapter to real users. The threat model doesn't explicitly forbid `inmemory` in production beyond gate 5; this is again operator-config territory.
