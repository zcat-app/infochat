---
target: M1-778
date: 2026-08-07
base: 53efdb26b1d31bfcf8efd8e0f9026febd82e1839
head: "working tree, branch m1/M1-778-generated-prose-ignores-scope-language (re-audit after in-branch remediation)"
verdict: CLEAN
out_of_model_count: 1
disposition: |
  Round-2 re-audit, run on the user's word after the round-1 FINDINGS were
  remediated in-branch (boundForEchoScan on the prose leg;
  CategoryRollupGenerator via DisplayHeadline.anchorFirst). The rendered
  prompt carried the mandatory re-audit framing: both prior findings were
  listed for verification, CLEAN was explicitly authorized, and the
  remediation code itself was named as new unaudited surface.

  The one out-of-model item is advisory; disposition pending the user's
  decision (redteam SKILL step 8).

  Gate mechanics: native in-session threat-actor subagent (kimi §8 6f),
  all paths absolute and worktree-rooted (§6.1(d) mitigation); threat
  model read from the main checkout. Porcelain before/after differed only
  by the intent-to-add of the round-1 report (orchestrator's own git add
  -N) — no gate contamination.
---
RED-TEAM VERDICT: CLEAN

FINDINGS: (none)

Re-audit verification of the two round-1 findings (both genuinely closed,
verified against the working-tree code, not the resolution claims):

1. medium/DOS — unbounded DisplayHeadline.displaysAsTheOriginal on
   TranslationPipeline's prose leg. CLOSED. The diff adds
   TranslationPipeline.boundForEchoScan (diff lines 835-839), which cuts
   both operands at DisplayHeadline.BODY_SCAN_LIMIT before the walk, and
   the call site (diff lines 795-797) routes BOTH operands through it.
   With original and translated each capped at 2000 chars, the
   String.indexOf word walk is bounded at ~n * (total needle chars)
   <= 4e6 comparisons — the same bound the display-hit leg already pays
   (DisplayHeadline.java:736, :344). Truncating both prefixes can only
   make a word fail to match, never invent one, so the bound's failure
   direction is a MISSED late-diverging echo — the residual direction
   displaysAsTheOriginal's own javadoc (DisplayHeadline.java:483-486)
   already states and accepts. No attacker capability is added: a hostile
   endpoint could always evade the predicate by rewording; the bound does
   not widen that. Pinned by
   TranslationPipelineTest.nearEchoIsCaughtOnAnInputLongerThanTheEchoScanBound,
   which drives an 8 400-char padded echo through the bound and still
   expects the fallback note (and asserts sanitizer-2 is not invoked).

2. low/DOS — uncapped post.title_en into DisplayHeadline.of's unbounded
   title slot. CLOSED. CategoryRollupGenerator.buildPrompt now calls
   DisplayHeadline.anchorFirst(p.title(), null, p.titleEn(), null,
   llmOutputSanitizer).readerLine() (diff lines 641-648). Verified in
   DisplayHeadline.java: anchorFirst (line 258) chooses the field via
   titleIsRenderable(title) on the ORIGINAL, then derive (lines 302-341)
   applies boundForScan to BOTH the anchor and the original (lines
   311-314) before the single pair sanitize call, and truncates both
   output lines at MAX_LENGTH. The uncapped TEXT column therefore never
   reaches the sanitizer unbounded — the exact property the finding
   demanded. The null body AND null body anchor keep the body fallback
   off (anchorFirst lines 264-266), so the M1-728 "titles only" prompt
   bound is unchanged; and choosing the field from the original also
   closes round-1's out-of-model item 1 (the translated-UNTITLED_TITLE
   sentinel resurrecting a headline and defeating the M1-743
   empty-headline skip), pinned by
   CategoryRollupGeneratorTest.titlelessPostWithATranslatedSentinelAnchorContributesNoLine
   and .titleAnchorIsBoundedBeforeTheSanitizerSeesIt (sanitizer input
   <= 2*BODY_SCAN_LIMIT+1).

Remediation code audited as new, previously unaudited surface:

- boundForEchoScan uses substring(0, LIMIT) without the codePointSafeCut
  boundForScan uses. Acknowledged in its javadoc and sound: a split
  surrogate is compared, never rendered or stored; worst case is one word
  losing its match (missed-echo direction, safe).
- The new condition-(d) Latin arm (diff lines 795-801) is gated on
  sourceLanguage.equalsIgnoreCase("en"); every production caller uses the
  two-argument overload that hardcodes "en", so the gate is not
  attacker-reachable. On match it returns
  fallbackWithNote(postSanitizer1Text, scopeLanguage) — the caller's
  already-sanitizer-1 text plus a bundle string; the translator reply is
  discarded whole, the identical posture conditions (b)/(c)/(d) already
  have, so there is no LLM_OUTPUT_SANITIZED audit loss and no new
  delivery path for endpoint-chosen text. The WARN log carries only
  target_language — no user content (D37 clean). Evaluation order (after
  (c), before the non-Latin script arm) judges the same pre-sanitize
  operand as the other conditions.
- ChatAgent.REPLY_LANGUAGE_DIRECTIVE and the "Always write in English"
  pins in SUMMARIZER_SYSTEM_PROMPT / ROLLUP_SYSTEM_PROMPT are static
  constants appended to system prompts; they carry no user- or
  feed-derived text, cannot land inside an UNTRUSTED_CONTENT region, and
  the tool-instruction concatenation order (baseSystemPrompt +
  TOOL_INSTRUCTIONS) is unchanged. The directive rides the base prompt,
  so the iteration-cap final call inherits it (pinned by
  ChatAgentReplyLanguageTest.replyLanguageDirectiveRidesTheIterationCapFinalCall).
- The roll-up's sanitize unit widens from one field to that field's
  ingest PAIR — the unit security.md §Prompt-injection defenses
  explicitly accepts ("The anchor-first surfaces widen the unit by
  exactly one step, to the field PAIR ... joined by a renderer-authored
  newline no feed byte can forge"); the join never spans posts or
  authors, so no flag-span deletion can consume a third party's bytes.
- Test-only additions (ChatAgentReplyLanguageTest, SummaryProseLanguageTest,
  CategoryRollupGeneratorTest, TranslationPipelineTest) carry no
  production surface.
- docs/spec/llm.md's condition-(b)/(d) amendment narrows when the
  fallback note may fire (declared-English inputs only) and documents the
  Latin-arm echo predicate; security.md (the threat model under audit)
  is not modified by this diff.

OUT-OF-MODEL:
  - Carried over from round 1 (item 2), unchanged by the remediation and
    still standing: title_en / body_en reaching the summarizer and
    roll-up prompts are LLM-authored text the Stage 1 prompt-injection
    regex set never scans — an attacker owning a non-English feed can
    phrase an injection in the source language (where an English-pattern
    Stage 1 catalogue does not match) and have the ingest translator
    render it into canonical English that no Stage 1 pass sees. Not a
    FINDING because the spec's promised defense for this surface is the
    per-call random wrapper plus the system prompt (both intact, and the
    ingest translation leg runs LlmOutputSanitizerCore over the anchors
    before storage), and security.md §Ingest pipeline explicitly
    declines to promise Stage 1 as a complete defense ("Stage 1 is a
    coarse filter, not a complete defense ... Stage 2 is the actual
    security boundary"). Recorded so the next auditor does not
    re-derive it; the user may decide whether to extend the model.
