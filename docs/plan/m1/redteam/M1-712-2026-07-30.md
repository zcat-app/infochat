---
target: M1-712
date: 2026-07-30
base: e0710a444ef4e7f89a64a8ffb6d2aaa4dfb7cc26
head: working-tree (uncommitted branch form, /m1-tick run redteam gate)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  Gate audit fired ahead of review per run.md step 4. The diff is
  test-only: it adds a refuse-leg assertion pinning the existing
  command-injection guard at SimpleXMessageCodec:175 and touches no
  main-source file. No findings, nothing deferred.
---

RED-TEAM VERDICT: CLEAN

Scope audited
-------------
Base e0710a444ef4e7f89a64a8ffb6d2aaa4dfb7cc26 .. working-tree, via
target/redteam-diff-M1-712.diff. Three files touched:

  - docs/plan/m1/STATUS.md                                  (status bookkeeping)
  - docs/plan/m1/tickets/M1-712-...md                        (front-matter only)
  - infochat-messaging-adapter/src/test/java/app/zcat/infochat/messaging/
    impl/simplex/SimpleXMessageCodecTest.java               (+23 lines, one new
                                                             @Test, additive)

No production source, migration, config, or spec file is modified. The diff
adds one refuse-leg test method
(encodeJoinGroupCommandRejectsAdapterGroupIdWithCommandInjectionChars) and
deletes/retargets nothing: no existing assertion is weakened, moved, renamed,
or repointed at a different code path, so no incidental control travels away
with the change (engineering-rules §10 shape checked explicitly against the
diff hunk — it is pure insertion at line 1205 between two untouched methods).

Adversarial checks performed (all negative)
-------------------------------------------
1. INJECTION — the control the test pins. SimpleXMessageCodec:176 builds
   "/_join #" + adapterGroupId by raw concatenation into a simplex-chat
   command string, guarded by requireValidQueueAddressId(adapterGroupId,
   "adapterGroupId") at :175. The guard is an ALLOWLIST
   (QUEUE_ADDRESS_CHARSET = ^[A-Za-z0-9_=.-]+$, :91) applied via
   Matcher.matches(), plus a non-empty check (:223). Verified there is no
   trailing-terminator escape: Matcher.matches() runs with ENDANCHOR, so a
   value such as "2\n/_send @victim json {}" cannot satisfy the pattern even
   though '$' alone would assert before a final line terminator. The command
   separators that matter to the simplex-chat grammar — SPACE, '@', '#',
   CR, LF — are all outside the admitted class. The three inputs the new test
   feeds (newline+forged verb, space+forged verb, empty string) each exercise
   a distinct rejection reason and each fails if the :175 call is deleted, so
   the previously-SURVIVED mutant is genuinely killed rather than covered by
   an assertion that would pass vacuously.

2. Reachability of the guarded value. joinGroup(String) is a public SPI
   method (SimpleXAdapter:965) whose argument can arrive from Provider state,
   so the encode-time guard is a real boundary, not defensive code between two
   internal callers. The decode side independently rejects a hostile
   receivedGroupInvitation frame with the same predicate
   (SimpleXMessageCodec:421, isValidQueueAddressId) before the id is ever
   surfaced as ReceivedGroupInvitation, and the inviter leg fails closed on a
   non-"contact" invitedBy.type (:432). Defense at both ends is intact and
   untouched by this diff.

3. INFO-LEAK on the refuse path. requireValidQueueAddressId (:211-219)
   deliberately does not interpolate the raw id into the MessagingException
   message — only fieldName and id.length() — consistent with
   security.md §Secrets handling ("Contact IDs are logged in redacted form")
   and §User content in exceptions. The new test asserts only the
   FailureCategory, so it does not pin, and therefore cannot later be read as
   licensing, an id-bearing message.

4. DOS. QUEUE_ADDRESS_CHARSET is a single bounded character class with one
   quantifier and no nesting or alternation — linear, no backtracking blowup.
   The new test adds no loop, no unbounded input, and no timing dependence.

5. AUTH-BYPASS / PERM-ESCAL / AUDIT-EVASION. The mechanically-extracted
   "Authentication" and "Ban handling" inventory hits are all incidental
   substring matches in STATUS.md prose and in pre-existing test fixture bytes
   (the verbatim v6.5.4.1 receivedGroupInvitation frame at test line 1183),
   not code the diff introduces or alters. The inventory reports "(none
   touched)" for authorization/admin-tier gates and for audit-log writes, and
   reading the diff confirms it: no admin-tier gate, no per-(user, scope)
   isolation predicate, and no audit emission is added, removed, or reordered.
   The /_join path draws no rate token by documented design
   (SimpleXAdapter:968-971), which is pre-existing behaviour this diff neither
   establishes nor changes.

No commitment in docs/spec/security.md is left undelivered by this diff. The
change strictly increases the evidence pinning an existing spec-aligned
control.
