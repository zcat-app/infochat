---
target: M1-737
date: 2026-07-31
base: 2e6b7a6c325132fdf5663ab0ab2e4dce667ed974
head: working tree (pre-commit branch tip, /m1-tick run gate)
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 2
out_of_model_count: 0
disposition: |
  Both findings are spec-text issues, not code defects. Finding 1
  (residual per-occurrence commitments at security.md:421 and :547) was
  already surfaced as an incidental finding during implementation; the
  user chose a post-merge `spec:` commit to amend exactly those two
  sentences. Finding 2 (/saved body-scan claim undelivered) is a
  worktree-staleness artifact: M1-730 landed on main at e9bd3fc0 after
  this branch's fork point (2e6b7a6c325132fdf5663ab0ab2e4dce667ed974), and on main
  SavedCommandHandler.java:328 sanitizes via
  DisplayHeadline.of(row.title, row.body, ...) — the claimed unit
  exists post-merge. Auditor: kimi threat-actor via run-gate.sh,
  contamination=none.
---
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: AUDIT-EVASION
    SEVERITY: low
    PROMISE: docs/spec/security.md:421 — the /save -t render-side
      redaction "emits the per-occurrence `LLM_OUTPUT_SANITIZED` audit
      row on every hit"; and docs/spec/security.md:547 — "within one
      field, per-occurrence `LLM_OUTPUT_SANITIZED` rows fire at least
      as often as before the narrowing." Both lines survived the diff
      unamended and still commit to one audit row PER OCCURRENCE.
    GAP: The diff amends only security.md:454 to the aggregated shape
      ("rows aggregate per distinct token per sanitize call") and
      delivers exactly that in
      infochat-provider/.../llm/LlmOutputSanitizer.java:665-696
      (emitAuditRows iterates aggregateMatchCounts — one row per
      distinct token per call) and :595-601 (one WARN per distinct
      token). The two residual per-occurrence commitments at :421 and
      :547 were not amended, so the threat model now contradicts
      itself: the delivered code violates the still-standing
      row-per-hit promise on two paragraphs of the same section.
    REPRO: A feed publisher titles a post "urgent: /ban /ban /ban".
      A subscriber's /saved (SavedCommandHandler.java:275) or /summary
      sanitize call matches /ban three times in one field. The system
      writes ONE LLM_OUTPUT_SANITIZED row (match_count=3) where the
      standing spec text at :421 promises a row "on every hit" (3
      rows). Any operator alerting or forensics over /audit (which
      reads audit_log_view) that counts rows per the unamended
      commitment silently undercounts hits 3:1. The count field keeps
      the signal lossless, but the contract an auditor reads is
      violated by the delivery.
    SUGGESTED-FIX-CLASS: other
  - CATEGORY: AUDIT-EVASION
    SEVERITY: low
    PROMISE: docs/spec/security.md:526-527 (amended by this diff) —
      "every call site is built that way: ... the `/saved` reply one
      row's title OR up to `BODY_SCAN_LIMIT` chars of its body, plus
      one row's tags" — i.e., the threat model now asserts the /saved
      sanitize unit includes a bounded body scan.
    GAP: The diff delivers no change to any /saved render path, and
      the working tree's SavedCommandHandler.java:264-278 sanitizes
      only the row's title and its joined tags; no body text is
      rendered or scanned (BODY_SCAN_LIMIT exists only in
      render/DisplayHeadline.java:77, used by the summary/digest
      headline path, not by /saved). The claimed sanitize unit for
      /saved does not exist in the delivered code — the spec text
      describes the post-M1-730 state while M1-730 is still pending
      (docs/plan/m1/STATUS.md:37 lists M1-730 as pending even though
      M1-737 declares blocked_by [M1-730]).
    REPRO: Not exploitable today — the /saved reply never renders the
      post body, so no unsanitized body text reaches a user. The gap
      is contractual: the threat model asserts an audited sanitize
      coverage (title-OR-body) that the code does not provide, so any
      future change that starts rendering saved-post bodies in
      reliance on this claim ships with zero closed-list coverage and
      zero LLM_OUTPUT_SANITIZED rows for body content, and no auditor
      reading the spec would expect that.
    SUGGESTED-FIX-CLASS: other
