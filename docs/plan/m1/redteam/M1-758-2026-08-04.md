---
target: M1-758
date: 2026-08-04
base: 29aa59afbd80e13df6265e6934c77f1a4a06ffe6
head: "<working tree>"
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 2
out_of_model_count: 2
disposition: |
  Round 1, run at the /m1-tick run redteam gate (ahead of review), against the
  uncommitted working tree at fork point 29aa59afbd80e13df6265e6934c77f1a4a06ffe6. All three findings
  are INFO-LEAK against the operator-facing disclosure this ticket rewrites.
  Two are DEFECTS THE DIFF INTRODUCED, not pre-existing gaps: (1) the added
  text asserts a hard negative — an "/lang en scope is a strict no-op and
  sends nothing" — which is false for the task as a whole, because the
  Collector's IngestTranslationWorker is gated on source.language, never on
  any scope's /lang, and ships whole post titles+bodies on a @Scheduled tick;
  (2) the diff deletes the pre-existing "translation of the bot's replies to
  you" line while asserting the enumeration is complete at FOUR legs, dropping
  the still-live ChatAgent reply-prose leg, and the added test pins that
  removal. The third (SETUP_GUIDE.md still describes chat as the sole
  private-message task) is pre-existing and sits OUTSIDE files_scope, so it
  cannot be fixed without a frontmatter refine. Escalated to the user.
---
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: medium
    PROMISE: docs/spec/security.md §Secrets handling — "The post-setup
              `prod/switch-llm.sh` backend switcher records the LLM API key in
              `secrets.env` through the same dotenv-escaped channel and prints a
              per-task privacy disclosure naming exactly which generative tasks
              now call a remote provider and what each exposes". The same
              section, at line 1941, names the missing consumer explicitly:
              "the Collector's ingest translation worker is a separate
              continuously-scheduled consumer of the same task."
    GAP: The new disclosure block gates the ENTIRE translator exposure on a
          scope's `/lang` and asserts a hard negative:
          prod/switch-llm.sh:408-409 — "Only for a chat or group whose /lang is
          not English; an /lang en scope is a strict no-op and sends nothing.
          Where it is set, FOUR separate legs reach the remote provider".
          That claim is false. The Collector's ingest translation leg is gated
          on `source.language`, a per-SOURCE column (V74), never on any scope's
          `/lang`:
            - infochat-collector/.../eval/translation/IngestTranslationWorker.java:604-612
              — pickup SQL `SELECT p.id, p.fetched_at, p.title, p.body,
              s.language FROM post p JOIN source s ... WHERE p.status='RAW' AND
              p.tagger_done AND NOT p.translation_done`; no scope, no `/lang`.
            - IngestTranslationWorker.java:264 — the only skip arm is
              `if ("en".equals(row.language()))`, i.e. the SOURCE's declared
              language.
            - IngestTranslationWorker.java:274 and :318 —
              `llmRouter.forTask(ModelTask.TRANSLATOR, row.language())` /
              `provider.generate(ModelTask.TRANSLATOR, "", userPrompt)`.
            - IngestTranslationWorker.java:358-366 (`renderPrompt`) — the prompt
              interpolates `{{title}}` AND the full `{{body}}`, untruncated. The
              four enumerated legs all send headlines or one query; this leg
              sends whole post bodies, on a `@Scheduled` tick
              (IngestTranslationWorker.java:232-234), unattended and forever.
          The diff additionally DELETES the only line that previously placed
          translator in the public-post/topic-interest tier — the removed
          `translator) echo "  -  translator — ..."` case at old
          prod/switch-llm.sh:403 — so after this diff the loop at
          prod/switch-llm.sh:422-430 deliberately prints NOTHING for translator
          (comment at :400-401), and the ingest leg is named on no line at all.
          The added test pins the wording that carries the false claim
          (SwitchLlmWiringTest.java, new test
          `privacyDisclosurePutsTranslatorInTheLoudTierAndNamesEveryLeg`,
          asserting `"strict no-op"` is present), so the defect is now
          regression-guarded in place.
    REPRO: Operator runs a deployment where every scope is on the default
            `/lang en` and at least one configured source is non-English
            (`source.language <> 'en'` — exactly the corpus D29/V74 anchor
            translation exists for; the repo ships cs/es/ru/tr language work,
            so non-English sources are the expected shape, not a corner case).
            The operator runs `./prod/switch-llm.sh`, selects `remote`, and
            reads the printed PRIVACY DISCLOSURE. For `translator` it says an
            `/lang en` scope "is a strict no-op and sends nothing", and lists
            FOUR legs, every one of them `/lang`-conditional. The operator
            reasonably concludes translator costs them nothing and accepts the
            switch (or, under the per-task routing the spec supports —
            §Failure handling, "when an operator configures per-task
            providers" — deliberately leaves translator remote as the
            "sends-nothing" task). From the next Collector tick onward,
            `IngestTranslationWorker` ships the complete title and body of
            every non-English post to the remote endpoint, continuously, with
            no user present and no disclosed line describing it. The script's
            own comment sets the standard this violates: "A wrong claim here is
            a security defect" (prod/switch-llm.sh:391).
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — the disclosure names
              "what each exposes"; and §Rate limiting, `/saved` bullet — "The
              leg shares `ModelTask.TRANSLATOR` with the ingest,
              query-anchoring and /summary presentation legs", i.e. the spec
              treats the presentation-prose legs as part of this task's
              surface.
    GAP: prod/switch-llm.sh:409-410 asserts completeness ("FOUR separate legs
          reach the remote provider") and the diff simultaneously removes the
          only disclosure of a fifth: the deleted line at old
          prod/switch-llm.sh:403 read "translator — translation of the bot's
          replies to you; exposes the bot-reply text (which can echo your
          queries)". That leg is still live and is not among the four:
            - infochat-provider/.../chat/ChatAgent.java:558 —
              `reply = translationPipeline.run(sanitized, scopeLanguage);`
              translates the chat-mode REPLY prose, text generated from and
              echoing the user's private conversation.
            - infochat-provider/.../digest/DigestRenderer.java:825 and
              .../digest/CategoryRollupGenerator.java:213 — the same
              `translationPipeline.run(...)` for cluster prose and section
              roll-ups. The disclosure attributes these to the DIGEST only
              (prod/switch-llm.sh:418-421), but `/summary` renders through the
              same `DigestRenderer.forSummaryRendering` path
              (DigestRenderer.java:684-714), so a user-initiated `/summary`
              also sends LLM prose derived from that user's request.
          The new test actively pins the removal
          (`assertFalse(r.output.contains("translation of the bot's replies to
          you"))`), so the dropped exposure cannot be restored without failing
          the suite as written.
    REPRO: Operator wants to keep private conversation local and the cheap
            tasks remote. They read the loud tier: `chat` = private messages;
            `translator` = four named legs (a search query, saved headlines,
            /summary headlines, digest headlines). Nothing states that the
            bot's own chat REPLY prose — which quotes and paraphrases the
            user's private message — is translated through this task for any
            non-English scope. They route `chat` local and `translator` remote.
            Every reply the bot composes in a `cs`/`ru`/`es`/`tr` scope is then
            sent verbatim to the remote provider, disclosing the private
            conversation the operator explicitly kept local. The pre-diff text
            warned about exactly this ("which can echo your queries"); the diff
            removed the warning without replacing it.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: docs/spec/security.md §Secrets handling — "prints a per-task
              privacy disclosure naming exactly which generative tasks now call
              a remote provider and what each exposes — `chat` (private user
              messages) flagged loudest ... — see `SETUP_GUIDE.md` §'Switching
              your AI backend later'", as amended by the M1-746 bullet:
              "**`translator` carries private user messages too (M1-746, D58).**
              ... routing `translator` to a remote provider therefore exposes
              private user messages, exactly as `chat` does".
    GAP: The spec bullet designates SETUP_GUIDE.md §"Switching your AI backend
          later" as the operator-facing description of this disclosure, and the
          diff leaves it describing the pre-M1-746 world. SETUP_GUIDE.md:563-567
          still reads: "a **privacy disclosure** naming exactly which tasks now
          call the remote provider and what each one exposes — loudest for
          **chat** (it sends your private messages), versus the ingest tasks
          (`security`/`tagger`/`entity`/`classifier`), which only ever see the
          **public** posts infochat fetches". `translator` appears in neither
          tier, and `chat` is still presented as the sole private-message task —
          directly contradicting both the spec bullet the diff relies on and the
          script text the diff writes. No SETUP_GUIDE.md hunk exists in the diff.
    REPRO: An operator evaluating a move to a cloud API reads SETUP_GUIDE.md
            §"Switching your AI backend later" BEFORE running the switcher —
            that is what the section is for. It tells them exactly one task
            carries private data (`chat`) and four carry public-post data. They
            decide the exposure is acceptable and proceed. The consent decision
            is therefore made against a document the spec itself amended two
            tickets ago; the corrected statement only appears in the script's
            Phase 4 output, which prints AFTER Phase 3 has already rewritten
            `prod/runtime/application.properties`
            (prod/switch-llm.sh:370-384 writes, :402-431 discloses).
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - Disclosure-after-write ordering. `prod/switch-llm.sh` performs its Phase 3
    property writes (`set_prop`, :370-372) before printing the Phase 4 privacy
    disclosure (:402). The threat model commits to the disclosure's CONTENT, not
    to it preceding the write, and the script prints a rollback command and does
    not recreate containers itself, so no data actually leaves the deployment
    until the operator runs the Phase 5 command. Flagged only so the user can
    decide whether "consent before mutation" belongs in the model.
  - `LLM_TASKS` drives the loop but `translator` now has no `case` arm and the
    `case` has no default arm (prod/switch-llm.sh:422-430). A future task added
    to `LLM_TASKS` without an arm silently prints no exposure line. This is a
    pre-existing shape the diff does not introduce (no arm existed for an
    unlisted task before either), so it is not counted as a finding — but the
    diff makes a silently-empty arm the intended state for one task, which
    removes the "every task prints a line" reading that made the omission
    visible.
