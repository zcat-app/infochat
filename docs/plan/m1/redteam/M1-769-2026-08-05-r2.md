---
target: M1-769
date: 2026-08-05
base: 3ffd1293a3ac97d199f764db41fb214d7fb0df3a
head: working-tree
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 0
out_of_model_count: 1
disposition: |
  Round 2, run as an explicit RE-AUDIT of the round-1 remediation: the
  prompt listed all four round-1 findings, instructed the adversary not
  to assume they were closed and not to re-report them if they were, and
  explicitly authorised a CLEAN verdict.

  All four round-1 findings are gone from the verdict. The two taken into
  scope (probe burn; unreachable reserve) were not re-reported, and
  neither were the two dispositioned as residuals — including the
  interrupt-exemption one, which round 1 raised on the ground that the
  property was unowned and which the re-audit prompt answered with the
  M1-764 characterisation test.

  The single new medium is a REAL and previously unnoticed defect, but it
  is PRE-EXISTING and not this diff's: `LlmHttpSupport.sendForBody` maps
  an InterruptedException onto a plain `LlmCallFailedException`, which
  `CircuitBreakingLlmProvider`'s second catch records as REACHABLE,
  un-tripping a breaker on an endpoint nobody contacted. Both the
  classification and the recording catch are verbatim at the fork point
  (verified: `git show 3ffd1293:...CircuitBreakingLlmProvider.java`, and
  `LlmHttpSupport.java` is absent from this diff's file list). M1-769
  neither creates nor widens it — a refused call throws before reaching
  the transport, so if anything the exposure narrows. The classification
  is M1-764's transport-interrupt contract, which M1-769 `out_of_scope`
  item 5 fences.

  Disposition: FIXED IN-BRANCH. An initial deferral to a new ticket
  (M1-770) did not survive scrutiny and that ticket was deleted:
  out_of_scope item 5's stated concern is that "the temporal bound stays
  exactly as it is", and reading the interrupt FLAG in the breaker
  decorator touches neither the temporal bound, nor the exception type,
  nor the M1-764 assertion pinning it; the claimed ChatAgent blast radius
  was false (its `catch (Exception e)` arm already yields the identical
  outcome, as its own comment says); and the fix lands in the
  releaseProbeForTask machinery this same diff built. Both recording arms
  of both breaker decorators now consult the flag and release the probe
  rather than record. The embedding twin came in with it — /stop cancels
  a chat turn that embeds, and the embedding providers share sendForBody.
  Cost measured: 2 files new to the diff, 15 -> 17 against budget 18.

  The out-of-model item (shared endpoint-string breaker keying letting
  `releaseProbeForTask` return a probe the embedding path holds) is
  acknowledged: the keying pre-dates this diff, the new release only
  widens who can mutate a shared entry, and the worst case is one extra
  probe admitted. UNRESOLVED — M1-770 (which would have carried it) was
  deleted when the medium came in-branch, so this item still needs a
  disposition from the user.
---
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: DOS
    SEVERITY: medium
    PROMISE: docs/spec/security.md §Failure handling, "Fail-fast on a
              known-unreachable provider (circuit breaker)": "After a
              configured count of CONSECUTIVE transport-unreachable failures
              (connection refused, DNS failure, no route, connect/read
              timeout) the endpoint's breaker trips OPEN and subsequent calls
              short-circuit with the typed provider-unreachable signal
              **without an HTTP attempt**; after a configured cooldown a
              single probe is admitted — success closes the breaker, failure
              re-opens it. **Only transport failures trip it: an application
              error (non-2xx status, over-cap body, unparseable reply) proves
              the endpoint answers and counts as reachable**". Reinforced by
              §Failure handling, chat-mode replies: "Once the chat endpoint's
              circuit breaker (below) is OPEN, the pre-fetch is **skipped
              entirely** — no translation, no embed round-trip, no pgvector
              probe — so an outage window pays that bounded cost at most once
              per breaker cycle."
    GAP: The round-1 remediation states a GENERAL invariant — "a call that
          reports nothing about the endpoint returns the probe it acquired and
          leaves the breaker as it found it" — and implements it only for
          exceptions OUTSIDE the `LlmCallFailedException` family. The
          no-evidence call class that this very ticket reasons about
          elsewhere, the M1-763 interrupted caller, lands INSIDE that family
          and is therefore not merely unreleased: it is recorded as positive
          reachability evidence and CLOSES an OPEN breaker with zero
          observation. The fix closed the "falsely deny" direction of the same
          seam and left the "falsely admit" direction open.
          - infochat-llm-adapter/src/main/java/app/zcat/infochat/llm/metrics/CircuitBreakingLlmProvider.java:36-43
            (javadoc rewritten by this diff) now asserts the invariant: "Any
            other exception records nothing — it carries no evidence either
            way — but it does RELEASE the HALF-OPEN probe the call may have
            acquired, so an unobserved endpoint is not left denied for a
            further cooldown by a call that never looked at it."
          - infochat-llm-adapter/src/main/java/app/zcat/infochat/llm/metrics/CircuitBreakingLlmProvider.java:78-92
            is the new release catch; it is reached only for exceptions that
            escaped the two preceding catches.
          - infochat-llm-adapter/src/main/java/app/zcat/infochat/llm/metrics/BudgetedLlmProvider.java:97-99
            deliberately SKIPS the draw for an already-interrupted caller and
            still delegates, so the interrupted render's remaining calls do
            reach the breaker decorator's try block.
          - infochat-llm-adapter/src/main/java/app/zcat/infochat/llm/impl/LlmHttpSupport.java:190-195
            turns that call's `InterruptedException` into
            `failure.create(..., false)` — i.e. a PLAIN
            `LlmCallFailedException`, explicitly "Not unreachable: an
            interrupt is caller-side cancellation, no evidence about the
            endpoint".
          - infochat-llm-adapter/src/main/java/app/zcat/infochat/llm/metrics/CircuitBreakingLlmProvider.java:75-77
            catches that plain type and calls `recordReachableForTask(task)`.
          - infochat-llm-adapter/src/main/java/app/zcat/infochat/llm/routing/LlmCircuitBreakerRegistry.java:334-345
            `recordReachable()` is an UNCONDITIONAL close: `state =
            State.CLOSED; consecutiveUnreachableFailures = 0;` regardless of
            prior state.
          So a call that sent no bytes (LlmHttpSupport.java:186 never runs on
          an armed interrupt, per
          `HttpProviderSharedPipelineTest.interruptedCallerSendsNoRequestAndKeepsTheInterruptArmed`)
          both consumes the HALF-OPEN probe and reports "endpoint reachable".
          The comment at LlmHttpSupport.java:144-149 justifies the
          classification on the ground that the safe mis-classification
          direction is "not unreachable ... a missed trip merely keeps today's
          fail-slow behaviour" — that reasoning holds for the trip counter but
          not for the decorator, which converts "not unreachable" into an
          active un-trip. The new
          `BudgetedLlmProviderTest.refusalReturnsTheBreakersRecoveryProbeInsteadOfBurningIt`
          leg exercises only the budget-refusal exception type; no leg drives
          an interrupted caller against an OPEN breaker
          (BudgetedLlmProviderTest.java:interruptedCallerDelegatesAndDrawsNothing
          runs on the setUp chain whose breaker is CLOSED and whose stub never
          throws).
    REPRO: 1) The configured LLM endpoint hangs rather than refusing (the
            common degraded-backend shape). Three consecutive read timeouts
            surface as `HttpTimeoutException` → `ProviderUnreachableException`
            → the endpoint breaker trips OPEN
            (`infochat.llm.breaker.failure-threshold=3`, `cooldown-ms=30000`).
            Chat, `/summary`, `/saved` and query-anchoring translation now
            fail fast and `ChatAgent` skips the D28 pre-fetch, exactly as the
            spec promises.
          2) The same hung endpoint made a scheduled digest render overrun its
            slot window, so `DigestWorker.executeSlot` cancels it:
            infochat-provider/src/main/java/app/zcat/infochat/provider/digest/DigestWorker.java:287
            `renderFuture.cancel(true)` interrupts the render thread, and the
            render LOOP continues to completion by design
            (DigestWorker.java:279-286).
          3) The cooldown elapses. One of that orphaned render's remaining
            `provider.generate` calls is the next caller: it acquires the
            single HALF-OPEN probe
            (LlmCircuitBreakerRegistry.java:298-312), skips the budget draw
            because the interrupt is armed (BudgetedLlmProvider.java:97-99),
            reaches `http.send` with the flag armed, sends NOTHING, and
            surfaces a plain `LlmCallFailedException` ("HTTP call interrupted
            for ...").
          4) `CircuitBreakingLlmProvider:75-77` records that as REACHABLE.
            The breaker goes CLOSED and the consecutive-failure counter is
            reset to zero — on an endpoint nobody has contacted since it
            tripped.
          5) Every user-facing LLM surface immediately resumes real HTTP
            attempts against the hung endpoint and pays the full
            connect/read timeout again; `wouldShortCircuit` is false, so each
            chat turn also re-runs the D28 pre-fetch (one local embed + one
            pgvector probe) that the spec bounds to "at most once per breaker
            cycle". While the orphaned render's tail keeps issuing interrupted
            calls, each one re-zeroes the consecutive counter, so the breaker
            cannot re-trip until the render loop ends.
          The system should not have allowed it: the diff's own stated rule is
          that a call yielding no evidence about the endpoint must leave the
          breaker as it found it, and the interrupted digest call — the exact
          call class this diff exempts from the budget draw at
          BudgetedLlmProvider.java:97-99 on the ground that it "sends no
          request" — is simultaneously treated as proof that the endpoint
          answers.
    SUGGESTED-FIX-CLASS: other

OUT-OF-MODEL:
  - `LlmCircuitBreakerRegistry` keys breaker state by the resolved endpoint
    STRING (`breakersByEndpoint`, LlmCircuitBreakerRegistry.java:104-105,
    244-247). A deployment whose `infochat.embeddings.base-url` equals the
    shared `infochat.llm.default.base-url` (the ordinary single-Ollama shape)
    therefore has the generative and embedding paths sharing ONE
    `EndpointBreaker`, so the new `releaseProbeForTask` can return a probe the
    embedding path is holding. §Failure handling says "the embedding endpoint
    is tracked separately"; the keying predates this diff and the new release
    only widens who can mutate that shared entry, so it is flagged for the
    user rather than counted as this diff's gap.
