---
target: M1-726
date: 2026-07-31
base: 96217c955ca1962b7ed066b75b090a190dbc9f41
head: working tree (uncommitted branch m1/M1-726-tagger-empty-is-not-failure)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  Round 2 at the /m1-tick run redteam gate, ahead of review — a re-audit of
  the reworked diff after the round-1 escalation resolved as refine. The
  re-audit framing (prior findings listed, no assumed closure, CLEAN
  explicitly authorized) was appended to the rendered prompt per the skill's
  re-audit rules. Round-1 medium finding verified closed: a tags array
  holding elements but no strings is now schema-violating (parseTags), and
  the pre-existing !isArray guard covers scalar/object/null shapes. Round-1
  low finding unchanged and not worsened; remediation stays deferred to
  M1-735 (filed, blocked_by M1-726). Both round-1 out-of-model items carry
  over unchanged.
---
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - A feed author who prompt-injects the tagger into answering {"tags":[]}
    for their OWN post can make that single post evade topic-keyed retrieval
    (it still reaches bare /summary and the D62 Other bucket). Tag control by
    injection is pre-existing — the same injection could already elect any
    in-vocabulary tag — and the effect is self-limiting to the attacker's own
    post, so this is noted rather than filed (carried over from round 1,
    unchanged by this diff). It becomes interesting only if a future feature
    makes "untagged" mean "unreviewed" or "excluded from moderation sampling".
  - The new test helper
    infochat-provider/src/test/java/app/zcat/infochat/provider/digest/UntaggedPostRetrievalIT.java:1082-1085
    builds its cleanup DELETEs by string concatenation
    ("DELETE FROM post WHERE uid LIKE '" + PREFIX + "%'"). The interpolated
    value is a compile-time constant, so there is no injectable input; flagged
    only because the pattern is the shape a later edit could turn into a real
    injection if the prefix ever becomes a parameter (carried over from
    round 1, unchanged by this diff).
