---
target: M1-723
date: 2026-07-30
round: 2
base: 312e6f46de01327a677aea13cb776d05154ed12f
head: "<working tree, round 2>"
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 3
disposition: |
  Re-audit of the REMEDIATED diff after the round-1 low finding (asInt()
  truncating-cast wrap) was fixed. The adversary was given the round-1
  GAP verbatim, instructed not to assume it closed merely because
  remediation was attempted, told to audit the new sign-extraction path
  as previously-unaudited code, and explicitly authorized to return CLEAN.
  It returned CLEAN with zero findings at every severity.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Round-1 closure verified, not assumed. The remediation was audited
    against the wrap boundaries rather than taken on trust, and it holds
    for every integral shape a hostile feed can express.
    BlueskyResponseParser.java:146-155 and RedditResponseParser.java:166-176
    now gate on JsonNode.canConvertToInt() before asInt(). Walking the
    Jackson node types that isNumber() admits: LongNode (2147483648,
    4294967296, -2147483649) and BigIntegerNode (-99999999999999999999)
    all report canConvertToInt()==false and take the saturating branch,
    so decimalValue().signum() reads the sign off the UNNARROWED value
    and yields Integer.MAX_VALUE / MIN_VALUE by sign. That value then
    reaches NormalizedPost.clampCount (NormalizedPost.java:157-168)
    intact: Integer.MIN_VALUE hits the `count < -MAX_ENGAGEMENT_COUNT`
    branch (no Math.abs, so no MIN_VALUE self-negation bug) and floors at
    -536870911. deriveSocialScore's worst cases are
    2*536870911 + 536870911 = 1610612733 and -1610612733, both inside
    int, so the 2*reposts+likes arithmetic cannot overflow in either
    direction. The 4294967296 -> non-NULL 0 conflation is gone: that
    value now saturates to +MAX_ENGAGEMENT_COUNT, and PostPersister
    binds it with setObject(..., Types.INTEGER) so a genuine null still
    reaches the column as SQL NULL rather than 0. Six new tests pin
    exactly the boundaries the round-1 tests stopped one below. The
    round-1 finding is closed and is deliberately not re-reported.
  - Non-finite double in an engagement field aborts the whole page
    rather than degrading the one entry. JSON permits `1e400`; Jackson's
    default config (no USE_BIG_DECIMAL_FOR_FLOATS, no
    ALLOW_NON_NUMERIC_NUMBERS) parses it into a DoubleNode holding
    Double.POSITIVE_INFINITY. isNumber() is true, canConvertToInt() is
    false, so control reaches child.decimalValue() at
    BlueskyResponseParser.java:152 / RedditResponseParser.java:174 —
    which cannot represent a non-finite double and throws an unchecked
    exception the parser's own contract does not declare (it declares
    BlueskyParseException). This is NOT counted as a finding: the
    exception is caught by FetchScheduler.java:499 `catch (Exception e)`
    and routed to the exact path security.md §Failure handling already
    promises for this input class — "Fetcher failure (HTTP error,
    connection timeout, feed parse failure on an HTTP-shaped source) ->
    retry on the next scheduled tick ... After N consecutive per-source
    failures ... the source status transitions to 'failed'" — with the
    throttled admin notify and UrlRedactor-scrubbed logging intact, and
    no user-authored prose in the message. Nor does it grant incremental
    capability: only a party who already controls the entire HTTP
    response can place the literal, and that party can reach the
    identical outcome by returning HTTP 500 or malformed JSON. The note
    is recorded because the remediation newly introduced this throw path
    (the prior asInt(0) returned Integer.MAX_VALUE for Infinity without
    throwing) and because it sits in tension with the parser's own
    stated per-entry degradation discipline for the sibling untrusted
    field ("one feed entry with a non-ISO indexedAt must not throw and
    kill every well-formed post in the same page",
    BlueskyResponseParser.java:174-182). If the user wants that
    discipline to be uniform across untrusted fields, catching the
    coercion failure and returning null — the same "the API did not
    report a count" state the helper already produces for a non-numeric
    node — is the shape that matches the neighbouring code.
  - In-range engagement inflation (carried forward from round 1, still
    open and still out of model). Saturation bounds the magnitude but a
    source-controlled likeCount of exactly MAX_ENGAGEMENT_COUNT is
    accepted verbatim and yields the maximum permitted social_score.
    security.md commits to a specific, reproducible ordering for
    searchPosts (COALESCE(published_at, fetched_at) DESC, with an
    explicit adversarial argument about why an undated post cannot seize
    the head) but makes no commitment at all about engagement-derived
    ranking. Grep confirms no main-source reader of likes / reposts /
    social_score exists in this diff, so the value is corrupt-free but
    inert at rest. M1-724 wires the first consumer; if it or any later
    ticket feeds social_score into ranking or into the
    {{#has_social}} summarizer-prompt block, the threat model should be
    extended with the same kind of adversarial-ordering argument it
    already writes for published_at / ready_at / fetched_at, because
    those are the positions re-injected first into the chat prompt via
    the D28 pre-fetch.
