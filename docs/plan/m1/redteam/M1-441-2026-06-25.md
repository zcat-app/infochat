---
target: "M1-441 --in-progress"
date: 2026-06-25
base: main
head: m1/M1-441-admin-prompt-context-aware
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  Audit ran on the in-progress branch tip (working-tree vs merge-base with main)
  between review APPROVE and commit. No findings. The change only moves the
  single-adapter bootstrap-admin requirement earlier (prompt-time) with local
  feedback; the load-bearing non-empty-union gate (6-adapter.sh:247) is unchanged
  and still backstops the multi-adapter all-blank case. Two out-of-model advisory
  items recorded below; neither is a threat-model gap this ticket introduces.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - The entire diff lives in operator-facing setup tooling
    (prod/scripts/6-adapter.sh, run by the operator during first-run
    wizard step 6) plus a test that drives it. The threat model
    (security.md §Threat model) places "Operator-set config (properties
    files, bootstrap JSON) is trusted" and frames the adversary as
    someone who "can send arbitrary text on any [adapter]", not the
    operator at a terminal. The wizard's stdin is the operator; there is
    no adapter-reachable, network-reachable, or LLM-reachable surface in
    this diff. So adversarial scenarios against the wizard prompt
    (malicious stdin, race on the secrets.env write) fall outside the
    documented v1 model.

  - The one security property the script enforces — the non-empty
    bootstrap-admin union (§7.6.3 / §Authorization model last-admin
    protection: "cannot leave the deployment with zero admins") — is
    only STRENGTHENED by this diff, not weakened:
      * The union gate at 6-adapter.sh:247-252 is unchanged and still
        aborts (exit 1) when admin_union == 0, BEFORE any adapters
        config is written. The multi-adapter all-blank test
        (AdapterAdminPromptWiringTest twoAdaptersBothBlankStillHitsUnionFailGate)
        confirms the backstop still fires.
      * The new single-adapter branch (6-adapter.sh:120-150) ADDS a
        required-prompt requirement on top of the gate; a blank
        re-prompts instead of silently falling through, and an EOF/blank
        fails closed via exit 1 (6-adapter.sh:140-142). No path lets a
        zero-admin deployment proceed.
      * The pre-existing-secret short-circuit (6-adapter.sh:116) only
        returns 0 when the key was genuinely set on a prior run that
        itself passed the gate — not a bypass.
    No threat-model commitment is left undelivered, so this is not a
    FINDING; it is noted here only to record that the audited security
    gate was checked and holds.
