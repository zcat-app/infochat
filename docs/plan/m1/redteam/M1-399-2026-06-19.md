---
target: M1-399
date: 2026-06-19
base: 68f552a44207bdd92cbef4e0ca8ec1112704abbf
head: m1/M1-399-secrets-env-escape-datadir-paths
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  In-progress audit between APPROVE (round 1) and commit. CLEAN — the two
  data-dir writes now route through the same dotenv_escape helper M1-397
  hardened. One out-of-model advisory recorded below (advisory only, no
  threat-model gap). No remediation required; proceed to /m1-tick commit.
---

RED-TEAM VERDICT: CLEAN

FINDINGS: (none)

OUT-OF-MODEL:
  - The same operator-typed values (simplex_data_dir / signal_data_dir) are
    ALSO written UN-escaped into application.properties at 6-adapter.sh:246
    and :254 (`infochat.adapters.simplex.data-dir=%s` /
    `...signal.data-dir=%s`). The diff escapes only the secrets.env write
    (lines 230-231), not these properties-file writes. This is NOT a threat-
    model finding because (a) operator-set config is explicitly trusted
    (security.md "Threat model": "Operator-set config (properties files,
    bootstrap JSON) is trusted"), so a malformed path is operator-self-
    inflicted, not an adversary crossing a defended boundary; and (b) the
    properties-file path is pre-existing and outside the diff under audit.
    The Java .properties format does not treat `"`/`$` the way the dotenv
    --env-file parser does, so the asymmetry is a format difference, not an
    injection gap. Flagged only so the user can decide whether to extend the
    model to cover operator-typo robustness; the diff itself only adds
    hardening (escaping) and weakens no spec commitment.
