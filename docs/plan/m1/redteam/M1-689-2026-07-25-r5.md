---
target: M1-689
date: 2026-07-25
round: 5
base: e691f30f555c0a2b74c702b126612abbfe8c164c
head: "<working tree>"
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 5
disposition: |
  Re-audit of the round-4 remediation. The only change since round 4 was
  a doc-only rewrite of the bound sentence in docs/spec/security.md's
  spec-CLOSED searchPosts allowlist row. That narrow scope was stated in
  the prompt, along with the fact that this one sentence had already
  over-claimed twice (rounds 2 and 4), so the adversary was told not to
  grant it the benefit of the doubt -- and equally told that an
  under-claim is not a security defect and that a fabricated fifth
  finding was now the more likely failure mode than a missed one. CLEAN
  was explicitly authorised.

  Verdict CLEAN. Rounds 1-4 all re-verified closed against the current
  bytes. The replacement bound sentence was checked clause by clause
  against the four ORDER BY sites and the ingest clamp, and
  cross-checked against the three sibling statements of the same bound
  (schema.md, commands.md §Content, the EligiblePostQuery inline
  comment) for new divergence. None found.

  Five out-of-model items, all advisory. Four are carried forward and
  previously dispositioned: post-V64 sort-plan cost, app-clock vs
  DB-clock skew on the window boundary, ready_at having no DB-level
  CHECK tying it to status='READY', and the widened release-time
  re-entry blast radius. ONE IS NEW and is recorded as a durability
  note, not a finding: the NULL-ordering remediation is pinned by a
  regression test at ONE of the four shipped sort sites
  (SearchPostsToolTest.undatedPostSortsByFetchCeilingNotByPromotionInstant,
  which is a strong test -- its fixture makes ready_at and fetched_at
  disagree, so it pins the round-2 fallback choice too). The other three
  sites carry the identical ORDER BY with no NULL-ordering coverage. The
  property IS delivered at all four sites today (the adversary verified
  this), so nothing is broken; the risk is that a future revert of
  EligiblePostQuery.java:277 or DigestPostCollector.java:151/:173 to a
  bare published_at DESC would reintroduce the round-1 high and ship
  green.

  That gap is NOT remediated here. This ticket's acceptance item reads
  "A test pins the ordering property directly", singular, and it is
  satisfied; widening to the other three sites does not trace to the
  acceptance criteria, and CLAUDE.md §"Better alternatives surface as
  proposals, not scope expansion" directs it to a follow-up ticket
  rather than an eighth refine. Filed as M1-692.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - (carried forward, rounds 1-4) Sort-plan cost after V64.
    `V64__post_window_index_on_ready_at.sql:36` drops `idx_post_published`, and
    no plain btree can serve `COALESCE(published_at, fetched_at) DESC`, so all
    four window queries sort their window set per call. Bounded by the window
    clamp (`SearchPostsTool.java:31-32,72-73`, WINDOW_MIN 1h / WINDOW_MAX 30d),
    the dispatcher's `limit` clamp (`ChatToolDispatcher.java:270-274`, cap 200),
    the cluster cap, the per-turn tool-call cap, and the armed
    `statement_timeout`. The threat model states no query-cost budget, so this
    is a cost note rather than a violation. The migration comment and
    `docs/design/03-commands.md` §"Index and sort cost" state it explicitly.
    I confirmed the one remaining `published_at` consumer — the Nostr reconnect
    cursor `SELECT MAX(published_at) FROM post WHERE source_id = ?`
    (`NostrStreamSource.java:630-631`) — keeps its own index
    `idx_post_source_published` (V36__schema_hardening.sql:33), so the drop
    strands no other query.

  - (carried forward, round 2) App-clock vs DB-clock now spans the window
    DECISION. The cutoff is sampled Java-side from the injected Clock
    (`EligiblePostQuery.java` fetch, `SearchPostsTool.java:89`) while the column
    it is compared against, `ready_at`, is stamped DB-side by `now()`
    (`ReadyPromoter.java:209-212`). `published_at` was a Java-bound value, so
    the comparison used to be same-sided. The DB is internal and
    operator-controlled (§Trust boundaries item 3) and the threat model makes no
    clock-skew commitment, so this is out of model — but it is a new coupling
    this diff introduces, and CLAUDE.md §"Injectable time in decision logic" has
    an adjacent rule about not splitting one component across two clocks. Noted
    for the user, not filed.

  - (carried forward, rounds 1-4; widened here) Release-time re-entry into the
    CURRENT window. `approve_quarantine` and the re-eval release path both
    re-stamp `ready_at` (`ReadyPromoter.java:209-216`; the approve procedure
    revisions), so an arbitrarily old post re-enters the current retrieval
    window of every scope in its source's world — the next digest and the next
    `/summary`, which a publication-keyed window would not have done. The
    admin-approve half is now documented by this diff
    (`docs/spec/commands.md` §quarantine approve). The **re-eval BENIGN** half —
    the `QUARANTINED -> READY` auto-release the threat model already concedes an
    attacker may aim for (§Re-evaluation job, "content that initially looks
    UNKNOWN to the judge but flips to BENIGN on a model swap or warm-up") — is
    not documented anywhere in the diff. It is still not a promise-vs-delivery
    gap: the spec's committed control for that exact attacker is the
    `RE_EVAL_RELEASED` audit row plus the throttled admin notification, both
    untouched here, and "released to users" already implies retrieval
    reachability. Flagged so the user can decide whether the widened
    re-delivery blast radius deserves a spec sentence on the re-eval side too.

  - (new, advisory — not an attack) The NULL-ordering remediation is pinned by a
    regression test at ONE of the four shipped sort sites. `SearchPostsToolTest.
    undatedPostSortsByFetchCeilingNotByPromotionInstant` (diff lines 1736-1778)
    pins it for `searchPosts`, and it is a strong test — the fixture is built so
    `ready_at` and `fetched_at` disagree, so it also pins the round-2 fallback
    choice. The other three sites carry the identical `ORDER BY COALESCE(...)`
    with no NULL-ordering coverage: `EligiblePostQueryIT.
    deterministicOrderingByPublishedAtThenIdDesc:294` seeds dated posts only,
    `DigestPostCollectorIT.nullPublishedAtPostIsCollected` asserts collection and
    null-survival but returns a single row so it asserts no order, and
    `DigestPostCollector`'s two SQLs have no ordering test at all. The promise
    IS delivered today at all four sites (verified below), so this is not a
    finding — it is a durability note: a future change reverting
    `EligiblePostQuery.java:277` or `DigestPostCollector.java:151/:173` to a bare
    `published_at DESC` would reintroduce the round-1 high and ship green.

  - (carried forward, round 2) `ready_at` carries no DB-level guarantee.
    `V7__joins_post.sql` declares it nullable with no CHECK tying it to
    `status='READY'`. The no-COALESCE-needed argument for the PREDICATE rests on
    a code-level invariant held across `ReadyPromoter` plus every
    `approve_quarantine` revision. I re-confirmed the invariant holds for every
    writer in the tree today (`ReadyPromoter.java:209-212` sets `status='READY'`
    and `ready_at=now()` in one UPDATE; no other production statement sets
    `status='READY'`). A future writer flipping `status='READY'` without
    stamping `ready_at` would make those posts permanently invisible to all
    three retrieval surfaces, silently. Not adversary-reachable — all writers are
    internal code behind the trust boundary — so noted rather than filed.

VERIFIED-CLOSED (rounds 1-4, re-derived from the current tree, not assumed):
  - Round 1 high/INJECTION (bare `published_at DESC` handing NULL-dated rows the
    head): closed. All four sort sites read
    `ORDER BY COALESCE(p.published_at, p.fetched_at) DESC, p.id DESC` —
    `EligiblePostQuery.java:277`, `DigestPostCollector.java:151` and `:173`,
    `SearchPostsTool.java:165`.
  - Round 1 medium/INFO-LEAK (the spec-CLOSED `searchPosts` row asserting the
    inverted contract): closed. `docs/spec/security.md:326` now states the
    shipped predicate (`ready_at >= ?`, matching `SearchPostsTool.java:150`,
    `EligiblePostQuery.java:226` and `:347`, `DigestPostCollector.java:144` and
    `:162`) and the shipped sort key.
  - Round 2 medium (fallback on `ready_at` insufficient): closed. The fallback is
    `fetched_at` at all four sites. I re-derived its properties rather than
    inheriting them: `fetched_at` is `TIMESTAMPTZ NOT NULL`, the partition key
    and part of the PK (`V7__joins_post.sql:146,163-166`), and no production
    statement mutates it or `published_at` after INSERT (grepped every
    `UPDATE post` in the tree — `ReadyPromoter`, `Stage2VerdictHandler`,
    `ReEvaluationJob`, `AdminReviewTtlJob` and the approve procedures touch only
    status/flags/`ready_at`/`status_changed_at`/`body`). `PostPersister`'s
    cross-tick dedup (`WHERE NOT EXISTS (SELECT 1 FROM post WHERE uid = ?)`)
    means a re-fetch cannot mint a fresher `fetched_at` for the same item.
  - Round 3 high/DOS (`RetryCommandHandler.mapPost` unguarded
    `published_at` read on the `/retry` replay path): closed at
    `RetryCommandHandler.java:346,357`
    (`publishedTs == null ? null : publishedTs.toInstant()`), with the mapped
    type being `EligiblePostQuery.Post` whose `publishedAt` is `@Nullable`
    (`EligiblePostQuery.java:124`). I re-ran the reader enumeration rather than
    trusting round 4's: the complete set of `published_at` JDBC readers in
    provider main is `EligiblePostQuery.java:294`, `DigestPostCollector.java:106`,
    `SaveCommandHandler.java:341`, `RetryCommandHandler.java:346` — all four
    null-safe — and no main-code consumer dereferences `Post.publishedAt`
    (`SaveCommandHandler.java:374` is the only consumer and branches on null).
  - Round 4 low/INFO-LEAK (the replacement bound sentence claiming an undated
    post "cannot outrank later fetches"): closed, and the replacement is TRUE.
    Checked clause by clause against the shipped key
    `COALESCE(published_at, fetched_at) DESC, id DESC` and against the ingest
    clamp (`PostPersister.java:191-195`,
    `publishedAt != null && publishedAt.isAfter(fetchedAt) ? fetchedAt :
    publishedAt` — an upper bound only, NULL binds as SQL NULL; mirrored in
    `NostrEvent.java:89-95`):
      (a) "sorts at the top of its own fetch cycle — level with the ceiling a
          dated post from that same fetch could claim — and no higher": true.
          An undated row keys at exactly its own `fetched_at`; a dated row from
          the same fetch keys at `published_at <= fetched_at`, and a dated row
          can reach that same instant by claiming any future date. Ties break on
          `id DESC`, so the position is reachable by a dated row.
      (b) "it cannot move on release": true. Neither `approve_quarantine` nor
          the re-eval path writes `published_at` or `fetched_at`; only `ready_at`
          moves, and `ready_at` is not in the sort key.
      (c) "a later fetch does not automatically displace it, because the clamp
          bounds `published_at` from above only and an honest backfilled item can
          key arbitrarily far in the past": true, and it is exactly the
          correction round 4 asked for.
      (d) "What the bound denies is any position a dated post from the same fetch
          could not already have taken": true, by (a).
      (e) "a bounded, self-decaying position": an under-claim-safe summary, not
          an over-claim — the explicit caveat in (c) immediately precedes it, and
          two independent decay mechanisms exist (newer dated arrivals key above
          `fetched_at`, and the `ready_at` window itself expires the row).
    Sibling cross-check (round-5 instruction 2): the rewrite introduces NO new
    divergence. `docs/spec/schema.md:374-376` ("an undated row sorts at the top
    of its own fetch batch and no higher"), `docs/spec/commands.md:393-398`
    ("at the top of the batch it arrived in — no higher — because a dated post
    from that same fetch is capped at the same instant") and the inline comment
    at `EligiblePostQuery.java:257-275` all state the same narrow bound and none
    of them carries a "later fetches dominate" claim, so security.md is now the
    strictly more explicit statement of the identical property rather than a
    conflicting one. (`schema.md:358-362` still quotes the historic
    `ORDER BY published_at DESC` when naming what the clamp defends, but the same
    bullet corrects it four lines later — "never a bare `published_at DESC`" —
    so no reader can take the stale clause as the shipped key. Not flagged.)
