---
target: M1-255
date: 2026-06-09
base: main (650b4cd)
head: working tree on branch m1/M1-255-signal-membership-aci-gate (uncommitted, --in-progress)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  CLEAN — the diff is a focused security tightening (adds the canonical-UUID
  gate to the membership member-delta parser, matching the DM and
  group-message paths). No finding.

  One OUT-OF-MODEL observation: the membership branch (dispatchMembership)
  dispatches join/leave events with no bot-mention gate, unlike the inbound
  group-message branch (SignalGroupHandler.handleReceive line ~155 requires
  SignalMentionParser.botMentioned). Verified accurate against the full file
  (the threat-actor only saw the diff): the asymmetry is real. However it is
  correctly out of model — membership joins/leaves have no per-message
  "sender mention" semantics, and signal-cli is trusted as the identity
  boundary (trust boundary 1 / D10). This diff's job is to ensure only
  canonical-UUID join keys cross the boundary, which it delivers. Whether
  Provider-side reconciliation of membership events against authorized group
  state deserves explicit spec coverage is a design question for the user;
  it is NOT a gap in this ticket and NOT a remediation ticket.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Forged membership deltas from a compromised/hostile signal-cli daemon or relay: a `memberLeft`/`memberJoined` entry carrying any well-formed canonical UUID (now passing `isAcceptableAci`) is dispatched as a `MembershipEvent` with no proof the originating group update was authored by a group admin and no bot-mention gate (unlike the inbound-message path at SignalGroupHandler.java:155). An attacker who controls the daemon stream could thus drive `group_membership` mutations in Provider for arbitrary real ACIs. This is outside the documented threat model: the adapter trusts signal-cli (the local subprocess) as the identity-asserting boundary (trust boundary 1), and Signal's group-update protocol authenticity is assumed handled below the adapter. The diff's job — ensuring only canonical-UUID join keys cross the boundary — is delivered. Flagging only so the user can decide whether Provider-side reconciliation of membership events against authorized group state deserves explicit spec coverage; it is not a gap in this diff.
