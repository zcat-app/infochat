---
target: M1-053
date: 2026-05-24
base: main
head: m1/M1-053-source-management-admin-commands (tip c11ed17)
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 0
out_of_model_count: 1
disposition: |
  /redteam M1-053 --in-progress ran post-/m1-tick-commit, pre-/m1-tick-merge.
  Branch tip is c11ed17 (process: resolve MANUAL verdict path-a Shape B
  authorization); preceding commits 1271c98 (implementation) and 40a7423
  (pre-implementation acceptance corrections) are the substantive surface.

  The audit was triggered by crash-recovery: the previous session committed
  the implementation, captured the MANUAL reviewer verdict in frontmatter,
  flipped status to done, then crashed before redteam could run and before
  merge. Resumed session applied path-a (amend acceptance item 3 to
  authorize the Shape B @QuarkusTest pattern), then ran this audit.

  Single finding (medium AUDIT-EVASION): ListSourcesCommandHandler
  explicitly opts out of writing audit rows on the admin-only --all /
  --include-deleted paths, with handler javadoc citing an "Invariant 7"
  rationale (audit-on-intent only on confirm-gated destructive admin
  actions). The spec at §Authorization model step 8 commits to audit-on-
  intent for every inbound message that reaches permission check, and
  §Source URL visibility flags --all as a privilege boundary
  (deployment-wide URL disclosure). The asymmetry — destructive writes
  audited via *_INTENT verbs, privileged reads not — is the gap.

  The OUT-OF-MODEL TOCTOU observation about the unlocked preflight read
  and outbound probe is forward-looking (only relevant if a future ticket
  adds /rename-source or /edit-source-url); v1 freezes source.identifier
  so the window is closed by the immutability invariant.

  Disposition: status is "done" per the literal ticket frontmatter, but
  the branch is not yet merged into main. Two paths surfaced to the user
  in the chat summary:
    (a) Fold the audit fix into THIS branch before merge — flip status
        back to in-progress, add LIST_SOURCES / LIST_SOURCES_ALL to
        AuditAction enum + AuditLogWriter calls in
        ListSourcesCommandHandler, extend test scenarios, re-run mvn
        verify, then merge. Single coherent ticket lands; redteam audit
        artifact is committed on the same branch.
    (b) Merge as-is, file a remediation ticket whose `remediates: M1-053`
        adds the LIST_SOURCES audit verbs. Matches the skill's literal
        rule for done-tickets.
  User decision pending at audit-write time.

  CAUTION: this audit file and the ticket-frontmatter updates land AFTER
  the implementation commit. If committed on this branch they become the
  branch tip — `/m1-tick merge`'s `-C <tip>` then carries the wrong
  canonical message. Standard mitigation (memory file
  feedback_redteam_postcommit_merge_pitfall.md): verify merge-commit
  subject post-merge and amend with the implementation commit SHA if
  needed. Or merge first then commit audit on main with `process:` prefix.
---

# /redteam M1-053 — adversarial verdict

**Note on duplication.** The threat-actor subagent emitted the FINDINGS block
twice (apparent thinking-vs-final-output leak); the two emissions describe
the same single finding with identical CATEGORY / SEVERITY / GAP / REPRO.
Treated as one finding for the audit record.

```
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: AUDIT-EVASION
    SEVERITY: medium
    PROMISE: docs/spec/security.md §Authorization model — "Authorization
              evaluation order on every inbound message: ... 7. Permission
              check ... 8. Audit-log the intent. 9. Execute." Combined
              with §Source URL visibility — "every URL added via
              /add-source (DM or group) is visible to bot admins through
              /list-sources --all. Users adding private feeds should
              treat the URL as visible to operators" — and §Secrets
              handling — "The audit log records intent (command name,
              actor, scope, target)". Step 8 is committed to fire on
              every inbound message that passes the permission step;
              the `--all` form of `/list-sources` is an admin-gated
              privileged disclosure (every source identifier across the
              deployment, including soft-deleted rows under
              `--include-deleted`).
    GAP: infochat-provider/src/main/java/app/zcat/infochat/provider/command/ListSourcesCommandHandler.java
         lines 56-64 (javadoc) and the entire `handle(...)` body lines
         111-167 — the handler explicitly opts out of writing any
         `audit_log` row ("a list-only command writes no audit_log row
         even on the admin --all path"), and no `auditLogWriter.write(...)`
         call appears in any code path including the privileged `--all` /
         `--all --include-deleted` admin paths.
         infochat-core/src/main/java/app/zcat/infochat/core/audit/AuditAction.java
         is not extended with a `LIST_SOURCES`/`LIST_SOURCES_ALL` verb
         either.
    REPRO: A rogue or session-compromised bot admin issues
            `/list-sources --all --include-deleted` once or repeatedly to
            harvest every source identifier (URL) across the deployment
            — including private feeds that §Source URL visibility flags
            as operator-visible and historical soft-deleted rows. The
            harvest leaves no trace in `audit_log`; subsequent forensic
            review cannot determine which admin enumerated the source
            catalog, when, or how often, even though the same admin's
            destructive operations on those rows (`/remove-source`,
            `/source-enable`) would be fully recorded under
            `REMOVE_SOURCE_INTENT` / `SOURCE_ENABLE_INTENT`. This
            asymmetry — destructive writes audited, privileged reads not
            — is exactly the gap step-8's per-inbound commitment is
            meant to close.
    SUGGESTED-FIX-CLASS: audit-log-coverage

OUT-OF-MODEL:
  - The pre-flight `lookupSource(...)` reads in
    `SourceEnableCommandHandler.executeRevive` and
    `SourceDisableCommandHandler.handle` run outside the transaction and
    then re-check inside `SELECT … FOR UPDATE`; the time-of-check /
    time-of-use window is correctly closed by the locked re-read. The
    `urlProbe.probe(URI)` call between the unlocked preflight and the
    locked re-check, however, drives outbound HTTP I/O against the
    source's `identifier`. In v1 the spec freezes `source.identifier`
    (no command mutates it), so the URL the probe hits matches the URL
    the locked re-read implicitly trusts; if a future amendment adds a
    `/rename-source` or `/edit-source-url` command without revisiting
    these handlers, an attacker who races the URL change between
    preflight read and probe could induce the probe to hit an
    attacker-controlled URL while the SOURCE_ENABLE audit row records
    the row id (no URL). Not in v1's threat model but worth flagging if
    identifier ever becomes mutable.
```
