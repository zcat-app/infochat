---
target: M1-114
date: 2026-05-31
base: main
head: HEAD (089ee08 — implementation commit on branch m1/M1-114-d47-status-and-roundtrip-it, pre-merge)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  No in-model findings. Two out-of-model hardening observations were
  noted (test-only audit-trigger disable pattern; lookupIsAdmin lacks
  defense-in-depth is_banned filter). Neither is exploitable under the
  current upstream gates; both surface to the user as informational and
  do not block merge. If acted upon, they would land as separate
  remediation tickets (M1-114 is done and immutable).
---

# M1-114 red-team audit (2026-05-31)

Base: `main` (519229a — spec-refine commit)
Head: `089ee08` (M1-114 implementation commit on per-ticket branch, pre-merge)

## Verbatim verdict (threat-actor subagent return)

The subagent return preserved verbatim below — including its in-band
falsification reasoning before the final structured verdict. The
canonical RED-TEAM VERDICT line is at the bottom.

---

OK — `/help` doesn't audit. Same pattern. Not a finding specific to this diff.

Let me re-evaluate the diff for any actual security gaps:

1. **SQL injection in StatusCommandHandler.lookupIsAdmin**: parameterized — no.
2. **SQL injection in countPendingGroups**: no parameters; static query — no.
3. **Admin bypass**: lookupIsAdmin returns false when no user row found — fail-closed, good.
4. **Information leak via error path**: `IllegalStateException` includes adapter name in the message. The adapter is internal-known config value. Not user-controlled. The exception would propagate up the call stack. Looking at the §Secrets handling — "Exception messages and stack traces emitted via the application logger MUST NOT contain user-authored prose" — the adapter name is not user-authored. Fine.

5. **Concurrent admin lookup race**: between admin check and any subsequent operation? There's no subsequent privileged operation — just a count read. No TOCTOU concern.

6. **DOS via `/status` floods**: spec covers this in the parser-only DB-read bucket.

7. **Audit gap on `/status`**: not unique to this diff; the existing pattern (HelpCommandHandler) doesn't audit either. This is an existing convention, not a new gap.

8. **Admin enumeration via length probing**: a non-admin observing a `/status` response sees 2 lines; an admin sees 3. A user trying to "is X an admin" question can't probe — they can only observe their OWN response. Not exploitable to leak others' admin status.

Let me also check the IT test for any harmful patterns. The test seeds invite codes and exercises ban paths. The test disables audit_log triggers temporarily — but this is in test-only code. Tests are not in the threat model's enforcement surface.

Let me check one more thing — the `lookupIsAdmin` query doesn't filter by `is_banned`. A banned admin would still register as admin. Per spec: a banned admin can't reach step 9 (ban check at step 4 fires first) — so the admin branch in `/status` wouldn't execute for a banned user (they'd be intercepted earlier). But wait — what about a NOT-banned user with `is_admin=true`? That's the normal case. What if a row has `is_admin=true AND is_banned=true`? Per spec, you can't ban an admin without first revoking admin (last-admin protection), but it might be possible if there's at least one other admin. Let me think about this attack...

Actually if a user is banned, step 4 short-circuits before step 9 executes (handler). So StatusCommandHandler is never reached for banned users. So `lookupIsAdmin` only runs for non-banned callers. No exposure.

I don't see actual exploitable findings in this diff. The changes are minimal: a new read-only `/status` handler with a properly parameterized admin check, a SQL COUNT query, bundle keys, and tests.

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - The integration test (GroupAuthorizationRoundtripIT) temporarily disables `trg_audit_log_no_update` and `trg_audit_log_no_delete` triggers during its per-test cleanup. While scoped to test code and not a production attack surface, the pattern of disabling audit-log integrity triggers normalizes a sensitive operation; if a future change accidentally hoists this helper into shared/production code paths, the trigger-disable would silently weaken the audit-log immutability invariant. Out of model because the test profile is not exposed to adversaries, but worth noting as an operational hygiene item.
  - StatusCommandHandler.lookupIsAdmin does not filter on `is_banned = false`; the spec's authorization order means a banned caller cannot reach the handler in production (step 4 short-circuits), so this is not currently exploitable. If a future change reuses this query in a path that skips the upstream ban check, a banned-but-still-admin row could surface the pending-groups count to a banned user. Out of model because the current upstream router enforces the gate, but a defense-in-depth `AND is_banned = false` predicate would harden the query against future call-site drift.
