---
target: "M1-480 --in-progress"
date: 2026-06-29
base: origin/main (4edfef05)
head: working-tree (m1/M1-480 branch)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  Adversarial audit of the M1-480 /unban in-transaction admin re-gate (both
  legs) ran CLEAN — no findings against docs/spec/security.md. One out-of-model
  advisory item recorded in the body below; advisory only, no lifecycle action
  required for this ticket.
---

RED-TEAM VERDICT: CLEAN

The diff adds an in-transaction SELECT ... FOR UPDATE admin re-check to both
mutating legs of /unban (the non-preban Step-5 path and the preban-delete path
executeDeletePrebanUser), closing the TOCTOU window where a caller demoted by a
concurrent /revoke-admin or /demote between the dispatch-time read (Step 1) and
the commit could still complete the admin-only operation. This strengthens, and
does not weaken, the threat model's commitments.

Adversarial checks performed against docs/spec/security.md:

- Deterministic authorization (Trust boundary 3; Authorization step 7): the
  re-gate is plain Java with the row lock held until commit/rollback; no LLM is
  on the path. Delivered.
- Audit-on-intent + audit-before-effect (Invariant 7; §Authorization model
  step 8): the UNBAN_INTENT row is written only after the Step-1 admin gate
  (UnbanCommandHandler.java:160 refuses non-admins before the intent write at
  :184), so no non-admin can produce an intent row. On a demote-race refusal the
  intent row persists on its separate auto-commit connection — extra audit, not
  AUDIT-EVASION. The non-preban path keeps insertUnbanAudit (:235) inside the txn
  before updateUserUnbanned (:238). Invariant preserved.
- TOCTOU completeness: the lock is taken on the ACTOR row (:223, :307) — the
  correct row to serialize against actor demotion — and the refusal path rolls
  back and returns error.admin_only (:225-226, :309-310). The reverse
  interleaving (unban locks first) correctly commits for an actor still admin at
  lock time. UnbanCommandHandlerToctouIT asserts the blocking + post-commit
  refusal + target-stays-banned behavior.
- No new INFO-LEAK: IllegalStateException messages carry only adapter +
  ContactIds.redact(...) (:244-246, :330-332), never user prose;
  registeredContactSet.markRegistered (:263) fires only after a committed,
  admin-gated non-preban unban.
- No skipped mutating leg: the only legs without the re-check (unknown-contact
  error :189-190, not-banned no-op :203-204) perform no privileged mutation.

No promise in the threat model is left undelivered by this diff, and no new gap
is introduced.

OUT-OF-MODEL:
  - The branch decision (preban vs non-preban) is made from a non-locking
    dispatch-time read of the TARGET row (lookupUser at :188); only the ACTOR
    row is locked FOR UPDATE. A racing change to the target's registration_state
    could in principle route to the wrong leg. In practice 'preban' has no legal
    transition out of preban under the v1 flow (preban rows are minted by /ban
    and removed by /unban only), so the branch cannot be raced into a wrong leg.
    This is pre-existing (not introduced by this diff), is not an
    adversary-reachable path, and is not a documented threat-model promise —
    flagged only for awareness.
