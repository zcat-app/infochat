---
target: M1-269 --in-progress
date: 2026-06-10
base: cccc741cc91026767c5cff36cd994308394f0066
head: m1/M1-269-revoke-update-on-pricesnapshot (working tree, pre-commit)
verdict: CLEAN
findings_count: {critical: 0, high: 0, medium: 0, low: 0}
out_of_model_count: 0
disposition: |
  Pre-commit audit between round-1 APPROVE and /m1-tick commit. Test-only
  diff (DbRoleMatrixIT privilege-matrix pin) plus lifecycle byproducts;
  no findings, nothing to remediate.
---

RED-TEAM VERDICT: CLEAN

Audited diff M1-269 (base cccc741, head m1/M1-269-revoke-update-on-pricesnapshot,
working tree) against the threat model in docs/spec/security.md.

The diff contains only:
  1. docs/plan/m1/STATUS.md — status-board counts and one ticket row (documentation).
  2. docs/plan/m1/tickets/M1-269-price-snapshot-revoke-update.md — premise-fail
     refinement of the ticket body (documentation).
  3. infochat-collector/src/test/java/app/zcat/infochat/collector/db/DbRoleMatrixIT.java
     — adds one test method, priceSnapshotPrivilegeMatrixMatchesImmutabilityGrants.

No runtime/production code path is touched. The sensitive-surface inventory confirms
nothing in authentication, authorization, input validation, ban handling, or audit-log
writes is touched (the single STATUS.md hit at line 86 is a status-board reference to
an unrelated ticket M1-254, not a code change).

Adversarial review of the one code change (a test-only addition):

- Promise check. docs/spec/security.md §DB roles commits that the Collector is
  "INSERT-only on price_snapshot ... no UPDATE", the Provider is "SELECT-only on
  price_snapshot", DELETE is not granted to either service role (retention is
  operator partition-drop), and "a SQL-injection bug in the Provider cannot ...
  mutate price snapshots". The added test asserts collector={SELECT,INSERT},
  provider={SELECT}, with UPDATE and DELETE false for both. This matches the
  threat-model commitment cell-for-cell.

- Direction of effect. The change is defense-in-depth: it pins an already-correct
  grants end-state (V17 grants + V39 UPDATE revoke) so a future migration that
  widens either service role's write surface on price_snapshot fails the build. It
  iterates all four DML privileges (SELECT/INSERT/UPDATE/DELETE) and compares full
  sets, so it catches widening in either direction. has_table_privilege accounts for
  direct, role-membership, and PUBLIC grants, so a privilege re-introduced via any of
  those paths is also caught.

- No test-integrity violation. The pre-existing applicationRolesHaveExpectedLoginAttributes
  test is untouched; the behavioral 42501 probes in DbGrantsRevocationIT are out of
  scope and untouched. No test is weakened, disabled, or bypassed.

- No false pin. Every asserted cell matches the spec; the test does not pin any
  privilege the threat model forbids, nor omit any the threat model requires.

There is no gap between what the threat model promises and what the diff delivers.

FINDINGS: (none)

OUT-OF-MODEL: (none)
