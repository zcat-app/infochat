---
target: M1-224
date: 2026-06-08
base: HEAD (fork point of m1/M1-224-bounded-inbound-dispatch-queue with main)
head: working tree (uncommitted impl, in-review round-1 APPROVE)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  Adversarial audit of the M1-224 bounded-inbound-dispatch-queue DoS
  remediation (the fix for the deferred M1-205 high-severity DOS finding),
  run on the in-progress branch tip (working tree) before commit, per
  `/redteam M1-224 --in-progress`. CLEAN verdict: the diff ADDS the memory
  bound the threat model's DOS category requires and weakens no existing
  commitment. No FINDINGS. Two OUT-OF-MODEL advisories recorded below — both
  advisory, neither blocks the existing round-1 APPROVE; no remediation
  ticket required. The operator may later choose to extend the threat model
  to bound adapter WARN log volume (advisory 1).
---

# Red-team audit — M1-224 (2026-06-08)

Target: `M1-224` (single ticket, `--in-progress`)
Base: `HEAD` (fork point of `m1/M1-224-bounded-inbound-dispatch-queue` with `main`)
Head: working tree (uncommitted implementation; ticket status `in-review`, round-1 APPROVE)

Diff audited: 2 source files (`SignalJsonRpcClient.java`, `SimpleXWebSocketClient.java`),
2 new tests (`SignalInboundQueueBoundTest`, `SimpleXInboundQueueBoundTest`), and the
design/reference/ticket/STATUS metadata hunks. Sensitive-surface inventory: only the
INPUT (message-intake / inbound-dispatch) surface was touched; AUTH, AUTHZ, BAN, and
AUDIT surfaces were `(none touched)`.

## Verbatim verdict returned by the threat-actor subagent

> All the throttling commitments in security.md are about *admin notifications*, not adapter WARN logs. The overflow WARN is a plain log line, not an admin notification, so there's no security.md promise it breaches.
>
> I've now audited the diff comprehensively against every relevant security.md commitment:
>
> - **DOS**: The diff *adds* a memory bound (bounded queue + drop-newest). It does not remove or weaken any security.md DOS commitment (rate caps step 1.5 / per-group, regex watchdog are untouched). The bound holds; tests confirm it.
> - **INFO-LEAK / Secrets (D37)**: The overflow WARN logs the sender via SHA-256 prefix (non-reversible) or "non-dm" — no raw contact id, no user prose. Stronger than the spec baseline.
> - **No exception/user-content leak**: The WARN interpolates only the capacity int, redacted hash, and counter — no `Throwable`, no body bytes.
> - **AUTH/PERM/AUDIT**: No auth, permission, ban, or audit-log surfaces touched.
> - **INJECTION**: Log format strings use parameterized SLF4J / `warnf`; the only interpolated string is a SHA-256 hex hash (no log-injection vector from raw contact id).
>
> The diff delivers what the threat model requires and breaches no security.md promise.
>
> RED-TEAM VERDICT: CLEAN
>
> OUT-OF-MODEL:
>   - Log-volume amplification: under a sustained inbound flood that keeps the bounded queue full, each dropped delivery emits one WARN line (`SignalJsonRpcClient.java:659`, `SimpleXWebSocketClient.java:522`), so attacker inbound volume drives unbounded application-log output (disk / log-pipeline pressure). This is not a security.md commitment gap — every throttling promise in security.md (§Failure handling lines 846, 936–940; §Prompt-injection LLM output sanitizer line 284) governs *admin notifications* and *audit rows*, not adapter-level WARN logs, and the drop only fires downstream of the upstream rate caps that should already be throttling the sender. Flagged as advisory in case the operator wants the model extended to bound adapter WARN log volume (e.g. coalesce the overflow WARN per sender/window, as the design's `droppedInboundCount` counter already supports a periodic-summary pattern).
>   - Stale lifecycle comment: `SimpleXWebSocketClient.java:120-123` still says the dispatch worker is "created lazily on first dispatch," but the diff makes `dispatchExecutor` a `final` field constructed eagerly in the constructor. No behavioral or security impact (the executor is still bounded and shutdown-once); noted only because it could mislead a future auditor about thread lifecycle.
