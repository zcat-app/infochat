---
target: M1-081
date: 2026-05-26
base: main
head: m1/M1-081-quarantine-workflow-umbrella
verdict: CLEAN
findings_count: 0
out_of_model_count: 0
disposition: |
  Umbrella-ticket diff is test-only (QuarantineWorkflowIT.java). The
  production security surface (admin-only gates, stored procedure state
  transitions, audit logging, non-admin rejection) was already landed
  and reviewed in M1-081a and M1-081b. The IT exercises all eight
  acceptance steps through the InMemoryAdapter and verifies the
  cross-cutting security properties end-to-end. No gap between the
  threat model's promises and the diff's delivery.
---

RED-TEAM VERDICT: CLEAN

No findings. The diff adds an integration test that exercises the
quarantine admin workflow through the InMemoryAdapter and stored
procedures. The production security surface (admin-only command gates,
stored procedure state transitions, body restoration, audit log
visibility, non-admin rejection) was implemented in M1-081a and
M1-081b. This umbrella IT verifies the cross-cutting property that
those pieces work together correctly.

The adversary noted one pre-existing concern (not a finding): the
approve_quarantine stored procedure's replace() on post.body could
interact unexpectedly if original_html itself contains the
[REDACTED:...] placeholder pattern. This is a V21/M1-081a concern,
not introduced by this diff.
