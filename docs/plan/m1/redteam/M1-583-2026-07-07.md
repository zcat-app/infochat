---
target: M1-583 --in-progress
date: 2026-07-07
base: main (3c59a356)
head: m1/M1-583-secret-disposal-gaps (working tree, pre-commit)
verdict: CLEAN
findings_count: {critical: 0, high: 0, medium: 0, low: 0}
out_of_model_count: 3
disposition: >-
  Pre-commit audit of the in-progress branch per /m1-tick run step 5
  (security_relevant: true). CLEAN — the audit file folds into the ticket
  commit. Out-of-model items reported to the user as advisory; none
  auto-filed.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Silent no-shred fallback in pack.sh cleanup: prod/scripts/pack.sh:164-169 skips the overwrite pass and falls back to plain `rm -rf` when `shred` is absent from PATH, with no operator-visible warning — the staging tree's complete secret set (DB dump, identities tar, secrets.env copy) is then left recoverable in freed blocks. Disk-block forensics on the operator host is outside the documented threat model (security.md declares DB-at-rest/storage protection the operator's responsibility via full-disk encryption, D34), and the diff's own comment declares the fallback deliberate, so this is flagged for awareness only.
  - Widened irreversible-destruction eligibility as an operator footgun: prod/scripts/shred-bundle.sh:101-132 now accepts any bare `*.pgc` file anywhere, any directory containing `db/*.pgc` one level down, and any directory whose resolved basename matches `.infochat-pack.*` — an unrelated operator directory matching one of these shapes (e.g. a personal backups dir holding `db/old.pgc` plus sibling material) is fully sweep-eligible. Exploitation requires either local filesystem control (planting a decoy shape) or social-engineering the operator's shell, both outside the documented model; the consent gate (resolved absolute path + file count + size, --yes or interactive y/N) is the remaining control.
  - Inventory-to-destroy TOCTOU in shred-bundle.sh: the file count/size printed for consent (prod/scripts/shred-bundle.sh:138-148) and the destroy pass (prod/scripts/shred-bundle.sh:165-173) are separate filesystem walks; content swapped in between is destroyed without having been shown. Requires an attacker with write access to the operator host filesystem, which the threat model does not include.
