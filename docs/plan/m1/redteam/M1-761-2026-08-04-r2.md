---
target: M1-761
date: 2026-08-04
round: 2
base: 9310aca8
head: "<working tree>"
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  Re-audit of the remediated diff, per the /redteam rule that a fix
  invalidates the audit it answers. The prompt named round 1's low/DOS
  finding and the attempted remediation explicitly, instructed the
  adversary neither to assume it closed nor to re-report it if it
  genuinely was, and expressly authorised a CLEAN verdict so a second
  round under momentum would not manufacture a finding to justify itself.

  Round 1's finding is verified closed: the condition-(d) rejection now
  records REJECTED_BY_TARGET_SCRIPT_CHECK under the same display-hit key
  a translation would occupy, so /saved and /summary converge and the
  probing callers spend no budget slot or LLM bucket token. No new
  finding was raised against the remediation itself.
---
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Sticky degradation replaces self-healing degradation for a rejected
    headline. The recorded rejection is written into the SAME 24h
    expireAfterWrite cache as a translation (TranslationCache.java:43-46,
    written at TranslationPipeline.java:377), so a condition-(d) failure
    is not retried for up to a day in that scope, where before this diff
    every render re-attempted it. Verified NOT adversary-steerable and
    NOT a promise gap: (i) the delivered bytes are identical either way
    (original headline + the bundle "translation unavailable" note), so
    an adversary who steers the translator into a refusal gains nothing
    they did not already have pre-diff; (ii) the rejection is keyed on
    SHA-256(headline) inside the per-(scope_kind, scope_id) display-hit
    partition (TranslationPipeline.java:421-423), and the sanitize/
    translate unit is ONE headline per call, so no publisher can push a
    rejection onto a third party's headline or into another scope's
    partition; (iii) security.md commits nothing about translation
    availability, freshness, or retry for a given headline — §Failure
    handling's rule is "a complete LLM outage degrades quality, not
    safety", and the fallback is that documented degradation. Flagged
    only so the user can decide whether per-headline translation
    availability should become a modeled property (it currently is not).
  - Condition (d) is still a quality guard, not an adversarial control,
    and the diff widens its reach to a second leg. §Trust boundaries
    item 9 puts a hostile or compromised LLM endpoint in scope, and the
    check is satisfied by a single target-script code point anywhere in
    the reply (TranslationPipeline.java:205-228) — a hostile translator
    wanting to deliver arbitrary Latin-script text to a `ru` scope under
    the D30 machine-translation marker appends one Cyrillic character.
    That matches llm.md's own wording ("contains zero target-script
    characters") and security.md makes no target-script commitment, so
    this is conformant, not a gap; restated here only so an operator does
    not size trust in a remote `ModelTask.TRANSLATOR` route from it.

NOTES ON THE REMEDIATION UNDER RE-AUDIT (round 1 low/DOS — verified
CLOSED, not re-reported):
  - Convergence is delivered at all three call sites. SavedCommandHandler
    .java:439 and DigestRenderer.java:853 decide a row is free by probing
    `translationCache.get(headline, displayHitKey).isPresent()`; the
    rejection now occupies that exact key, so a re-render draws no
    per-user LLM token, no D47 per-group sub-bucket and no budget slot,
    and ClusterBlockRenderer.java:132 (which carries no budget) makes no
    translator call on a re-render. security.md §Rate limiting's "a
    fully-converged page never draws" is reachable again.
  - The sentinel is UNFORGEABLE by translator output, verified by reading
    the whole chain rather than trusting the javadoc: every value written
    into the display-hit keyspace passes DisplayHeadline
    .prepareTranslatedHeadline (TranslationPipeline.java:346, 387), whose
    private flattenToOneLine collapses `(?:\R|\s)+` — which covers U+000A,
    U+000D, U+000B, U+000C, U+0085, U+2028, U+2029 and the ASCII space set
    — to a single space (DisplayHeadline.java:232-234); the sanitizer
    chain that follows can only delete or substitute (NFKC, then
    IngestTextNormalizer.stripBidiAndZeroWidth, which only skips code
    points, LlmOutputSanitizerCore.java:583-586 / IngestTextNormalizer
    .java:54-79), and its only insertions are the fixed literals
    "[redacted command]", " (" and "] (" — none carries a line separator,
    and NFKC has no mapping producing one. The sentinel leads with U+000A
    and is compared with String.equals, so no reachable translator output
    can occupy that value.
  - The sentinel is UNRENDERABLE: the read path tests it BEFORE
    finishDisplayHit (TranslationPipeline.java:320-322), so it is never
    truncated, never marked, never delivered. Grepped every reader of
    TranslationCache in main: only TranslationPipeline (both legs),
    DigestRenderer:853 and SavedCommandHandler:439, and the latter two
    read presence only, never the value. No other writer of the
    display-hit keyspace exists (only lines 377 and 387).
  - Storing a non-translation in a translation-typed cache breaks no
    security invariant. The reason cached values skip sanitizer-2 is that
    they are already post-sanitizer-2; the sentinel is a compile-time
    literal carrying no endpoint-chosen bytes, and the read path never
    hands it to a render transform. (TranslationCache.put's javadoc and
    fallbackWithNote's "the cache is NOT written on this path" comment
    are now inaccurate for this leg — a documentation drift with no
    security consequence, noted, not filed.)
  - No control was dropped by the reroute: sanitizer-2 still runs on the
    rejected translation BEFORE it is discarded (line 346 precedes the
    check at 361-381), so closed-list matches inside a rejected reply are
    still redacted-and-audited; the new WARN interpolates only an enum
    and a closed-set language code, so no user content and no log
    injection; the prose leg's condition (d) is refactored into
    missingTargetScript with byte-equivalent semantics; and the added
    tests are purely additive (no existing assertion retargeted or
    weakened).
