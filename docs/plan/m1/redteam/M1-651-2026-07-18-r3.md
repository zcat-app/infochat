---
target: M1-651
date: 2026-07-18
base: 2f809f59 (fork point)
head: working tree on m1/M1-651-guard-helptier-against-the-spe (round 3, second remediation)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 5
disposition: >-
  Third audit, second re-audit. CLEAN. Both round-2 findings confirmed closed
  and each was tested hardest per an explicit instruction to try to break them:
  finding A by checking the spec amendment is a real designation (binding the
  qualifier to the closed list's spec-amendment gate) rather than prose
  restating the parser's assumption, and verifying it faithful to production
  handler semantics rather than an invented contract; finding B by attempting
  to construct a quiet-subset path and failing. The auditor identified an
  emergent property the implementation was not explicitly designed for: the
  bot-admin and group-admin regions now CROSS-CHECK each other, because a decoy
  region label necessarily pushes the real bullet into the other region's
  interior, where the list-item assertion fails loudly. The residual on finding
  A (a spec editor authoring a genuinely group-only command WITH the qualifier)
  is now the same irreducible class as "a privileged command never declared
  privileged anywhere" — a spec-side error in the anchor itself, which no
  spec-derived guard can detect — and is documented rather than undisclosed.
  The audit was explicitly told it could return CLEAN and that manufacturing a
  finding to justify itself would be worse than no audit.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Round-2 finding A (the "in groups" qualifier used as a tier discriminator
    without spec designation) is CLOSED, tested hardest as instructed. The
    amendment at docs/spec/commands.md:1325-1337 does not merely restate the
    parser's assumption in prose — it (a) declares the qualifier "part of the
    closed list's authorization content", (b) binds adding/removing it to the
    same spec-amendment gate the closed list already carries ("a **tier
    change** ... not a copy-edit"), and (c) states the disclosure consequence
    that makes the rule self-motivating. I verified the designation is
    FAITHFUL to production rather than an invented contract: the amendment's
    "any user may invoke it in a DM, and only a group admin (or bot admin) may
    invoke it inside a group" is byte-for-byte the semantics of
    HelpCommandHandler.java:350 (USER_OR_GROUP_ADMIN -> `!caller.group() ||
    caller.groupAdmin() || caller.botAdmin()`) and its "never offered in DM at
    all" is :349 (GROUP_ADMIN -> `caller.group() && (...)`), matching
    HelpTier's own javadoc at :96-105. Placement was verified against the live
    file, not accepted from the ticket: the paragraph sits AFTER the closing
    "The full per-actor-tier" prose at :1319, so it is outside the guard's
    group-admin region [1313, 1319) and cannot perturb the token set. The
    residual — an editor could still author a genuinely group-only command
    WITH the qualifier, and the guard would bless the matching CATALOGUE
    entry — is now the same irreducible class as "a privileged command never
    declared privileged at all" (item 2 below): a spec-side error in the
    anchor itself, which no spec-derived guard can detect. It is no longer an
    undocumented dependency. Not a finding.

  - Round-2 finding B (residual quiet-subset window in `closedListRegion`) is
    CLOSED. I attempted to construct a quiet-subset path and could not. Line
    anchoring (`indexOfLineStartingWith`, :1068-1075, `strip().startsWith`)
    means a prose mention such as "(distinct from the **Group-admin** tier
    below)" no longer terminates the bot-admin region. The only remaining way
    to terminate that region early is a line whose STRIPPED form begins
    "- **Group-admin" — necessarily a list item — and the two regions then
    cross-check each other: the group-admin region would begin at that decoy
    and its interior would contain the REAL group-admin bullet, which starts
    with "- " and trips the interior list-item assertion (:1053-1062) LOUDLY.
    A decoy bot-admin label earlier in the file fails the same way (the real
    bullet lands in the interior). A missing anchor in either direction fails
    at :1036-1046. Superset drift is loud too: an extra harvested token
    surfaces via `specCommandsMissingFromCatalogue` or the tier mismatch.
    Verified against the live spec that the guard is non-vacuous today: the
    bot-admin region is [1304, 1313) with no interior list item, and the
    group-admin region [1313, 1319) yields 6 qualified + 2 bare, matching
    CATALOGUE (HelpCommandHandler.java:129-134 USER_OR_GROUP_ADMIN, :140-141
    GROUP_ADMIN), so the both-tiers-must-occur collapse guard is live.

  - Round-2's javadoc-overstatement sub-point is CLOSED, with one cosmetic
    residue that is NOT a finding because it errs toward strictness, not
    safety. The rewritten guarantee at :1007-1018 ("It does NOT guarantee that
    any conceivable spec restructure is detected; it guarantees that the ones
    it cannot parse fail visibly instead of yielding a quiet subset") is TRUE
    of the code as written — I could not falsify it. The residue: the same
    passage lists "nested sub-bullets and headings inside a bullet" under
    "the region's INTERIOR formatting is irrelevant". A nested sub-bullet is
    not irrelevant — it fails the assertion at :1056. The very next clause
    says so explicitly ("and so does a second list item inside the region"),
    so the passage is self-correcting; and a reader misled by it is surprised
    by a RED build, never by a silent subset. Flagged for polish only.

  - `HelpCommandHandler.visible()` :343-345 still short-circuits the probation
    branch to `commandPermissions.allowedDuringProbation(entry.command())` and
    never consults `tier()`, so neither tier axis constrains the probation
    surface. Re-audited a third time and still compensated, not a finding: the
    probation allowlist is a closed enumeration drawn from security.md
    §Slow-start tier, contains no privileged command, and fails closed on
    unknown names. Unchanged from rounds 1 and 2.

  - New observation, safe-direction, flagged only so the user can decide. The
    sibling closed-list consumer `LlmOutputSanitizerTest.parseSpecClosedList`
    re-enters its bullet group on ANY line inside §Permission model that
    `contains("**Group-admin")` (:417-418 — a substring test, not the
    line-start test this diff adopted), and this diff adds a new prose
    paragraph INSIDE §Permission model that carries a backticked command token
    (`/help`, commands.md:1335). I verified the two do not interact today: the
    new paragraph's bold marker precedes "The", and "group-admin" appears
    lowercase and un-bolded, so `inBulletGroup` stays false and `/help` is not
    harvested — the sanitizer's spec-derived match set is unchanged, and
    security.md §LLM output sanitizer "Match-set derivation" is not perturbed.
    Should a future edit bold that phrase, the failure is LOUD and
    fail-safe (`onlyInSpec = {/help}` reddens `matchSetEqualsSpecClosedList`),
    never a silent shrink. Noted because the diff narrows the distance between
    a prose edit and that parser, while the guard it ships uses the stricter
    line-start rule the sibling still lacks.
