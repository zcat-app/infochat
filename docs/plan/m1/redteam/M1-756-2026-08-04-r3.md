---
target: M1-756
date: 2026-08-04
base: 6a524cd9
head: working-tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 5
disposition: |
  Round 3, re-audit of the round-2 remediation. CLEAN — the rewritten
  §Secrets handling bullet and the two corrected metering comments
  survived a sentence-by-sentence check against the code, and neither
  round-1 nor round-2 findings were re-reported. The one in-model item
  still open (no "aggregate system LLM budget" exists, security.md
  §Rate limiting) has now been re-checked and declined TWICE by
  independent audit rounds as not attributable to this diff; it is a
  pre-existing spec-vs-code gap and goes to a follow-up ticket. The audit
  chain for this ticket is rounds 1 (FINDINGS, 2 low) -> 2 (FINDINGS,
  1 low against the round-1 remediation) -> 3 (CLEAN); rounds 1 and 2 are
  superseded and retained only as the record of what was fixed and why.
---
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Round-1 finding 1 (INFO-LEAK, §Secrets handling did not name the digest) is
    CLOSED, and round-2 finding 3 (the remediation's "first UNATTENDED
    translator caller" claim) is CLOSED. I re-verified the rewritten bullet
    (docs/spec/security.md:1901-1920) sentence by sentence against main source
    rather than accepting the prior rounds' remediation notes:
      * "normal-mode headlines run through the same display-hit leg" — true and
        correctly mode-scoped: `appendHeadlines` is called only under
        `mode == DigestMode.NORMAL` (DigestRenderer.java:412-415), and it is the
        same `TranslationPipeline.runForDisplayHit` entry the /saved leg uses
        (DigestRenderer.java:858 vs SavedCommandHandler.java:453-458).
      * "the scheduled render ALREADY sends LLM-authored text through the same
        task — one per-cluster prose translation for the lead in every non-brief
        mode and for every cluster in `full`, plus one per-section roll-up" —
        verified: DigestRenderer.java:797 (`translationPipeline.run` in
        `appendClusterProse`), reached for the lead at :372-375 (non-brief,
        clusters >= leadMinimum) and for every cluster of every surviving
        section in FULL at :394-398; CategoryRollupGenerator.java:213 for the
        roll-up. Neither carries a per-render budget — true.
      * "the Collector's ingest translation worker is a separate
        continuously-scheduled consumer of the same task" — verified:
        IngestTranslationWorker.java:232 `@Scheduled(every =
        "{infochat.llm.translator.poll-interval}")` -> :318
        `provider.generate(ModelTask.TRANSLATOR, ...)`.
      * The metering topology stated in DigestRenderer.java:120-133 and
        application.properties:618-631 is accurate on both routes: the only
        caller of `renderSections` is DigestWorker.java:216, reached from
        DigestScheduler (no bucket drawn) and from
        DigestRetryService.retryDigest -> fallbackRerun (:141) -> execute, where
        RetryCommandHandler.java:552-558 draws the per-user LLM token then the
        D47 per-group sub-bucket with a refund on group rejection, behind a
        group-admin gate (:522-527), a `digest_enabled` gate (:536), an
        audit-before-effect row (:540) and the 2-minute cooldown
        (DigestRetryService.java:87, :108-112). "TWO ROUTES" is exhaustive.
    Nothing in the rewritten text is false, overstated or understated in the
    direction that would inflate an operator's remote-routing consent, so
    neither prior finding is re-reported.
  - Pre-existing enumeration gap, NOT attributable to this diff and therefore
    not filed: §Secrets handling still does not name the /summary display-hit
    leg (M1-747, ClusterBlockRenderer.java:130-134), which sends the same
    source-authored feed headlines to `ModelTask.TRANSLATOR` on a user-initiated
    trigger. The new bullet's honesty caveat is scoped to "the unattended legs
    named above", so an ATTENDED leg stays both undisclosed and uncaveated. The
    residual harm is small — the data class that leg carries is exactly the one
    this diff's bullet now discloses (a `DisplayHeadline` output, title-or-body
    prefix, bounded at 200 chars by DisplayHeadline.MAX_LENGTH) — and round 1
    already scoped it out as a merged ticket's leg. Flagged so the user can
    decide whether M1-758's disclosure text should name four legs, not three.
  - Phrasing, not a finding, direction is conservative: "the digest's own prose
    and roll-up legs dominate it" holds in the ordinary shapes (FULL: up to 40
    prose translations vs 0 headlines; BRIEF: up to `max-categories` roll-ups vs
    0 headlines; NORMAL with a lead: up to `leadSize` + `max-categories`
    vs <= 5) but is not universal: a NORMAL-mode digest with one surviving
    section and fewer than `leadMinimum` clusters spends ONE roll-up translation
    against up to five headline translations. The sentence exists to stop an
    operator sizing consent DOWN from the headline budget, so the corner case
    errs safe; noted only because the round-3 brief asks for sentence-level
    scrutiny of the remediation text.
  - Round-1 finding 2's MAIN claim re-checked and again NOT re-filed:
    §Rate limiting (security.md:1727-1729) still names "the aggregate system LLM
    budget" as the backstop for digest cost, and no such control exists in code.
    Attribution is unchanged — the leg this diff adds is the ONLY per-render
    bounded generative consumer on the digest path, so the diff strictly
    narrows the gap it would be blamed for. Two adjacent observations recorded
    rather than filed, both "code stricter than spec": (a) the new
    `infochat.digest.translation-max-per-render` budget appears nowhere in
    §Rate limiting, unlike the M1-746 and M1-755 translator legs which each got
    an entry there, so removing the budget later would need no spec amendment;
    (b) DigestRenderer.java:114-115's "10 per day (two slots)" worst case counts
    the scheduled route only — a section-less slot plus a `/retry --digest` every
    cooldown window adds renders — but the same javadoc's next paragraph
    discloses that route and its meters, and `retryDigest` re-renders only when
    no persisted sections exist (DigestRetryService.java:137-141), so the figure
    is not misleading in context.
  - Verified DELIVERED on the new path (recorded so a later audit does not
    re-derive them): the translator's output is re-sanitized through the
    closed-list pass with the bound -> flatten -> sanitize order owned by
    `DisplayHeadline.prepareTranslatedHeadline` (TranslationPipeline.java:247),
    and the cache-hit path applies no post-sanitize rewrite beyond truncate +
    bundle marker (:221-223, :300-309), so §LLM output sanitizer's "periodic
    group digests" surface stays covered and DigestWorker's persisted-bytes-are-
    post-sanitize invariant (DigestWorker.java:270-280) still holds for the
    translated line; the sanitize unit stays ONE author's field on both calls
    (DigestRenderer.java:837 via `DisplayHeadline.of`, then one headline per
    pipeline call), preserving §"Flag position mirrors the parser's own scan";
    the translator prompt keeps the per-call random `{{id}}` delimiter filled in
    a single scan (LlmTranslationProvider.java:79-96), which matters because
    this leg newly feeds raw source-authored text into that prompt; the digest's
    cache partition `hit/group/<groupId>/<lang>`
    (DigestRenderer.java:851-852 via the shared
    `TranslationPipeline.displayHitCacheLanguage`, never a re-derived copy) is
    disjoint from the /saved leg's `hit/saved/<userId>/<lang>`
    (SavedCommandHandler.java:437-438) and from the prose leg's bare
    language-code keyspace, so no per-user D13 state can be read by a group
    broadcast and no unflattened prose value can be read into a headline
    position; `translatesUnderThisScope` (DigestRenderer.java:895-902) omits the
    pipeline's ISO-639 shape gate, but the pipeline re-gates before any
    `Locale.of` (TranslationPipeline.java:204-210), so a malformed
    `source.language` can only waste a budget slot, never reach the prompt's
    instruction region — and that column is operator/admin-written, not
    attacker-reachable; the degraded arms make zero translator calls
    (DigestRenderer.java:784-794, DegradedDigestRenderer javadoc), matching
    §Failure handling's degraded pin, and the new test asserts it against a
    non-degraded control arm rather than a fixture that could never translate;
    DigestPostCollector's two SELECTs (:157-173, :177-197) grew only their
    projection list — the D59 world predicate, the parameter binding and the
    absence of DISTINCT/GROUP BY are untouched, so the added `s.language` column
    changes no row set; and no test assertion that pinned a security property
    was deleted or retargeted (the test edits are signature threading plus new
    coverage for the en no-op, unknown-language no-op, budget bound, cache-hit
    exemption and degraded skip).
