---
target: M1-468 --in-progress
date: 2026-06-27
base: 02be0840d54d2fe8786e7e5d84125718565cf5d1
head: working-tree (pre-commit, in-progress audit)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  Doc-only ticket (security.md + 07-deployment.md). Adversarial audit found no
  gap between the threat model and the diff. One out-of-model observation
  recorded below (advisory; the user decides whether to extend the model). No
  remediation required before commit.
---

RED-TEAM VERDICT: CLEAN

FINDINGS: (none)

OUT-OF-MODEL:
  - Phantom / unreachable bootstrap admin lockout (the very thing this diff
    documents). A reachable co-admin can `/revoke-admin` or ban the other
    reachable admins down to only a phantom admin whose seeded `contact_id`
    never byte-matches inbound, leaving the deployment locked out of admin
    while the global last-admin trigger still believes one admin remains
    (docs/spec/security.md:364-386). This is an availability/lockout hazard,
    but it is outside the documented threat model: (a) the phantom only
    exists because of a mistyped OPERATOR-SET bootstrap contact id, and
    operator-set config is trusted per §Threat model ("Operator-set config
    (properties files, bootstrap JSON) is trusted"); and (b) the actor who
    weaponises it is "already a trusted bot admin," not an untrusted
    adversary. Per the audit rules, out-of-model attacks (here: trusted-config
    misconfiguration + trusted-insider abuse) are noted, not scored as
    FINDINGS. The diff is a pure-documentation change (security.md spec text +
    07-deployment.md operator runbook + STATUS/ticket bookkeeping) that
    records this residual risk and the operator detect/recover procedure; it
    introduces no code, no SQL/trigger change, and weakens no enforced
    defense, so it delivers exactly what it promises and creates no new
    promise-vs-delivery gap. A reachability-aware "confirmed admin" count that
    would close the gap is correctly deferred behind a spec amendment
    (schema column + intake write + trigger change), not silently bolted on.
