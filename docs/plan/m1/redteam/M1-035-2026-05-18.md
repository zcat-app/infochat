---
target: M1-035
date: 2026-05-18
base: 6378e6dd91a4f942d4e72f743c0d6e292e3442ea^
head: 6378e6dd91a4f942d4e72f743c0d6e292e3442ea
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  M1-035's diff is test-only: one new @QuarkusTest IT (AdapterRouterIT.java)
  plus STATUS.md regen and the umbrella ticket's review-block frontmatter.
  No production-code path, authorization gate, intake logic, audit-log
  behavior, rate-limiter, ban check, or LLM tool surface is changed by
  this diff. The handler / auto-register / registry / router production
  code landed in subtickets M1-035a/b/c/d, each of which was red-teamed
  individually (see docs/plan/m1/redteam/M1-035a-2026-05-17.md and
  M1-035b-2026-05-17.md). The threat-model promises (T1–T9) are
  unaffected by the umbrella IT.

  Two OUT-OF-MODEL advisories are noted below. Neither is converted
  into a remediation ticket here; both are surfaced for the user's
  review:

  1. The IT asserts `registration_state='invited'` for a first-DM
     auto-registered user that arrived with no invite code. Per
     security.md §Authorization step 2 + §Invite-code registration,
     an unknown DM contact should hit the fixed "access requires an
     invitation" reply path and NOT be registered. The 'invited'
     state is reserved for invite-code-consumed DM users. This is
     a behavioural question about the production wiring landed in
     M1-035d (`AutoRegisterService` invocation from the inbound
     router) — outside this diff but worth a retrospective look at
     whether M1-035d's intake path implements the spec's step-2
     invite gate, or whether the umbrella's MVP-§3 acceptance
     deliberately pre-dates T2-A invite-gating. (The umbrella's
     `out_of_scope` block explicitly defers invite-gating to T2-A;
     the question is whether the spec's step-2 gate is meant to
     hold uniformly or whether T2-A introduces it.)
  2. The test profile sets `infochat.adapters.inmemory.allow-low-trust=true`
     to satisfy AdapterRegistry's LOW-trust opt-in gate. Out-of-model
     because the override is `@TestProfile`-scoped and cannot leak
     into a production deployment; surfaced so the user can confirm
     no production config path (env var, operator-set property)
     can satisfy the same gate without intentional opt-in.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
- The test asserts `registration_state='invited'` for a first-DM auto-registered user (file `/home/ubuntu5/Projects/quarkus-projects/infochat/infochat-provider/src/test/java/io/infochat/provider/messaging/AdapterRouterIT.java:147`). Per `docs/spec/security.md` §Authorization model step 2 and §Invite-code registration, an unknown DM contact whose first message is not a valid invite code should receive a fixed "access requires an invitation" reply and **no** registration should occur. The `'invited'` value is the state expected *after* a valid invite consume — not after a bare `/help` DM. This is a possible signal of a production gap landed in M1-035d (DM auto-register without invite-code gate), but that production code is not in *this* diff. Surfaced here so the user can decide whether to retrospectively re-audit M1-035d's intake-gate path against §Authorization step 2 (invite required for unknown DM contacts).
- The `MvpProfile.allow-low-trust=true` override sets the in-memory adapter (LOW-trust per messaging.md) into the operator-opt-in posture inside tests. Out-of-model because it is `@TestProfile`-scoped (cannot affect production startup); flagged so the user can confirm `AdapterRegistry`'s gate 6 (LOW-trust opt-in) cannot be unintentionally satisfied through a similar config path in production.

Files referenced:
- `/home/ubuntu5/Projects/quarkus-projects/infochat/infochat-provider/src/test/java/io/infochat/provider/messaging/AdapterRouterIT.java`
- `/home/ubuntu5/Projects/quarkus-projects/infochat/docs/spec/security.md`
- `/tmp/m1-035-redteam.diff`
