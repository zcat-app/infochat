---
target: M1-174
date: 2026-06-06
base: main (69b4cc6)
head: working tree of branch m1/M1-174-grant-admin-intent-coverage (pre-commit, --in-progress)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  Pre-commit audit (--in-progress) run after round-1 APPROVE, before
  /m1-tick commit. CLEAN — no findings to disposition. The single
  out-of-model observation (audit_log growth from authorized probe
  attempts now leaving surviving GRANT_ADMIN_INTENT rows) is the
  intended trade-off of the ticket and requires no action; noted for
  operator awareness only.
---

# Red-team audit: M1-174 (2026-06-06)

Verbatim threat-actor verdict:

RED-TEAM VERDICT: CLEAN

Audit notes (evidence for the verdict, not findings):

- **Intent-row coverage is complete for every state-disclosing refusal leg.** The three probe replies that disclose target state (`error.contact_not_registered`, `error.grant_admin.banned_target`, `error.grant_admin.already_admin`) all fire inside `executeGrant` (steps 5c–5e, `infochat-provider/src/main/java/app/zcat/infochat/provider/command/GrantAdminCommandHandler.java:296-314`), which is only reachable after the unconditional `insertIntentAudit` at line 246. The intent write throws on failure (line 415-420), so no probe reply or `is_admin=TRUE` UPDATE can ever occur without a committed `GRANT_ADMIN_INTENT` row — fail-closed in the correct direction.
- **Refusals that leave no audit row are spec-conformant.** The row-less legs (group scope, missing arg, non-admin actor, probation) disclose nothing about the target and fall at spec steps 6–7, which precede step 8 "Audit-log the intent"; the spec's 7→8→9 ordering explicitly puts the permission check before the intent log, and refusing unauthorized senders before the append-only write is the right anti-flood direction.
- **The M1-046 PERM-ESCAL closure is not weakened.** The new step-3 pre-check (lines 222-229) is refuse-only on a plain MVCC read; execution authorization still happens exclusively at the in-tx `SELECT ... FOR UPDATE` gate (lines 273-278). The only residual race is a spurious intent row for an attempt the transaction then refuses — intent-without-effect, the benign direction, and documented in-code.
- **No effect-without-intent path.** Step 4 strictly precedes step 5 and throws on failure; the success-path test asserts the request_id correlation between the committed `GRANT_ADMIN` effect row and exactly one `GRANT_ADMIN_INTENT` row.
- **No injection surface added.** All new SQL goes through `PreparedStatement` (`AuditLogWriter.java:105`); `details_json` values pass `JsonEscaper` or are boolean literals; the pre-existing `SET LOCAL` concatenation at line 281 interpolates a DB-typed `UUID`, not attacker bytes, and is unchanged by this diff.
- **No migration needed for the new enum value.** `audit_log.action` is unconstrained `TEXT NOT NULL` by documented design (V5__identity_audit.sql:26-29, 256 — verb catalogue is comment-documented, no CHECK), so `GRANT_ADMIN_INTENT` inserts cannot fail on a stale constraint.
- **Secrets handling upheld.** Both new/changed exception messages redact the contact id via `ContactIds.redact` (lines 346, 419), consistent with "Contact IDs are logged in redacted form ... outside the audit log"; the full contact id lands only in `audit_log` itself, which the spec permits (the Provider reads through the redacting `audit_log_view`).
- **Pattern parity verified.** The implementation byte-for-byte mirrors the already-landed `REVOKE_ADMIN_INTENT` structure in `RevokeAdminCommandHandler.java` (step-3 pre-check at 222, pre-tx intent at 245, auto-commit-connection `insertIntentAudit` at 413), so this diff introduces no novel mechanism beyond extending established intent-row coverage to the mirror command.

OUT-OF-MODEL:
- An authorized (or compromised) bot admin can now grow the append-only `audit_log` with one surviving `GRANT_ADMIN_INTENT` row per probe, where the refusal legs previously rolled back row-less. This is the intended trade-off of the ticket (probe visibility), inbound volume is bounded by the transport-level rate cap (spec step 1.5), and audit-log storage growth from authorized actors is not a threat the model defends against — flagged only so the operator is aware the audit table's growth rate now includes refused grant attempts.
