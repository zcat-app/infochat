---
target: M1-651
date: 2026-07-18
base: 2f809f59 (fork point / main tip)
head: working tree on m1/M1-651-guard-helptier-against-the-spe (uncommitted)
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 2
out_of_model_count: 3
disposition: >-
  User chose to fix BOTH findings in-branch (2026-07-18). Finding 1
  (parser subset-truncation) is a defect in this diff and was reproduced
  mechanically: a blank line mid-bullet truncates the guarded set 19 -> 8
  and a nested "- " sub-bullet truncates it to 9, both passing the
  non-emptiness vacuity guard. Finding 2 (group-admin tier unguarded) was
  the ticket's own out_of_scope bullet 1; fixing it requires an
  escalate -> refine to widen scope, since the dual-tier mapping was the
  deferred design decision. That mapping was verified determinate: the
  spec's own "in groups" qualifier discriminates USER_OR_GROUP_ADMIN from
  GROUP_ADMIN and matches CATALOGUE exactly for all 8 entries.
  NOTE ON DIFF RANGE: the standard --in-progress form (main...branch)
  yields an EMPTY diff here because the branch carries zero commits; the
  audit was driven against working-tree-vs-fork-point instead.
---

RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: security.md §LLM output sanitizer, "Match-set derivation": "The
              sanitizer's match set is **derived from the closed privileged-tier
              list at spec level** (`commands.md` §Permission model — 'Closed
              list of privileged-tier commands'), not from the design-tier
              per-actor matrix and not hand-maintained. ... CI fails on a
              mismatch ... Because the closed list is spec, adding or removing a
              privileged-tier command is a spec amendment that forces a paired
              sanitizer update; this makes 'admin commands never leak through
              LLM output' a structural property of the codebase rather than a
              discipline." The anchor list itself (commands.md:1295-1302) commits
              that the closed set "cannot silently shrink across versions." The
              diff makes HelpTier a second consumer derived from that same closed
              list, but derives it in a way that CAN silently shrink.
    GAP: infochat-provider/src/test/java/app/zcat/infochat/provider/command/CommandCatalogueParityTest.java:257-304
         (`parseBotAdminBaseCommands`). The only completeness check is the
         non-emptiness assertion at lines 167-171, whose own comment claims "a
         spec restructure must not silently disable the guard." Non-emptiness
         prevents TOTAL disablement, not PARTIAL truncation. The bullet-end
         detection at line 292 breaks on the first line whose `strip()` is empty,
         starts with "- ", or starts with "#". The bot-admin bullet currently
         spans commands.md:1304-1312 (9 lines, 26 tokens, 19 base commands); any
         one of those break characters appearing mid-bullet after a future spec
         reformat silently reduces the guarded set to a non-empty subset and the
         test still passes. Contrast the sibling parser this diff deliberately
         declines to share with (line 251-255): `LlmOutputSanitizerTest`
         :396-445 stops only on blank line / heading / the closing-prose
         sentinel, and — decisively — its consumer at :343-354 asserts
         BIDIRECTIONAL equality (`onlyInSpec.isEmpty() && onlyInRuntime.isEmpty()`)
         against the hardcoded runtime CLOSED_LIST, so a truncated parse there
         fails loudly via `onlyInRuntime`. The new guard has no reverse-direction
         check and no count pin (lines 163-166 explicitly decline to pin a count),
         so subset-truncation is indistinguishable from a correct parse.
    REPRO: Not adversary-reachable at runtime; the attack is on the CI guard's
           durability. (1) A spec editor reformats the closed list for
           readability — e.g. splits the long comma-run with a blank line, or
           promotes the `/invite …` family to a nested "- " sub-bullet inside the
           bot-admin bullet. (2) `parseBotAdminBaseCommands` now returns only the
           tokens before the break — say {grant-admin, revoke-admin, ban} instead
           of all 19. (3) `assertFalse(specBotAdmin.isEmpty())` passes;
           `misTieredBotAdminCommands` and `specCommandsMissingFromCatalogue` are
           evaluated over the 3-element subset; the test is green.
           `LlmOutputSanitizerTest.matchSetEqualsSpecClosedList` still catches its
           own drift via the reverse direction, so the two closed-list consumers
           diverge with no signal. (4) A later ticket adds or edits a bot-admin
           command that fell outside the truncated window and types it
           `HelpTier.USER`. M1-646's name-parity axis is satisfied (a CATALOGUE
           entry exists), the tier axis no longer sees the command, and
           `HelpCommandHandler.visible()` (:346-351) returns true for every
           non-probation caller. Any registered post-probation non-admin then
           reads the command's short line via `/help` and its full argument
           syntax via `/help <cmd>` (`composeDetail`, :243-258). Execution still
           requires `is_admin=true`, so this is disclosure and recon, not
           escalation — but it is precisely the failure mode this diff exists to
           close, re-opened silently.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: INFO-LEAK
    SEVERITY: low
    PROMISE: security.md §Authorization model: "Two admin tiers (decision D9): **Bot
              admin** — global ... **Group admin** — one group only." And
              security.md §LLM output sanitizer, "Match-set derivation": "Every
              command in the **bot-admin and group-admin** tiers of that closed
              list is in the sanitizer set." The threat model treats both tiers of
              the closed privileged-tier list as equally load-bearing; the diff
              derives a guard from one tier only.
    GAP: CommandCatalogueParityTest.java:100 pins
         `BOT_ADMIN_BULLET_LABEL = "**Bot-admin only:**"` and :271-274 anchors the
         parse on that single label; :292 then breaks the scan at the next "- "
         line, which is exactly the "- **Group-admin (or bot admin acting in the
         group):**" bullet at commands.md:1313-1317. That bullet's eight entries
         (`/add-source` in groups, `/unfollow-source` in groups,
         `/follow-all-sources` in groups, `/lang` in groups, `/group-timezone`,
         `/digest`, `/follow-tag` in groups, `/unfollow-tag` in groups) are never
         compared against `HelpTier.GROUP_ADMIN` / `USER_OR_GROUP_ADMIN`. The
         guard's own javadoc (:128-155) enumerates its known limitation as
         "a new privileged command never declared privileged anywhere" and does
         not name this second asymmetry. The only existing coverage of the
         group-admin tier is hand-enumerated per-command assertions in
         HelpCommandHandlerTest.java:128-144 (`group-timezone`, `add-source`,
         `follow-tag` only), which by construction cannot cover a command name
         that does not yet exist.
    REPRO: (1) A spec amendment adds a new group-admin command to the group-admin
           bullet of commands.md §Permission model. (2) The implementer adds the
           required `HelpCommandHandler.CATALOGUE` entry — M1-646's name-parity
           axis now DEMANDS one — and types it `HelpTier.USER`, the same slip the
           bot-admin half of this diff exists to catch. (3)
           `botAdminSpecCommandsCarryBotAdminHelpTier` never reads the group-admin
           bullet, so it is green; `everyCommandHandlerHasAHelpCatalogueEntry` is
           green; the hand-written group-tier assertions do not mention the new
           name, so they are green. (4) Any plain (non-group-admin, non-banned,
           post-probation) member of an approved group sends `/help` and sees the
           command's short line — `visible()` :347 returns true unconditionally
           for `HelpTier.USER` in group scope — then sends `/help <cmd>` and
           receives its full usage and examples block. The system should not have
           allowed it: the spec places that command in a privileged tier, and the
           diff installs a mechanism that would have caught the identical mistake
           had the command been in the other bullet of the same list.
    SUGGESTED-FIX-CLASS: trust-boundary-tightening

OUT-OF-MODEL:
  - The guard's self-disclosed limitation (CommandCatalogueParityTest.java:146-155)
    — a privileged command that is never declared privileged in the spec closed
    list at all, and so escapes both this guard and
    `LlmOutputSanitizer.CLOSED_LIST` — is acknowledged in the diff and would
    require deriving each command's tier from its handler's own authorization
    code. security.md makes no commitment that HelpTier is derived from handler
    authorization logic, so this is outside the documented model; flagged only so
    the user can decide whether to extend it.
  - `HelpCommandHandler.visible()` :343-345 short-circuits the probation branch to
    `commandPermissions.allowedDuringProbation(entry.command())` and never
    consults `tier()` at all, so the new tier guard does not constrain the
    probation surface. Audited and found compensated, not a finding:
    `CommandPermissions.ALLOWED` (:53-64) is a closed eleven-name allowlist
    enumerated verbatim from security.md §Slow-start tier, contains no admin
    command, and fails closed on unknown names — so a mis-tiered admin command
    cannot surface to a probation caller through this path regardless of its tier.
  - The `TIER_EXEMPT_BASE_COMMANDS = {list-sources}` exemption (:119) was checked
    and found sound: the bot-admin `--all` / `--include-deleted` flags render from
    the separate `HELP_CMD_LIST_SOURCES_USAGE_ADMIN` key behind an explicit
    `caller.botAdmin()` gate (HelpCommandHandler.java:250-253), independently
    covered by HelpCommandHandlerTest:414, and the exemption set is pinned by an
    equality assertion at :158-160 so widening it cannot be silent.
