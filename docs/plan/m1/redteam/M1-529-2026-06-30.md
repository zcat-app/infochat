---
target: "M1-529 (--in-progress, working tree)"
date: 2026-06-30
base: 5d94d2d12eae507ba2e4a1c940e7f1200915fdfd
head: "working-tree (m1/M1-529-remote-backend-local-embeddings, uncommitted)"
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  CLEAN — no findings against M1-529. One out-of-model item: the backend-switch
  secret-retention asymmetry (the ollama/llamacpp branches of 4-llm.sh never
  clear a stale infochat.llm.<task>.api-key property or the INFOCHAT_LLM_API_KEY
  value in secrets.env when an operator re-runs the wizard and switches back from
  the remote backend). It is pre-existing (not introduced or changed by this
  diff), operator-side and not adversary-reachable, on a chmod-600 config file
  the threat model classifies as trusted. Sibling of pending M1-530 (stale
  bootstrap-admin credential cleanup in a different setup script). Recommend
  considering the symmetric 4-llm.sh cleanup within M1-530's scope rather than a
  standalone ticket; not a blocker for M1-529. Advisory only — not auto-filed.
---
RED-TEAM VERDICT: CLEAN

FINDINGS: (none)

OUT-OF-MODEL:
  - Backend-switch secret retention (operator-side, not adversary-reachable).
    This diff is careful to sed-delete a stale infochat.embeddings.api-key on the
    remote branch (prod/scripts/4-llm.sh:475). The symmetric case is NOT closed
    and is pre-existing (outside this diff): re-running the wizard and switching
    FROM remote back to the `ollama` branch leaves the prior
    infochat.llm.<task>.api-key=${INFOCHAT_LLM_API_KEY} property lines and the
    INFOCHAT_LLM_API_KEY value in secrets.env in place — the ollama branch
    (lines 266-272) never clears them. The lingering key now references an
    unused local endpoint. This is operator-config hygiene on a chmod-600 file
    the threat model classifies as trusted (§Threat model: "Operator-set config
    ... is trusted"), analogous to the operator-misconfiguration risks the spec
    itself documents (e.g. §Authorization model phantom-admin). It is also
    pre-existing and not introduced or changed by this diff, so it is not a
    finding against M1-529 — flagged only so the operator can decide whether a
    follow-up ticket should make backend-switch secret cleanup symmetric.
    (Note: a pending ticket M1-530 already targets a related stale-credential
    cleanup gap in a different setup script.)
