---
target: M1-715
date: 2026-08-01
base: 7f9f621215e2b0303dcc8d7a0255bf93e4f30966
head: working tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  Re-audit after in-ticket remediation of the two r1 low findings:
  the [refused-action] structured refusal marker (prompt + parse branch +
  summarizer.refusal notification class) and the switch-llm.sh
  summarizer ingest-side disclosure line. Both r1 findings verified
  closed by the auditor; no new findings on the remediation surface.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Attacker-coaxed refusals: a feed publisher who learns the (now shipped, effectively public) "[refused-action]" convention can craft body text that nudges the model into answering with the refusal marker, forcing body_summary=NULL and reverting that post's embedding input to the first-800-char fallback. This is quality degradation to the pre-M1-715 baseline, not a new capability — the threat model's own black-box admission ("LLMs ... can be coaxed into emitting attacker-chosen output") accepts model-compliance residuals once the delimiter + refusal-marker + cap defenses are delivered, and they are. Noted so the user can decide whether a canary/second-look pass is worth modelling.
  - Per-post summarizer LLM cost amplification: every over-threshold ingested post now costs 1–2 generative LLM calls, so a hostile feed publishing many long posts drives summarizer spend. Bounded by the same fetch scheduling, per-source failure/auto-disable handling, and the worker's serial one-call-at-a-time, 4-posts-per-tick shape that already bound the tagger/entity/classifier per-post calls; the incremental exposure is one more call of the same existing class, not a new unbounded surface.
