---
target: M1-657
date: 2026-07-18
base: 46dad2864d8649401ac8d4fcb7f5fe0e36ceaf31
head: working tree on m1/M1-657 (r1)
verdict: CLEAN
findings_count: {critical: 0, high: 0, medium: 0, low: 0}
out_of_model_count: 0
disposition: |
  Pre-commit audit of the /approve-group gate-ordering fix. Confirms the
  is_admin gate now precedes parseGroupId (mirroring
  RejectGroupCommandHandler), so error.group_not_found — which
  interpolates the raw argument — is unreachable below bot admin. The
  in-tx SELECT FOR UPDATE re-check in executeApprove is retained
  (M1-046 TOCTOU closure). Closes the r2 finding raised against M1-656.
---
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - None. The reordered gate closes the M1-656 r2 reflect-before-authorize
    hole in ApproveGroupCommandHandler: the null-check and the
    lookupActor + !isAdmin gate (handle(), lines 122-128) both return the
    static error.admin_only bundle string and execute BEFORE parseGroupId
    (line 133) and the reflecting error.group_not_found interpolation of
    parseGroupIdRaw (lines 135-137), so a non-admin can no longer coax the
    raw argument back through the friendly error. The in-tx SELECT FOR
    UPDATE admin re-check (executeApprove, lines 156-158) is unchanged and
    remains the authoritative M1-046 TOCTOU closure; the new non-locking
    lookupActor (lines 227-230) can only suppress the reflect, never
    authorize the mutation, so it introduces no gap. The remaining
    error.group_not_found reflect (lines 174-177) is behind that in-tx
    admin gate and interpolates a canonical parsed UUID, not raw text. The
    commands.md edit now states a true property: /approve-group's gate
    precedes its parse (mirroring RejectGroupCommandHandler, which was
    already correct at lines 129-153), and the other listed bot-admin-only
    reflecting errors were asserted-fine pre-diff and were never in the
    removed "known violation" carve-out. No existence oracle (uniform
    error.admin_only), no MessageFormat argument re-interpretation, no
    audit regression (non-admin attempts short-circuit before step-8
    audit, matching pre-diff behavior and the added test's assertion).
