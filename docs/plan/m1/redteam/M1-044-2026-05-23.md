---
target: M1-044
date: 2026-05-23
base: main
head: m1/M1-044-invite-intake-umbrella (working tree — pre-commit)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  CLEAN. The diff is purely a test-only addition (one new IT class
  `InviteIntakeRoundtripIT.java`), with no production-source touches.
  The IT exercises the documented intake-step ordering and asserts
  the spec contracts (invite-required reply for unknown DMs, ban-
  fixed reply for banned users, preban-revokes-pending-invite,
  /unban-deletes-preban via the V5 stored procedure). All seven
  steps pin spec promises; none weakens them.

  Two OUT-OF-MODEL observations (advisory only):
    - The cleanup() method temporarily DISABLEs trg_audit_log_no_update
      and trg_audit_log_no_delete via ALTER TABLE, then re-enables
      them in a finally block. This requires elevated DB privileges
      that the production Provider role does NOT have (the role is
      INSERT-only on audit_log per docs/spec/security.md §DB roles).
      The pattern matches pre-existing tests (ConfirmFlowIT and the
      M1-044c handler tests). Out-of-model because the threat model
      treats the test DB role as operator-controlled — the production
      Provider role cannot reach this path.
    - The DELETE FROM audit_log WHERE target_contact_id LIKE
      'm1-044-roundtrip-%' pattern (line 125) could in principle
      collide with another test that shared the same prefix; current
      prefix is unique to this IT, so no collision exists. Pure
      test-hygiene concern, not a production-security boundary.

  No findings to remediate. No tickets to open. The two OUT-OF-MODEL
  notes are advisory; the user may extend the threat model to
  explicitly document test-role privilege expectations if desired
  (separate spec discussion, not part of this audit).
---

# Red-team audit: M1-044 — 2026-05-23

**Target:** M1-044 (pre-commit, working tree on branch `m1/M1-044-invite-intake-umbrella`)
**Base:** `main`
**Head:** `m1/M1-044-invite-intake-umbrella` (working tree — ticket is in-review pre-commit)

## Verbatim threat-actor verdict

```
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - The test cleanup temporarily disables `trg_audit_log_no_update`
    and `trg_audit_log_no_delete` via `ALTER TABLE`
    (infochat-provider/src/test/java/app/zcat/infochat/provider/messaging/InviteIntakeRoundtripIT.java:121-141).
    This requires table-owner DB privileges and only works because
    the test DB connection runs as a superuser-like role, not the
    production Provider role (which the spec restricts to `INSERT`-
    only on `audit_log`). The pattern is bracketed in try/finally
    and matches the convention already established in pre-existing
    tests (`BanCommandHandlerTest`, `UnbanCommandHandlerTest`,
    `InviteCommandHandlerTest`, `ConfirmFlowIT`). Production Provider
    code cannot reach this path because its DB role lacks
    `ALTER TABLE` privileges. Out-of-model because the threat model
    assumes the operator controls DB role grants
    (docs/spec/security.md §DB roles), and the test fixture connects
    as the migration/admin role by design, not as the Provider role.
  - The `cleanup()` method's
    `DELETE FROM audit_log WHERE target_contact_id LIKE
    'm1-044-roundtrip-%'` (line 125) and similar wildcard deletes
    could in principle match unrelated rows if another test ever
    shared the same prefix. This is purely a test-hygiene concern
    (cross-test interference, not a production security boundary).
    The prefix is unique to this IT class, so no current collision
    exists.
```

## Adversary's analysis summary

The diff strictly adds a test that pins existing spec promises:
- The file is a test, not production code. It does not modify any
  auth, intake, or audit path.
- All SQL uses parameterized PreparedStatement — no SQL injection
  risk in the test.
- The test asserts the spec contracts (invite-required reply, no
  users row written, audit row written, etc.) — this is
  verification, not weakening.
- The trigger disable is per-test, with try/finally, and only runs
  against the test DB owner role.
- The test does NOT modify any production source.

No production behavior changes, no new authentication paths, no new
tool surfaces, no new SQL queries in production code, no new LLM
call sites.

Falsifiers checked:
- Could the test be hiding a production gap? — The test exists to
  demonstrate the gap was closed by prior tickets (M1-044a, b, c).
  The diff only adds the test.
- Could the trigger-disable in test cleanup leak into production? —
  No, it's local to the test JVM's DB connection, and ALTER TABLE
  in a test transaction does not propagate to production deployments.
- Could the per-test DELETE of audit_log violate the audit-evasion
  threat? — The threat model commits to audit immutability in the
  production trust boundary, where the Provider role has INSERT-only.
  The test fixture runs against the test DB with broader privileges;
  this is standard test infrastructure.
- Could the `LIKE` patterns let cleanup accidentally delete other
  rows? — The prefix `m1-044-roundtrip-` is unique and test-scoped;
  even if it deleted broader rows, this is the test database, not
  production.

The diff is a pure test-only addition. No production security
commitments are broken or weakened.
