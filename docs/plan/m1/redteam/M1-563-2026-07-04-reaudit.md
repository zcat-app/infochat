---
target: M1-563 --in-progress (re-audit after redteam-finding refine)
date: 2026-07-04
base: main
head: m1/M1-563-leave-cleanup-posture
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  Re-audit of the branch tip after the in-branch remediation of the
  first audit's MEDIUM (PERM-ESCAL, M1-563-2026-07-04.md): the refine
  widened scope to docs/spec/schema.md + docs/design/02-schema.md and
  the round-2 diff reconciled both with the amended posture. CLEAN —
  the internal contradiction is resolved. One out-of-model advisory
  item (see body); assessed below in the ticket's redteam_audits note.
RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - The underlying behavior the amendment documents — on both v1
    production adapters (supportsMembershipEvents=false) a departed
    group admin's group_membership row and is_group_admin flag persist,
    the auto-promote path never treats the group as admin-less, and the
    departed admin silently resumes group admin on rejoin — remains an
    attacker-relevant privilege-retention surface (e.g. an admin who
    leaves under scrutiny keeps the slot indefinitely until a bot admin
    notices). It is now an explicitly documented, accepted v1
    non-commitment in security.md §Authorization model with a bot-admin
    /demote remediation, so it is acknowledged residual risk inside the
    documented model, not a finding. Re-audit verification notes: the
    prior finding's stale over-promise is fully reconciled —
    docs/spec/schema.md:141-174 now scopes the soft-clear lifecycle to
    supportsMembershipEvents=true adapters, removes the unfireable
    per-user permanent-send-failure trigger, and qualifies the
    no-reclaim-on-rejoin promise with the non-commitment;
    docs/design/02-schema.md:198-206 mirrors it; the only remaining
    cross-reference (docs/spec/commands.md:957, "soft-cleared via
    left-group event" as a /promote ineligibility case) is consistent
    with the amended posture, and the /demote remediation's precondition
    (target row removed_at IS NULL, commands.md:953-958) is satisfied by
    the persisted departed-admin row, so the documented remediation is
    actually executable.
