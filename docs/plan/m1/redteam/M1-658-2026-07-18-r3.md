---
target: M1-658
date: 2026-07-18
base: 11abf0507ac1a7f00752b750d40df564fc169738
head: working tree on m1/M1-658 (r3 re-audit)
verdict: CLEAN
findings_count: {critical
out_of_model_count:  0, high: 0, medium: 0, low: 0}:2
disposition: |
  r3 re-audit after the spec-honesty fix. CLEAN: the security.md/commands.md
  wording no longer overclaims (exemption = residual risk, live instance named
  and tracked as M1-659); dispositioning the reply-surface fix to M1-659 is
  consistent with M1-658's deliberate error-only scope. Two OUT-OF-MODEL notes
  (persisting reply-surface gap, tracked; String.format/concat forms).
---
RED-TEAM VERDICT: CLEAN

Scope of this r3 re-audit (per the rendered prompt): (1) is the reworded
security.md/commands.md wording now HONEST — does it still overclaim any
proven-safe property it cannot back? (2) is dispositioning the live
/add-source --name reply reflection to M1-659, rather than fixing it in
M1-658, consistent with M1-658's deliberately error-only guard scope?
The guard code (InboundReflectionGuardTest.java) and the 50-entry baseline
are unchanged since r2 and were already audited CLEAN on the r1-hole-closed
/ baseline-honest claims; I did not re-derive those, but re-read the census
forms to ground the concatenation note below.

Claim (1) — spec honesty — PASS.
  The r2 overclaim was security.md asserting deterministic command output
  "interpolates no inbound-derived text" and that "M1-647/M1-656 removed
  those echoes" as a blanket over ALL deterministic output. The r3 reword
  (security.md:322 region, dd013af0) now:
    - frames the exemption conditionally ("safe only to the extent
      deterministic output is bot-authored"), not as an affirmative
      whole-surface claim;
    - explicitly separates the guarded ERROR surface from the not-yet-
      guaranteed REPLY/SUCCESS surface;
    - names the exact live instance (the /add-source --name display-name
      echo) and tracks it as M1-659;
    - labels the exemption a "residual risk on non-error deterministic
      output, not a proven-safe blanket."
  commands.md §Discovery (36e0904d) matches: it scopes the mechanical guard
  to error.* templates, states "a green guard means 'no unrecorded error
  reflection', not 'reflection is impossible'", names the live /add-source
  reply instance + M1-659, and states the underlying rule "binds success/
  reply templates too; only the mechanical guard is error-scoped." No
  affirmative proven-safe property is asserted over the reply/success
  surface. The wording no longer overclaims.

Claim (2) — disposition consistency — PASS.
  M1-658's census scope was narrowed (by recorded user decision) to
  error-keyed templates (^ERROR_ constants / ^"error." literals). The
  /add-source --name echo is a reply.* (success) template, so it is
  genuinely outside the error-only census by design — not an evasion of
  the guard, but a surface the guard openly discloses as out of view. The
  new M1-659 ticket faithfully captures the live vulnerability
  (AddSourceCommandHandler result.displayName() -> REPLY_ADD_SOURCE_FRESH_
  INSERT; SourceUpsertService.defaultDisplayName returns the --name
  override verbatim; CommandTokenizer unwraps quotes; reachable by a group
  admin, broadcast to group) and mandates a value-source fix (constrain at
  parse/upsert, not output-side byte-filtering). This mirrors the accepted
  M1-656 -> M1-657 disposition. Consistent.

No production source is touched by this diff (docs, the process/ticket
artifacts, and the test + baseline that are unchanged since r2), so the
diff introduces no new attack surface. The reworded threat-model section
now documents the one known reply-surface gap as tracked residual risk
rather than promising a safety it does not deliver — which is the honest
resolution of the r2 finding, not a re-occurrence of it.

FINDINGS: none.

OUT-OF-MODEL:
  - PERSISTING LIVE REFLECTION (tracked, not re-reported as a finding per
    the prompt's explicit instruction): the /add-source success reply still
    echoes the unconstrained --name override verbatim into
    REPLY_ADD_SOURCE_FRESH_INSERT (and into /list-sources via displayName),
    reachable by a group admin in an approved group and broadcast to every
    member. A group admin can plant a plausible /grant-admin <uuid> approved
    string that a bot admin might copy-paste. This is a genuine shipping
    deterministic-reply reflection, but it is (a) outside M1-658's
    deliberate error-only scope, (b) now documented as residual risk in
    security.md §LLM output sanitizer, and (c) tracked by ticket M1-659
    (remediates: M1-658). It is therefore an accepted-with-documented-
    residual-risk item pending M1-659, not a gap in THIS docs-only diff.
    Flagged so the user keeps M1-659 on the critical path — the residual
    risk is real until that ticket lands.

  - GUARD BLIND SPOT NOT SURFACED IN THE SPEC SUMMARY (hardening): the
    census recognizes only three placeholder-interpolation forms
    (MessageFormat.format, new Failure(...), the bare format(...) helper).
    A below-admin error reply assembled by raw string concatenation
    (bundleLoader.get(ERROR_X, lang) + rawToken) or via
    String.format(bundleLoader.get(...), rawToken) is NOT a
    placeholder-interpolation site and would pass the build green while
    reflecting inbound text into a friendly error. The HELPER_FORMAT
    pattern's negative lookbehind (?<![A-Za-z0-9_.]) deliberately excludes
    String.format, and no census form matches concatenation. This is
    explicitly disclosed in the test-class Javadoc (lines 65-68: "No such
    site exists today; the property to preserve is ...") and the spec frames
    the guard as incomplete ("not 'reflection is impossible'"; the taint-
    type complete fix is future work). Because the gap is disclosed, has no
    live instance, and the spec does not claim the guard is exhaustive, this
    does not rise to a promise-vs-delivery finding. Noted only so the user
    is aware the spec-summary phrase "a green guard means 'no unrecorded
    error reflection'" is, read literally, one notch stronger than the
    guard's actual (interpolation-only) coverage; the precise boundary lives
    in the Javadoc, not the spec. The deferred taint-type project (§3a)
    would close it.
