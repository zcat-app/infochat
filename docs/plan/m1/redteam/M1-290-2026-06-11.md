---
target: M1-290
date: 2026-06-11
base: a2514ca93f0d7ba8b69133e8e89c315398a7ef45
head: working-tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: |
  Audit ran on the in-progress working-tree diff (post-APPROVE, pre-commit)
  for security_relevant ticket M1-290. CLEAN: zero findings across all
  severities. One out-of-model advisory observation recorded in the verdict
  body below; advisory only, no remediation required for this ticket.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - U-56 rider (`ALTER TABLE scope_preferences DROP COLUMN digest_enabled`,
    V50__banned_admin_actor_checks.sql:289) is a schema change bundled into a
    security-hardening migration. It is unrelated to the banned-admin actor
    check and is not governed by any threat-model promise, so it is not a
    security finding. It is a scope-drift / engineering-rules concern (the
    column-drop should ride its own ticket or at minimum not be hidden in a
    SECURITY DEFINER migration). Flagged so the user can decide whether the
    bundling is acceptable. The comment claims zero readers/writers; if that
    is false the drop could break a digest-enable read path, but no
    threat-model commitment covers it.

Audit notes (no findings):
  - The diff hardens the actor predicate of three SECURITY DEFINER routines
    (delete_preban_user, approve_quarantine, reject_quarantine) from
    `is_admin = TRUE` to `is_admin = TRUE AND is_banned = FALSE`
    (V50:33,80,152). This matches the spec's live-admin definition
    (security.md §Authorization model: "a banned admin does not count as a
    live admin"; the last-admin counter is `is_admin = true AND is_banned =
    false`). The change strengthens defense-in-depth under the
    SQL-injection-foothold threat model; it does not leave any promise unmet.
  - `users.is_banned` is `BOOLEAN NOT NULL DEFAULT FALSE` (V5__identity_audit.sql:63),
    so `is_banned = FALSE` cannot evaluate to NULL and falsely reject a live
    admin — no availability regression and no three-valued-logic bypass.
  - The three V50 bodies carry forward the prior V45/V48 bodies verbatim
    (audit-before-effect ordering, SET search_path pin, FOR UPDATE locks,
    fetched_at-guarded post UPDATE, NOTIFY payloads, stage2_failed clear,
    actor_contact_id/actor_adapter denormalization). The only semantic change
    is the actor predicate. No security-relevant check was silently dropped
    by the CREATE OR REPLACE.
  - A sweep of all current-head migrations confirms these three procedures
    were the only SECURITY DEFINER routines whose actor check was
    `is_admin = TRUE` without `AND is_banned = FALSE`. Remaining `is_admin =
    TRUE` matches are last-admin trigger logic (already paired with
    is_banned-aware counting) or superseded earlier bodies of these same
    three procedures. No unhardened admin-actor gate remains.
  - New tests assert the banned-admin actor is rejected with the same error
    shape a non-admin gets (DeletePrebanUserAuditDenormIT,
    QuarantineActorCheckTest), so the existence-vs-no-access distinction is
    not leaked. No AUDIT-EVASION, PERM-ESCAL, AUTH-BYPASS, INFO-LEAK,
    INJECTION, or DOS gap is introduced or left open by this diff.
