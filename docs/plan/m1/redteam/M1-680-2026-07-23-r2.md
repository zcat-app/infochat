---
target: M1-680
date: 2026-07-23
base: c5aef23cb1ec86eeb76e57705be189d777db5336
head: working tree (uncommitted branch m1/M1-680-sanitizer-flag-position)
verdict: FINDINGS
findings_count:
  critical: 0
  high: 0
  medium: 1
  low: 1
out_of_model_count: 0
disposition: |
  RECONSTRUCTED RECORD. The original round-2 verdict file lived in target/
  and was wiped by `mvn clean`. The two findings were verified in-session
  and are summarized below:
    - medium/DOS: multi-anchor quadratic from the whole-line lazy regex
      (many command-word occurrences on one line made an unbounded lazy
      `find()` re-anchor and rescan to end-of-line, O(P x L)).
    - low/AUDIT-EVASION: `\r` was not treated as a token separator — the
      parser's `split("\\s+")` treats `\r` as whitespace while the line-
      scoped regex did not, so `/list-sources\r--all` dispatched but
      evaded the match.
  Remediation chosen by the user: replace the regex with a code tokenizer
  (the `redactFlagEntry` two-pointer scan). The round-3 audit followed the
  re-implementation and found the cross-line evasion that led to the
  message-scoped fix.
---
RED-TEAM VERDICT: FINDINGS

FINDINGS:
  - CATEGORY: DOS
    SEVERITY: medium
    PROMISE: |
      docs/spec/security.md §Trust boundaries item 9: a hostile or
      compromised LLM endpoint is in scope; the response body is capped,
      so pathological input cannot exhaust the JVM.
    GAP: |
      The line-scoped lazy regex for flag-bearing entries re-anchors at
      every command-word occurrence and rescans to end-of-line. On a line
      with P occurrences and length L, total work is O(P x L), so an
      attacker-chosen reply with many `/list-sources` anchors pins a
      worker thread. No watchdog or timeout wraps the sanitizer pass.
    REPRO: |
      A hostile endpoint returns a line consisting of `/list-sources `
      repeated many times with no `--all`. The lazy matcher restarts at
      each occurrence and walks the remainder of the line, producing
      quadratic CPU with no upper bound other than the body cap.
    SUGGESTED-FIX-CLASS: other

  - CATEGORY: AUDIT-EVASION
    SEVERITY: low
    PROMISE: |
      docs/spec/security.md §LLM output sanitizer, "Flag position mirrors
      the parser's own scan": the scan treats whitespace the way the
      parser's `split("\\s+")` treats it.
    GAP: |
      The line-scoped regex treated `\r` as an end-of-line boundary rather
      than whitespace, so `/list-sources\r--all` dispatched `all=true` but
      was not redacted.
    REPRO: |
      A hostile or injected reply returns `/list-sources` followed by a
      bare carriage return and `--all`. The parser tokenizes on `\s+`,
      setting all=true; the sanitizer regex does not match across the `\r`
      and ships the output verbatim.
    SUGGESTED-FIX-CLASS: input-sanitization

OUT-OF-MODEL:
  - (none)
