---
target: M1-705
date: 2026-08-01
round: 2
base: b8db467923432e4ad49a3eb30441cfd316aba708
head: working-tree
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 1
disposition: >-
  Re-audit of the post-remediation diff. Round-1 finding 2 (%remote-llm
  cheap cap == transport cap) was remediated in-branch (60 -> 40 with the
  ordering invariant documented) and the adversary verified it closed.
  Round-1 finding 1 (no key-space cap on cheapCommandBuckets) was
  falsified by the orchestrating session (stranger-limiter throughput
  bound + idle eviction compose a tight bound even under the chained
  regression); the adversary upheld the falsification. The gate's
  headless process hit a provider 403 (usage limit) on exit AFTER the
  verdict artifact was written; the on-disk verdict is complete and was
  accepted per verdict-file readback, and the contamination check ran
  manually (worktree and primary clean of stray writes).
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - Per-process bucket state, widened: all three M1-705 bucket families
    (cheapCommandBuckets, addSourceBuckets, quarantineBuckets in
    RateCapBucket.java) are in-memory ConcurrentHashMaps exactly like the
    pre-existing transport and group buckets, so a multi-replica Provider
    deployment would multiply every per-user and per-admin cap by the
    replica count. Round 1 carried the same note for the pre-existing
    buckets; this diff extends the shape by three more families without
    changing its nature. The documented threat model and deployment
    topology assume a single Provider process, so this stays out of
    model — flagged again only because the blast radius of a future
    multi-replica topology grew with this diff.
