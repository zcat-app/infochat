---
target: M1-235
date: 2026-06-09
base: c378bcf
head: working tree (uncommitted, branch m1/M1-235-delete-preban-user-audit-denorm)
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 2
disposition: |
  CLEAN. V45 is a pure audit-completeness fix: it restores actor_contact_id /
  actor_adapter to the delete_preban_user audit INSERT via the V5 JOIN pattern,
  carrying V24's guards (search_path pin, actor-admin EXISTS check, preban-state
  check, DELETE) forward verbatim. The change tightens audit coverage rather than
  loosening any boundary; the adversary found no gap between threat-model promise
  and delivery. Two OUT-OF-MODEL advisories recorded below — both predate this
  diff and are not findings against it:
    1. The trg_audit_log_actor_check GUC-unset branch (V24) is the load-bearing
       precondition for the attributability this migration restores; the GUC
       wiring lives in the Provider's /unban transaction setup (untouched here).
       Worth confirming that call path sets infochat.actor_id. Not actionable in
       this ticket (out of scope — only the migration is in scope).
    2. Spec/impl naming divergence: docs/spec/security.md:581 names the action
       UNBAN_DELETED_PREBAN_ROW while the enum, design notes, and all migrations
       (V5/V24/V45) use UNBAN_PREBAN_DELETE. The diff faithfully matches the
       existing implementation; this is a pre-existing spec-vs-design wording
       inconsistency, candidate for a separate `spec:` reconciliation, not a
       security gap.
---

RED-TEAM VERDICT: CLEAN

OUT-OF-MODEL:
  - The `trg_audit_log_actor_check` integrity trigger (V24) and the
    `infochat.actor_id` / `infochat.request_id` session GUCs are the mechanism
    that prevents audit-row actor spoofing. This diff relies on them but does not
    exercise them — if the Provider invokes `delete_preban_user` without first
    setting `infochat.actor_id` to the acting admin's UUID, the GUC-unset branch
    in `trg_audit_log_actor_check` (V24:104, "GUC-unset path: allowed") lets the
    procedure-supplied `p_actor_id` through unverified. That GUC-wiring is
    upstream of this diff (it lives in the Provider's transaction setup, untouched
    here), so it is not a finding against this diff, but it is the load-bearing
    precondition for the attributability this migration is trying to restore;
    worth confirming the `/unban` call path sets the GUC.
  - The spec (`docs/spec/security.md:581`) names the audit action
    `UNBAN_DELETED_PREBAN_ROW`, while the implemented enum, design notes
    (`docs/design/02-schema.md:407`), and all prior migrations (V5/V24) use
    `UNBAN_PREBAN_DELETE`. This naming divergence predates the diff (the diff
    faithfully matches the existing implementation/enum) and is a spec-vs-design
    wording inconsistency, not a security gap — flagged only so the user can
    reconcile the spec text if desired.
