---
target: M1-329
date: 2026-06-14
base: main
head: "m1/M1-329-listsaves-tool-byte-budget (working tree, uncommitted)"
verdict: CLEAN
findings_count:
  critical: 0
  high: 0
  medium: 0
  low: 0
out_of_model_count: 0
disposition: |
  In-progress audit run between /m1-tick review (APPROVE r1) and
  /m1-tick commit, per the documented in-progress redteam scenario.
  Diff captured as working-tree-vs-merge-base since the change is not yet
  committed. CLEAN: the ListSavesTool aggregate byte budget + per-title
  truncation faithfully closes the unbounded-title reinjection path the
  threat model's §Prompt-injection defenses requires; no gaps found.
---

RED-TEAM VERDICT: CLEAN

The diff adds an aggregate UTF-8 byte budget (MAX_RESULT_BYTES = 16 KiB)
and a per-title byte cap (MAX_TITLE_BYTES = 2 KiB) to the listSaves LLM
tool's result serialization. This DELIVERS rather than weakens the
threat-model commitments in §Prompt-injection defenses (LLM call sites):

- "Every output is a typed structured value, never a passthrough of
  free-form upstream text outside the post body / saved snapshot already
  vetted by the ingest pipeline." The snapshot_title (external post data,
  uncapped on the provider write path) is now byte-bounded before
  reinjection into the chat prompt, and the whole array is capped, closing
  a context-window-exhaustion (DOS) vector through the tool boundary.

Adversarial checks performed against the diff, all passing:

1. INJECTION (prompt/JSON): truncated titles are still escaped via
   jsonStr(...) AFTER truncation; truncateUtf8 cuts on code-point
   boundaries on the raw string (GetPostTool.java:101-116), so no
   broken escape sequence or split surrogate reaches the prompt.

2. DOS (unbounded work in the budget loop): rows are pre-bounded by the
   pre-existing LIMIT 200 (RESULT_LIMIT); each title is truncated to 2 KiB
   before its per-entry byte count is computed; the loop breaks once the
   aggregate budget is reached. Work is bounded; no new unbounded path.

3. INFO-LEAK / per-(user,scope) isolation (D13): the WHERE user_id = ?
   clause, the scope-bound cancellation arming, and the bound-parameter
   switch are unchanged by the diff. Truncation drops bytes from the
   caller's own rows only; it cannot surface another user's saves.

4. Input-cap promise ("All free-form string and list inputs ... are
   length-bounded ... rejected by the tool dispatcher before any SQL
   runs"): the diff does not touch the input parsing of `tags`/`window`
   (ListSavesTool.java:69-79 are pre-existing context lines, unchanged
   by the diff); it neither adds nor removes the input-cap behavior, so
   no input-side regression is introduced. Per the diff-only mandate,
   pre-existing untouched parsing is out of scope for this audit.

No gap between threat-model promise and diff delivery was found.

FINDINGS: (none)

OUT-OF-MODEL: (none)
